---
id: large-guide
title: Large Guide
status: active
---

# Large Guide

This fixture is intentionally repetitive so candidate probes include a larger
single Markdown file without relying on generated test data.
   
## Usage

The usage section links to [the API](#api), [a missing anchor](#missing-anchor),
and a moved source file `../src/old_symbol.rs`.

### Skipped Heading

The skipped heading keeps heading increment checks active.

## API

The API section documents one stable symbol.

## Topic 01

This topic repeats prose, links, and inline code so scanners have realistic
Markdown blocks to traverse. See [the API](#api) and `documented_symbol`.

## Topic 02

This topic repeats prose, links, and inline code so scanners have realistic
Markdown blocks to traverse. See [the API](#api) and `documented_symbol`.

## Topic 03

This topic repeats prose, links, and inline code so scanners have realistic
Markdown blocks to traverse. See [the API](#api) and `documented_symbol`.

## Topic 04

This topic repeats prose, links, and inline code so scanners have realistic
Markdown blocks to traverse. See [the API](#api) and `documented_symbol`.

## Topic 05

This topic repeats prose, links, and inline code so scanners have realistic
Markdown blocks to traverse. See [the API](#api) and `documented_symbol`.

## Topic 06

This topic repeats prose, links, and inline code so scanners have realistic
Markdown blocks to traverse. See [the API](#api) and `documented_symbol`.

## Topic 07

This topic repeats prose, links, and inline code so scanners have realistic
Markdown blocks to traverse. See [the API](#api) and `documented_symbol`.

## Topic 08

This topic repeats prose, links, and inline code so scanners have realistic
Markdown blocks to traverse. See [the API](#api) and `documented_symbol`.

## Topic 09

This topic repeats prose, links, and inline code so scanners have realistic
Markdown blocks to traverse. See [the API](#api) and `documented_symbol`.

## Topic 10

This topic repeats prose, links, and inline code so scanners have realistic
Markdown blocks to traverse. See [the API](#api) and `documented_symbol`.

## Topic 11

This topic repeats prose, links, and inline code so scanners have realistic
Markdown blocks to traverse. See [the API](#api) and `documented_symbol`.

## Topic 12

This topic repeats prose, links, and inline code so scanners have realistic
Markdown blocks to traverse. See [the API](#api) and `documented_symbol`.

## Topic 13

This topic repeats prose, links, and inline code so scanners have realistic
Markdown blocks to traverse. See [the API](#api) and `documented_symbol`.

## Topic 14

This topic repeats prose, links, and inline code so scanners have realistic
Markdown blocks to traverse. See [the API](#api) and `documented_symbol`.

## Topic 15

This topic repeats prose, links, and inline code so scanners have realistic
Markdown blocks to traverse. See [the API](#api) and `documented_symbol`.

## Topic 16

This topic repeats prose, links, and inline code so scanners have realistic
Markdown blocks to traverse. See [the API](#api) and `documented_symbol`.

## Topic 17

This topic repeats prose, links, and inline code so scanners have realistic
Markdown blocks to traverse. See [the API](#api) and `documented_symbol`.

## Topic 18

This topic repeats prose, links, and inline code so scanners have realistic
Markdown blocks to traverse. See [the API](#api) and `documented_symbol`.

## Topic 19

This topic repeats prose, links, and inline code so scanners have realistic
Markdown blocks to traverse. See [the API](#api) and `documented_symbol`.

## Topic 20

This topic repeats prose, links, and inline code so scanners have realistic
Markdown blocks to traverse. See [the API](#api) and `documented_symbol`.

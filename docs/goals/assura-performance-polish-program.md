---
id: goal-assura-performance-polish-program
type: goal
title: Assura performance polish program
status: planned
created: 2026-07-02
owners:
  - assura-maintainers
related:
  - ./assura-ls-lint-no-slower-performance-gate.md
  - ./assura-performance-floor-and-fixture-gate.md
  - ./assura-ls-lint-performance-reassessment.md
  - ./assura-supported-document-graph.md
  - ./assura-markdownlint-compatible-rust-engine.md
  - ./assura-true-daemon-mode.md
  - ../analysis/2026-07-02-ls-lint-performance-reassessment.md
  - ../analysis/2026-06-28-content-runtime-index-performance.md
  - ../analysis/2026-06-28-project-intelligence-store-spike.md
  - ../../benches/history/current.json
  - ../../website/public/data/performance/current.json
  - ../../.trellis/spec/assura/roadmap.md
---

# Assura Performance Polish Program

## Post-Merge Revalidation

Still planned after PR #139. That PR landed the first checked
`assura performance-report --suite native` suite, native checked history,
website native performance data, CI/native regression gate wiring, and phase
attribution rows. The broader goal remains open because the next lane still
needs calibrated native regression policy, focused optimization of the highest
impact native and CLI-floor rows, and documentation that keeps LS-Lint parity,
Assura-native diagnostics, cold CLI, and warm daemon/session evidence distinct.

## Objective

Make Assura performance boringly trustworthy across both sides of the product:

- LS-Lint-equivalent structure validation remains no slower than native
  LS-Lint for every accepted fixture row.
- Assura-native capabilities that LS-Lint does not provide, including content
  collection validation, document graph querying, context packs, Markdown and
  reference workflows, daemon/session requests, editor/agent surfaces, and
  safe fixes, get their own tracked performance report, budgets, history, and
  regression gates.
- Remaining cold CLI-floor confusion is explained by evidence and reduced
  where it matters without making the normal CLI cumbersome.

This goal is the next large performance iteration to kick off after the
`v0.3.0` beta increment.

## Current Gap

Assura has several valuable performance assets, but they are not yet one
complete product-grade system.

- `assura performance-report` and CI enforce the LS-Lint comparison path.
- `cargo xtask performance-no-slower` fails accepted LS-Lint-equivalent rows
  that are slower than native LS-Lint.
- PR #139 added checked native performance history and website data under
  `benches/history/native-current.json` and
  `website/public/data/performance/native-current.json`.
- `benches/content_runtime.rs` and `benches/project_intelligence.rs` informed
  the first native suite, but native budgets and regression policy still need
  product-grade calibration from checked report rows.
- The public LS-Lint data under `benches/history/current.json` and
  `website/public/data/performance/current.json` remains distinct from the
  Assura-native report artifacts.
- The remaining cold 2x misses are understood at a high level, but the
  highest-impact rows need a focused optimization pass with before/after proof.

## User Certainty Bar

Nick should be able to ask two questions and get machine-checkable answers:

1. "Are we slower than LS-Lint anywhere on accepted LS-Lint-equivalent
   fixtures?"
2. "Are Assura-only capabilities, especially validating and querying
   collections, document graph search, context packs, Markdown fixes, and
   daemon/session queries, fast enough and protected against regressions?"

The answer must not rely on anecdotes, aggregate-only speedups, or local-only
Criterion output. It should point to checked report JSON, CI gates, benchmark
history, and documentation that explain the accepted rows, diagnostic rows,
budgets, and optimization opportunities.

## Performance Test Plan

### Fixture Matrix

Create one shared native-performance fixture matrix with these sizes:

- `small`: 25 goals, 25 specs, 5 ADRs, representative Markdown headings and
  references.
- `medium`: 250 goals, 250 specs, 50 ADRs, matching the current content
  runtime scale.
- `large`: 2,500 goals, 2,500 specs, 500 ADRs, enough to expose nonlinear
  content, graph, search, serialization, and daemon costs.
- `reference-heavy`: many-to-many relations, duplicate IDs, missing targets,
  ambiguous references, moved docs, renamed code paths, and broken anchors.
- `adapter-mix`: Markdown frontmatter, JSON, YAML, and JSONL collections.
- `real-project`: Assura itself plus the existing realistic project fixtures
  used by project-intelligence proof.

Each fixture must define record counts, file counts, directory counts, relation
counts, heading counts, reference counts, accepted/diagnostic classification,
and expected query shapes.

### Required Rows

Add native performance rows for:

- content repository cold load;
- warm content repository validation;
- full `assura check --format json` with content runtime enabled;
- `assura content collections`;
- `assura content instances`;
- `assura content show`;
- `assura content expand`;
- `assura content search`;
- `assura content missing-relations`;
- `assura content references`;
- `assura content context-pack`;
- `assura content agent-query`;
- Markdown lint scan;
- Markdown safe-fix dry run;
- Markdown safe-fix apply plus idempotence check;
- repository reference validation;
- daemon cold start;
- daemon warm check request;
- daemon warm content/query request;
- session/editor repeated query request;
- JSON/text output serialization for large context packs;
- create/update write path validation for modeled collections.

Rows must separate cold subprocess cost, cold in-process load, warm repository
query, warm daemon/session query, IPC cost, serialization cost, and report
formatting cost.

### Budgets And Gates

Establish the first checked native report as baseline, then enforce these
policies:

- accepted LS-Lint-equivalent rows remain strict no-slower-than-native-LS-Lint
  merge gates;
- native Assura rows fail on material regression versus checked baseline after
  the first calibrated baseline lands;
- warm daemon/session rows get separate budgets from cold CLI rows;
- large fixtures must remain approximately linear in file and modeled-object
  count unless a written exception explains the cost;
- aggregate speedups cannot hide an accepted-row failure;
- diagnostic rows may regress only with explicit classification and reviewer
  approval.

Initial native budgets should be calibrated from measured baseline rather than
invented. After calibration, use row-specific thresholds instead of one global
number.

## Optimization Opportunities To Investigate First

Use the current evidence to rank optimization work before changing code.

1. `many_configured_scopes_regression`: current LS-Lint reassessment shows
   roughly equal time in config loading and walk/validate work. This is the
   highest structure-check row for real optimization.
2. Small cold fixtures such as `simple_library` and `web_app`: current misses
   against the stricter 2x target are floor dominated. Investigate release
   payload, binary linking, startup, config discovery, and output setup without
   requiring users to opt into a daemon for normal `assura check`.
3. Content runtime validation: prior local evidence measured about 21 ms for
   240 goals and 240 specs. Determine whether schema compilation, file
   indexing, relation validation, diagnostic construction, or serialization
   owns the cost, then optimize only with before/after proof.
4. Project-intelligence query store: current warm search and graph rows are
   fast, but incremental replacement is expensive because indexes rebuild.
   Measure daemon/session incremental update behavior before choosing any
   durable store or partial-index design.
5. Markdown engine integration: attribute time across file discovery,
   Markdown parsing, rule execution, safe-fix planning, fix application, and
   Assura diagnostic mapping. The supported Rust path should be the fastest
   practical markdownlint-compatible path that preserves deterministic output.
6. Context-pack and agent-query serialization: large bounded context payloads
   should report selection cost separately from JSON/text rendering.
7. Daemon/session IPC: prove warm requests are actually dominated by useful
   validation/query work, not process or protocol overhead.

## Scope

- Extend or add a native performance report suite that produces
  machine-readable JSON and website-ready data.
- Reuse existing benchmark infrastructure where possible; do not create a
  parallel benchmark system disconnected from `benches/`, `xtask`, CI, and
  website data.
- Promote content runtime and project-intelligence Criterion evidence into
  tracked report rows or documented source rows.
- Add CI/reporting gates for native capability regressions after the baseline
  is calibrated.
- Preserve the LS-Lint no-slower gate for accepted structure fixtures.
- Add phase attribution for native rows, not only LS-Lint rows.
- Optimize the highest-impact rows with before/after report artifacts.
- Update docs and website data so users can distinguish LS-Lint comparison,
  Assura-native performance, cold CLI, warm daemon/session, and diagnostic
  rows.

## Non-Goals

- No replacing product behavior with benchmark-only fast paths.
- No using warm daemon/session results to prove cold CLI LS-Lint parity.
- No relaxing accepted LS-Lint no-slower gates.
- No adding a persistent database, cache, or search engine dependency without
  proving it beats the in-memory baseline on the same fixtures and packaging
  constraints.
- No broad rewrite before phase attribution identifies the cost.
- No performance claim based on one local ad hoc run.

## Definition Of Done

- A native Assura performance report exists with checked JSON history and
  website data.
- CI publishes the native performance report and blocks material regressions
  once the baseline is calibrated.
- Every accepted LS-Lint-equivalent row remains no slower than native LS-Lint.
- Content collection validation and querying have explicit cold and warm
  performance rows.
- Document graph search, expansion, references, missing relations, and context
  packs have explicit performance rows.
- Markdown lint/fix rows attribute parser, rule, fix, and mapping cost.
- Daemon/session rows prove warm check and query behavior separately from cold
  process startup.
- The highest-impact current optimization opportunities are either improved
  with before/after evidence or recorded with bounded follow-up rationale.
- Docs explain how to interpret LS-Lint comparison versus Assura-native
  performance.
- Independent review confirms that gates cannot pass by hiding slow accepted
  rows, warm-only substitutions, or diagnostic reclassification.

## Validation Commands

Start with focused evidence:

```bash
cargo bench --bench content_runtime -- --noplot
cargo bench --bench project_intelligence -- --noplot
cargo build --release --bin assura --no-default-features --features json-output,yaml-config
cargo build --release --bin assura-full
cargo build --release -p assura-check-cli
target/release/assura performance-report \
  --output target/performance/ls-lint-current.json \
  --history target/performance/ls-lint-current.jsonl \
  --website-dir target/performance/ls-lint-website \
  --iterations 5
cargo xtask performance-no-slower target/performance/ls-lint-current.json
```

The implementation should add a native report command or suite. When it
exists, the final gate should include:

```bash
target/release/assura performance-report \
  --suite native \
  --output benches/history/native-current.json \
  --history benches/history/native-history.jsonl \
  --website-dir website/public/data/performance \
  --iterations 5
cargo xtask native-performance-no-regression benches/history/native-current.json
cargo xtask performance-no-slower benches/history/current.json
cargo xtask target-state
cargo run --quiet -- check --format json .
cargo xtask docs
cargo xtask evidence
git diff --check
```

If the final command names differ, update this goal before execution closure so
the checked proof is exact.

## Review Tasks

- R1: Confirm native Assura rows are separate from LS-Lint comparison rows.
- R2: Confirm accepted LS-Lint-equivalent rows still fail merge when slower
  than native LS-Lint.
- R3: Confirm content validation and querying performance is covered by public
  CLI surfaces, not only internal library benches.
- R4: Confirm daemon/session performance is measured as warm state and not
  used as cold CLI proof.
- R5: Confirm Markdown lint/fix performance attributes parser, rule, fix, and
  diagnostic-mapping costs.
- R6: Confirm any optimization has before/after evidence from comparable
  builds and comparable machines.
- R7: Confirm website/docs do not overclaim native performance before checked
  artifacts exist.

## Reviewer Blocking Criteria

Block completion if any accepted LS-Lint-equivalent fixture is slower than
native LS-Lint, if native Assura performance remains local-only Criterion
evidence, if aggregate speedups hide slower accepted rows, if warm daemon
results are used as cold CLI proof, if fixture classifications can be changed
without review, or if normal CLI usage becomes cumbersome to satisfy a
benchmark.

## Kickoff Text

Use this prompt to start the large goal-driven work:

```text
Execute docs/goals/assura-performance-polish-program.md.

Revalidate the goal against the current roadmap, checked LS-Lint performance
data, content runtime benchmarks, project-intelligence benchmarks, CI gates,
and website performance artifacts before coding. Preserve the strict
no-slower-than-native-LS-Lint gate for every accepted LS-Lint-equivalent
fixture. Then add a tracked native Assura performance report for content
collection validation/querying, document graph operations, context packs,
Markdown lint/fix, repository references, daemon/session warm queries, and
agent/editor-facing query paths. Calibrate baseline budgets from measured
evidence, wire merge gates after calibration, optimize the highest-impact rows
with before/after proof, update docs/website data, and get independent review
focused on whether slow rows can be hidden or misclassified.
```

## Progress Log

| Date | Entry | Evidence |
| --- | --- | --- |
| 2026-07-05 | Stepped back from the cold micro-optimization loop and made the stop rule explicit. The active checked state is now treated as release-useful but not worth indefinite strict-2x chasing: accepted cold LS-Lint-equivalent rows are `8/8` no-slower while only `2/8` meet strict cold 2x, and the warm/session row family is already `8/8` at 2x. I added a durable decision matrix that lists each accepted fixture, the current gap, and the stop/next action; the result is to pause further `many_configured_scopes_regression` micro-tuning unless a profiler-backed hypothesis can improve the target public `assura-cli` row, the exact `assura check --quiet` tie-breaker, and spillover rows at the same time. I also captured the latest reverted naming-convention fast-path experiment so it does not get rediscovered: the remote proof at `/home/ubuntu/data/projects/assura-perf-20260705-naming-convention-fast-path` improved neither the no-slower gate nor public-row confidence enough to keep, and the local product-code diff for `src/config/config/validation.rs` and `src/cli/check/rules.rs` remained empty afterward. The `cargo perf-vps` helper now prints an all-accepted-fixture before/after table and exact-command percent deltas, so future experiments must show quoted progress by test case instead of only a target-row or phase-row story. | `docs/analysis/2026-07-05-performance-decision-matrix.md`; `scripts/perf-vps-ls-lint-compare.sh`; `benches/history/current.json`; `benches/history/native-history.jsonl`; `/home/ubuntu/data/projects/assura-perf-20260705-naming-convention-fast-path`; `git diff -- src/config/config/validation.rs src/cli/check/rules.rs` |
| 2026-07-04 | Revalidated the active performance lane after PR #139, implemented baseline-calibrated native regression metadata and gate wiring, and refreshed the checked native current artifacts in place from checked native history so the JSON contract and gate surface now reflect the calibrated policy without weakening the separate LS-Lint no-slower lane. Dirty-tree local proof on `codex/post-merge-backlog-truth-pass` still showed 58 native regressions, but a representative `native_small` cold `assura-full check --format json` repro on a generated fixture measured clean `HEAD` and the locally modified binary at effectively the same runtime (`43.0 ms` versus `43.6 ms` mean with `hyperfine --warmup 1 --runs 10`), so the remaining mismatch is in suite comparability or local proof conditions rather than this metadata slice. The highest-impact measured native follow-up remains the `native_large` fact-ingest/index-load and incremental-generation-replace lane, which still dominates the largest checked native phase rows. | `python3 ./.trellis/scripts/workflow_gate.py --platform codex`; `git status --short`; `cargo xtask performance-no-slower benches/history/current.json`; `target/release/assura performance-report --suite native --output target/performance/native-proof.json --history target/performance/native-proof.jsonl --website-dir target/performance/native-proof-website --iterations 5`; `cargo xtask native-performance-no-regression target/performance/native-proof.json`; `hyperfine --warmup 1 --runs 10 '/tmp/assura-head-target/release/assura-full check . --format json >/dev/null' '/Users/nroth/workspace/assura/target/release/assura-full check . --format json >/dev/null'`; `benches/history/native-current.json`; `website/public/data/performance/native-current.json` |
| 2026-07-04 | Closed the major review gaps in the native regression slice before handoff. The calibrated baseline is now scoped by row environment and build profile, checked-history parse errors no longer silently degrade to a weaker baseline, the gate cross-checks `median_runtime_ms`, `native_regression_threshold_ms`, and `native_regression_delta_ms` instead of trusting `native_regression_status` alone, and the threshold itself is now the highest observed scoped checked sample rather than the earlier doubled-tail envelope. Updated checked native current artifacts still pass the native gate, LS-Lint accepted rows still pass the separate no-slower gate, and the focused Rust/contract/Trellis/docs evidence gates all pass after the reviewer-driven fixes. | `cargo test -p assura native_regression --lib`; `cargo test -p xtask native_performance_gate_ --quiet`; `cargo test --test performance_report_contract_tests --quiet`; `cargo xtask native-performance-no-regression benches/history/native-current.json`; `cargo xtask performance-no-slower benches/history/current.json`; `cargo xtask target-state`; `cargo run --quiet -- check --format json .`; `cargo xtask docs`; `cargo xtask evidence`; `git diff --check`; independent review agent `Dirac` |
| 2026-07-04 | Optimized the highest-impact checked native hotspot by removing repeated linear scans from project-intelligence fact ingest and generation replacement while preserving the public `(stable_id, generation)` contract and last-write-wins replacement semantics. An independent review surfaced one real generation-safety regression in the first draft and one misleading test; both are now fixed, and the refreshed release-profile proof still shows the targeted `native_large` phase rows collapsing from multi-second costs to roughly `195 ms` (`fact-ingest-load`) and `157 ms` (`incremental-replace-generation`), with corresponding large drops in `content search` and `agent-query keyword search`. I did not refresh the checked native artifacts from this local run because `cargo xtask native-performance-no-regression target/performance/native-post-opt.json` still reported unrelated calibrated-row regressions elsewhere in the suite, so the local proof advances hotspot ranking and optimization evidence without overclaiming a new checked baseline. | `cargo test --test project_intelligence_fact_model_tests --quiet`; `cargo test --test project_intelligence_store_spike_tests --quiet`; `cargo test -p assura native_regression --lib --quiet`; independent review agent `Peirce`; `cargo build --release --bin assura --no-default-features --features json-output,yaml-config`; `cargo build --release --bin assura-full`; `cargo build --release -p assura-check-cli`; `target/release/assura performance-report --suite native --output target/performance/native-post-opt.json --history target/performance/native-post-opt.jsonl --website-dir target/performance/native-post-opt-website --iterations 5`; `cargo xtask native-performance-no-regression target/performance/native-post-opt.json`; `cargo xtask native-performance-no-regression benches/history/native-current.json`; `cargo xtask performance-no-slower benches/history/current.json`; `cargo run --quiet -- check --format json .`; `cargo xtask evidence`; `git diff --check`; `docs/analysis/2026-07-04-performance-polish-program-revalidation.md` |
| 2026-07-04 | Narrowed the next native-regression lane without forcing a bad optimization through. Live comparison against `benches/history/native-current.json` showed that many remaining calibrated failures are still judged against only one independent checked native report row with five in-report samples, so I extended the native report schema and xtask gate to expose baseline report/sample counts for new native rows and failure messages. I also tested one `object-load-validate` optimization hypothesis by retaining parsed repo-object data as a top-level `Value`, but comparable native-suite proof made the target validation phases about `14%` slower on `native_medium` and `native_large`, so that experiment was reverted immediately. The worktree therefore keeps the earlier ingest optimization, adds better reviewer-facing baseline-coverage evidence, and records that `object-load-validate` still needs a different optimization approach before the checked native artifacts can move. | `python3 ./.trellis/scripts/workflow_gate.py --platform codex`; `git status --short --branch`; `benches/history/native-current.json`; `target/performance/native-post-opt.json`; `cargo test -p assura native_regression --lib --quiet`; `cargo test -p xtask native_performance_gate_ --quiet`; `cargo test --test performance_report_contract_tests --quiet`; `cargo test --test project_intelligence_fact_model_tests --quiet`; `cargo test --test project_intelligence_store_spike_tests --quiet`; `cargo test content_repository --quiet`; `cargo xtask native-performance-no-regression target/performance/native-post-opt.json`; `cargo xtask performance-no-slower benches/history/current.json`; `docs/analysis/2026-07-04-performance-polish-program-revalidation.md` |
| 2026-07-04 | Landed a narrower content-runtime validation optimization that actually moved the remaining hotspot in the right direction. The valid-object schema-validation path now returns early on `validator.is_valid(...)` and only constructs detailed jsonschema errors for invalid objects. Comparable five-iteration native proof brought `native:phase:object-load-validate` and `native:phase:repository-validate-total` back within calibrated baseline across the larger fixtures, cut the local native gate from many failures down to exactly one remaining row, and preserved the separate LS-Lint no-slower gate. I still did not refresh the checked native artifacts because the comparable local run fails one final native-only row, `native_real_project native:phase:factset-serialize-json`, by a sub-millisecond margin against a thin checked baseline (`baseline reports=1`, `samples=5`). | `cargo test content_repository --quiet`; `cargo test --test project_intelligence_fact_model_tests --quiet`; `cargo test --test project_intelligence_store_spike_tests --quiet`; `cargo build --release --bin assura --no-default-features --features json-output,yaml-config`; `cargo build --release --bin assura-full`; `cargo build --release -p assura-check-cli`; `target/release/assura performance-report --suite native --output target/performance/native-post-opt.json --history target/performance/native-post-opt.jsonl --website-dir target/performance/native-post-opt-website --iterations 5`; `cargo xtask native-performance-no-regression target/performance/native-post-opt.json`; `cargo xtask native-performance-no-regression benches/history/native-current.json`; `cargo xtask performance-no-slower benches/history/current.json`; `cargo run --quiet -- check --format json .`; `cargo xtask evidence`; `git diff --check`; `docs/analysis/2026-07-04-performance-polish-program-revalidation.md` |
| 2026-07-04 | Tightened the native regression policy for rows that still only have one checked native report behind them. Native rows now distinguish provisional single-report baselines from calibrated multi-report baselines: provisional rows carry `within-provisional-baseline` or `regressed-vs-provisional-baseline`, and their threshold uses the checked row's observed spread plus a small jitter floor instead of treating one five-sample report as a finished budget. Comparable release-profile proof on this worktree now passes the full native gate (`target/performance/native-provisional-proof-third.json`) while preserving the separate LS-Lint no-slower gate. I still did not refresh the checked native artifacts in this turn because generating `benches/history/native-current.json` from the current dirty worktree would record `source_worktree_dirty = true`, which would fail the checked native report contract; the next step is a clean-worktree refresh of the checked native files. | `cargo test -p assura native_regression --lib --quiet`; `cargo test -p xtask native_performance_gate_ --quiet`; `cargo test --test performance_report_contract_tests --quiet`; `cargo build --release --bin assura --no-default-features --features json-output,yaml-config`; `cargo build --release --bin assura-full`; `cargo build --release -p assura-check-cli`; `target/release/assura performance-report --suite native --output target/performance/native-provisional-proof.json --history target/performance/native-provisional-proof.jsonl --website-dir target/performance/native-provisional-proof-website --iterations 5`; `target/release/assura performance-report --suite native --output target/performance/native-provisional-proof-rerun.json --history target/performance/native-provisional-proof-rerun.jsonl --website-dir target/performance/native-provisional-proof-rerun-website --iterations 5`; `target/release/assura performance-report --suite native --output target/performance/native-provisional-proof-third.json --history target/performance/native-provisional-proof-third.jsonl --website-dir target/performance/native-provisional-proof-third-website --iterations 5`; `cargo xtask native-performance-no-regression target/performance/native-provisional-proof-third.json`; `cargo xtask native-performance-no-regression benches/history/native-current.json`; `cargo xtask performance-no-slower benches/history/current.json`; `docs/analysis/2026-07-04-performance-polish-program-revalidation.md` |
| 2026-07-04 | Incorporated the review follow-up on the provisional native baseline policy. Independent review correctly pointed out that the first draft could hide regressions when a single checked report contained one extreme outlier and that the checked-artifact contract still assumed calibrated-only passing native rows. The current implementation now drops a single extreme top sample before deriving provisional thresholds, the checked native contract accepts passing provisional rows for future clean refreshes, and a fresh release-profile comparable proof still passes the full native gate (`target/performance/native-provisional-proof-reviewed.json`). I still did not refresh the checked native artifacts themselves because the clean-source requirement remains unchanged: a checked refresh from this dirty worktree would still stamp `source_worktree_dirty = true`. | `cargo test -p assura native_regression --lib --quiet`; `cargo test -p xtask native_performance_gate_ --quiet`; `cargo test --test performance_report_contract_tests --quiet`; independent review agent `Hegel`; `cargo build --release --bin assura --no-default-features --features json-output,yaml-config`; `cargo build --release --bin assura-full`; `cargo build --release -p assura-check-cli`; `target/release/assura performance-report --suite native --output target/performance/native-provisional-proof-reviewed.json --history target/performance/native-provisional-proof-reviewed.jsonl --website-dir target/performance/native-provisional-proof-reviewed-website --iterations 5`; `cargo xtask native-performance-no-regression target/performance/native-provisional-proof-reviewed.json`; `cargo xtask native-performance-no-regression benches/history/native-current.json`; `cargo xtask performance-no-slower benches/history/current.json`; `cargo run --quiet -- check --format json .`; `cargo xtask evidence`; `cargo xtask docs`; `git diff --check`; `docs/analysis/2026-07-04-performance-polish-program-revalidation.md` |
| 2026-07-04 | Completed the deferred clean-source native artifact refresh. I materialized the active performance-polish lane into a temporary clean worktree snapshot, regenerated the tracked native checked files there, and copied the refreshed artifacts back into the main worktree. The checked native data now reflects the reviewed provisional-versus-calibrated policy, includes baseline report/sample counts on native rows, and still passes the native gate without weakening the separate LS-Lint no-slower lane. I also tightened `tests/performance_report_contract_tests.rs` so checked native rows must keep those baseline-count fields present and positive going forward. | temporary clean worktree at `/tmp/assura-native-refresh-lgczba7d`; `cargo build --release --bin assura --no-default-features --features json-output,yaml-config`; `cargo build --release --bin assura-full`; `cargo build --release -p assura-check-cli`; `target/release/assura performance-report --suite native --output benches/history/native-current.json --history benches/history/native-history.jsonl --website-dir website/public/data/performance --iterations 5`; `cargo xtask native-performance-no-regression benches/history/native-current.json`; `cargo xtask performance-no-slower benches/history/current.json`; `cargo test --test performance_report_contract_tests --quiet`; `docs/analysis/2026-07-04-performance-polish-program-revalidation.md` |
| 2026-07-04 | Proved and checked in the narrower content-runtime validation optimization. A fresh comparable native proof (`target/performance/native-content-validate-proof.json`) passed the full native gate while the separate LS-Lint no-slower gate stayed green. I then reran the clean-source refresh correctly from inside a temporary worktree snapshot, normalized the copied-back checked provenance back to the live lane branch and base `HEAD`, and updated the checked native files to calibrated `2`-report / `10`-sample baselines with tracked `native_large` validation phases at roughly `196.1 ms` for `object-load-validate` and `216.9 ms` for `repository-validate-total`. Independent review also caught a helper-level regression in the schema-backed content validation fast path, so declared field checks now still run even when a schema validator is present. The next native optimization target is now narrower again: `native_large object-load-validate` remains the biggest native phase cost, with `fact-ingest-load` and `incremental-replace-generation` next, while the separate LS-Lint-equivalent structure lane is still `many_configured_scopes_regression`. | `target/release/assura performance-report --suite native --output target/performance/native-content-validate-proof.json --history target/performance/native-content-validate-proof.jsonl --website-dir target/performance/native-content-validate-proof-website --iterations 5`; `cargo xtask native-performance-no-regression target/performance/native-content-validate-proof.json`; temporary clean worktree at `/tmp/assura-native-refresh-myywuF`; `cargo xtask native-performance-no-regression benches/history/native-current.json`; `cargo xtask performance-no-slower benches/history/current.json`; `cargo test -p assura native_regression --lib --quiet`; `cargo test --test content_runtime_validation --test content_runtime_create --test content_runtime_update --test content_runtime_references --test content_runtime_adapters --quiet`; `cargo test --test performance_report_contract_tests --quiet`; `cargo test -p xtask native_performance_gate_ --quiet`; `cargo run --quiet -- check --format json .`; `cargo xtask evidence`; `cargo xtask docs`; `git diff --check`; independent review agent `Curie`; `docs/analysis/2026-07-04-performance-polish-program-revalidation.md` |
| 2026-07-04 | Closed the remaining checked-native provenance gap and made the actual gains easier to read. The report schema now carries both the exact measured snapshot (`commit_sha`, `branch`) and the original source-lane provenance (`source_commit_sha`, `source_branch`, `source_patch_id`) when a clean temp snapshot is materialized from a dirty lane, with `source_worktree_dirty` describing that source lane instead of the temp checkout. This was provenance truth, not the main performance win: the checked native history still shows the substantive `native_large` product-path reductions from earlier work, especially `fact-ingest-load` (`2993.6 ms` -> `137.0 ms`, `-95.4%`) and `incremental-replace-generation` (`2733.0 ms` -> `118.3 ms`, `-95.7%`), while the remaining validation hotspot is now about `188.7 ms` for `object-load-validate` and `209.8 ms` for `repository-validate-total` with calibrated `3`-report / `15`-sample baselines. | temporary clean worktree at `/tmp/assura-native-refresh-rgH8Qm`; `target/release/assura performance-report --suite native --output benches/history/native-current.json --history benches/history/native-history.jsonl --website-dir website/public/data/performance --iterations 5`; `cargo xtask native-performance-no-regression benches/history/native-current.json`; `cargo test --test performance_report_contract_tests --quiet`; `docs/analysis/2026-07-04-performance-polish-program-revalidation.md`; `website/src/content/docs/reference/performance-implementation.mdx`; `website/src/content/docs/reference/performance.mdx` |
| 2026-07-04 | Re-centered the open structure-performance lane around quoted test-case evidence instead of speculative fast-path churn. I validated two candidate optimizations against the same `many_configured_scopes_regression` fixture and rejected both. First, deferring `rel_to_string(...)` in the LS-Lint fast path lost a same-machine back-to-back A/B on the manual harness: reverting returned `assura check --quiet` to `37.77 ms` median and `assura-check --quiet` to `40.92 ms`, versus the patched `39.23 ms` and `41.16 ms`. Second, reusing fast-scope metadata during compiled-config setup produced mixed local evidence and then failed the quieter `vps` run: on the 64 GB host, clean `HEAD` measured `23.41 ms` for `assura check --quiet` and `22.61 ms` for `assura-check --quiet`, while the copied current worktree measured `23.73 ms` and `23.50 ms`. I reverted both code experiments. The durable output of this iteration is the evidence split itself: `many_configured_scopes_regression` remains the right structure target, but the next optimization should prove a stable win on the public `assura-cli` row and its phase rows before touching checked history or docs. | `target/release/assura check --quiet` from `target/performance/manual-many-scopes`; `target/release/assura-check --quiet` from `target/performance/manual-many-scopes`; `target/performance/manual-many-scopes/manual-structure-after.json`; `target/performance/manual-many-scopes/manual-structure-reverted.json`; `ssh vps 'bash /tmp/assura-remote-bench.sh'`; `/home/ubuntu/data/projects/assura-bench-head/target/performance/remote-manual-structure-head.json`; `/home/ubuntu/data/projects/assura-bench-current/target/performance/remote-manual-structure-current.json`; `git diff -- src/cli/check/ls_fast.rs src/cli/check/ls_fast_naming.rs src/cli/check/compiled_config.rs src/cli/check/ls_fast_plan.rs` |
| 2026-07-04 | Tested a third structure-side slice against the same `many_configured_scopes_regression` case and rejected it for the same reason: it did not improve the public cold row enough to keep. I tried memoizing repeated naming-convention semantic validation during config load because the generated `.assura/config.yml` repeats `kebab-case` across 800 child scopes. On the quieter `vps` host, the direct same-lane manual A/B looked mildly positive (`assura check --quiet`: `25.98 ms` -> `23.75 ms`; `assura-check --quiet`: `23.98 ms` -> `23.64 ms`), but the row/phase proof from `performance-report` was not good enough to land. `many_configured_scopes_regression assura:phase:config-load` improved from `6.61 ms` to `4.85 ms` and `assura:phase:walk-and-validate` improved from `5.43 ms` to `4.00 ms`, yet the public `assura-cli` row stayed effectively flat (`20.32 ms` -> `20.40 ms`) and `assura-check-cli` regressed (`19.64 ms` -> `22.35 ms`). I reverted this patch too. The next structure attempt should keep using `vps` as the benchmark source and should only land if the named cold row improves alongside, or at least not against, the phase rows. | `ssh vps 'bash /tmp/assura-remote-config-bench.sh'`; `/home/ubuntu/data/projects/assura-bench-current/target/performance/remote-config-bench-current_unpatched.json`; `/home/ubuntu/data/projects/assura-bench-current-patched/target/performance/remote-config-bench-current_patched.json`; `ssh vps 'bash /tmp/assura-remote-report-ab.sh'`; `/home/ubuntu/data/projects/assura-bench-current/target/performance/current_unpatched-report.json`; `/home/ubuntu/data/projects/assura-bench-current-patched/target/performance/current_patched-report.json`; `git diff -- src/config/config/validation.rs` |
| 2026-07-04 | Tested a fourth structure-side slice aimed at the `assura:phase:checker-init` cost in the same `many_configured_scopes_regression` lane and rejected it. The experiment replaced the LS-Lint fast-scope `HashMap<PathBuf, usize>` with a sorted `configured_dirs` plus `Vec<usize>` index so the checker could reuse compiled structure order instead of cloning every literal scope path. The targeted phase moved in the right direction on the quieter `vps` host: `many_configured_scopes_regression assura:phase:checker-init` dropped from `2.74 ms` to `1.75 ms`, `config-load` dropped from `6.61 ms` to `4.67 ms`, and `walk-and-validate` dropped from `5.43 ms` to `4.47 ms`. But the public cold rows regressed on the same host, so I reverted it: `many_configured_scopes_regression assura-cli` worsened from `20.32 ms` to `25.33 ms` (`+24.6%`) and `assura-check-cli` worsened from `19.64 ms` to `20.72 ms` (`+5.5%`). Fixture-by-fixture comparison also showed mixed spillover outside the target case: `simple_library` improved for both CLIs, `rule_heavy_repo` improved slightly, but `web_app assura-check-cli` regressed from `1.49 ms` to `1.90 ms` and `monorepo_policy assura-check-cli` regressed from `2.97 ms` to `3.52 ms`. This slice therefore produced better internal phase accounting without a publishable structure win. | `ssh vps 'cargo test -p assura fast_plan_indexed_lookup_matches_linear_lookup_for_literal_scopes --quiet'`; `/home/ubuntu/data/projects/assura-bench-current-patched/target/performance/remote-checker-init-manual.json`; `/home/ubuntu/data/projects/assura-bench-current-patched/target/performance/checker-init-report.json`; `/home/ubuntu/data/projects/assura-bench-current/target/performance/current_unpatched-report.json`; `git diff -- src/cli/check.rs src/cli/check/ls_fast.rs src/cli/check/ls_fast_plan.rs src/cli/check/ls_fast_plan_tests.rs` |
| 2026-07-04 | Tested a fifth structure-side slice that tried to stop recompiling identical descendant fast rules when stripping direct-content policy would be a no-op. The raw idea was plausible because the `manual-many-scopes` fixture repeats the same `*.js -> kebab-case` child rule 800 times. A first same-lane `vps` manual run looked good (`assura check --quiet` median about `22.87 ms`, `assura-check --quiet` median about `23.08 ms`), but the authoritative comparable `performance-report` after fixing the helper to respect `exists`-based descendant policy showed this was not a keepable public win. On the same `many_configured_scopes_regression` row, `assura-cli` regressed from `20.32 ms` to `23.98 ms` (`+18.0%`), `assura:phase:walk-and-validate` regressed from `5.43 ms` to `6.63 ms` (`+22.2%`), and `assura:phase:config-load` plus `checker-init` barely moved (`6.61 ms` -> `6.52 ms`, `2.74 ms` -> `2.73 ms`). `assura-check-cli` did improve (`19.64 ms` -> `17.94 ms`), but the target cold `assura-cli` row and several other structure rows also regressed, including `monorepo_packages assura-cli` (`1.63 ms` -> `2.17 ms`) and `multipart_extension_regression assura-cli` (`3.53 ms` -> `4.06 ms`). I reverted this slice too. | `ssh vps 'bash /tmp/assura-remote-descendant-reuse.sh'`; `/home/ubuntu/data/projects/assura-bench-current-patched/target/performance/remote-descendant-reuse-manual.json`; `/home/ubuntu/data/projects/assura-bench-current-patched/target/performance/descendant-reuse-report.json`; `/home/ubuntu/data/projects/assura-bench-current/target/performance/current_unpatched-report.json`; `git diff -- src/cli/check/ls_fast_plan.rs src/cli/check/ls_fast_plan_tests.rs` |
| 2026-07-04 | Tested a sixth structure-side slice that disabled the LS-Lint fast-path parallel traversal heuristic for large literal-scope sets, keeping parallel mode only for direct-count or scope-magic cases. This did produce the first clear `many_configured_scopes_regression assura-cli` improvement on the quieter `vps` host: `20.32 ms` -> `18.41 ms` (`-9.4%`), with `config-load` `6.61 ms` -> `5.40 ms`, `checker-init` `2.74 ms` -> `1.70 ms`, and `walk-and-validate` `5.43 ms` -> `4.93 ms`. But it still was not a keepable structure slice because the same run regressed the paired `assura-check-cli` row on that target fixture (`19.64 ms` -> `21.16 ms`, `+7.8%`) and hurt several other structure fixtures at the same time: `web_app assura-cli` `1.74 ms` -> `2.03 ms`, `monorepo_policy assura-cli` `3.02 ms` -> `3.75 ms`, and both `web_app` and `monorepo_policy` regressed for `assura-check-cli` as well. The same-lane manual cold run also stayed noisy and mixed (`assura check --quiet` median about `23.59 ms`, `assura-check --quiet` median about `24.19 ms`). I reverted this heuristic too. | `ssh vps 'bash /tmp/assura-remote-serial-heuristic.sh'`; `/home/ubuntu/data/projects/assura-bench-current-patched/target/performance/remote-serial-heuristic-manual.json`; `/home/ubuntu/data/projects/assura-bench-current-patched/target/performance/serial-heuristic-report.json`; `/home/ubuntu/data/projects/assura-bench-current/target/performance/current_unpatched-report.json`; `git diff -- src/cli/check/ls_fast_parallel.rs` |
| 2026-07-04 | Tested a seventh structure-side slice that removed one duplicate fast-rule lookup per recursed directory in the LS-Lint fast path. The change passed a focused compile/test check on `vps`, but the comparable same-host evidence still rejected it. On `many_configured_scopes_regression`, `assura:phase:config-load` improved from `6.61 ms` to `5.45 ms` and `checker-init` improved from `2.74 ms` to `2.35 ms`, yet `walk-and-validate` regressed from `5.43 ms` to `5.96 ms` and the public cold rows regressed with it: `assura-cli` worsened from `20.32 ms` to `21.16 ms` (`+4.1%`) and `assura-check-cli` worsened from `19.64 ms` to `24.25 ms` (`+23.5%`). Spillover outside the target case was also bad, especially for `assura-check-cli`: `web_app` rose from `1.49 ms` to `2.14 ms` and `monorepo_policy` from `2.97 ms` to `3.38 ms`. The same-lane manual medians were also worse than the unpatched host baseline (`assura check --quiet` about `25.76 ms`, `assura-check --quiet` about `25.29 ms`). I reverted this slice too. | `ssh vps 'bash /tmp/assura-remote-fast-rule-reuse.sh'`; `/home/ubuntu/data/projects/assura-bench-current-patched/target/performance/remote-fast-rule-reuse-manual.json`; `/home/ubuntu/data/projects/assura-bench-current-patched/target/performance/fast-rule-reuse-report.json`; `/home/ubuntu/data/projects/assura-bench-current/target/performance/current_unpatched-report.json`; `git diff -- src/cli/check/ls_fast.rs` |
| 2026-07-04 | Closed off another misleading structure lane before it consumed more time. I re-tested the earlier "large literal scopes stay serial" heuristic on `vps` with a patched temp repo and a direct 40-run manual A/B against `target/performance/manual-many-scopes`; that ad hoc loop again looked mildly positive for the public launcher on that one fixture (`assura check --quiet` median `25.36 ms` baseline versus `23.57 ms` patched), but the full comparable `performance-report` row on the same host did not reproduce a publishable win (`many_configured_scopes_regression assura-cli` came out `25.39 ms`, worse than the accepted `20.32 ms` unpatched report). The useful progress was the build-path audit: `src/cli/check/ls_fast_parallel.rs` is guarded by `full-cli`, while the shipped `assura` headline binary for release-style `assura check` evidence is built with `--no-default-features --features json-output,yaml-config`. `strings` on `target/release/assura` on `vps` showed no `jwalk` or `walk_lslint_fast_dir_parallel` symbols, while `assura-full` did. So this heuristic is not part of the stable beta structure path at all when we build evidence correctly. I reverted the local experiment and narrowed the next optimization scope to shared check-only code that both `assura` and `assura-check` actually link: `src/cli/check/ls_fast.rs`, `ls_fast_plan.rs`, `compiled_config.rs`, and config-load helpers. | `scp src/cli/check/ls_fast_parallel.rs vps:/home/ubuntu/data/projects/assura-bench-heuristic-test/src/cli/check/ls_fast_parallel.rs`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-heuristic-test && cargo test -p assura large_literal_scope_sets_stay_serial --lib --quiet && cargo test -p assura direct_count_constraints_still_parallelize --lib --quiet && cargo test -p assura wildcard_scope_patterns_still_parallelize --lib --quiet && cargo build --release --bin assura --no-default-features --features json-output,yaml-config'`; `ssh vps 'python3 ... manual-many-scopes A/B loop ...'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-heuristic-test && target/release/assura performance-report --output target/performance/heuristic-report.json --history target/performance/heuristic-history.jsonl --website-dir target/performance/heuristic-website --iterations 5'`; `ssh vps 'strings /home/ubuntu/data/projects/assura-bench-current/target/release/assura | rg \"jwalk|RayonDefaultPool|parallel_jwalk_strategy|walk_lslint_fast_dir_parallel\" || true'`; `ssh vps 'strings /home/ubuntu/data/projects/assura-bench-current/target/release/assura-full | rg \"jwalk|RayonDefaultPool|parallel_jwalk_strategy|walk_lslint_fast_dir_parallel\" || true'`; `Cargo.toml`; `crates/assura-check-cli/Cargo.toml`; `src/cli/check.rs` |
| 2026-07-04 | Kept the first shared structure-path candidate that produced a quoted public-row win on `vps`: `StructureChecker` now uses a hybrid configured-directory membership index, keeping the old sorted `Vec<PathBuf>` lookup for small configs and switching to hashed membership only when the configured-directory set is large. On the authoritative `performance-report` run from the patched temp repo, the targeted `many_configured_scopes_regression` public row improved from `20.32 ms` to `19.56 ms` (`-3.7%`), with the measured phase buckets dropping from `6.61 ms` to `4.00 ms` for `config-load` (`-39.4%`), `2.74 ms` to `1.56 ms` for `checker-init` (`-43.1%`), and `5.43 ms` to `4.40 ms` for `walk-and-validate` (`-18.9%`). The only row family that actually exercises the new hashed branch in this slice is `many_configured_scopes_regression`; the smaller accepted fixtures stay on the unchanged sorted-path branch, so any movement there should be treated as benchmark variance rather than attributable improvement or regression. The paired cold `assura-check-cli` row on the target fixture still drifted the wrong way in the same patched report, though more mildly than earlier rejected slices: `many_configured_scopes_regression` `19.64 ms` -> `19.94 ms` (`+1.5%`). A direct 35-run manual A/B on `manual-many-scopes` was consistent with the public launcher win (`assura check --quiet` median `24.68 ms` -> `23.29 ms`, `-5.6%`) while the cold check-only binary stayed roughly flat-to-slightly-worse (`22.71 ms` -> `23.04 ms`, `+1.5%`). I am keeping the hybrid index locally because it advances the stable-beta `assura check` headline lane with clear named-fixture proof, but the remaining decision pressure is now explicit: either accept a public-row-first improvement with minor `assura-check-cli` drift on the target hashed-path fixture, or continue tuning until the check-only cold row also stays flat. | `scp src/cli/check.rs vps:/home/ubuntu/data/projects/assura-bench-configured-dir-set/src/cli/check.rs`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-configured-dir-set && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli'`; `ssh vps 'python3 ... manual-many-scopes four-command A/B loop ...'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-configured-dir-set && target/release/assura performance-report --output target/performance/configured-dir-set-report.json --history target/performance/configured-dir-set-history.jsonl --website-dir target/performance/configured-dir-set-website --iterations 5'`; `/home/ubuntu/data/projects/assura-bench-current/target/performance/current_unpatched-report.json`; `/home/ubuntu/data/projects/assura-bench-configured-dir-set/target/performance/configured-dir-set-report.json`; `cargo test -p assura small_configured_dir_sets_keep_sorted_lookup --lib --quiet`; `cargo test -p assura large_configured_dir_sets_keep_membership_correct --lib --quiet` |
| 2026-07-04 | Validated the retained hybrid-index slice against the real merge gates and clarified what still is and is not attributable to this change. On the patched `vps` repo copy, `cargo xtask performance-no-slower target/performance/configured-dir-set-report.json` still passed for the accepted LS-Lint-equivalent cohort, and `cargo test --test ls_lint_parity_regression_tests --quiet` also stayed green. After splitting the index helper into `src/cli/check/configured_dir_index.rs`, the patched repo self-check no longer reported `src/cli/check.rs` as over the repo's 500-line limit; the remaining `cargo run --quiet -- check --format json .` failure on that repo copy was the pre-existing unrelated `src/cli/check/ls_fast_plan.rs` max-lines violation (`540` lines). By inspection of the fixture shapes, `web_app` and `monorepo_policy` remain under the `>128 configured dirs` hash threshold, so their small `assura-check-cli` movement on this patched report is likely ambient benchmark variance on an unchanged sorted-path code path rather than a causal regression from the new index. The only row that actually exercises the hashed branch in this slice is still the intended target, `many_configured_scopes_regression`. | `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-configured-dir-set && cargo xtask performance-no-slower target/performance/configured-dir-set-report.json'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-configured-dir-set && cargo test --test ls_lint_parity_regression_tests --quiet'`; `scp src/cli/check.rs src/cli/check/configured_dir_index.rs vps:/home/ubuntu/data/projects/assura-bench-configured-dir-set/src/cli/check/`; `ssh vps 'wc -l /home/ubuntu/data/projects/assura-bench-configured-dir-set/src/cli/check.rs /home/ubuntu/data/projects/assura-bench-configured-dir-set/src/cli/check/configured_dir_index.rs /home/ubuntu/data/projects/assura-bench-configured-dir-set/src/cli/check/ls_fast_plan.rs'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-configured-dir-set && cargo run --quiet -- check --format json .'`; `src/cli/performance_report/fixtures.rs`; `src/cli/performance_report/monorepo_policy.rs`; `src/cli/performance_report/realistic_fixtures.rs` |
| 2026-07-05 | Rechecked the retained hybrid-index slice with a higher-sample same-host manual A/B so the target-fixture conclusion does not rest on one 5-iteration `performance-report` blip. On `vps` against `target/performance/manual-many-scopes`, 80 measured cold runs after warmup showed both binaries improving on the target hashed-path fixture. `assura check --quiet` moved from `23.91 ms` to `22.47 ms` median (`-6.0%`), and `assura-check --quiet` moved from `24.30 ms` to `23.07 ms` median (`-5.1%`). The bootstrap median-delta confidence interval for `assura-check` stayed below zero (`[-2.69 ms, -0.16 ms]`), so the earlier 5-iteration `performance-report` reading (`19.64 ms` -> `19.94 ms`) is best treated as an under-sampled noisy blip rather than evidence that the hybrid index hurts the cold check-only binary on the target case. That leaves the slice in a stronger state: public `assura-cli` proof still improves on the named fixture and the higher-sample direct same-host evidence now improves for `assura-check` too. | `ssh vps 'python3 ... 80-run manual-many-scopes A/B with bootstrap delta ...'`; `/home/ubuntu/data/projects/assura-bench-current/target/release/assura`; `/home/ubuntu/data/projects/assura-bench-current/target/release/assura-check`; `/home/ubuntu/data/projects/assura-bench-configured-dir-set/target/release/assura`; `/home/ubuntu/data/projects/assura-bench-configured-dir-set/target/release/assura-check` |
| 2026-07-05 | Refreshed the checked LS-Lint comparison artifacts from the retained structure slice and moved the public website data with them. I rebuilt the full release bundle in the clean `vps` snapshot, regenerated `benches/history/current.json` plus the matching website/history files there, and copied the refreshed tracked artifacts back into the repo only after the same-host no-slower gate and `performance_report_contract_tests` both passed on `vps`. The checked headline claim is still `not-complete`, but the refreshed artifact is materially stronger than the old noisy macOS one: `claim_summary.total_assura_runtime_ms` dropped from `74.68 ms` to `39.66 ms`, `total_ls_lint_runtime_ms` dropped from `98.25 ms` to `58.49 ms`, and the warm session total dropped from `4.92 ms` to `2.39 ms`. On the core target row, the checked `many_configured_scopes_regression assura-cli` value moved from `41.67 ms` to `23.54 ms`, while the matching checked `assura-check-cli` row moved from `41.11 ms` to `23.47 ms`; the phase rows moved to `config-load 6.07 ms`, `checker-init 2.09 ms`, and `walk-and-validate 5.00 ms`. This refresh is partly a code improvement and partly a benchmark-host normalization from the older local artifact to the quieter `vps` snapshot, so that distinction remains important when comparing against older checked history. | `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-configured-dir-set && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output benches/history/current.json --history benches/history/ls-lint-comparison-history.jsonl --website-dir website/public/data/performance --iterations 5'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-configured-dir-set && cargo xtask performance-no-slower benches/history/current.json && cargo test --test performance_report_contract_tests --quiet'`; `scp vps:/home/ubuntu/data/projects/assura-bench-configured-dir-set/benches/history/current.json benches/history/current.json`; `scp vps:/home/ubuntu/data/projects/assura-bench-configured-dir-set/benches/history/ls-lint-comparison-history.jsonl benches/history/ls-lint-comparison-history.jsonl`; `scp vps:/home/ubuntu/data/projects/assura-bench-configured-dir-set/website/public/data/performance/current.json website/public/data/performance/current.json`; `scp vps:/home/ubuntu/data/projects/assura-bench-configured-dir-set/website/public/data/performance/ls-lint-comparison-history.jsonl website/public/data/performance/ls-lint-comparison-history.jsonl`; `cmp -s benches/history/current.json website/public/data/performance/current.json`; `cmp -s benches/history/ls-lint-comparison-history.jsonl website/public/data/performance/ls-lint-comparison-history.jsonl` |
| 2026-07-05 | Rejected the next checker-init hypothesis before it polluted the tracked baseline. The attempted slice reused compiled fast-scope metadata to skip separate configured-dir and naming-regex collection passes inside `CompiledStructureConfig::new_for_check`, but same-host `vps` evidence showed it moved the target structure fixture the wrong way. On comparable 5-iteration `performance-report` runs, `many_configured_scopes_regression` phase rows all worsened (`config-load 4.19 ms` -> `5.31 ms`, `checker-init 1.76 ms` -> `1.98 ms`, `walk-and-validate 5.28 ms` -> `5.51 ms`). The higher-sample direct 80-run loop on `target/performance/manual-many-scopes` confirmed the regression on the public launcher: `assura check --format json` median moved from `22.70 ms` to `27.79 ms` (`+22.4%`). I reverted the local code change immediately and kept only this rejection log so the next pass stays focused on a different shared structure-path hypothesis. | `cargo test -p assura fast_scope_helpers_match_runtime_fast_check_metadata --lib --quiet`; `ssh vps 'cp -a /home/ubuntu/data/projects/assura-bench-configured-dir-set /home/ubuntu/data/projects/assura-bench-init-reuse-base && cp -a /home/ubuntu/data/projects/assura-bench-configured-dir-set /home/ubuntu/data/projects/assura-bench-init-reuse-patched'`; `scp /tmp/assura-compiled_config-head.rs vps:/home/ubuntu/data/projects/assura-bench-init-reuse-base/src/cli/check/compiled_config.rs`; `scp src/cli/check/compiled_config.rs vps:/home/ubuntu/data/projects/assura-bench-init-reuse-patched/src/cli/check/compiled_config.rs`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-init-reuse-base && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-init-reuse-patched && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-init-reuse-base && target/release/assura performance-report --output target/performance/init-reuse-base.json --history target/performance/init-reuse-base.jsonl --website-dir target/performance/init-reuse-base-website --iterations 5'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-init-reuse-patched && target/release/assura performance-report --output target/performance/init-reuse-patched.json --history target/performance/init-reuse-patched.jsonl --website-dir target/performance/init-reuse-patched-website --iterations 5'`; `ssh vps 'python3 ... 80-run manual-many-scopes assura A/B loop ...'` |
| 2026-07-05 | Rejected another shared structure-path hypothesis after a clean same-host `vps` A/B. This slice fused the repeated config-tree metadata collectors for configured dirs, naming regexes, glob patterns, and direct-count detection into one recursive pass before LS-Lint fast-scope compilation. The no-slower gate still passed, but the target structure rows regressed enough to reject it. On comparable 5-iteration `performance-report` runs, `many_configured_scopes_regression assura-cli` worsened from `19.59 ms` to `20.40 ms` (`+4.1%`) and `assura-check-cli` worsened from `19.01 ms` to `20.35 ms` (`+7.0%`). The phase rows also all moved the wrong way on the target fixture: `config-load` `3.73 ms` -> `7.71 ms`, `checker-init` `1.60 ms` -> `3.09 ms`, and `walk-and-validate` `4.68 ms` -> `6.58 ms`. Spillover was also bad on `web_app`, where `assura-cli` regressed from `1.63 ms` to `2.03 ms` (`+24.2%`) and `assura-check-cli` from `1.65 ms` to `1.77 ms` (`+7.5%`). I reverted the local code change immediately and kept only this rejection note so the next pass does not revisit this fused-metadata approach. | `cargo test -p assura fused_metadata_collection_matches_legacy_collectors --lib --quiet`; `scp src/cli/check/compiled_config.rs vps:/home/ubuntu/data/projects/assura-bench-fused-metadata-patched/src/cli/check/compiled_config.rs`; `scp src/cli/check/rules.rs vps:/home/ubuntu/data/projects/assura-bench-fused-metadata-patched/src/cli/check/rules.rs`; `scp src/cli/check/patterns.rs vps:/home/ubuntu/data/projects/assura-bench-fused-metadata-patched/src/cli/check/patterns.rs`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-fused-metadata-base && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-fused-metadata-patched && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-fused-metadata-base && target/release/assura performance-report --output target/performance/fused-metadata-base.json --history target/performance/fused-metadata-base.jsonl --website-dir target/performance/fused-metadata-base-website --iterations 5'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-fused-metadata-patched && target/release/assura performance-report --output target/performance/fused-metadata-patched.json --history target/performance/fused-metadata-patched.jsonl --website-dir target/performance/fused-metadata-patched-website --iterations 5'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-fused-metadata-patched && cargo xtask performance-no-slower target/performance/fused-metadata-patched.json'` |
| 2026-07-05 | Rejected the follow-up `ls_fast_plan` descendant-rule reuse hypothesis because it did not produce a stable quoted test-case win on `vps`. The idea was narrow: when stripping direct-content policy would be a no-op, reuse the already-compiled exact fast rules instead of building a second descendant rule set. Focused local tests passed, and the first 10-iteration same-host `performance-report` pass looked promising on the public target row (`many_configured_scopes_regression assura-cli` `23.74 ms` -> `18.59 ms`) while still passing `cargo xtask performance-no-slower`. But the confirmation pass on the already-built binaries with the run order reversed flipped the result hard enough to reject the slice: `many_configured_scopes_regression assura-cli` moved `18.45 ms` -> `23.38 ms` (`+26.7%`), `assura-check-cli` moved `19.30 ms` -> `19.71 ms` (`+2.2%`), and the target phase rows all regressed (`config-load` `3.85 ms` -> `6.95 ms`, `checker-init` `1.59 ms` -> `2.70 ms`, `walk-and-validate` `3.78 ms` -> `5.37 ms`). Spillover also looked bad on smaller fixtures in the confirmation pass: `web_app assura-cli` `1.53 ms` -> `1.86 ms` and `simple_library assura-cli` `1.50 ms` -> `1.82 ms`. Because the quoted per-test-case evidence is unstable and order-sensitive, I reverted the local code and did not count this as structure-lane progress. | `cargo test -p assura fast_plan_reuses_exact_rules_for_descendants_without_direct_content_policy --lib --quiet`; `cargo test -p assura fast_plan_strips_direct_content_policy_for_descendants --lib --quiet`; `cargo test -p assura ls_fast_plan --lib --quiet`; `rsync -az --delete --exclude '.git' --exclude 'target' --exclude 'website/node_modules' /Users/nroth/workspace/assura/ vps:/home/ubuntu/data/projects/assura-bench-ls-fast-plan-base/`; `ssh vps 'cp -a /home/ubuntu/data/projects/assura-bench-ls-fast-plan-base /home/ubuntu/data/projects/assura-bench-ls-fast-plan-patched'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-ls-fast-plan-base && patch -p1 -R < ls_fast_plan.patch'`; `/home/ubuntu/data/projects/assura-bench-ls-fast-plan-base/target/performance/ls-fast-plan-base.json`; `/home/ubuntu/data/projects/assura-bench-ls-fast-plan-patched/target/performance/ls-fast-plan-patched.json`; `/home/ubuntu/data/projects/assura-bench-ls-fast-plan-base/target/performance/ls-fast-plan-base-confirm.json`; `/home/ubuntu/data/projects/assura-bench-ls-fast-plan-patched/target/performance/ls-fast-plan-patched-confirm.json`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-ls-fast-plan-patched && cargo xtask performance-no-slower target/performance/ls-fast-plan-patched.json'` |
| 2026-07-05 | Rejected another structure-side metadata hypothesis after same-host `vps` proof stayed split across the two public cold launchers. This slice stopped the fast runtime/compiler paths from recursing the config tree for regex/glob/count metadata when LS-Lint fast scopes were already available to summarize the needed patterns. Local scoped tests passed, and the first 10-iteration `vps` report showed the target internal phases improving on `many_configured_scopes_regression` (`config-load` `4.52 ms` -> `4.12 ms`, `checker-init` `1.78 ms` -> `1.67 ms`, `walk-and-validate` `5.71 ms` -> `4.75 ms`), while `assura-check-cli` also improved (`22.00 ms` -> `20.72 ms`). But the same pass regressed the public `assura-cli` row badly (`17.47 ms` -> `21.74 ms`, `+24.5%`) and worsened nearby rows like `web_app assura-cli` (`1.66 ms` -> `1.76 ms`) and `simple_library assura-cli` (`1.74 ms` -> `1.85 ms`). The confirmation pass with run order reversed flipped the public split instead of resolving it: `many_configured_scopes_regression assura-cli` improved (`21.12 ms` -> `19.35 ms`, `-8.4%`), but `assura-check-cli` regressed hard (`19.89 ms` -> `25.80 ms`, `+29.7%`). The no-slower gate still passed, but the quoted per-test-case outcome is too unstable to count as progress, so I reverted the code and kept only this rejection log. | `cargo test -p assura fast_scope_metadata_collectors_match_runtime_fast_check_metadata --lib --quiet`; `cargo test -p assura ls_fast_plan --lib --quiet`; `cargo test -p assura compiled_config --lib --quiet`; `cargo test -p assura compiled_artifact --lib --quiet`; `rsync -az --delete --exclude '.git' --exclude 'target' --exclude 'website/node_modules' /Users/nroth/workspace/assura/ vps:/home/ubuntu/data/projects/assura-bench-fast-metadata-base/`; `ssh vps 'cp -a /home/ubuntu/data/projects/assura-bench-fast-metadata-base /home/ubuntu/data/projects/assura-bench-fast-metadata-patched'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-fast-metadata-base && patch -p1 -R < fast-metadata-slice.patch'`; `/home/ubuntu/data/projects/assura-bench-fast-metadata-base/target/performance/fast-metadata-base.json`; `/home/ubuntu/data/projects/assura-bench-fast-metadata-patched/target/performance/fast-metadata-patched.json`; `/home/ubuntu/data/projects/assura-bench-fast-metadata-base/target/performance/fast-metadata-base-confirm.json`; `/home/ubuntu/data/projects/assura-bench-fast-metadata-patched/target/performance/fast-metadata-patched-confirm.json`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-fast-metadata-patched && cargo xtask performance-no-slower target/performance/fast-metadata-patched.json'` |
| 2026-07-05 | Rejected a configured-structure skip hypothesis after same-host `vps` evidence stayed order-sensitive instead of producing a stable public fixture win. The idea was to precompute whether the config had any required-path checks and skip `validate_configured_structure` entirely for naming-only LS-Lint-converted trees like `many_configured_scopes_regression`. Focused local tests passed, and one 40-run `vps` pass looked promising on the public launcher (`assura check` `22.95 ms` -> `21.41 ms`, `-6.7%`). But the same pass already had a conflicting signal: `assura-check` moved `22.99 ms` -> `23.37 ms` (`+1.7%`). The confirmation pass with the order reversed broke the public story completely: patched `assura check` landed at `23.83 ms` while base fell to `19.74 ms` (`+20.7%` patched versus base), and patched `assura-check` also worsened from `24.03 ms` to `27.93 ms` (`+16.2%`). The in-process medians did improve in the reversed run (`total` `14.54 ms` -> `13.10 ms`, `configured-structure` `2.10 ms` -> `1.97 ms`, `walk-and-validate` `7.82 ms` -> `7.15 ms`), but because the quoted public cold rows are unstable by run order, I reverted the code and kept only this rejection note. | `cargo test -p assura configured_structure --lib --quiet`; `cargo test -p assura fast_plan_supports_wildcard_directory_scopes --lib --quiet`; `cargo test -p assura profiled_check_reports_stage_timings_without_changing_result --test cli_check_tests --quiet`; `ssh vps '... 40-run base/patched many-scopes manual A/B plus in-process timing probe ...'`; `ssh vps '... 40-run patched/base confirmation A/B plus in-process timing probe ...'` |
| 2026-07-05 | Rejected a recursive fast-rule reuse hypothesis after balanced same-host `vps` A/B still showed the public target case regressing. The slice threaded already-resolved `FastRules` through the recursive LS-Lint fast walk so each child directory would not immediately re-look up its own scope on entry. Local behavior tests passed. Two ordered 60-run `vps` passes showed real in-process gains on `many_configured_scopes_regression` (`walk-and-validate` roughly `11.99 ms` -> `7.22 ms` in one pass and `9.19 ms` -> `7.84 ms` in the reverse-order pass), but the cold public launcher rows did not hold up. The first ordered pass regressed `assura check` from `24.19 ms` to `25.58 ms` (`+5.8%`) while slightly improving `assura-check` from `26.64 ms` to `26.35 ms` (`-1.1%`). The reverse-order pass flipped both public rows positive (`assura check` `24.76 ms` -> `24.20 ms`, `-2.3%`; `assura-check` `29.68 ms` -> `24.24 ms`, `-18.3%`), so I ran one more 80-pair interleaved A/B with balanced order. That higher-signal pass settled the public result in the wrong direction: `assura check` `23.58 ms` -> `24.52 ms` (`+4.0%`) and `assura-check` `24.40 ms` -> `26.13 ms` (`+7.1%`). Because the quoted public rows still regress after balancing order, I reverted the code and did not count the internal walk-phase win as product progress. | `cargo test --test cli_check_tests check_validates_converted_ls_lint_dir_rules --quiet`; `cargo test --test cli_check_tests profiled_check_reports_stage_timings_without_changing_result --quiet`; `scp src/cli/check/ls_fast.rs vps:/home/ubuntu/data/projects/assura-bench-fast-rules-patched/src/cli/check/ls_fast.rs`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-fast-rules-base && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-fast-rules-patched && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli'`; `ssh vps '... 60-run base/patched many-scopes manual A/B plus in-process timing probe ...'`; `ssh vps '... 60-run patched/base confirmation A/B plus in-process timing probe ...'`; `ssh vps '... 80-pair interleaved balanced-order many-scopes A/B ...'` |
| 2026-07-05 | Rejected a lazy path-stringification hypothesis after a clean balanced `vps` A/B still regressed both public cold rows on the target fixture. The idea was to stop allocating `rel_to_string(...)` for every fast-path directory and file naming check when the compiled naming rule was a simple static case that never substitutes `${ancestor}` path segments. Local focused tests passed. But the first balanced 80-pair same-host A/B on `many_configured_scopes_regression` moved the public rows in the wrong direction: `assura check` `24.47 ms` -> `25.01 ms` (`+2.2%`) and `assura-check` `23.93 ms` -> `28.08 ms` (`+17.4%`). Because the quoted public rows regressed immediately under the higher-signal balanced loop, I reverted the code and did not spend additional benchmark cycles on this slice. | `cargo test -p assura fast_naming_simplifies_common_regex_literals --lib --quiet`; `cargo test --test cli_check_tests profiled_check_reports_stage_timings_without_changing_result --quiet`; `scp src/cli/check/ls_fast.rs src/cli/check/ls_fast_naming.rs vps:/home/ubuntu/data/projects/assura-bench-lazy-path-patched/src/cli/check/`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-lazy-path-patched && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli'`; `ssh vps '... 80-pair interleaved balanced-order many-scopes A/B ...'` |
| 2026-07-05 | Strengthened the rejection evidence for the recursive fast-rule reuse slice by removing the fixture-path confound from the comparison. I copied the same generated `manual-many-scopes` project to one shared `vps` path and pointed both the baseline and patched binaries at that exact fixture. Under this same-fixture balanced 80-pair A/B, the public cold rows still regressed: `assura check` `22.49 ms` -> `22.65 ms` (`+0.8%`) and `assura-check` `22.85 ms` -> `23.21 ms` (`+1.6%`). The matching in-process probe on the same shared fixture also moved the wrong way (`total` `14.03 ms` -> `14.99 ms`, `walk-and-validate` `7.58 ms` -> `8.07 ms`, `checker-init` `1.68 ms` -> `1.81 ms`). That closes the door on this candidate more cleanly than the earlier repo-local comparisons: the slice is not just noisy under different repo roots, it is genuinely not a win on the same structure fixture path either. | `ssh vps 'cp -a /home/ubuntu/data/projects/assura-bench-configured-dir-set /home/ubuntu/data/projects/assura-bench-fast-rules-samepath && cp -a /home/ubuntu/data/projects/assura-bench-configured-dir-set/target/performance/manual-many-scopes /home/ubuntu/data/projects/manual-many-scopes-common'`; `ssh vps 'python3 ... patch recursive fast-rule reuse into assura-bench-fast-rules-samepath/src/cli/check/ls_fast.rs ...'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-fast-rules-samepath && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli'`; `ssh vps '... 80-pair interleaved balanced-order same-fixture A/B against /home/ubuntu/data/projects/manual-many-scopes-common ...'`; `ssh vps '... 80-run in-process timing probe against /home/ubuntu/data/projects/manual-many-scopes-common ...'` |
| 2026-07-05 | Rejected an exact-self-directory lookup shortcut after it again improved only internal sub-rows while making the public structure fixture worse. The idea was to let LS-Lint fast directory validation reuse the parent fast rules for configured child directories that only inherit file rules, avoiding an exact child-scope lookup during the parent-side self-directory check. Local behavior checks stayed green, and the same-fixture `vps` A/B against `/home/ubuntu/data/projects/manual-many-scopes-common` looked only slightly negative on the public launchers: `assura check` `22.17 ms` -> `22.35 ms` (`+0.8%`) and `assura-check` `23.02 ms` -> `23.30 ms` (`+1.2%`). The official 3-iteration `performance-report` comparison was decisively worse on the named structure case, so I reverted the slice. On `many_configured_scopes_regression`, `assura-cli` regressed from `18.91 ms` to `28.55 ms` (`+51.0%`) and `assura-check-cli` regressed from `18.73 ms` to `26.72 ms` (`+42.6%`), even though `assura:phase:config-load` improved from `4.18 ms` to `3.68 ms` (`-12.1%`) and `assura:phase:walk-and-validate` improved from `4.61 ms` to `4.29 ms` (`-7.0%`). Spillover was also poor on adjacent structure fixtures, including `simple_medium assura-cli` `3.53 ms` -> `3.84 ms`, `simple_medium assura-check-cli` `2.72 ms` -> `3.45 ms`, `monorepo_packages assura-cli` `1.76 ms` -> `2.03 ms`, and `monorepo_policy assura-check-cli` `2.59 ms` -> `3.35 ms`. This is another case where better phase accounting does not translate into a shippable beta-facing structure win. | `cargo test --lib compiled_config::tests::exact_self_directory_rule_set_skips_inherited_file_only_children --quiet`; `cargo test --test cli_check_tests check_validates_converted_ls_lint_dir_rules --quiet`; `cargo test --test cli_check_tests profiled_check_reports_stage_timings_without_changing_result --quiet`; `cargo test --test ls_lint_rule_coverage_tests converted_lslint_dir_rule_validates_scoped_directory_itself --quiet`; `rsync -az --delete --exclude target /tmp/assura-bench-base.8EtYot/ vps:/home/ubuntu/data/projects/assura-bench-exact-self-base/`; `rsync -az --delete --exclude target /tmp/assura-bench-patched.GuYYJd/ vps:/home/ubuntu/data/projects/assura-bench-exact-self-patched/`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-exact-self-base && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release -p assura-check-cli && cargo build --release --bin assura-full'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-bench-exact-self-patched && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release -p assura-check-cli && cargo build --release --bin assura-full'`; `ssh vps 'python3 ... 80-pair interleaved same-fixture A/B for assura and assura-check against /home/ubuntu/data/projects/manual-many-scopes-common ...'`; `/home/ubuntu/data/projects/assura-bench-exact-self-base/target/performance/exact-self-base.json`; `/home/ubuntu/data/projects/assura-bench-exact-self-patched/target/performance/exact-self-patched.json` |
| 2026-07-05 | Revalidated the live dirty-tree native/content lane on the quieter `vps` host against clean `HEAD` and finally captured the incremental gains by named test case. I copied clean `HEAD` and the current dirty worktree to separate `vps` directories, built the same release bundle in both, and ran identical 5-iteration `performance-report --suite native` commands. The dirty tree is a real win on the highest-impact large native case: `native_large assura-full check --format json` improved from `216.99 ms` to `193.20 ms` (`-11.0%`), `native_large object load and validation` improved from `85.48 ms` to `71.56 ms` (`-16.3%`), and `native_large repository validation total` improved from `95.88 ms` to `84.45 ms` (`-11.9%`). The previously optimized project-intelligence rows stayed dramatically better than clean `HEAD`: `native_large fact ingest and index load` `2323.72 ms` -> `130.02 ms` (`-94.4%`), `native_large incremental generation replace` `2441.87 ms` -> `122.19 ms` (`-95.0%`), `native_large content search` `3592.45 ms` -> `919.32 ms` (`-74.4%`), and `native_large agent-query keyword-search` `3398.52 ms` -> `891.84 ms` (`-73.8%`). The current dirty tree is not ready to count as a finished native refresh yet, though, because the same proof regressed the smaller `native_real_project` validation slice: `assura-full check --format json` `43.27 ms` -> `46.26 ms` (`+6.9%`), `object load and validation` `3.15 ms` -> `4.82 ms` (`+53.2%`), and `repository validation total` `3.58 ms` -> `5.58 ms` (`+55.8%`). `native_real_project` did still improve slightly on facts ingest (`5.29 ms` -> `5.25 ms`) and meaningfully on incremental generation replace (`3.70 ms` -> `2.88 ms`, `-22.1%`). That narrows the next native follow-up: preserve the large-project content-runtime wins, but recover the small real-project validation regression before treating this tree as the new checked native baseline. | `git archive HEAD | tar -x -C /tmp/assura-native-base.wt7z8G`; `rsync -a --delete --exclude .git --exclude target ./ /tmp/assura-native-patched.6G9Dvj/`; `rsync -az --delete --exclude target /tmp/assura-native-base.wt7z8G/ vps:/home/ubuntu/data/projects/assura-native-proof-base/`; `rsync -az --delete --exclude target /tmp/assura-native-patched.6G9Dvj/ vps:/home/ubuntu/data/projects/assura-native-proof-patched/`; `ssh vps 'cd ~/data/projects/assura-native-proof-base && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --suite native --output target/performance/native-proof-base.json --history target/performance/native-proof-base.jsonl --website-dir target/performance/native-proof-base-website --iterations 5'`; `ssh vps 'cd ~/data/projects/assura-native-proof-patched && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --suite native --output target/performance/native-proof-patched.json --history target/performance/native-proof-patched.jsonl --website-dir target/performance/native-proof-patched-website --iterations 5'`; `/home/ubuntu/data/projects/assura-native-proof-base/target/performance/native-proof-base.json`; `/home/ubuntu/data/projects/assura-native-proof-patched/target/performance/native-proof-patched.json` |
| 2026-07-05 | Strengthened the native `real_project` conclusion with higher-signal manual probes before treating that 5-iteration suite regression as real. I added a temporary `native_real_project_probe` only to the ephemeral comparison trees on `vps`, recreated the same `native_real_project` fixture shape there, and measured the public validation core for 100 iterations. In that isolated probe, the dirty tree's validation path was actually slightly better than clean `HEAD`: `repository_validate_total_ms` improved from `3.547 ms` to `3.395 ms` (`-4.3%`), while `fact_ingest_load_ms` improved from `5.143 ms` to `3.098 ms` (`-39.8%`) and `incremental_replace_generation_ms` improved from `3.600 ms` to `1.693 ms` (`-53.0%`). I then kept one recreated fixture alive and ran a balanced 80-pair manual CLI A/B against that exact path using the two `assura-full` binaries. That shared-fixture cold check also favored the dirty tree slightly: `assura-full check --format json` median moved from `38.18 ms` to `37.76 ms` (`-1.1%`). So the earlier `native_real_project` regression from the 5-iteration native suite is not stable enough to drive the next code slice by itself. The highest-confidence remaining native hotspot is still the large-project validation lane, especially `native_large object load and validation` and `native_large repository validation total`, where the same-host proof showed durable double-digit wins but the checked artifact still needs a reviewer-backed refresh path. | temporary helper `src/bin/native_real_project_probe.rs` only inside `/tmp/assura-native-base.wt7z8G` and `/tmp/assura-native-patched.6G9Dvj`; `ssh vps 'cd ~/data/projects/assura-native-proof-base && cargo run --release --bin native_real_project_probe --features full-cli -- 100'`; `ssh vps 'cd ~/data/projects/assura-native-proof-patched && cargo run --release --bin native_real_project_probe --features full-cli -- 100'`; `ssh vps 'cd ~/data/projects/assura-native-proof-base && ASSURA_KEEP_FIXTURE=1 cargo run --release --bin native_real_project_probe --features full-cli -- 1'`; `ssh vps 'cd ~/data/projects/assura-native-proof-patched && ASSURA_KEEP_FIXTURE=1 cargo run --release --bin native_real_project_probe --features full-cli -- 1'`; `ssh vps 'python3 ... balanced 80-pair assura-full check A/B against /tmp/assura-native-real-project-probe-1700315-1783240549981250697 ...'` |
| 2026-07-05 | Landed another structure-core slice and tightened the evidence language after review. The code under test now includes two retained structure-path changes in `check.rs`: the earlier hybrid `ConfiguredDirIndex` membership path and a new exact/ancestor index for literal `rule_scopes`, while configs with any scope-magic path still fall back to the old linear rule resolution path. On `vps`, I compared the current dirty tree against an otherwise identical copy with those structure-core changes reversed in the active `check.rs` / `rule_plan.rs` path, then built the same release bundle and ran identical 5-iteration cold `performance-report` commands. The quoted per-test-case gains below are from that temporary same-host A/B report, not from the checked public artifact surfaces: `simple_library` `1.690 ms` -> `1.418 ms` (`-16.1%`), `monorepo_packages` `1.790 ms` -> `1.702 ms` (`-4.9%`), `monorepo_policy` `2.972 ms` -> `2.542 ms` (`-14.5%`, passing the 2x target in that temporary report), `rule_heavy_repo` `2.268 ms` -> `1.738 ms` (`-23.4%`), `ignored_generated_heavy_repo` `1.709 ms` -> `1.277 ms` (`-25.3%`), `multipart_extension_regression` `3.456 ms` -> `3.189 ms` (`-7.8%`), and `many_configured_scopes_regression` `19.481 ms` -> `19.184 ms` (`-1.5%`). `web_app` was effectively flat-to-noisy (`1.685 ms` -> `1.690 ms`, `+0.3%`). On the targeted structure-heavy fixture, the more diagnostic rows moved in the expected direction: `assura-check-cli` `20.30 ms` -> `19.36 ms` (`-4.6%`), `assura-in-process` `11.49 ms` -> `10.74 ms` (`-6.6%`), `config-load` `4.39 ms` -> `4.15 ms` (`-5.4%`), `checker-init` `1.99 ms` -> `1.72 ms` (`-13.6%`), and `walk-and-validate` `5.30 ms` -> `4.89 ms` (`-7.6%`). The tiny `configured-structure` row regressed from `0.0177 ms` to `0.0326 ms`, but that stayed deep in noise while the temporary report total still improved. In that same temporary A/B report, the cold claim summary moved from `2` to `3` fixtures meeting the 2x headline target and total Assura runtime on the `realistic-equivalent` cohort dropped from `35.05 ms` to `32.74 ms` (`-6.6%`). I then reran the standard tracked refresh command inside the patched repo copy on `vps`; that standalone report still improved several named rows versus the current checked artifact (`many_configured_scopes_regression` `23.54 ms` -> `21.01 ms`, `monorepo_policy` `3.18 ms` -> `2.72 ms`, `rule_heavy_repo` `2.27 ms` -> `1.98 ms`) but it fell back to `2` headline 2x passes and kept a few small-fixture regressions (`simple_library`, `web_app`, `ignored_generated_heavy_repo`). So I am keeping the code and the quoted A/B proof, but I am not refreshing `benches/history/current.json` or website data from this slice yet. Review also asked for checker-level fallback coverage, so I added a mixed literal-plus-dynamic scope test through `StructureChecker::resolve_rules` to prove the full-validator branch still disables indexing when any scope-magic path is present. I also ran the same A/B through the native suite, but those results were mixed on the validation subrows, so I am counting this as a structure-lane win only. | `cargo check --lib --quiet`; `cargo test --lib indexed_rules_match_linear_resolution_for_literal_scopes --quiet`; `cargo test --lib dynamic_scopes_do_not_build_rule_index --quiet`; `cargo test --lib mixed_literal_and_dynamic_scopes_disable_rule_index_but_preserve_resolution --quiet`; `cargo test --test cli_check_tests check_validates_converted_ls_lint_dir_rules --quiet`; `cargo test --test cli_check_tests profiled_check_reports_stage_timings_without_changing_result --quiet`; `cargo run --quiet -- check --format json .`; `git diff --check`; independent review agents `Locke`, `Euclid`, `Laplace`; `git diff -- src/cli/check.rs src/cli/check/rule_plan.rs > /tmp/assura-rule-scope-index.patch`; `rsync -a --delete --exclude target --exclude .git /Users/nroth/workspace/assura/ /tmp/assura-rule-scope-patched.D2iTh5/`; `rsync -a --delete --exclude target --exclude .git /Users/nroth/workspace/assura/ /tmp/assura-rule-scope-base.rhCJY2/`; `cd /tmp/assura-rule-scope-base.rhCJY2 && patch -p1 -R < /tmp/assura-rule-scope-index.patch`; `rsync -az --delete --exclude target /tmp/assura-rule-scope-patched.D2iTh5/ vps:~/data/projects/assura-rule-scope-patched/`; `rsync -az --delete --exclude target /tmp/assura-rule-scope-base.rhCJY2/ vps:~/data/projects/assura-rule-scope-base/`; `ssh vps 'cd ~/data/projects/assura-rule-scope-base && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/rule-scope-current.json --history target/performance/rule-scope-current.jsonl --website-dir target/performance/website-data --iterations 5 && target/release/assura performance-report --suite native --output target/performance/native-rule-scope.json --history target/performance/native-rule-scope.jsonl --website-dir target/performance/native-website-data --iterations 5'`; `ssh vps 'cd ~/data/projects/assura-rule-scope-patched && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/rule-scope-current.json --history target/performance/rule-scope-current.jsonl --website-dir target/performance/website-data --iterations 5 && target/release/assura performance-report --suite native --output target/performance/native-rule-scope.json --history target/performance/native-rule-scope.jsonl --website-dir target/performance/native-website-data --iterations 5 && cargo xtask performance-no-slower target/performance/rule-scope-current.json && target/release/assura performance-report --output benches/history/current.json --history benches/history/ls-lint-comparison-history.jsonl --website-dir website/public/data/performance --iterations 5'`; `/home/ubuntu/data/projects/assura-rule-scope-base/target/performance/rule-scope-current.json`; `/home/ubuntu/data/projects/assura-rule-scope-patched/target/performance/rule-scope-current.json`; `/home/ubuntu/data/projects/assura-rule-scope-base/target/performance/native-rule-scope.json`; `/home/ubuntu/data/projects/assura-rule-scope-patched/target/performance/native-rule-scope.json`; `/home/ubuntu/data/projects/assura-rule-scope-patched/benches/history/current.json` |
| 2026-07-05 | Rejected a semantic-validation cache experiment after a same-state `vps` A/B showed it hurt the exact structure lane we were targeting. I copied the current dirty tree to `~/data/projects/assura-perf-config-cache-after`, cloned that to `...-before`, reversed only the config-validation cache slice in the baseline copy, built the same release bundle in both, and ran identical 3-iteration cold `performance-report` commands. The targeted fixture clearly regressed under the patched tree: `many_configured_scopes_regression assura-cli` `19.196 ms` -> `19.703 ms` (`+2.6%`), `assura-check-cli` `18.974 ms` -> `20.891 ms` (`+10.1%`), `assura-in-process` `10.145 ms` -> `12.892 ms` (`+27.1%`), `assura:phase:config-load` `3.917 ms` -> `6.206 ms` (`+58.4%`), `assura:phase:checker-init` `1.648 ms` -> `2.512 ms` (`+52.4%`), and `assura:phase:walk-and-validate` `4.643 ms` -> `4.858 ms` (`+4.6%`). Some smaller fixtures did improve on the public cold row, including `simple_library` `1.833 ms` -> `1.415 ms` (`-22.8%`), `monorepo_policy` `2.962 ms` -> `2.445 ms` (`-17.4%`), `rule_heavy` `4.210 ms` -> `3.781 ms` (`-10.2%`), and `real_project_agentic_feedback` `2.300 ms` -> `2.047 ms` (`-11.0%`), but that is not enough to justify a regression on the named structure hotspot. Other public rows also moved the wrong way, including `web_app assura-cli` `1.652 ms` -> `1.778 ms` (`+7.6%`) and `monorepo_large assura-check-cli` `6.750 ms` -> `9.211 ms` (`+36.5%`). I reverted the cache slice locally and kept the earlier retained rule-scope/configured-dir structure work unchanged. The next structure attempt should stay focused on `many_configured_scopes_regression`, and it should only land if the public cold rows improve along with the phase rows on the same quieter host. | `cargo run --quiet -- check --format json .`; `git diff -- src/config/config/validation.rs src/config/config/validation_caches.rs src/config/config/validation_extensions.rs > /tmp/assura-config-cache-slice.patch`; `rsync -az --delete --exclude .git --exclude target --exclude website/node_modules --exclude .trellis/.runtime ./ vps:~/data/projects/assura-perf-config-cache-after/`; `scp /tmp/assura-config-cache-slice.patch vps:~/data/projects/assura-perf-artifacts/assura-config-cache-slice.patch`; `ssh vps 'rsync -a ~/data/projects/assura-perf-config-cache-after/ ~/data/projects/assura-perf-config-cache-before/ && cd ~/data/projects/assura-perf-config-cache-before && git apply -R ~/data/projects/assura-perf-artifacts/assura-config-cache-slice.patch'`; `ssh vps 'cd ~/data/projects/assura-perf-config-cache-before && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/cache-before.json --iterations 3 --baseline-id config-cache-ab-20260705 --suite ls-lint'`; `ssh vps 'cd ~/data/projects/assura-perf-config-cache-after && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/cache-after.json --iterations 3 --baseline-id config-cache-ab-20260705 --suite ls-lint'`; `/home/ubuntu/data/projects/assura-perf-config-cache-before/target/performance/cache-before.json`; `/home/ubuntu/data/projects/assura-perf-config-cache-after/target/performance/cache-after.json`; `git apply -R /tmp/assura-config-cache-slice.patch`; `git diff --check` |
| 2026-07-05 | Rejected a success-path semantic-validation context-builder refactor after a same-state `vps` A/B showed it regressed the exact structure case we were targeting. I copied the current dirty tree to `/home/ubuntu/data/projects/assura-perf-20260705-validation-context/after`, cloned that to `.../before`, reversed only the context-builder slice in the baseline copy, built the same release bundle in both, and ran identical 3-iteration cold `performance-report --suite ls-lint` commands. The target public row got worse: `many_configured_scopes_regression assura-cli` `19.317 ms` -> `21.451 ms` (`+11.1%`) and `assura-check-cli` `19.154 ms` -> `21.190 ms` (`+10.6%`). The in-process core on that same fixture improved slightly (`11.113 ms` -> `10.528 ms`, `-5.3%`) and the named internal phases moved only modestly (`config-load` `4.157 ms` -> `4.207 ms`, `+1.2%`; `checker-init` `1.901 ms` -> `1.797 ms`, `-5.5%`; `walk-and-validate` `4.540 ms` -> `4.486 ms`, `-1.2%`), which is exactly the kind of internal-only movement we do not want to mistake for a beta-facing win. A number of smaller public cold rows did improve, including `simple_medium` `3.912 ms` -> `2.913 ms` (`-25.5%`), `web_app` `1.544 ms` -> `1.227 ms` (`-20.5%`), `monorepo_policy` `2.872 ms` -> `2.449 ms` (`-14.7%`), `real_project_agentic_feedback` `2.311 ms` -> `2.003 ms` (`-13.3%`), and `ignored_generated_heavy_repo` `1.685 ms` -> `1.360 ms` (`-19.3%`), but those are not enough to justify regressing the named structure hotspot. Other public rows also moved the wrong way, including `multipart_extension_regression` `3.380 ms` -> `4.481 ms` (`+32.6%`) and `rule_heavy` `4.517 ms` -> `5.007 ms` (`+10.8%`). I reverted the slice locally and kept the earlier retained structure-lane work unchanged. The next structure attempt should keep using `vps` for proof, but it needs a design that improves the public `many_configured_scopes_regression` launcher rows, not just the in-process core. | `cargo test config::loader --lib --quiet`; `cargo test test_parse_invalid_nested_child_naming_reports_full_context --lib --quiet`; `cargo run --quiet -- check --format json .`; `git diff -- src/config/config/validation.rs src/config/config/validation_context.rs src/config/config/validation_extensions.rs src/config/loader.rs > /tmp/assura-validation-context.patch`; `rsync -az --delete --exclude .git --exclude target ./ vps:/home/ubuntu/data/projects/assura-perf-20260705-validation-context/after/`; `scp /tmp/assura-validation-context.patch vps:/home/ubuntu/data/projects/assura-perf-20260705-validation-context/validation-context.patch`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-validation-context && cp -a after before && cd before && git apply -R ../validation-context.patch'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-validation-context/before && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/before.json --history target/performance/before.jsonl --website-dir target/performance/before-website --iterations 3 --suite ls-lint'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-validation-context/after && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/after.json --history target/performance/after.jsonl --website-dir target/performance/after-website --iterations 3 --suite ls-lint'`; `/home/ubuntu/data/projects/assura-perf-20260705-validation-context/before/target/performance/before.json`; `/home/ubuntu/data/projects/assura-perf-20260705-validation-context/after/target/performance/after.json`; `/tmp/assura-before.json`; `/tmp/assura-after.json`; `git diff --check` |
| 2026-07-05 | Rejected a `resolve_rules` hot-cache simplification after same-state `vps` A/B showed it regressed the exact structure lane we are trying to improve. The experiment replaced the full `HashMap<PathBuf, EffectiveRules>` cache with a one-entry last-path cache on the theory that `many_configured_scopes_regression` mostly resolves the same directory rules in immediate succession. Local focused validation stayed green, but the comparable 3-iteration `performance-report --suite ls-lint` run on `vps` was clearly worse on the target row: `many_configured_scopes_regression assura-cli` regressed from `20.211 ms` to `22.174 ms` (`+9.7%`), `assura-check-cli` regressed from `18.927 ms` to `30.090 ms` (`+59.0%`), `assura-in-process` regressed from `10.942 ms` to `11.806 ms` (`+7.9%`), `config-load` regressed from `4.356 ms` to `4.579 ms` (`+5.1%`), `checker-init` regressed from `1.768 ms` to `1.909 ms` (`+8.0%`), and `walk-and-validate` regressed from `4.649 ms` to `5.192 ms` (`+11.7%`). The tiny `configured-structure` row improved (`0.0254 ms` -> `0.0217 ms`), but that is noise relative to the public regression. Spillover was mostly negative across the public cold rows too: `ignored_generated_heavy` worsened by `+29.3%`, `monorepo_large` by `+9.2%`, `monorepo_packages` by `+9.9%`, `simple_medium` by `+9.7%`, and `web_app` by `+4.2%`. A few cases did improve, including `rule_heavy` (`-21.6%`), `rule_heavy_repo` (`-15.4%`), and `monorepo_policy` (`-6.9%`), but not enough to justify regressing the named hotspot and the paired check-only launcher so badly. I reverted the slice locally and kept the earlier retained structure-lane work unchanged. The next structure attempt should avoid touching the shared rule-resolution cache shape unless it can prove a win on the public cold rows, not just a plausible per-directory locality story. | `cargo test repeated_literal_scope_resolution_reuses_last_path_cache --lib --quiet`; `cargo test mixed_literal_and_dynamic_scopes_disable_rule_index_but_preserve_resolution --lib --quiet`; `cargo run --quiet -- check --format json .`; `git diff --check`; `cp src/cli/check.rs /tmp/assura-check-hotcache-before.rs`; `cp src/cli/check/resolve_rules_tests.rs /tmp/assura-resolve-rules-before.rs`; `patch -p0 -R` against `/home/ubuntu/data/projects/assura-perf-20260705-resolve-rules-hotcache/before`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-resolve-rules-hotcache/before && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/before.json --history target/performance/before.jsonl --website-dir target/performance/before-website --iterations 3 --suite ls-lint'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-resolve-rules-hotcache/after && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/after.json --history target/performance/after.jsonl --website-dir target/performance/after-website --iterations 3 --suite ls-lint'`; `/home/ubuntu/data/projects/assura-perf-20260705-resolve-rules-hotcache/before/target/performance/before.json`; `/home/ubuntu/data/projects/assura-perf-20260705-resolve-rules-hotcache/after/target/performance/after.json`; `/tmp/assura-hotcache-before.json`; `/tmp/assura-hotcache-after.json` |
| 2026-07-05 | Rejected the `ls_fast_plan` exact/descendant rule reuse slice after same-state `vps` A/B showed a small win on the named structure case but broader public cold-row regressions. I copied the current dirty tree to `/home/ubuntu/data/projects/assura-perf-20260705-ls-fast-plan/after`, cloned that to `.../before`, reversed only the `src/cli/check/ls_fast_plan.rs` and `src/cli/check/ls_fast_plan_tests.rs` slice in the baseline copy, built the same release bundle in both, and ran identical 3-iteration cold `performance-report --suite ls-lint` commands. The targeted fixture did improve on several setup rows: `many_configured_scopes_regression assura-cli` `19.632 ms` -> `19.317 ms` (`-1.6%`), `assura-check-cli` `20.619 ms` -> `19.160 ms` (`-7.1%`), `assura-in-process` `15.555 ms` -> `11.550 ms` (`-25.8%`), `config-load` `7.051 ms` -> `4.280 ms` (`-39.3%`), `checker-init` `3.145 ms` -> `1.543 ms` (`-51.0%`), and `configured-structure` `0.0353 ms` -> `0.0219 ms` (`-37.9%`). But the same fixture's `walk-and-validate` row regressed from `5.173 ms` to `5.688 ms` (`+10.0%`), and spillover on the public `assura-cli` row was bad enough to fail the keep bar: `monorepo_large` worsened from `6.687 ms` to `9.724 ms` (`+45.4%`), `real_project_agentic_feedback` from `2.090 ms` to `2.665 ms` (`+27.5%`), `web_app` from `1.319 ms` to `1.537 ms` (`+16.5%`), `multipart_extension_regression` from `3.189 ms` to `3.620 ms` (`+13.5%`), `monorepo_policy` from `2.359 ms` to `2.673 ms` (`+13.3%`), `simple_medium` from `2.979 ms` to `3.203 ms` (`+7.5%`), and `rule_heavy` from `3.986 ms` to `4.266 ms` (`+7.0%`). A few public rows did improve, including `simple_library` (`-15.6%`) and `monorepo_packages` (`-7.1%`), but the aggregate headline also moved the wrong way: `claim_summary.two_x_pass_count` fell from `3` to `2`, and `claim_summary.total_assura_runtime_ms` rose from `34.219 ms` to `34.493 ms` (`+0.8%`). I reverted the slice locally, reran focused validation, and kept the earlier retained structure-lane work unchanged. The next structure attempt should keep targeting the LS-Lint fast path, but it has to improve the named structure row without paying it back as walk/validate or cross-fixture public regressions. | `git diff -- src/cli/check/ls_fast_plan.rs src/cli/check/ls_fast_plan_tests.rs > /tmp/assura-ls-fast-plan-slice.patch`; `rsync -az --delete --exclude .git --exclude target --exclude website/node_modules --exclude .trellis/.runtime ./ vps:/home/ubuntu/data/projects/assura-perf-20260705-ls-fast-plan/after/`; `scp /tmp/assura-ls-fast-plan-slice.patch vps:/home/ubuntu/data/projects/assura-perf-20260705-ls-fast-plan/artifacts/assura-ls-fast-plan-slice.patch`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-ls-fast-plan && cp -a after before && cd before && patch -p1 -R < ../artifacts/assura-ls-fast-plan-slice.patch'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-ls-fast-plan/before && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/before.json --history target/performance/before.jsonl --website-dir target/performance/before-website --iterations 3 --suite ls-lint && cd /home/ubuntu/data/projects/assura-perf-20260705-ls-fast-plan/after && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/after.json --history target/performance/after.jsonl --website-dir target/performance/after-website --iterations 3 --suite ls-lint'`; `/home/ubuntu/data/projects/assura-perf-20260705-ls-fast-plan/before/target/performance/before.json`; `/home/ubuntu/data/projects/assura-perf-20260705-ls-fast-plan/after/target/performance/after.json`; `/tmp/assura-ls-fast-before.json`; `/tmp/assura-ls-fast-after.json`; `cargo test fast_plan_allows_direct_file_and_directory_policies --lib --quiet`; `cargo run --quiet -- check --format json .`; `git diff --check` |
| 2026-07-05 | Rejected an LS-Lint fast-walk recursion lookup reuse slice after a stricter same-fixture manual benchmark disproved the first suite-level signal. The change threaded already-computed child `FastRules` through `walk_lslint_fast_dir` so recursive descent would not look up the same child scope twice. Local focused validation stayed green. The first 3-iteration `performance-report --suite ls-lint` comparison on `vps` looked promising on the named structure row: `many_configured_scopes_regression assura-cli` improved from `23.625 ms` to `19.773 ms` (`-16.3%`), `assura-in-process` improved from `14.945 ms` to `11.534 ms` (`-22.8%`), `config-load` improved from `6.299 ms` to `4.598 ms` (`-27.0%`), `checker-init` improved from `2.644 ms` to `1.928 ms` (`-27.1%`), and `walk-and-validate` improved from `5.666 ms` to `4.964 ms` (`-12.4%`). But that same report also showed `assura-check-cli` regressing on the exact target row from `19.289 ms` to `26.059 ms` (`+35.1%`) and several adjacent public `assura-cli` rows moving the wrong way, including `rule_heavy_repo` (`+27.8%`), `real_project_agentic_feedback` (`+22.5%`), `simple_library` (`+21.2%`), `rule_heavy` (`+18.2%`), and `monorepo_policy` (`+17.5%`). Because the target-row evidence was internally contradictory, I recreated the exact `many_configured_scopes_regression` fixture once on `vps`, generated the Assura config with `assura-full migrate`, and ran 25 interleaved manual timings against the same shared fixture using the built `assura-full` and `assura-check` binaries. That tighter proof showed the slice was actually slower on both public launchers: `assura-full check --format json` median worsened from `23.26 ms` to `24.20 ms` (`+4.0%`), and `assura-check --format json` worsened from `20.33 ms` to `21.45 ms` (`+5.5%`). I reverted the slice locally and kept the earlier retained structure-lane work unchanged. The next structure attempt should not rely on suite-level noise alone when `many_configured_scopes_regression` and `assura-check-cli` disagree; it should confirm the target row with a same-fixture manual A/B before we consider keeping the code. | `git diff -- src/cli/check/ls_fast.rs > /tmp/assura-ls-fast-recursion.patch`; `rsync -az --delete --exclude .git --exclude target --exclude website/node_modules --exclude .trellis/.runtime ./ vps:/home/ubuntu/data/projects/assura-perf-20260705-ls-fast-recursion/after/`; `scp /tmp/assura-ls-fast-recursion.patch vps:/home/ubuntu/data/projects/assura-perf-20260705-ls-fast-recursion/artifacts/assura-ls-fast-recursion.patch`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-ls-fast-recursion && cp -a after before && cd before && patch -p1 -R < ../artifacts/assura-ls-fast-recursion.patch'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-ls-fast-recursion/before && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/before.json --history target/performance/before.jsonl --website-dir target/performance/before-website --iterations 3 --suite ls-lint && cd /home/ubuntu/data/projects/assura-perf-20260705-ls-fast-recursion/after && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/after.json --history target/performance/after.jsonl --website-dir target/performance/after-website --iterations 3 --suite ls-lint'`; `/home/ubuntu/data/projects/assura-perf-20260705-ls-fast-recursion/before/target/performance/before.json`; `/home/ubuntu/data/projects/assura-perf-20260705-ls-fast-recursion/after/target/performance/after.json`; `ssh vps 'bash -s'` manual fixture recreation and 25-run interleaved timing loop at `/tmp/assura-manual-many-scopes-20260705`; `cargo test --lib fast_file_stem_matches_path_semantics_for_common_names --quiet`; `cargo test --test cli_check_tests check_validates_converted_ls_lint_dir_rules --quiet`; `cargo run --quiet -- check --format json .`; `git diff --check` |
| 2026-07-05 | Rejected a fused `CompiledStructureConfig::new_for_check` prepass after same-fixture manual timing failed to confirm the suite-level target-row gains. The change merged the `configured_dirs`, naming-regex, glob-pattern, and direct-count collection work into one recursive walk for the check-path compiler, leaving rule semantics and the LS-Lint traversal path unchanged. Local focused validation stayed green. The first 3-iteration `performance-report --suite ls-lint` comparison on `vps` looked materially positive on the named structure case and several adjacent public rows: `many_configured_scopes_regression assura-cli` improved from `20.635 ms` to `19.269 ms` (`-6.6%`), `assura-check-cli` improved from `25.258 ms` to `18.940 ms` (`-25.0%`), `config-load` improved from `4.147 ms` to `3.915 ms` (`-5.6%`), and `checker-init` improved from `1.700 ms` to `1.544 ms` (`-9.2%`). Several public `assura-cli` rows also improved in that suite A/B, including `real_project_agentic_feedback` (`-39.9%`), `rule_heavy_repo` (`-22.5%`), `monorepo_packages` (`-17.3%`), `monorepo_large` (`-11.9%`), `simple_small` (`-11.1%`), `simple_medium` (`-9.6%`), and `ignored_generated_heavy` (`-9.2%`). But the internal structure-core rows for the same target fixture moved the wrong way: `assura-in-process` regressed from `10.625 ms` to `11.495 ms` (`+8.2%`) and `walk-and-validate` regressed from `4.758 ms` to `5.996 ms` (`+26.0%`). Because the suite still mixed strong public wins with slower core validation on the exact target, I recreated the same 800-package `many_configured_scopes_regression` fixture once on `vps`, converted it with `assura-full migrate`, and ran 25 interleaved manual timings against the shared fixture using the built `assura-full` and `assura-check` binaries. That tighter proof did not reproduce a real target-row win: `assura-full check --format json` median regressed from `23.01 ms` to `23.24 ms` (`+1.0%`), while `assura-check --format json` was effectively flat from `21.631 ms` to `21.622 ms` (`-0.0%`). I reverted the slice locally and kept the earlier retained structure-lane work unchanged. The next structure attempt should continue to treat same-fixture manual A/B as the tie-breaker when setup improvements and core-walk regressions pull in opposite directions. | `git diff -- src/cli/check/compiled_config.rs src/cli/check/rules.rs src/cli/check/patterns.rs > /tmp/assura-compiled-config-prepass.patch`; `rsync -az --delete --exclude .git --exclude target --exclude website/node_modules --exclude .trellis/.runtime ./ vps:/home/ubuntu/data/projects/assura-perf-20260705-compiled-config-prepass/after/`; `scp /tmp/assura-compiled-config-prepass.patch vps:/home/ubuntu/data/projects/assura-perf-20260705-compiled-config-prepass/artifacts/assura-compiled-config-prepass.patch`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-compiled-config-prepass && cp -a after before && cd before && patch -p1 -R < ../artifacts/assura-compiled-config-prepass.patch'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-compiled-config-prepass/before && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/before.json --history target/performance/before.jsonl --website-dir target/performance/before-website --iterations 3 --suite ls-lint && cd /home/ubuntu/data/projects/assura-perf-20260705-compiled-config-prepass/after && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/after.json --history target/performance/after.jsonl --website-dir target/performance/after-website --iterations 3 --suite ls-lint'`; `/home/ubuntu/data/projects/assura-perf-20260705-compiled-config-prepass/before/target/performance/before.json`; `/home/ubuntu/data/projects/assura-perf-20260705-compiled-config-prepass/after/target/performance/after.json`; `ssh vps 'bash -s'` manual fixture recreation and 25-run interleaved timing loop at `/tmp/assura-manual-many-scopes-compiled-config-20260705`; `cargo test --test cli_check_tests check_validates_converted_ls_lint_dir_rules --quiet`; `cargo run --quiet -- check --format json .`; `git diff --check` |
| 2026-07-05 | Rejected a narrow naming-convention semantic-validation fast path after correcting the manual benchmark method to match the actual public launcher rows. The code change itself was minimal: single-token naming conventions such as `kebab-case` stopped going through the split-and-recurse path during config semantic validation. Local focused validation stayed green. A same-state 3-iteration `performance-report --suite ls-lint` A/B on `vps` looked like the strongest structure-target win of the session: `many_configured_scopes_regression assura-cli` improved from `26.246 ms` to `18.791 ms` (`-28.4%`), `assura-check-cli` improved from `19.323 ms` to `18.426 ms` (`-4.6%`), `assura-in-process` improved from `15.828 ms` to `11.042 ms` (`-30.2%`), `config-load` improved from `6.719 ms` to `4.538 ms` (`-32.5%`), `checker-init` improved from `3.035 ms` to `1.662 ms` (`-45.2%`), and `walk-and-validate` improved from `6.144 ms` to `4.797 ms` (`-21.9%`). But the first manual confirmation attempt still used the wrong public command shape: I measured `assura-full check --quiet` and `assura-check --quiet`, which does not match the `assura-cli` row in `performance-report`. After tracing `measure_assura_cli_row`, I corrected the manual benchmark to use the actual public launcher commands, `assura check --quiet` and `assura-check --quiet`, on a recreated shared `many_configured_scopes_regression` fixture. That exact-command 50-run interleaved manual A/B no longer showed a real win: `assura check --quiet` median moved from `19.708 ms` to `19.794 ms` (`+0.4%`) and `assura-check --quiet` moved from `20.066 ms` to `20.141 ms` (`+0.4%`). I also reran the suite at 5 iterations on the same `vps` before/after trees; that longer same-host report likewise failed the keep bar on the named public launcher row: `many_configured_scopes_regression assura-cli` `19.512 ms` -> `20.139 ms` (`+3.2%`) and `assura-check-cli` `19.624 ms` -> `19.955 ms` (`+1.7%`), even though the diagnostic phase rows still improved (`config-load -7.4%`, `checker-init -10.0%`, `walk-and-validate -1.7%`). I reverted the slice locally and kept the retained structure-lane code unchanged. The durable progress from this iteration is methodological: future same-fixture tie-breakers for public rows must use the exact `performance-report` command shapes, especially `assura check --quiet` for `assura-cli`, rather than `assura-full` or `--format json` stand-ins. | `git diff -- src/config/config/validation.rs > /tmp/assura-validation-naming-fastpath.patch`; `rsync -az --delete --exclude .git --exclude target --exclude website/node_modules --exclude .trellis/.runtime ./ vps:/home/ubuntu/data/projects/assura-perf-20260705-validation-naming-fastpath/after/`; `scp /tmp/assura-validation-naming-fastpath.patch vps:/home/ubuntu/data/projects/assura-perf-20260705-validation-naming-fastpath/artifacts/assura-validation-naming-fastpath.patch`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-validation-naming-fastpath && cp -a after before && cd before && patch -p1 -R < ../artifacts/assura-validation-naming-fastpath.patch'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-validation-naming-fastpath/before && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/before.json --history target/performance/before.jsonl --website-dir target/performance/before-website --iterations 3 --suite ls-lint && cd /home/ubuntu/data/projects/assura-perf-20260705-validation-naming-fastpath/after && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/after.json --history target/performance/after.jsonl --website-dir target/performance/after-website --iterations 3 --suite ls-lint'`; `/home/ubuntu/data/projects/assura-perf-20260705-validation-naming-fastpath/before/target/performance/before.json`; `/home/ubuntu/data/projects/assura-perf-20260705-validation-naming-fastpath/after/target/performance/after.json`; `ssh vps 'bash -s'` recreated fixture at `/tmp/assura-manual-many-scopes-validation-naming-fastpath-assura-cli-20260705` with 50-run interleaved exact-command manual timing using `assura check --quiet` and `assura-check --quiet`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-validation-naming-fastpath/before && target/release/assura performance-report --output target/performance/before-5.json --history target/performance/before-5.jsonl --website-dir target/performance/before-5-website --iterations 5 --suite ls-lint && cd /home/ubuntu/data/projects/assura-perf-20260705-validation-naming-fastpath/after && target/release/assura performance-report --output target/performance/after-5.json --history target/performance/after-5.jsonl --website-dir target/performance/after-5-website --iterations 5 --suite ls-lint'`; `cargo test --test cli_check_tests check_validates_converted_ls_lint_dir_rules --quiet`; `cargo run --quiet -- check --format json .`; `git diff --check` |
| 2026-07-05 | Rejected a second naming-convention semantic-validation experiment after the same-machine `vps` A/B showed that the extra cache bookkeeping made the structure lane slower, not faster. This slice memoized successful naming-convention validations during one config semantic-validation pass so repeated `kebab-case` strings in generated scope-heavy configs would only be parsed once. The direct target regressed in exactly the public row and phase buckets we care about: on `many_configured_scopes_regression`, `assura-cli` worsened from `22.345 ms` to `25.565 ms` (`+14.4%`), `assura-check-cli` from `19.546 ms` to `19.985 ms` (`+2.2%`), `assura-in-process` from `11.576 ms` to `13.907 ms` (`+20.1%`), `config-load` from `5.085 ms` to `6.467 ms` (`+27.2%`), `checker-init` from `1.778 ms` to `2.786 ms` (`+56.7%`), and `walk-and-validate` from `4.719 ms` to `5.044 ms` (`+6.9%`); only `configured-structure` improved, from `0.0280 ms` to `0.0241 ms` (`-13.9%`), which is not enough to matter when the surrounding cold path gets slower. The public `assura-cli` row summary by fixture also makes the regression obvious instead of mixed: improvements were limited to `ignored_generated_heavy` (`-15.8%`), `rule_heavy` (`-12.8%`), `simple_small` (`-11.7%`), `web_app` (`-10.7%`), `simple_library` (`-6.0%`), and `real_project_agentic_feedback` (`-5.7%`), while `monorepo_packages` worsened `+40.8%`, `monorepo_policy` `+32.1%`, `rule_heavy_repo` `+31.8%`, `monorepo_large` `+22.4%`, `simple_medium` `+17.3%`, `many_configured_scopes_regression` `+14.4%`, `multipart_extension_regression` `+6.4%`, and `ignored_generated_heavy_repo` `+1.1%`. The aggregate headline moved the wrong way too: `claim_summary.total_assura_runtime_ms` rose from `37.917 ms` to `43.274 ms` (`+14.1%`) while `two_x_pass_count` stayed stuck at `2`. I reverted this slice locally after the report run. The next structure attempt should keep using `vps` and should target shared cold-path structure work only when the named public fixture and its setup phases both move in the right direction together. | `git diff -- src/config/config/validation.rs > /tmp/assura-validation-naming-cache.patch`; `rsync -az --delete --exclude .git --exclude target --exclude website/node_modules --exclude .trellis/.runtime ./ vps:/home/ubuntu/data/projects/assura-perf-20260705-validation-naming-cache/after/`; `scp /tmp/assura-validation-naming-cache.patch vps:/home/ubuntu/data/projects/assura-perf-20260705-validation-naming-cache/artifacts/assura-validation-naming-cache.patch`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-validation-naming-cache && cp -a after before && cd before && patch -p1 -R < ../artifacts/assura-validation-naming-cache.patch'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-validation-naming-cache/before && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/before.json --history target/performance/before.jsonl --website-dir target/performance/before-website --iterations 3 --suite ls-lint && cd /home/ubuntu/data/projects/assura-perf-20260705-validation-naming-cache/after && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/after.json --history target/performance/after.jsonl --website-dir target/performance/after-website --iterations 3 --suite ls-lint'`; `/home/ubuntu/data/projects/assura-perf-20260705-validation-naming-cache/before/target/performance/before.json`; `/home/ubuntu/data/projects/assura-perf-20260705-validation-naming-cache/after/target/performance/after.json`; `cargo test test_validate_naming_convention_valid --lib --quiet`; `cargo test test_split_naming_conventions_preserves_regex_pipes --lib --quiet`; `cargo test --test cli_check_tests check_validates_converted_ls_lint_dir_rules --quiet`; `cargo run --quiet -- check --format json .`; `git diff --check` |
| 2026-07-05 | Rechecked a previously weak fast-scope metadata hypothesis on the current retained structure lane and rejected it again. The narrow code change stopped the check-only compiler from recursively collecting configured-dir, glob-pattern, and direct-count metadata from the config tree when LS-Lint fast scopes were already available to summarize the needed runtime artifacts. Focused local tests passed, but the same-host `vps` evidence stayed split across the two public cold launchers instead of producing a stable shared structure-core win. On the paired 3-iteration `performance-report --suite ls-lint` run, the named public `assura-cli` row on `many_configured_scopes_regression` improved from `21.368 ms` to `20.116 ms` (`-5.9%`), yet the paired `assura-check-cli` row regressed from `20.728 ms` to `23.421 ms` (`+13.0%`). The diagnostic target rows also mostly moved the wrong way: `assura-in-process` `14.667 ms` -> `15.161 ms` (`+3.4%`), `config-load` `6.094 ms` -> `6.597 ms` (`+8.3%`), `configured-structure` `0.0341 ms` -> `0.0395 ms` (`+15.8%`), and `walk-and-validate` `5.443 ms` -> `5.683 ms` (`+4.4%`); only `checker-init` improved, `3.072 ms` -> `2.940 ms` (`-4.3%`). The aggregate cold summary looked superficially better (`total_assura_runtime_ms` `38.448 ms` -> `36.661 ms`), but the spillover was still too mixed to keep: `assura-cli` got better on `monorepo_packages` (`-37.5%`), `web_app` (`-22.9%`), and `rule_heavy` (`-19.0%`), while regressing `real_project_agentic_feedback` (`+34.3%`), `multipart_extension_regression` (`+25.6%`), `monorepo_large` (`+13.5%`), and `ignored_generated_heavy_repo` (`+12.5%`). The decisive tie-breaker was an exact-command same-fixture manual A/B on a recreated shared `many_configured_scopes_regression` project using the real public commands, `assura check --quiet` and `assura-check --quiet`: `assura check --quiet` improved from `20.857 ms` to `19.706 ms` (`-5.5%`), but `assura-check --quiet` regressed from `20.445 ms` to `21.548 ms` (`+5.4%`). Because the exact same fixture still splits the two public cold launchers in opposite directions, I reverted this slice locally and kept only the rejection log. The next structure attempt should treat compiler-side metadata reuse as closed unless a materially different design can move both public launchers and the target phase rows together. | `git diff -- src/cli/check.rs src/cli/check/compiled_config.rs src/cli/check/ls_fast_plan.rs src/cli/check/ls_fast_scope_artifacts.rs > /tmp/assura-fast-scope-compiler-inputs.patch`; `rsync -az --delete --exclude .git --exclude target --exclude website/node_modules --exclude .trellis/.runtime ./ vps:/home/ubuntu/data/projects/assura-perf-20260705-fast-scope-compiler-inputs/after/`; `scp /tmp/assura-fast-scope-compiler-inputs.patch vps:/home/ubuntu/data/projects/assura-perf-20260705-fast-scope-compiler-inputs/artifacts/assura-fast-scope-compiler-inputs.patch`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-fast-scope-compiler-inputs && rm -rf before && cp -a after before && rm -f before/src/cli/check/ls_fast_scope_artifacts.rs && cd before && patch -p1 -R < ../artifacts/assura-fast-scope-compiler-inputs.patch'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-fast-scope-compiler-inputs/before && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/before.json --history target/performance/before.jsonl --website-dir target/performance/before-website --iterations 3 --suite ls-lint && cd /home/ubuntu/data/projects/assura-perf-20260705-fast-scope-compiler-inputs/after && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/after.json --history target/performance/after.jsonl --website-dir target/performance/after-website --iterations 3 --suite ls-lint'`; `/home/ubuntu/data/projects/assura-perf-20260705-fast-scope-compiler-inputs/before/target/performance/before.json`; `/home/ubuntu/data/projects/assura-perf-20260705-fast-scope-compiler-inputs/after/target/performance/after.json`; `ssh vps 'python3 ... recreate shared 800-package manual-many-scopes fixture, run assura-full migrate, then 30 interleaved exact-command timings for before/after assura check --quiet and assura-check --quiet ...'`; `cargo test --lib indexed_rules_match_linear_resolution_for_literal_scopes --quiet`; `cargo test --test cli_check_tests check_validates_converted_ls_lint_dir_rules --quiet`; `cargo test --test cli_check_tests profiled_check_reports_stage_timings_without_changing_result --quiet`; `cargo run --quiet -- check --format json .`; `git diff --check` |
| 2026-07-05 | Rejected a checker-init precompute slice after the same-fixture exact-command tie-breaker contradicted the promising phase/suite signal. The code change moved three lookup builds out of `StructureChecker::new` and into `CompiledStructureConfig`: the large `ConfiguredDirIndex`, the literal `rule_scope_index`, and the literal LS-Lint fast-scope index, plus a tiny follow-up fix so empty `rule_scopes` no longer build `Some(empty HashMap)`. Focused local tests and `assura check --format json .` stayed green. The first same-host `vps` `performance-report --suite ls-lint` run looked like a real structure-core win on the target fixture: `many_configured_scopes_regression assura-cli` improved from `23.792 ms` to `20.868 ms` (`-12.3%`), `assura-in-process` from `15.652 ms` to `11.944 ms` (`-23.7%`), `config-load` from `6.227 ms` to `4.423 ms` (`-29.0%`), `checker-init` from `2.632 ms` to `1.883 ms` (`-28.5%`), `configured-structure` from `0.0426 ms` to `0.0318 ms` (`-25.2%`), and `walk-and-validate` from `6.256 ms` to `4.958 ms` (`-20.7%`). The aggregate cold summary also improved (`total_assura_runtime_ms` `40.914 ms` -> `38.060 ms`, `-7.0%`). But the paired public `assura-check-cli` row on that same named target still regressed from `20.044 ms` to `21.494 ms` (`+7.2%`), and spillover was mixed even before the tie-breaker: `assura-cli` improved on `multipart_extension_regression` (`-18.8%`), `simple_medium` (`-13.3%`), and `monorepo_policy` (`-8.5%`), while regressing `simple_library` (`+37.5%`), `rule_heavy` (`+23.1%`), `real_project_agentic_feedback` (`+22.9%`), and `monorepo_large` (`+16.9%`). The decisive exact-command same-fixture A/B on a recreated shared `many_configured_scopes_regression` project again split the two public launchers in opposite directions: `assura check --quiet` got slower from `21.648 ms` to `22.764 ms` (`+5.2%`), while `assura-check --quiet` got faster from `24.204 ms` to `21.277 ms` (`-12.1%`). Because the real public launcher for the headline cold row still lost on the shared fixture, I reverted this slice locally and kept only the rejection log. I did not request an independent review after the code change because the exact-command performance proof already failed the keep bar before there was anything worth carrying forward to review. | `rsync -az --delete --exclude .git --exclude target --exclude website/node_modules --exclude .trellis/.runtime ./ vps:/home/ubuntu/data/projects/assura-perf-20260705-precomputed-checker-indexes/after/`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-precomputed-checker-indexes && rm -rf before && cp -a after before && cp /home/ubuntu/data/projects/assura-perf-20260705-fast-scope-compiler-inputs/before/src/cli/check.rs before/src/cli/check.rs && cp /home/ubuntu/data/projects/assura-perf-20260705-fast-scope-compiler-inputs/before/src/cli/check/compiled_config.rs before/src/cli/check/compiled_config.rs && cp /home/ubuntu/data/projects/assura-perf-20260705-fast-scope-compiler-inputs/before/src/cli/check/configured_dir_index.rs before/src/cli/check/configured_dir_index.rs && cp /home/ubuntu/data/projects/assura-perf-20260705-fast-scope-compiler-inputs/before/src/cli/check/rule_plan.rs before/src/cli/check/rule_plan.rs'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-precomputed-checker-indexes/before && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/before.json --history target/performance/before.jsonl --website-dir target/performance/before-website --iterations 3 --suite ls-lint && cd /home/ubuntu/data/projects/assura-perf-20260705-precomputed-checker-indexes/after && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/after.json --history target/performance/after.jsonl --website-dir target/performance/after-website --iterations 3 --suite ls-lint'`; `/home/ubuntu/data/projects/assura-perf-20260705-precomputed-checker-indexes/before/target/performance/before.json`; `/home/ubuntu/data/projects/assura-perf-20260705-precomputed-checker-indexes/after/target/performance/after.json`; `ssh vps 'python3 ... recreate shared 800-package manual-many-scopes fixture, run assura-full migrate, then 30 interleaved exact-command timings for before/after assura check --quiet and assura-check --quiet ...'`; `cargo test --lib indexed_rules_match_linear_resolution_for_literal_scopes --quiet`; `cargo test --test cli_check_tests check_validates_converted_ls_lint_dir_rules --quiet`; `cargo test --test cli_check_tests profiled_check_reports_stage_timings_without_changing_result --quiet`; `cargo run --quiet -- check --format json .`; `git diff --check` |
| 2026-07-05 | Revalidated the later literal `rule_scope_index` extension on the quieter remote host and cut it back out of the retained structure lane. First I compared clean `HEAD` against the current local structure diff on `vps` with identical 5-iteration cold `performance-report --suite ls-lint` runs. That comparison showed exactly why the progress had become hard to read: the internal setup/core rows improved on the named hotspot, but the public cold launchers got slower. On `many_configured_scopes_regression`, `assura-cli` regressed from `17.547 ms` to `21.010 ms` (`+19.7%`) and `assura-check-cli` from `18.977 ms` to `19.976 ms` (`+5.3%`), even while `assura-in-process` improved from `12.524 ms` to `9.998 ms` (`-20.2%`), `config-load` from `5.746 ms` to `3.911 ms` (`-31.9%`), `checker-init` from `2.029 ms` to `1.608 ms` (`-20.8%`), and `walk-and-validate` from `4.700 ms` to `4.441 ms` (`-5.5%`). The aggregate cold summary also moved the wrong way (`total_assura_runtime_ms` `32.826 ms` -> `37.112 ms`, `+13.1%`; `total_ls_lint_runtime_ms` `50.730 ms` -> `54.541 ms`, `+7.5%`). The `assura-cli` per-fixture spillover on that same run was mixed but clearly not a keep: wins were limited to `monorepo_policy` (`-18.3%`), `simple_medium` (`-9.8%`), `monorepo_large` (`-4.5%`), and `real_project_agentic_feedback` (`-3.2%`), while `many_configured_scopes_regression` regressed `+19.7%`, `multipart_extension_regression` `+17.9%`, `simple_library` `+14.7%`, `monorepo_packages` `+13.0%`, and `web_app` `+8.7%`. I then isolated the new rule-resolution change by cloning the current `after` tree on `vps`, removing only the `check.rs` runtime use of the literal `rule_scope_index`, and rerunning the same 5-iteration suite as a `hybrid configured-dir index only` baseline. That isolated A/B made the culprit clear: the current tree with rule-scope indexing was slower than `hybrid-only` on the target case for both public launchers and the nearby core rows. `many_configured_scopes_regression assura-cli` regressed from `19.403 ms` to `21.010 ms` (`+8.3%`), `assura-check-cli` from `19.057 ms` to `19.976 ms` (`+4.8%`), `assura-in-process` from `9.274 ms` to `9.998 ms` (`+7.8%`), `config-load` from `3.767 ms` to `3.911 ms` (`+3.8%`), `checker-init` from `1.581 ms` to `1.608 ms` (`+1.7%`), and `walk-and-validate` from `3.940 ms` to `4.441 ms` (`+12.7%`). The `assura-cli` spillover relative to `hybrid-only` was still mixed, but again not good enough to keep: improvements on `monorepo_large` (`-15.2%`), `real_project_agentic_feedback` (`-13.0%`), `simple_medium` (`-5.9%`), and `simple_small` (`-5.7%`) were offset by regressions on `many_configured_scopes_regression` (`+8.3%`), `rule_heavy_repo` (`+9.4%`), `multipart_extension_regression` (`+13.7%`), and `simple_library` (`+18.3%`). I finished with an exact-command same-fixture manual A/B on a canonical recreated `many_configured_scopes_regression` project using the real public launcher commands. That tie-breaker stayed mixed and therefore still below the keep bar: `assura check --quiet` was essentially noise-level better from `20.796 ms` to `20.574 ms` (`-1.1%`), but `assura-check --quiet` regressed from `19.896 ms` to `20.576 ms` (`+3.4%`). I reverted the literal rule-scope indexing locally, kept only the earlier configured-dir membership index, and did not refresh checked benchmark artifacts or website data from this slice. I also did not request another independent review because the stronger same-host performance proof was sufficient to reject the extension before there was anything worth carrying forward. | `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-retained-structure-current/before && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/before.json --history target/performance/before.jsonl --website-dir target/performance/before-website --iterations 5 --suite ls-lint && cd /home/ubuntu/data/projects/assura-perf-20260705-retained-structure-current/after && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/after.json --history target/performance/after.jsonl --website-dir target/performance/after-website --iterations 5 --suite ls-lint && cargo xtask performance-no-slower target/performance/after.json'`; `/home/ubuntu/data/projects/assura-perf-20260705-retained-structure-current/before/target/performance/before.json`; `/home/ubuntu/data/projects/assura-perf-20260705-retained-structure-current/after/target/performance/after.json`; `/home/ubuntu/data/projects/assura-perf-20260705-retained-structure-hybrid-only/target/performance/hybrid.json`; `ssh vps 'python3 ... canonical many_configured_scopes_regression fixture at /tmp/assura-manual-many-scopes-retained-structure-canonical-20260705, 30 interleaved exact-command timings for hybrid vs after assura check --quiet and assura-check --quiet ...'`; `cargo test --lib small_configured_dir_sets_keep_sorted_lookup --quiet`; `cargo test --lib large_configured_dir_sets_keep_membership_correct --quiet`; `cargo test --test cli_check_tests check_validates_converted_ls_lint_dir_rules --quiet`; `cargo test --test cli_check_tests profiled_check_reports_stage_timings_without_changing_result --quiet`; `cargo run --quiet -- check --format json .`; `git diff --check` |
| 2026-07-05 | Rejected a narrower configured-dir precompute slice after the exact-command shared-fixture tie-breaker showed that the promising suite deltas were still not a launcher-level win. This experiment kept the retained hybrid `ConfiguredDirIndex` behavior but moved only the configured-dir index construction into `CompiledStructureConfig`, so `StructureChecker::from_compiled{,_owned}` would reuse the prebuilt membership structure instead of rebuilding it on every checker construction. Focused local tests and `assura check --format json .` stayed green. On the first same-host `vps` 5-iteration `performance-report --suite ls-lint` comparison against the current retained baseline, the target fixture and several nearby public rows all improved in the direction we wanted: `many_configured_scopes_regression assura-cli` improved from `27.872 ms` to `25.760 ms` (`-7.6%`), `assura-check-cli` from `20.241 ms` to `19.812 ms` (`-2.1%`), `assura-in-process` from `15.072 ms` to `10.973 ms` (`-27.2%`), `config-load` from `6.553 ms` to `3.846 ms` (`-41.3%`), `checker-init` from `2.841 ms` to `1.662 ms` (`-41.5%`), `configured-structure` from `0.0288 ms` to `0.0271 ms` (`-5.9%`), and `walk-and-validate` from `5.788 ms` to `4.423 ms` (`-23.6%`). The aggregate cold summary also improved (`total_assura_runtime_ms` `44.007 ms` -> `42.416 ms`, `-3.6%`) while `two_x_pass_count` stayed flat at `2`. The `assura-cli` per-fixture summary looked materially better than the last few rejected structure slices: wins included `simple_small` (`-12.2%`), `ignored_generated_heavy_repo` (`-9.3%`), `many_configured_scopes_regression` (`-7.6%`), `rule_heavy` (`-6.1%`), `monorepo_policy` (`-6.1%`), and `monorepo_large` (`-6.1%`). But spillover was still real on a few rows: `simple_medium` regressed from `2.623 ms` to `3.416 ms` (`+30.2%`), `multipart_extension_regression` from `3.472 ms` to `3.953 ms` (`+13.9%`), `monorepo_packages` from `1.813 ms` to `1.965 ms` (`+8.4%`), `rule_heavy_repo` from `2.344 ms` to `2.510 ms` (`+7.1%`), and `web_app` from `1.711 ms` to `1.807 ms` (`+5.6%`). The same report also failed the separate no-slower gate on the target accepted fixture (`many_configured_scopes_regression assura-cli 25.760 ms > ls-lint-cli 20.124 ms`), so I treated the suite as encouraging but not decisive and ran the canonical exact-command same-fixture A/B on a recreated shared `many_configured_scopes_regression` project using the real public launchers. That tie-breaker went the wrong way for both public commands: `assura check --quiet` regressed from `21.604 ms` to `22.523 ms` (`+4.3%`) and `assura-check --quiet` regressed from `21.189 ms` to `21.556 ms` (`+1.7%`). Because the real public launcher behavior still lost on the shared fixture, I reverted this precompute slice locally, kept only the earlier configured-dir membership index, and did not refresh checked benchmark artifacts or website data from this experiment. I did not request an independent review because the exact-command performance proof failed before there was anything worth carrying forward. | `git diff -- src/cli/check.rs src/cli/check/compiled_config.rs src/cli/check/configured_dir_index.rs > /tmp/assura-configured-dir-precompute.patch`; `rsync -az --delete --exclude .git --exclude target --exclude website/node_modules --exclude .trellis/.runtime ./ vps:/home/ubuntu/data/projects/assura-perf-20260705-configured-dir-precompute/after/`; `scp /tmp/assura-configured-dir-precompute.patch vps:/home/ubuntu/data/projects/assura-perf-20260705-configured-dir-precompute/configured-dir-precompute.patch`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-configured-dir-precompute && cp -a after before && cd before && patch -p1 -R < ../configured-dir-precompute.patch'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-configured-dir-precompute/before && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/before.json --history target/performance/before.jsonl --website-dir target/performance/before-website --iterations 5 --suite ls-lint && cd /home/ubuntu/data/projects/assura-perf-20260705-configured-dir-precompute/after && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/after.json --history target/performance/after.jsonl --website-dir target/performance/after-website --iterations 5 --suite ls-lint && cargo xtask performance-no-slower target/performance/after.json'`; `/home/ubuntu/data/projects/assura-perf-20260705-configured-dir-precompute/before/target/performance/before.json`; `/home/ubuntu/data/projects/assura-perf-20260705-configured-dir-precompute/after/target/performance/after.json`; `ssh vps 'python3 ... canonical many_configured_scopes_regression fixture at /tmp/assura-manual-many-scopes-configured-dir-precompute-20260705, 30 interleaved exact-command timings for before vs after assura check --quiet and assura-check --quiet ...'`; `cargo test --lib small_configured_dir_sets_keep_sorted_lookup --quiet`; `cargo test --lib large_configured_dir_sets_keep_membership_correct --quiet`; `cargo test --test cli_check_tests check_validates_converted_ls_lint_dir_rules --quiet`; `cargo test --test cli_check_tests profiled_check_reports_stage_timings_without_changing_result --quiet`; `cargo run --quiet -- check --format json .`; `git diff --check` |
| 2026-07-05 | Rejected a faster-hash follow-up on the retained configured-dir membership index after the stricter same-fixture proof showed only marginal manual gains while the structured target row still moved the wrong way. This slice kept the current hybrid `ConfiguredDirIndex` thresholding and changed only the large-set backing store from `HashSet<PathBuf>` to `ahash::AHashSet<PathBuf>`. I isolated it in a clean temporary worktree and measured it only on `vps` to avoid the noisy local box. The first same-host 5-iteration `performance-report --suite ls-lint` run still passed the separate no-slower gate, but the core target evidence was not keepable. On `many_configured_scopes_regression`, the public `assura-cli` row regressed from `19.306 ms` to `22.435 ms` (`+16.2%`) even while the paired `assura-check-cli` row improved from `23.897 ms` to `19.477 ms` (`-18.5%`). The target structure-core rows also mostly worsened instead of tightening around the claimed hotspot: `assura-in-process` `10.014 ms` -> `10.797 ms` (`+7.8%`), `config-load` `3.843 ms` -> `3.898 ms` (`+1.4%`), `configured-structure` `0.0166 ms` -> `0.0187 ms` (`+12.9%`), and `walk-and-validate` `4.682 ms` -> `5.106 ms` (`+9.1%`); only `checker-init` improved slightly, `1.613 ms` -> `1.600 ms` (`-0.8%`). The per-fixture public `assura-cli` spillover was also too mixed to count as progress: improvements showed up on `simple_small` (`-15.3%`), `simple_library` (`-16.8%`), `rule_heavy_repo` (`-9.4%`), `multipart_extension_regression` (`-9.9%`), and `ignored_generated_heavy_repo` (`-11.3%`), but regressions hit `simple_medium` (`+19.7%`), `monorepo_policy` (`+7.1%`), `ignored_generated_heavy` (`+5.3%`), and the named target `many_configured_scopes_regression` itself (`+16.2%`). I then ran the tighter exact-command tie-breaker directly on the shared canonical fixture at `/home/ubuntu/data/projects/manual-many-scopes-common` with 3 warmups and 30 interleaved measured runs per launcher. That manual proof was only marginally positive, not a durable win: `assura check --quiet` improved from `21.668 ms` to `21.248 ms` (`-1.9%`) and `assura-check --quiet` improved from `22.076 ms` to `21.901 ms` (`-0.8%`). Because the exact-command gains were small while the structured report still regressed the public target row and several target phases, I reverted the `ahash` variant in the clean worktree, kept only the earlier retained hybrid index, and did not copy this slice back into the main repo. The next structure attempt should keep using the clean worktree plus `vps` combination, but it should aim for a clearer shared win on the exact `many_configured_scopes_regression` public row before we spend more time on configured-dir micro-tuning. | temporary clean worktree at `/private/tmp/assura-structure-baseline-cfgdir`; `cargo test --lib small_configured_dir_sets_keep_sorted_lookup --quiet`; `cargo test --lib large_configured_dir_sets_keep_membership_correct --quiet`; `cargo test --test cli_check_tests check_validates_converted_ls_lint_dir_rules --quiet`; `cargo test --test cli_check_tests profiled_check_reports_stage_timings_without_changing_result --quiet`; `cargo run --quiet -- check --format json .`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-configured-dir-ahash/before && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/before.json --history target/performance/before.jsonl --website-dir target/performance/before-website --iterations 5 --suite ls-lint && cd /home/ubuntu/data/projects/assura-perf-20260705-configured-dir-ahash/after && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/after.json --history target/performance/after.jsonl --website-dir target/performance/after-website --iterations 5 --suite ls-lint && cargo xtask performance-no-slower target/performance/after.json'`; `/home/ubuntu/data/projects/assura-perf-20260705-configured-dir-ahash/before/target/performance/before.json`; `/home/ubuntu/data/projects/assura-perf-20260705-configured-dir-ahash/after/target/performance/after.json`; `ssh vps 'python3 ... 3 warmups + 30 interleaved exact-command timings on /home/ubuntu/data/projects/manual-many-scopes-common for before/after assura check --quiet and assura-check --quiet ...'`; `git diff --check` |
| 2026-07-05 | Kept a new shared structure-path slice that caches identical compiled LS-Lint fast-rule shapes during scope compilation instead of rebuilding the same `FastFileNaming`/`FastRules` objects for every repeated sibling scope. The code change is narrow: `compile_lslint_fast_scopes` now routes `EffectiveRules` through a small `FastRuleCache`, keyed by the file/directory/self-directory rule fragments that actually affect the fast path, and the cache helper lives in `src/cli/check/ls_fast_rules_cache.rs` so `ls_fast_plan.rs` stays under the repo line-limit policy. I treated the evidence hierarchy explicitly because the first suite pass looked strong on the named public target but contradictory on the diagnostic in-process rows. On the first same-host `vps` 5-iteration `performance-report --suite ls-lint` run, `many_configured_scopes_regression assura-cli` improved from `24.473 ms` to `18.071 ms` (`-26.2%`) and `assura-check-cli` improved from `20.368 ms` to `16.947 ms` (`-16.8%`), while `claim_summary.total_assura_runtime_ms` improved from `39.856 ms` to `34.348 ms` (`-13.8%`) and the separate no-slower gate still passed. But those same runs reported worse diagnostic internals on the target row (`assura-in-process` `9.883 ms` -> `15.277 ms`, `config-load` `3.956 ms` -> `6.774 ms`, `checker-init` `1.703 ms` -> `3.205 ms`, `configured-structure` `0.0192 ms` -> `0.0393 ms`, `walk-and-validate` `4.138 ms` -> `5.096 ms`), so I did not trust that suite alone. The decisive proof was the exact-command same-fixture tie-breaker on the shared canonical fixture at `/home/ubuntu/data/projects/manual-many-scopes-common`: with 3 warmups and 30 interleaved measured runs per launcher, `assura check --quiet` improved from `22.708 ms` to `20.566 ms` (`-9.4%`) and `assura-check --quiet` improved from `22.429 ms` to `20.930 ms` (`-6.7%`). I then reran `performance-report` on the already-built `vps` binaries with the order reversed to reduce time-drift; that confirmation pass still improved the named public rows, `many_configured_scopes_regression assura-cli` `19.763 ms` -> `18.059 ms` (`-8.6%`) and `assura-check-cli` `19.389 ms` -> `18.111 ms` (`-6.6%`), even though the diagnostic phase rows remained directionally noisy (`config-load` `3.695 ms` -> `6.029 ms`, `checker-init` `1.600 ms` -> `2.909 ms`, `walk-and-validate` `4.227 ms` -> `4.905 ms`). Spillover on other public rows in the confirmation report stayed mixed rather than uniformly better, but the exact-command target win held for both public launchers and the no-slower gate stayed green, so this crosses the keep bar for the current core structure lane. I copied the slice back into the main worktree, kept it limited to the fast-scope compilation path, and left the checked benchmark history/website data untouched for now because the checked public claims have not been deliberately refreshed from a clean tracked artifact turn yet. The next structure step should start from this retained baseline and continue using the same evidence hierarchy: exact-command target-fixture A/B first, report rows second, tracked refresh only when the public claim changes are deliberate. | `/private/tmp/assura-structure-baseline-cfgdir/src/cli/check/ls_fast_plan.rs`; `/private/tmp/assura-structure-baseline-cfgdir/src/cli/check/ls_fast_rules_cache.rs`; `cargo test --lib fast_rules_key_ignores_hashmap_iteration_order --quiet`; `cargo test --lib fast_rule_cache_reuses_identical_shapes --quiet`; `cargo test --test cli_check_tests check_validates_converted_ls_lint_dir_rules --quiet`; `cargo test --test cli_check_tests profiled_check_reports_stage_timings_without_changing_result --quiet`; `cargo run --quiet -- check --format json /private/tmp/assura-structure-baseline-cfgdir`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-fast-rules-cache/before && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/before.json --history target/performance/before.jsonl --website-dir target/performance/before-website --iterations 5 --suite ls-lint && cd /home/ubuntu/data/projects/assura-perf-20260705-fast-rules-cache/after && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/after.json --history target/performance/after.jsonl --website-dir target/performance/after-website --iterations 5 --suite ls-lint && cargo xtask performance-no-slower target/performance/after.json'`; `/home/ubuntu/data/projects/assura-perf-20260705-fast-rules-cache/before/target/performance/before.json`; `/home/ubuntu/data/projects/assura-perf-20260705-fast-rules-cache/after/target/performance/after.json`; `ssh vps 'python3 ... 3 warmups + 30 interleaved exact-command timings on /home/ubuntu/data/projects/manual-many-scopes-common for before/after assura check --quiet and assura-check --quiet ...'`; `/home/ubuntu/data/projects/assura-perf-20260705-fast-rules-cache/after/target/performance/after-confirm.json`; `/home/ubuntu/data/projects/assura-perf-20260705-fast-rules-cache/before/target/performance/before-confirm.json`; `src/cli/check.rs`; `src/cli/check/ls_fast_plan.rs`; `src/cli/check/ls_fast_rules_cache.rs` |
| 2026-07-05 | Refreshed the checked LS-Lint comparison artifacts from the kept fast-rule-shape cache slice and copied the matching website data with them. I used the same clean `vps` snapshot that already held the kept structure code, rebuilt the release bundle there, regenerated `benches/history/current.json` plus `website/public/data/performance/current.json`, and only copied the four tracked LS-Lint files back after the same remote snapshot passed both `cargo xtask performance-no-slower benches/history/current.json` and `cargo test --test performance_report_contract_tests --quiet`. The refreshed checked claim is still `not-complete`, but the named target row materially improved in the checked artifact itself: `many_configured_scopes_regression assura-cli` moved from `23.538 ms` to `14.950 ms` (`-36.5%`), and the matching checked `assura-check-cli` row moved from `23.472 ms` to `16.055 ms` (`-31.6%`). The target setup rows in the checked artifact also improved overall even if not uniformly: `config-load` `6.072 ms` -> `4.153 ms` (`-31.6%`), `checker-init` `2.091 ms` -> `1.873 ms` (`-10.4%`), `configured-structure` `0.0287 ms` -> `0.0340 ms` (`+18.4%`), and `walk-and-validate` `4.996 ms` -> `5.117 ms` (`+2.4%`). The aggregate checked cold summary improved as well: `claim_summary.total_assura_runtime_ms` dropped from `39.663 ms` to `30.888 ms` (`-22.1%`), while `total_ls_lint_runtime_ms` dropped from `58.491 ms` to `50.043 ms` (`-14.4%`); `assura_faster_count` stayed `8`, `two_x_pass_count` stayed `2`, and the top-level verdict therefore remains `not-complete`. I did not update checked native artifacts or prose docs in this refresh because the native lane is separate and already green, while the website/public performance claim surface for LS-Lint comparison is data-driven from the copied JSON/JSONL files. | `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-fast-rules-cache/after && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output benches/history/current.json --history benches/history/ls-lint-comparison-history.jsonl --website-dir website/public/data/performance --iterations 5 --suite ls-lint && cargo xtask performance-no-slower benches/history/current.json && cargo test --test performance_report_contract_tests --quiet'`; `scp vps:/home/ubuntu/data/projects/assura-perf-20260705-fast-rules-cache/after/benches/history/current.json benches/history/current.json`; `scp vps:/home/ubuntu/data/projects/assura-perf-20260705-fast-rules-cache/after/benches/history/ls-lint-comparison-history.jsonl benches/history/ls-lint-comparison-history.jsonl`; `scp vps:/home/ubuntu/data/projects/assura-perf-20260705-fast-rules-cache/after/website/public/data/performance/current.json website/public/data/performance/current.json`; `scp vps:/home/ubuntu/data/projects/assura-perf-20260705-fast-rules-cache/after/website/public/data/performance/ls-lint-comparison-history.jsonl website/public/data/performance/ls-lint-comparison-history.jsonl`; `cargo xtask performance-no-slower benches/history/current.json`; `cargo test --test performance_report_contract_tests --quiet`; `cmp -s benches/history/current.json website/public/data/performance/current.json`; `cmp -s benches/history/ls-lint-comparison-history.jsonl website/public/data/performance/ls-lint-comparison-history.jsonl` |
| 2026-07-05 | Rejected a smaller fast-path naming allocation slice after both the `vps` suite comparison and the exact-command tie-breaker failed the keep bar. This experiment tried to avoid building relative-path strings for static fast naming conventions in the LS-Lint-compatible path and therefore targeted the same `many_configured_scopes_regression` structure lane from a different angle than the retained fast-rule-shape cache. The `vps` report showed some encouraging internal deltas on the target fixture: `assura-cli` improved from `19.251 ms` to `17.982 ms` (`-6.6%`), `assura-in-process` from `14.922 ms` to `11.608 ms` (`-22.2%`), `config-load` from `6.605 ms` to `4.493 ms` (`-32.0%`), and `checker-init` from `2.944 ms` to `2.046 ms` (`-30.5%`), with the aggregate cold summary also improving from `35.256 ms` to `32.826 ms` (`-6.9%`). But the paired public `assura-check-cli` row still regressed from `17.802 ms` to `18.759 ms` (`+5.4%`), `configured-structure` more than doubled from `0.0200 ms` to `0.0434 ms` (`+117.4%`), and `walk-and-validate` also nudged the wrong way from `4.944 ms` to `5.012 ms` (`+1.4%`). I then reran the canonical exact-command tie-breaker against the shared fixture at `/home/ubuntu/data/projects/manual-many-scopes-common` with a built-in Python loop on `vps` because `hyperfine` is not installed there, and the launcher-level result was worse than the suite summary: `assura check --quiet` regressed from `21.060 ms` to `22.966 ms` (`+9.1%`) and `assura-check --quiet` regressed from `22.146 ms` to `22.360 ms` (`+1.0%`). That made the rejection unambiguous, so I reverted the code slice locally and kept only the new repeatable `vps` helper for future structure comparisons: `scripts/perf-vps-ls-lint-compare.sh` now snapshots the current dirty worktree to `vps`, reverses just the requested patch onto a remote `before/` copy, runs the checked LS-Lint suite compare, enforces `cargo xtask performance-no-slower`, and runs the exact-command tie-breaker with `hyperfine` when available or the built-in Python fallback otherwise. I also whitelisted that script in `.assura/config.yml` and routed the existing `assura-performance-reporting` skill to it so the next structure slice can reuse the same dev-box workflow without reconstructing the commands by memory. | `cargo fmt --all`; `cargo test --lib fast_naming_simplifies_common_regex_literals --quiet`; `cargo test --lib static_fast_naming_does_not_require_path --quiet`; `cargo test --lib dynamic_fast_naming_still_requires_path --quiet`; `cargo test --lib fast_rules_key_ignores_hashmap_iteration_order --quiet`; `cargo test --lib fast_rule_cache_reuses_identical_shapes --quiet`; `cargo test --test cli_check_tests check_validates_converted_ls_lint_dir_rules --quiet`; `cargo test --test cli_check_tests profiled_check_reports_stage_timings_without_changing_result --quiet`; `scripts/perf-vps-ls-lint-compare.sh --help`; `ssh vps 'command -v hyperfine >/dev/null 2>&1 && echo hyperfine-present || echo hyperfine-missing; test -d /home/ubuntu/data/projects/manual-many-scopes-common && echo fixture-present || echo fixture-missing'`; `git diff -- src/cli/check/ls_fast.rs src/cli/check/ls_fast_naming.rs src/cli/check/ls_fast_plan_tests.rs > /tmp/assura-fast-naming-lazy-path.patch`; `rsync -az --delete --exclude .git --exclude target --exclude website/node_modules --exclude .trellis/.runtime ./ vps:/home/ubuntu/data/projects/assura-perf-20260705-fast-naming-lazy-path/after/`; `scp /tmp/assura-fast-naming-lazy-path.patch vps:/home/ubuntu/data/projects/assura-perf-20260705-fast-naming-lazy-path/artifacts/assura-fast-naming-lazy-path.patch`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-fast-naming-lazy-path && cp -a after before && cd before && patch -p1 -R < ../artifacts/assura-fast-naming-lazy-path.patch'`; `ssh vps 'cd /home/ubuntu/data/projects/assura-perf-20260705-fast-naming-lazy-path/before && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/before.json --history target/performance/before.jsonl --website-dir target/performance/before-website --iterations 5 --suite ls-lint && cd /home/ubuntu/data/projects/assura-perf-20260705-fast-naming-lazy-path/after && cargo build --release --bin assura --no-default-features --features json-output,yaml-config && cargo build --release --bin assura-full && cargo build --release -p assura-check-cli && target/release/assura performance-report --output target/performance/after.json --history target/performance/after.jsonl --website-dir target/performance/after-website --iterations 5 --suite ls-lint && cargo xtask performance-no-slower target/performance/after.json'`; `ssh vps 'python3 ... 3 warmups + 30 interleaved exact-command timings on /home/ubuntu/data/projects/manual-many-scopes-common for before/after assura check --quiet and assura-check --quiet ...'`; `cargo run --quiet -- check --format json .`; `cargo xtask evidence`; `git diff --check` |
| 2026-07-05 | Clarified the current second-run structure story from the live code paths and added a repo-native `vps` benchmark task so the next structure slice does not depend on memory. Plain one-shot `assura check` still loads `.assura/config.yml` fresh in each process through `ConfigLoader::parse_validated`, so there is no automatic cross-process config-load elimination on the default cold CLI. The repeated-run options are already present but are explicit surfaces: `assura-check-compiled` consumes a prebuilt `.assura/check-config.bin` artifact and therefore skips YAML parse/semantic validation, `PreparedStructureCheck` plus the daemon/session clients keep a compiled config in memory and only reload when the config hash changes, and the opt-in `--cache-dir` hot result cache still rereads and hashes the YAML file each run before deciding whether it can reuse a prior report. To make the quieter-host workflow repeatable, `cargo xtask perf-vps-ls-lint-compare -- <label> <repo-path>...` now delegates to the existing helper script, so future `many_configured_scopes_regression` before/after runs can be launched from a single repo-native command instead of reconstructing the remote rsync/patch/build sequence by hand. This iteration does not claim a new performance win by test case; it only makes the existing repeated-run options and the `vps` structure benchmark entrypoint explicit for the next optimization slice. | `src/config/loader.rs`; `src/cli/check/cache.rs`; `src/cli/check/prepared.rs`; `crates/assura-check-cli/src/compile_config.rs`; `crates/assura-check-cli/src/compiled.rs`; `xtask/src/main.rs`; `cargo xtask perf-vps-ls-lint-compare -- --help`; `cargo run --quiet -- check --format json .`; `cargo xtask evidence`; `git diff --check` |
| 2026-07-05 | Rejected a narrow public-launcher argv-copy slice after the quieter `vps` proof still failed the keep bar for the public cold row. The experiment removed one whole-argv clone from `assura` before the lightweight `check` fast path and added focused normalization coverage locally, targeting the remaining launcher-only overhead that does not affect `assura-check`. On the authoritative `vps` compare for `many_configured_scopes_regression`, the exact-command tie-breaker looked slightly better for `assura check --quiet` (`22.579 ms` -> `22.462 ms`, `-0.5%`), but the canonical suite row still regressed (`assura-cli` `18.409 ms` -> `18.549 ms`, `+0.8%`) and the diagnostic phases all moved the wrong way (`config-load` `4.263 ms` -> `6.280 ms`, `checker-init` `1.836 ms` -> `2.857 ms`, `walk-and-validate` `4.388 ms` -> `5.388 ms`). `assura-check-cli` improved in the suite (`24.017 ms` -> `17.839 ms`) while its exact command regressed slightly (`21.877 ms` -> `22.317 ms`), which makes this another contradictory slice that does not provide a stable beta-worthy public-launcher win. I reverted the code locally and kept the next structure target explicit: continue looking for cold-launcher or core-structure work that improves `assura check --quiet` without paying it back in the phase rows. | `cargo test -p assura --lib primary_check_normalization`; `cargo run -- check --format json .`; `cargo perf-vps public-launcher-argv-copy src/main.rs src/cli/check/fast_cli.rs`; `/home/ubuntu/data/projects/assura-perf-20260705-public-launcher-argv-copy/before/target/performance/before.json`; `/home/ubuntu/data/projects/assura-perf-20260705-public-launcher-argv-copy/after/target/performance/after.json`; `/home/ubuntu/data/projects/assura-perf-20260705-public-launcher-argv-copy/artifacts/exact-assura-check.json`; `/home/ubuntu/data/projects/assura-perf-20260705-public-launcher-argv-copy/artifacts/exact-assura-check-cli.json` |
| 2026-07-05 | Rejected the launcher-level compiled-config autouse experiment after finally getting a clean same-host A/B on `vps`. I narrowed the patch surface to just `fast_cli.rs` so the remote `before` tree stayed comparable to the retained structure baseline, then ran the standard repo-native helper with the shared `manual-many-scopes-common` fixture and the built-in Python exact-command fallback. The suite summary looked superficially promising on the target fixture: `many_configured_scopes_regression assura-cli` improved from `21.144 ms` to `17.864 ms` (`-15.5%`) and total Assura runtime dropped from `39.062 ms` to `33.511 ms` (`-14.2%`), with `walk-and-validate` also improving from `6.230 ms` to `5.537 ms` (`-11.1%`). But the stronger exact-command tie-breaker on the same shared fixture did not support keeping the slice for the public launcher: `assura check --quiet` regressed from `22.123 ms` to `22.358 ms` (`+1.1%`), while `assura-check --quiet` improved from `20.973 ms` to `20.497 ms` (`-2.3%`). The diagnostic setup phases also moved the wrong way in the suite report (`config-load` `6.032 ms` -> `6.953 ms`, `checker-init` `2.018 ms` -> `3.120 ms`, `configured-structure` `0.0207 ms` -> `0.0275 ms`), so this did not clear the keep bar for the stable-beta structure lane. I reverted the runtime autouse code, kept the explicit compiled-config workflow documentation, and kept the repaired `cargo xtask perf-vps-ls-lint-compare` wrapper because that remote evidence path is still the durable gain from this iteration. | `cargo xtask perf-vps-ls-lint-compare -- compiled-artifact-autouse-fast-cli src/cli/check/fast_cli.rs`; `/home/ubuntu/data/projects/assura-perf-20260705-compiled-artifact-autouse-fast-cli/before/target/performance/before.json`; `/home/ubuntu/data/projects/assura-perf-20260705-compiled-artifact-autouse-fast-cli/after/target/performance/after.json`; `/home/ubuntu/data/projects/assura-perf-20260705-compiled-artifact-autouse-fast-cli/artifacts/exact-assura-check.json`; `/home/ubuntu/data/projects/assura-perf-20260705-compiled-artifact-autouse-fast-cli/artifacts/exact-assura-check-cli.json`; `src/cli/check/fast_cli.rs`; `docs/validation.md`; `scripts/perf-vps-ls-lint-compare.sh` |
| 2026-07-05 | Rejected a literal-only rule-scope ancestor index after a cleaner manual `vps` A/B still failed the public-launcher tie-breaker. This slice added a conservative lookup index for `resolve_rules()`: it only activated when every compiled `RuleScope` path was literal, then resolved rules by walking the checked directory's ancestors instead of scanning the full scope list. That made it a plausible direct fit for `many_configured_scopes_regression`, where the generated config contains hundreds of literal child scopes. I could not use the generic helper patch path directly because reversing the whole dirty `check.rs` diff dropped unrelated retained module wiring from the remote baseline, so I created explicit local `before` and `after` copies, reverted only the rule-scope index in `before`, synced both to `vps`, and ran the same release report plus Python exact-command loop against `/home/ubuntu/data/projects/manual-many-scopes-common`. The suite report on the target fixture looked good enough to tempt another false positive: `many_configured_scopes_regression assura-cli` improved from `19.561 ms` to `17.949 ms` (`-8.2%`) and `assura-check-cli` improved from `18.686 ms` to `17.075 ms` (`-8.6%`), while `config-load` nudged down from `4.139 ms` to `4.089 ms` (`-1.2%`) and the separate no-slower gate stayed green. But the decisive exact-command result again rejected the slice for the stable-beta public launcher: `assura check --quiet` regressed from `22.211 ms` to `23.280 ms` (`+4.8%`) even though `assura-check --quiet` improved from `23.252 ms` to `22.099 ms` (`-5.0%`). The diagnostic in-process path also moved the wrong way in the suite (`assura-in-process` `10.061 ms` -> `10.792 ms`, `+7.3%`; `walk-and-validate` `4.075 ms` -> `4.624 ms`, `+13.5%`), so this did not clear the keep bar either. I reverted the literal-scope index locally and kept the retained fast-rule-shape cache plus configured-dir membership baseline unchanged. The next structure attempt should likely focus back on cold-launcher setup costs that can improve `assura check --quiet` directly without relying on contradictory suite-only wins. | local temp baseline at `/tmp/assura-rule-scope-index-phQd2F`; `cargo test --lib indexed_rules_match_linear_resolution_for_literal_scopes --quiet`; `cargo test --lib dynamic_scopes_do_not_build_rule_index --quiet`; `cargo test --lib mixed_literal_and_dynamic_scopes_disable_rule_index_but_preserve_resolution --quiet`; `cargo run --quiet -- check --format json .`; `ssh vps '... /home/ubuntu/data/projects/assura-perf-20260705-rule-scope-index-manual/before ... performance-report --suite ls-lint ...'`; `ssh vps '... /home/ubuntu/data/projects/assura-perf-20260705-rule-scope-index-manual/after ... performance-report --suite ls-lint ...'`; `/home/ubuntu/data/projects/assura-perf-20260705-rule-scope-index-manual/before/target/performance/before.json`; `/home/ubuntu/data/projects/assura-perf-20260705-rule-scope-index-manual/after/target/performance/after.json`; `/home/ubuntu/data/projects/assura-perf-20260705-rule-scope-index-manual/artifacts/exact-assura-check.json`; `/home/ubuntu/data/projects/assura-perf-20260705-rule-scope-index-manual/artifacts/exact-assura-check-cli.json` |
| 2026-07-05 | Rejected a shared fast-rule-shape cache slice after an isolated same-host `vps` A/B showed that rule sharing helped the check-only and prepared paths but hurt the canonical public structure row. I created explicit local `before` and `after` copies so the baseline preserved the retained configured-dir and literal rule-scope work while reversing only the new `ls_fast_plan` / compiled-plan fast-rule-sharing changes, then synced both copies to `vps` and ran identical 5-iteration cold `performance-report --suite ls-lint` commands plus a 30-run interleaved exact-command loop on `/home/ubuntu/data/projects/manual-many-scopes-common`. The quoted per-test-case `assura-cli` deltas were mixed and not stable-beta worthy: `simple_small` `2.057 ms` -> `1.977 ms` (`-3.9%`), `simple_medium` `3.610 ms` -> `2.988 ms` (`-17.2%`), `simple_library` `1.842 ms` -> `1.775 ms` (`-3.6%`), `multipart_extension_regression` `3.596 ms` -> `3.389 ms` (`-5.8%`), and `rule_heavy_repo` `2.290 ms` -> `2.236 ms` (`-2.3%`), but `web_app` regressed `1.583 ms` -> `1.710 ms` (`+8.0%`), `monorepo_packages` `1.872 ms` -> `2.239 ms` (`+19.6%`), `monorepo_policy` `2.791 ms` -> `2.957 ms` (`+6.0%`), and the target `many_configured_scopes_regression` public row got materially worse from `18.970 ms` to `22.123 ms` (`+16.6%`). The target diagnostic split explains why this was not keepable: `assura-check-cli` improved from `20.323 ms` to `16.924 ms` (`-16.7%`), `checker-init` improved from `1.713 ms` to `1.561 ms` (`-8.9%`), `walk-and-validate` improved from `4.866 ms` to `4.666 ms` (`-4.1%`), and the prepared diagnostic rows improved sharply (`assura-prepared-full-check` `7.715 ms` -> `5.594 ms`, `-27.5%`; `assura-prepared-five-changed-paths` `6.215 ms` -> `2.350 ms`, `-62.2%`). But the public `assura-cli` target row still regressed, `assura-in-process` moved the wrong way (`10.956 ms` -> `11.185 ms`, `+2.1%`), `config-load` regressed (`4.148 ms` -> `4.562 ms`, `+10.0%`), `configured-structure` regressed (`0.0262 ms` -> `0.0283 ms`, `+7.7%`), the cold claim summary fell from `3` 2x passes to `2`, and total Assura runtime rose from `34.798 ms` to `38.455 ms` (`+10.5%`). The exact-command tie-breaker alone looked encouraging (`assura check --quiet` `23.606 ms` -> `22.851 ms`, `-3.2%`; `assura-check --quiet` `24.917 ms` -> `21.492 ms`, `-13.7%`), but that was not enough to overcome the canonical suite regression on the named structure hotspot. I reverted the slice locally, kept the manual `before`/`after` workflow as a useful technique for mixed dirty-tree states, and did not request independent review because the performance proof failed before there was anything worth carrying forward. The next core structure attempt should avoid adding setup work that leaks back into `config-load` on the public launcher even when the check-only path gets faster. | local temp dirs `/tmp/assura-shared-fast-rules-before.zy5ViO` and `/tmp/assura-shared-fast-rules-after.x17KuK`; `/tmp/assura-shared-fast-rules-arc.patch`; `cargo test -p assura --lib identical_literal_scope_shapes_share_compiled_fast_rules`; `cargo test -p assura --lib fast_rule_cache_reuses_identical_shapes`; `cargo test --test cli_check_tests --quiet`; `cargo run --quiet -- check --format json .`; `rsync -az --delete --exclude .git --exclude target --exclude website/node_modules --exclude .trellis/.runtime /tmp/assura-shared-fast-rules-before.zy5ViO/ vps:~/data/projects/assura-shared-fast-rules-arc-before/`; `rsync -az --delete --exclude .git --exclude target --exclude website/node_modules --exclude .trellis/.runtime /tmp/assura-shared-fast-rules-after.x17KuK/ vps:~/data/projects/assura-shared-fast-rules-arc-after/`; `ssh vps '... build both copies, run target/release/assura performance-report --iterations 5 --suite ls-lint, cargo xtask performance-no-slower target/performance/after.json, then 30 interleaved exact-command timings on ~/data/projects/manual-many-scopes-common ...'`; `/home/ubuntu/data/projects/assura-shared-fast-rules-arc-before/target/performance/before.json`; `/home/ubuntu/data/projects/assura-shared-fast-rules-arc-after/target/performance/after.json`; `/home/ubuntu/data/projects/assura-shared-fast-rules-arc-after/target/performance/exact-assura-check.json`; `/home/ubuntu/data/projects/assura-shared-fast-rules-arc-after/target/performance/exact-assura-check-cli.json` |
| 2026-07-05 | Rejected a literal-scope recursion shortcut after the same-host `vps` evidence showed one real target-fixture win but too much collateral damage elsewhere in the public cold rows. The idea was to keep the existing literal-scope index but reuse the current exact/descendant fast-scope state while recursing so literal child directories would not need another scope resolution hop on every descent. I validated the behavior locally with a new nested-descendant file-naming case plus the existing converted LS-Lint directory test and a clean `assura check --format json .`, then benchmarked explicit local `before` and `after` copies on `vps` with identical 5-iteration `performance-report --suite ls-lint` commands and the same 30-run interleaved exact-command loop on `/home/ubuntu/data/projects/manual-many-scopes-common`. The target public row finally moved the right way on both proof surfaces: `many_configured_scopes_regression assura-cli` improved from `25.538 ms` to `20.971 ms` (`-17.9%`), and the exact `assura check --quiet` mean improved from `24.799 ms` to `23.805 ms` (`-4.0%`). `config-load` also improved slightly (`4.559 ms` -> `4.222 ms`, `-7.4%`) and `walk-and-validate` moved down a bit (`4.628 ms` -> `4.516 ms`, `-2.4%`). But the slice still failed the keep bar because several other public cold rows regressed sharply: `simple_library` `1.391 ms` -> `2.027 ms` (`+45.7%`), `multipart_extension_regression` `2.493 ms` -> `3.701 ms` (`+48.4%`), `rule_heavy_repo` `1.720 ms` -> `2.531 ms` (`+47.1%`), `monorepo_policy` `2.399 ms` -> `2.911 ms` (`+21.3%`), `monorepo_packages` `1.614 ms` -> `1.860 ms` (`+15.3%`), and even `simple_small` nudged worse (`+1.7%`). The paired target `assura-check-cli` row also regressed badly from `19.015 ms` to `25.317 ms` (`+33.1%`) even though its exact command improved modestly (`23.921 ms` -> `23.113 ms`, `-3.4%`). The aggregate summary did not collapse, but it also did not advance completion: `two_x_pass_count` stayed `2`, `two_x_fail_count` stayed `6`, and total Assura runtime only moved from `38.210 ms` to `37.418 ms` (`-2.1%`). Because the public cold spillover is too large for a stable-beta structure lane, I reverted the slice locally and kept only the rejection log. This result also sharpens the next target: a structure win that depends on changing recursive scope-state semantics can move the named hotspot, but it is still too easy for that change to hurt smaller or more irregular fixtures, so the next core slice should stay narrower than traversal-wide rule propagation unless the proof improves across more than one public fixture family. | local temp dirs `/tmp/assura-literal-fast-recursion-before.xP68eN` and `/tmp/assura-literal-fast-recursion-after.GtYd4T`; `/tmp/assura-literal-fast-recursion.patch`; `cargo test --test cli_check_tests check_validates_converted_ls_lint_dir_rules --quiet`; direct repro fixture at `/tmp/assura-literal-fast-scope.S0wCnJ`; `target/debug/assura check /tmp/assura-literal-fast-scope.S0wCnJ`; `cargo test --test cli_check_fast_scope_tests --quiet`; `cargo run --quiet -- check --format json .`; `rsync -az --delete --exclude .git --exclude target --exclude website/node_modules --exclude .trellis/.runtime /tmp/assura-literal-fast-recursion-before.xP68eN/ vps:~/data/projects/assura-literal-fast-recursion-before/`; `rsync -az --delete --exclude .git --exclude target --exclude website/node_modules --exclude .trellis/.runtime /tmp/assura-literal-fast-recursion-after.GtYd4T/ vps:~/data/projects/assura-literal-fast-recursion-after/`; `ssh vps '... build both copies, run target/release/assura performance-report --iterations 5 --suite ls-lint, cargo xtask performance-no-slower target/performance/after.json, then 30 interleaved exact-command timings on ~/data/projects/manual-many-scopes-common ...'`; `/home/ubuntu/data/projects/assura-literal-fast-recursion-before/target/performance/before.json`; `/home/ubuntu/data/projects/assura-literal-fast-recursion-after/target/performance/after.json`; `/home/ubuntu/data/projects/assura-literal-fast-recursion-after/target/performance/exact-assura-check.json`; `/home/ubuntu/data/projects/assura-literal-fast-recursion-after/target/performance/exact-assura-check-cli.json` |
| 2026-07-05 | Rejected a smaller configured-dir membership index after a clean same-host `vps` A/B showed the usual mixed signal: the suite hotspot improved, but the public cold launcher tie-breaker and spillover still were not beta-worthy. This slice only changed `StructureChecker::is_configured_dir(...)`, swapping the sorted `Vec<PathBuf>` binary search for a size-based `ConfiguredDirIndex` that keeps small config sets sorted and upgrades larger sets like `many_configured_scopes_regression` to a `HashSet<PathBuf>` membership check. I built explicit local `before` and `after` copies so the baseline reversed only that change, synced both to `vps`, built the same release bundle, ran identical 5-iteration `performance-report --suite ls-lint` commands, enforced `cargo xtask performance-no-slower` on the candidate, and then ran the shared-fixture exact-command tie-breaker with the built-in Python loop on `/home/ubuntu/data/projects/manual-many-scopes-common`. On the target fixture the suite rows looked strong: `many_configured_scopes_regression assura-cli` improved from `22.047 ms` to `19.336 ms` (`-12.3%`), `assura-check-cli` from `21.025 ms` to `19.763 ms` (`-6.0%`), `assura-in-process` from `16.011 ms` to `10.155 ms` (`-36.6%`), `config-load` from `7.003 ms` to `3.995 ms` (`-42.9%`), `checker-init` from `3.016 ms` to `1.680 ms` (`-44.3%`), `configured-structure` from `0.0312 ms` to `0.0188 ms` (`-39.7%`), and `walk-and-validate` from `5.982 ms` to `3.981 ms` (`-33.5%`), with `claim_summary.total_assura_runtime_ms` also improving from `37.644 ms` to `34.851 ms` (`-7.4%`). But the stronger exact-command result for the public launcher still moved the wrong way: `assura check --quiet` regressed from `23.741 ms` mean to `23.964 ms` (`+0.9%`) even though `assura-check --quiet` improved from `24.790 ms` to `23.896 ms` (`-3.6%`). Spillover on the public `assura-cli` suite row was also too mixed to justify keeping it with `two_x_pass_count` still flat at `2`: regressions hit `simple_medium` (`+12.2%`), `rule_heavy` (`+23.6%`), `simple_library` (`+4.6%`), `monorepo_packages` (`+9.3%`), `real_project_agentic_feedback` (`+6.0%`), `ignored_generated_heavy_repo` (`+3.0%`), and `multipart_extension_regression` (`+11.7%`), while wins were limited to `monorepo_large` (`-9.6%`), `web_app` (`-7.3%`), `monorepo_policy` (`-16.0%`), `rule_heavy_repo` (`-2.6%`), and `simple_small` (`-1.8%`). Because the public cold launcher did not actually get faster on the shared fixture and the broader row-family proof stayed mixed, I reverted the slice locally and did not request independent review before reverting; the performance evidence already failed the keep bar. This narrows the next structure target further: avoid optimizations that only improve configured-structure bookkeeping or suite-only hotspot rows unless they also improve the exact `assura check --quiet` launcher path on the same canonical fixture. | `cargo test --lib small_configured_dir_sets_keep_sorted_lookup --quiet`; `cargo test --lib large_configured_dir_sets_keep_membership_correct --quiet`; `cargo test --test cli_check_tests check_validates_converted_ls_lint_dir_rules --quiet`; local temp dirs `/tmp/assura-configured-dir-before.KKhvXh` and `/tmp/assura-configured-dir-after.WQWJqY`; `rsync -az --delete --exclude .git --exclude target --exclude website/node_modules --exclude .trellis/.runtime /tmp/assura-configured-dir-before.KKhvXh/ vps:/home/ubuntu/data/projects/assura-configured-dir-before/`; `rsync -az --delete --exclude .git --exclude target --exclude website/node_modules --exclude .trellis/.runtime /tmp/assura-configured-dir-after.WQWJqY/ vps:/home/ubuntu/data/projects/assura-configured-dir-after/`; `ssh vps '... build both copies, run target/release/assura performance-report --iterations 5 --suite ls-lint, cargo xtask performance-no-slower target/performance/after.json, then 30 interleaved exact-command timings on /home/ubuntu/data/projects/manual-many-scopes-common ...'`; `/home/ubuntu/data/projects/assura-configured-dir-before/target/performance/before.json`; `/home/ubuntu/data/projects/assura-configured-dir-after/target/performance/after.json`; `/home/ubuntu/data/projects/assura-configured-dir-after/target/performance/configured-dir-exact-assura-check.json`; `/home/ubuntu/data/projects/assura-configured-dir-after/target/performance/configured-dir-exact-assura-check-cli.json` |
| 2026-07-05 | Corrected the dev-VPS routing detail after checking the live machine config instead of relying on naming intent. The local SSH setup does not currently define a working `vps-dev` alias; `ssh -G vps-dev` resolves to the literal hostname `vps-dev`, while the reachable high-power benchmark host is the existing `vps` alias pointing at `135.148.149.62` with `ubuntu` and the configured project root. I therefore switched the repo-native structure helper and docs back to default to `vps`, while still keeping `--host` plus `ASSURA_PERF_VPS_HOST` available for any future alias cleanup. This does not change product performance behavior, but it fixes the benchmark entrypoint so the next structure lane can actually be rerun from one command on the intended remote box. | `ssh -G vps`; `ssh -G vps-dev`; `~/.ssh/config`; `scripts/perf-vps-ls-lint-compare.sh`; `docs/validation.md`; `.agents/skills/assura-performance-reporting/SKILL.md` |
| 2026-07-05 | Rejected the narrow public-launcher argv shortcut slice after the quieter `vps` proof showed the exact problem we want to stop tolerating: a change that helps an auxiliary entry point while making the public accepted cold row worse. This experiment only removed one extra argv collection on direct `assura check ...` invocations by splitting the primary-launcher normalization logic into `fast_cli_primary` and handing owned argv into the lightweight check parser before the companion fallback. Focused local tests and self-check passed, but the comparable same-host `vps` A/B failed before the exact shared-fixture tie-breaker even ran because `cargo xtask performance-no-slower` rejected the accepted target row: `many_configured_scopes_regression assura-cli` regressed from `19.755 ms` to `20.524 ms` (`+3.9%`) and finished slower than native LS-Lint on the same report (`19.713 ms`). The internal split was misleading in the familiar way: `assura-check-cli` improved sharply on that target row (`25.063 ms` -> `20.337 ms`, `-18.9%`) and `walk-and-validate` improved slightly (`4.677 ms` -> `4.398 ms`, `-6.0%`), but `config-load` worsened (`3.736 ms` -> `4.077 ms`, `+9.1%`), `checker-init` worsened (`1.609 ms` -> `1.699 ms`, `+5.6%`), `configured-structure` worsened (`0.0196 ms` -> `0.0243 ms`, `+24.0%`), and total Assura runtime still rose from `35.715 ms` to `36.374 ms` (`+1.8%`). Public `assura-cli` spillover was also too mixed to justify keeping a new launcher-specific path: wins on `simple_medium` (`-12.7%`), `web_app` (`-10.4%`), and `multipart_extension_regression` (`-18.6%`) were offset by regressions on `many_configured_scopes_regression` (`+3.9%`), `simple_library` (`+15.5%`), `monorepo_large` (`+10.2%`), `monorepo_packages` (`+4.7%`), `monorepo_policy` (`+8.2%`), and `rule_heavy_repo` (`+7.3%`). I reverted the launcher slice locally instead of adding yet another special entry-point behavior. This reinforces the next structure rule: do not keep optimizing via new entry-point-specific branching unless the ordinary `assura check --quiet` row itself gets faster on the accepted canonical fixture. | `cargo test -p assura primary_check_normalization --lib --quiet`; `cargo test --test cli_command_surface_tests check_help_uses_lightweight_primary_path --quiet`; `cargo run --quiet -- check --format json .`; `cargo perf-vps public-launcher-argv-copy src/main.rs src/cli/check.rs src/cli/check/fast_cli.rs src/cli/check/fast_cli_full_path.rs src/cli/check/fast_cli_primary.rs`; `/home/ubuntu/data/projects/assura-perf-20260705-public-launcher-argv-copy/before/target/performance/before.json`; `/home/ubuntu/data/projects/assura-perf-20260705-public-launcher-argv-copy/after/target/performance/after.json`; `cargo xtask performance-no-slower benches/history/current.json`; `cargo xtask native-performance-no-regression benches/history/native-current.json` |
| 2026-07-05 | Rejected a lazy configured-dir lookup slice after the same-host `vps` proof split in the now-familiar unkeepable way: the exact public command improved a little, but the accepted suite row and total cold runtime still got worse. This experiment only touched `src/cli/check/validators.rs` and `src/cli/check/ls_fast.rs`, deferring configured-dir membership checks until parent directory policy or parent file-name fallback could actually use that answer. Focused LS-Lint compatibility tests and `assura check --format json .` stayed green. The comparable `cargo perf-vps lazy-configured-dir-lookup ...` run kept the separate no-slower gate green, but the accepted hotspot still regressed in the checked suite: `many_configured_scopes_regression assura-cli` worsened from `20.314 ms` to `20.787 ms` (`+2.3%`) and `assura-check-cli` worsened from `20.484 ms` to `25.572 ms` (`+24.8%`). The diagnostic phase split looked tempting but still did not translate into a keepable public row: `assura-in-process` improved from `14.322 ms` to `10.743 ms` (`-25.0%`), `config-load` from `6.146 ms` to `3.881 ms` (`-36.8%`), `checker-init` from `2.636 ms` to `1.616 ms` (`-38.7%`), `configured-structure` from `0.0458 ms` to `0.0165 ms` (`-64.0%`), and `walk-and-validate` from `5.878 ms` to `5.024 ms` (`-14.5%`), yet `claim_summary.total_assura_runtime_ms` still rose from `35.848 ms` to `36.286 ms` (`+1.2%`). The exact shared-fixture tie-breaker for the real public launcher did improve modestly, `assura check --quiet` `23.850 ms` -> `23.613 ms` (`-1.0%`), and `assura-check --quiet` improved from `23.560 ms` to `22.324 ms` (`-5.2%`), but that was not enough to offset the checked accepted-row regression. Public `assura-cli` spillover also stayed too mixed for a beta keep: wins hit `ignored_generated_heavy` (`-14.2%`), `monorepo_large` (`-28.3%`), `simple_library` (`-6.3%`), `monorepo_policy` (`-4.8%`), `simple_small` (`-0.7%`), and `web_app` (`-24.1%`), while regressions hit `many_configured_scopes_regression` (`+2.3%`), `rule_heavy` (`+26.0%`), `simple_medium` (`+10.8%`), `real_project_agentic_feedback` (`+9.1%`), `ignored_generated_heavy_repo` (`+35.4%`), `monorepo_packages` (`+3.7%`), and `multipart_extension_regression` (`+0.8%`). I reverted the slice locally and did not request independent review before reverting because the performance proof already failed the keep bar. This closes off another variation of configured-dir lookup tuning: future structure work should require the accepted suite row and the exact `assura check --quiet` tie-breaker to agree, not just one of them. | `cargo test --test cli_check_tests check_validates_converted_ls_lint_dir_rules --quiet`; `cargo test --test ls_lint_tests --quiet`; `cargo run --quiet -- check --format json .`; `cargo perf-vps lazy-configured-dir-lookup src/cli/check/validators.rs src/cli/check/ls_fast.rs`; `/home/ubuntu/data/projects/assura-perf-20260705-lazy-configured-dir-lookup/before/target/performance/before.json`; `/home/ubuntu/data/projects/assura-perf-20260705-lazy-configured-dir-lookup/after/target/performance/after.json`; `/home/ubuntu/data/projects/assura-perf-20260705-lazy-configured-dir-lookup/artifacts/exact-assura-check.json`; `/home/ubuntu/data/projects/assura-perf-20260705-lazy-configured-dir-lookup/artifacts/exact-assura-check-cli.json` |
| 2026-07-05 | Rejected a recursive validation-context buffer slice after a same-host `vps` A/B showed a clear public-row regression on the accepted hotspot even though many adjacent public fixtures improved. This experiment rewired `src/config/config/validation.rs` so structure-semantic validation reused one mutable context string while recursing through `structure.*.children.*`, instead of allocating a fresh `format!("{context}...")` string on every successful child descent. I added narrow regression coverage to keep nested error paths intact, and local self-check plus the converted LS-Lint directory test stayed green before the benchmark. The comparable `cargo perf-vps validation-context-buffer ...` run failed before the exact-command tie-breaker stage because the separate no-slower gate rejected the accepted row outright: `many_configured_scopes_regression assura-cli` worsened from `19.207 ms` to `24.914 ms` (`+29.7%`) and landed slower than native LS-Lint on the same report (`23.582 ms`). The split was again misleading if you only looked at some neighboring rows: `assura-check-cli` actually improved on the target fixture from `21.581 ms` to `19.449 ms` (`-9.9%`), and `walk-and-validate` improved from `5.120 ms` to `4.891 ms` (`-4.5%`). But the public accepted row and its setup phases moved the wrong way: `assura-in-process` `10.883 ms` -> `11.072 ms` (`+1.7%`), `config-load` `3.810 ms` -> `3.939 ms` (`+3.4%`), `checker-init` `1.669 ms` -> `1.703 ms` (`+2.1%`), `configured-structure` `0.0200 ms` -> `0.0233 ms` (`+16.6%`), and total Assura runtime rose from `35.033 ms` to `38.207 ms` (`+9.1%`). Public `assura-cli` spillover stayed mixed rather than universally bad: wins hit `monorepo_large` (`-26.3%`), `web_app` (`-30.3%`), `simple_medium` (`-18.5%`), `simple_library` (`-24.4%`), `rule_heavy_repo` (`-25.1%`), and `real_project_agentic_feedback` (`-24.6%`), while regressions hit the exact accepted hotspot `many_configured_scopes_regression` (`+29.7%`), `monorepo_packages` (`+3.4%`), and `multipart_extension_regression` (`+3.5%`). Because the accepted target row got materially worse and the no-slower gate failed before exact same-fixture timing even ran, I reverted the slice locally and did not request independent review before reverting. This closes off another config-semantic micro-optimization idea: future structure work should treat recursive context-building tweaks as low-probability unless they improve the accepted public row itself, not just neighboring fixtures or check-only rows. | `cargo test --lib test_parse_invalid_nested_naming_reports_child_context -- --nocapture`; `cargo test --test cli_check_tests check_validates_converted_ls_lint_dir_rules --quiet`; `cargo run --quiet -- check --format json .`; `cargo perf-vps validation-context-buffer src/config/config/validation.rs src/config/config/context.rs src/config/config/support.rs src/config/loader.rs src/config/loader/validation_context_tests.rs`; `/home/ubuntu/data/projects/assura-perf-20260705-validation-context-buffer/before/target/performance/before.json`; `/home/ubuntu/data/projects/assura-perf-20260705-validation-context-buffer/after/target/performance/after.json`; `cargo xtask performance-no-slower benches/history/current.json`; `git diff --check` |
| 2026-07-05 | Revalidated the current dirty performance-polish lane from live repo state before attempting another cold-CLI optimization. The workflow gate still reports `blocked_dirty_worktree`, but the dirty paths are all in this same native-regression plus structure-performance lane rather than unrelated churn. The checked artifact mirrors still match (`current.json` and `native-current.json` each match their website copies), and the current machine-checkable summary is now explicit: accepted cold LS-Lint-equivalent rows are still `8/8` no-slower than native LS-Lint but only `2/8` meet the stricter 2x bar, while the warm session row family remains `8/8` complete at 2x. The native regression gate, LS-Lint no-slower gate, content-runtime and project-intelligence tests, docs build, evidence checks, target-state audit, self-check, and `git diff --check` all passed on the current worktree. That means the active dirty slice is coherent enough to keep building on. It also sharpens the next structure target: the remaining core public optimization problem is still the ordinary `assura check --quiet` path on `many_configured_scopes_regression`, not another internal-only or entrypoint-specific win. For repeat proof on the remote box, the repo now has `cargo perf-vps ...` as the remembered one-command `vps` harness, and config-load behavior is unchanged for the one-shot CLI: fresh processes still reload `.assura/config.yml` unless the explicit compiled-config flow is used. | `python3 ./.trellis/scripts/workflow_gate.py --platform codex`; `git status --short`; `shasum benches/history/current.json website/public/data/performance/current.json benches/history/native-current.json website/public/data/performance/native-current.json`; `jq '{claim_summary, warm_claim_summary}' benches/history/current.json`; `cargo test -p assura native_regression --lib --quiet`; `cargo test -p xtask native_performance_gate_ --quiet`; `cargo test --test performance_report_contract_tests --quiet`; `cargo test --test project_intelligence_fact_model_tests --test project_intelligence_store_spike_tests --quiet`; `cargo test --test content_runtime_validation --test content_runtime_create --test content_runtime_update --test content_runtime_references --test content_runtime_adapters --quiet`; `cargo xtask native-performance-no-regression benches/history/native-current.json`; `cargo xtask performance-no-slower benches/history/current.json`; `cargo run --quiet -- check --format json .`; `cargo xtask docs`; `cargo xtask evidence`; `cargo xtask target-state`; `git diff --check` |
| 2026-07-05 | Rejected an exact `--quiet` parser fast path after the same-host `vps` proof split in the wrong way for the stable-beta public cold lane. This slice bypassed `pico_args` only for the literal benchmarked invocation shapes `assura check --quiet` and `assura-check --quiet`, so it was narrower than the earlier launcher-specific argv experiment and mapped directly to the accepted headline command shape. Local unit coverage, command-surface coverage, and self-check all passed before benchmarking. On the comparable five-iteration suite run, the target accepted row looked promising at first: `many_configured_scopes_regression assura-cli` improved from `21.097 ms` to `19.245 ms` (`-8.8%`), `walk-and-validate` improved slightly from `5.476 ms` to `5.345 ms` (`-2.4%`), and `claim_summary.total_assura_runtime_ms` dropped from `37.605 ms` to `36.147 ms` (`-3.9%`). But the stronger same-fixture exact-command tie-breaker on `/home/ubuntu/data/projects/manual-many-scopes-common` decisively failed the keep bar: `assura check --quiet` regressed from `24.730 ms` mean to `27.055 ms` (`+9.4%`), and `assura-check --quiet` regressed from `26.379 ms` to `26.938 ms` (`+2.1%`). The internal phase split also got worse in the suite report despite the headline row win: `assura-in-process` `11.659 ms` -> `13.893 ms` (`+19.2%`), `config-load` `4.274 ms` -> `5.848 ms` (`+36.8%`), `checker-init` `1.806 ms` -> `2.649 ms` (`+46.7%`), `configured-structure` `0.0216 ms` -> `0.0259 ms` (`+19.8%`), and even `assura-check-cli` regressed from `19.894 ms` to `20.271 ms` (`+1.9%`). Because the exact public command got materially slower on the shared canonical fixture, I reverted the slice locally and did not retain the code. This closes off another plausible CLI-floor micro-optimization idea: future public cold work should not keep a suite-row improvement unless the exact `assura check --quiet` tie-breaker agrees on the same host and fixture. | `cargo test -p assura exact_quiet_options --lib --quiet`; `cargo test --test cli_command_surface_tests check_help_uses_lightweight_primary_path --quiet`; `cargo run --quiet -- check --format json .`; `cargo perf-vps exact-quiet-fast-path src/cli/check/fast_cli.rs src/cli/check/fast_cli_exact.rs`; `/home/ubuntu/data/projects/assura-perf-20260705-exact-quiet-fast-path/before/target/performance/before.json`; `/home/ubuntu/data/projects/assura-perf-20260705-exact-quiet-fast-path/after/target/performance/after.json`; `/home/ubuntu/data/projects/assura-perf-20260705-exact-quiet-fast-path/artifacts/exact-assura-check.json`; `/home/ubuntu/data/projects/assura-perf-20260705-exact-quiet-fast-path/artifacts/exact-assura-check-cli.json`; `cargo xtask performance-no-slower benches/history/current.json`; `git diff --check` |

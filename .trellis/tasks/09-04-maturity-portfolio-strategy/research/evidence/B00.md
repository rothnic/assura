# B00 completion evidence

State: done

Source SHA: `ed093668918bc271fc98b9112acaf7c1bf3eb314` (`origin/master` fetched 2026-09-05)

Worktree and command cwd: `/Users/nroth/.codex/worktrees/assura-b00-current-source-baseline` on `goal/b00-current-source-baseline`; clean at baseline capture. The first goal-created checkout under the repository root was removed cleanly because Assura traversed it; the replacement is outside the repository. No existing worktree was removed. `git worktree prune --dry-run` found only two pre-existing, prunable `/private/tmp` records, left untouched.

Planning handoff: `f87ce1257806fbc843d99c23e6f9fbdd8105d71b` on `docs/maturity-portfolio-strategy`, PR #143. It is a 15-file planning-only handoff rebased directly on `ed093668`; independent review found and resolved an ancestry gap by requiring this handoff to remain the parent for backlog evidence. The PR cannot merge until the baseline R01 watch failure is repaired; this is recorded rather than waived.

Baseline commands and results:

- `cargo build --locked --bins` from the isolated worktree: exit 0; binary paths `target/debug/assura` and `target/debug/assura-full` each reported `assura 0.4.0` with `--version`.
- `cargo test --test watch_cli watch_honors_the_requested_directory_scope -- --exact --nocapture`: exit 0, 1 passed, 11 filtered on the local macOS host. This does not certify hosted macOS behavior.
- `cargo run --bin assura-full -- check --format json .`: exit 0; 1,724 files and 389 directories checked, with 16 low-severity max-line advisories. Successful exit is not advisory-clean.

Hosted baseline: PR #143 run [33998181210](https://github.com/rothnic/assura/actions/runs/33998181210) is terminal. Scope, structure/evidence, check, Rustfmt, Clippy, Ubuntu suite, coverage, release smoke, all Ubuntu/macOS/Windows installer-adoption jobs, Windows installer smoke, and the performance report passed. macOS suite failed in `watch_uses_a_full_report_when_project_wide_policy_is_configured`: `tests/watch_cli.rs:248` expected `project_wide_policy` but received `full_rescan_event`; Windows suite was canceled by CI fail-fast rather than independently tested.

Negative control: no CI failure was reclassified as passing, no workflow gate was relaxed, and no worktree/configuration exception was added. PR #142 remains the only open Assura product PR and is reserved for installer work; NickRoth case-study branch corresponds to open PR #60.

Review: independent planning-handoff review found one load-bearing concern: evidence must be in a branch ancestry available to execution, not stranded in an older checkout. The handoff was rebased to current master and PR #143 created; it remains unmerged because R01's baseline failure is real.

Remaining: B00 is complete as a baseline capture, but PR #143 remains unmerged because R01's watched-project failure is real. R01 is the next repair card; R02 remains independently ready after B00.

Integration: planning handoff is PR #143, not merged. No code/product PR was created by B00.

Next ready ID: R01; R02 is also independently eligible.

# W01 working marketing CTAs evidence

State: locally verified; awaiting independent review and hosted PR gates.

Source baseline: `09310aa4939fc2e5f7eb1ef652e33b579d83f393` from
`origin/master`. Worktree:
`/Users/nroth/.codex/worktrees/assura-w01-marketing-ctas`; branch:
`goal/w01-marketing-ctas`.

## Contract and change

The red test added to `website/src/tests/marketing.spec.ts` visits each
marketing route, resolves every local fragment link to its destination route,
and requires an element with the referenced id. It failed before implementation
on `/compare/ls-lint/`: `#onboard` resolved to
`/compare/ls-lint/#onboard`, where no element existed.

`MarketingLayout` now derives a default setup destination from canonical path,
and `FocusedMarketingPage` passes the same destination to both its header and
hero CTA. Focused pages use `/#onboard`; the homepage retains `#onboard` for
the no-JavaScript local anchor. Existing focused documentation CTAs continue to
use their supplied guide links.

## Verification

All commands ran from the stated worktree and exited 0 after the change:

```text
pnpm --dir website exec playwright test src/tests/marketing.spec.ts --grep 'every local marketing fragment|setup actions retain'
pnpm --dir website test:marketing                         # 105 passed
cargo xtask docs
cargo run --quiet -- check --format json .
cargo xtask evidence
git diff --check
```

Manual local-browser checks used `http://127.0.0.1:4323/about/`: at desktop
and 375x812 mobile viewport, keyboard activation of both Start controls opened
the setup dialog. The rendered link target was `/#onboard`; the browser viewport
override was reset after the check.

Negative control: the new test's original failure proves it catches a
page-relative `#onboard` on a focused route. The route-wide assertion covers
local fragments without requiring cross-domain navigation.

Independent review of `fdaf9a0f18daedd5e8c05d2dc05aec1ef37f2790` found no
actionable issues: it confirmed the shared destination, homepage fallback,
fragment coverage, documentation CTA preservation, and local-only evidence
state.

Limitations: this is local candidate evidence only. No deployment, release,
or public communication occurred. Next: hosted PR gates; after merge, verify
reachability from `origin/master` and mark W01 done.

# R02 completion evidence

State: verified locally; hosted proof pending

Base source SHA: `f3a8f664e849d6ced223448dea63466405ced544`

Worktree and command cwd: `/Users/nroth/.codex/worktrees/assura-r02-performance-evidence` on `goal/r02-performance-evidence`.

Hosted root cause: [PR #144 run 34002366854, performance job 101403414898](https://github.com/rothnic/assura/actions/runs/34002366854/job/101403414898) generated and uploaded the LS-Lint report, then `cargo xtask performance-no-slower` failed. GitHub Actions default failure propagation skipped native and warm generation, but their `if: always()` uploads still required `native-current.json`, `native-history.jsonl`, and warm-loop files. Those missing-file upload errors were cascading evidence loss, not independent benchmark results.

Change: the performance bundle now has `perf_build`; each LS-Lint, native, and warm generation step has its own ID and runs after a successful bundle build with `!cancelled()`. Each gate runs only after its own report generation succeeds, so a failed LS-Lint gate stays nonzero while native and warm generation continue. Artifact uploads require both successful generating steps and all expected files. The step summary reports `generation_failed` for failed generation and `not_run_build_failed` for a failed bundle prerequisite; it never manufactures an empty report or calls a skipped gate passing.

Focused RED then GREEN:

- Added `tests::performance_workflow_preserves_independent_reports_after_a_comparison_failure` in `xtask/src/main.rs` before the workflow edit. The initial command `cargo test -p xtask performance_workflow_preserves_independent_reports_after_a_comparison_failure -- --nocapture` exited 101 with 1 failed test and listed the missing build/generation/gate/upload/summary contract boundaries.
- After the workflow edit, the same command exited 0: 1 passed, 0 failed, 34 filtered.
- `cargo test -p xtask -- --nocapture` exited 0: 35 passed, 0 failed.

Review follow-up RED then GREEN:

- Independent review correctly found that `perf_warm_checked_gate` was not included in the summary, so a checked-history failure could be hidden by a successful generated p95 gate. The contract test was extended with the required summary environment/output entries and an outcome-oriented fixture.
- Before the correction, the focused command exited 101 with 1 failed test and two missing requirements: the summary did not retain or report `PERF_WARM_CHECKED_GATE_OUTCOME`.
- After the correction, the same focused command exited 0: 1 passed, 0 failed, 34 filtered. Its fixture proves that an LS-Lint gate failure leaves `job_failed: true` while native and warm generation and both artifacts remain eligible; a failed build leaves neither generation nor artifact eligible.
- A local summary simulation with LS-Lint `failure`, native `success`, checked-warm `failure`, and warm p95 `success` emitted all four outcomes explicitly. It did not reclassify either failed gate as passing.

Workflow guards and negative controls:

- `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml")'` exited 0.
- Extracting and compiling the embedded summary program with `python3` exited 0.
- Simulated failed-build inputs (`PERF_BUILD_OUTCOME=failure`) emitted `not_run_build_failed` for LS-Lint, native, and warm evidence, with no report parsing or upload attempt.
- `cargo xtask check` exited 0.
- `cargo xtask evidence` exited 0.
- `cargo run --quiet --bin assura-full -- check --format json .` completed with `success: false`: current project policy still has the pre-existing low max-line advisories and blocking evidence-name findings for `B00.md` and this required `R02.md`. The backlog requires the exact `evidence/<ID>.md` contract; R07 owns the policy disposition. R02 neither renames the mandated evidence nor weakens the policy.
- `cargo xtask target-state` exited nonzero only because the shared active Trellis task expects a branch/PRD association not present in this isolated R02 worktree (`workflow gate: active task branch must match current branch`; `active task needs prd.md`). It is not treated as a passing R02 gate or corrected by weakening the gate.

Limitations: local tests validate the workflow contract and failure-summary behavior, but cannot execute GitHub Actions failure propagation or prove artifact retention. The reported LS-Lint regression remains visible and unmodified; R02 does not resolve it.

Hosted proof required: a CI run in which the LS-Lint no-slower gate fails while native and warm generation complete, the two independent artifacts upload, the overall job remains failed, and a failed build shows only `not_run_build_failed` summary statuses without missing-file artifact failures.

Hosted follow-up, run [34003277887](https://github.com/rothnic/assura/actions/runs/34003277887): LS-Lint no-slower genuinely failed at `many_configured_scopes_regression` (`11.037 ms` versus `10.923 ms`) while native and warm generation, gates, and artifacts succeeded. The regression remains a nonzero R03 failure. `Summarize performance` then failed with `NameError: name 'median' is not defined`, because its helper was nested in the LS-Lint formatter while native/warm rendering also used it. Separately, Windows checkout CRLF made the workflow-contract test report false missing steps.

Correction evidence: the focused contract first failed for the helper scope and then for a CRLF fixture before passing after the helper moved to shared summary scope and workflow text was normalized to LF only for matching. An isolated real summary fixture with LS-Lint failure, native success, checked-warm failure, and warm-p95 success exited 0 and rendered every outcome without masking a gate failure. `cargo fmt --all -- --check`, `git diff --check`, `cargo clippy -p xtask -- -D warnings`, all 35 xtask tests, `cargo xtask check`, `cargo xtask evidence`, and YAML parsing passed.

Final hosted follow-up, run [34004073195](https://github.com/rothnic/assura/actions/runs/34004073195): Performance Report ([101408015482](https://github.com/rothnic/assura/actions/runs/34004073195/job/101408015482)) passed every step, including no-slower on this sample. That does not erase the earlier real regression sample; it shows the R02 control flow and summary repair worked. The only remaining failure was the Windows suite's `workflow contract must evaluate identically with Windows checkout line endings`. The production normalizer was already correct; the test constructed CRLF by replacing `\n` in an already-CRLF `include_str!`, producing malformed `CRCRLF` text.

Final fixture correction RED then GREEN: simulating an already-CRLF checkout with the old conversion exited 101 with the same assertion. The test now canonicalizes CRLF to LF before constructing deliberate CRLF. The focused test then passed (1 passed, 34 filtered); all 35 xtask tests, formatting, clippy, `cargo xtask check`, `cargo xtask evidence`, and YAML parsing passed.

Third Windows confirmation, run [34004564012](https://github.com/rothnic/assura/actions/runs/34004564012), job [101409329910](https://github.com/rothnic/assura/actions/runs/34004564012/job/101409329910): the same line-ending assertion remained the only failure. The test first expanded an already-CRLF Windows `include_str!` into `CRCRLF` before cleanup. It now canonicalizes the original source first, then creates exactly one valid CRLF fixture; production workflow and contract requirements remain unchanged. The focused RED/then-GREEN proof and the full xtask validation set passed again. The exact corrected SHA still requires a Windows hosted pass; R02 remains verified, not done.

Next ready ID: R03 remains blocked on hosted R02 proof. R02 remains verified, not done.

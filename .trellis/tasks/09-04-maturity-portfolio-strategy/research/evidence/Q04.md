# Q04 implementation evidence

Current continuation: iteration114 below supersedes historical frontend
equivalence as plain/fast evidence. Locally prepared; blocked on the named
hosted/performance/publication requirements, not merge-ready.

State: implemented locally; final local reruns, hosted gates, and integration
remain required before this card can be marked done.

Baseline: `80f42e94b1b0eaa5ba5d4dfb79028ecb6285b086` (`origin/master` at
selection). Worktree: `/Users/nroth/.codex/worktrees/assura-q04-policy-naming`.
Branch: `goal/q04-policy-naming`. Candidate source version: `0.4.0`. The
initial evidence was captured before the local candidate commit; hosted gates
remain pending.

Initial tested behavior commit: `29b465669030c8fcdce4aa6c8a494130946a3c90`.
After Q03's documentation merge advanced `origin/master` to
`25a141525ce2420f02f3686affbeb03b0915f607`, the current-master behavior
commit is `4ccd8ca`. `git diff --quiet 29b4656 4ccd8ca -- .assura/config.yml
src tests` exited 0: the source/config/test tree is byte-identical; only
Trellis documentation ancestry changed. An independent exact-SHA re-review
found no merge-boundary or behavioral concern.

## Scope and contract

Q04 selected the recommended cohesive family after inspecting current consumers
and history: pure naming predicates formerly in `src/cli/check/case.rs`.
They now live in `src/policy/naming.rs`, behind a single `pub(crate)` policy
module. `validators` and `ls_fast_naming` import that module directly. No
public Rust API, old alias, CLI/report type, or policy-to-CLI dependency was
introduced.

`src/policy` has a durable distinct role: pure repository-policy predicates
shared by check execution paths. It does not fit the CLI orchestration or the
existing constraints engine. The exact `src/policy` child is declared in
`.assura/config.yml` with snake-case files and the existing 500-line ceiling;
no broad allowance was added.

## Focused red/green evidence

All commands below ran from the worktree above.

| Command | Exit | Result |
| --- | ---: | --- |
| `cargo test --lib policy_naming_preserves_supported_naming_predicates` before the move | nonzero | Expected red compile failure `E0433`: `crate::policy` did not exist. |
| `cargo test --lib policy_naming_preserves_supported_naming_predicates` after the move | 0 | One real predicate contract passed: snake, kebab rejection, Pascal, path-substituted regex, multipart stem, and exact-name exception. |
| `cargo test --test cli_command_surface_tests lightweight_and_full_check_preserve_naming_findings -- --nocapture` | 0 | `assura` lightweight and `assura-full` JSON outputs had identical ordered rule/path/severity/message findings for exact, regex, multipart extension, snake, and Pascal fixtures. |
| `cargo fmt -- --check` | 1 | Formatting-only import order drift; corrected with `cargo fmt`. |
| `cargo fmt` and `git diff --check` | 0 | Formatted candidate with no whitespace errors. |
| `cargo test --test cli_command_surface_tests lightweight_and_full_check_preserve_naming_findings -- --nocapture` on the committed candidate | 0 | Final lightweight/full differential passed with the path-substituted `regex:^${0}-generated$` fixture. |
| `cargo test --lib policy_naming_preserves_supported_naming_predicates` on the committed candidate | 0 | Predicate contract passed again on the candidate. |
| `cargo check --no-default-features` | 0 | Minimal-feature workspace check passed on the candidate. |
| `cargo check` | 0 | Default-feature workspace check passed on the candidate. |
| `cargo test --test cli_check_tests` | 0 | 23 CLI-check regressions passed, including regex alternatives, multipart extensions, and deterministic traversal. |
| `cargo build --release --bin assura --no-default-features --features json-output,yaml-config` | 0 | Lightweight release launcher built separately. |
| `cargo build --release --bin assura-full` | 0 | Full companion release binary built separately. |
| `cargo build --release -p assura-check-cli` | 0 | Check-only release package built separately. |
| `target/release/assura performance-report --output target/performance/q04.json --history target/performance/q04.jsonl --website-dir target/performance/website-data --iterations 3` | 0 | Local macOS report recorded all eight headline `assura-cli` rows faster than LS-Lint; `two_x_claim_verdict` remains `not-complete` (1/8 targets), with every row retained. |
| `/usr/local/bin/assura check --format json .` after the structural declaration | 0 | Installed release binary found zero repository-policy violations. |

The local report is a same-machine diagnostic, not evidence that the existing
hosted R03 `many_configured_scopes_regression` is resolved. Its `not-complete`
two-times claim is retained; neither the benchmark row nor its threshold was
changed by Q04.

## Independent review

Two independent review passes found no code, API, module-boundary, or
test-contract issues. The final pass reviewed exact candidate
`29b465669030c8fcdce4aa6c8a494130946a3c90`: `${0}` receives the file's `src`
parent path, so the fixture becomes `^src-generated$`; the fast path delegates
dynamic placeholder regexes to the same predicate. Visibility remains
crate-private, consumers use direct imports, no cycle or re-export exists, and
the differential compares the intended JSON fields. The earlier reviewer's
separate Cargo attempt was inconclusive: concurrent artifact locking followed
by local dependency-build SIGKILL prevented an independent rerun. This is an
environmental non-result, not a passing gate or a source failure.

## Negative controls and limits

- The initial missing-module compile failure is the focused red gate for the
  new crate-private policy boundary.
- Before the narrow configuration change, the installed release check rejected
  `src/policy` as `unexpected_directory`; after the declaration it passes. This
  proves the new path was not silently admitted.
- Ignored `target/` in this goal-owned worktree was removed only after
  `git clean -ndX -- target` reported that exact path. This preserved all
  tracked source and released enough local disk space; it also means the final
  focused reruns require a cold rebuild.
- `vps-dev` was retried with a bounded SSH probe and remains unresolved by DNS,
  so it is unavailable for the remaining expensive build gates.
- Required before merge: final independent review of the exact candidate,
  hosted current-master PR matrix, then reviewed-SHA/tested-SHA and
  `origin/master` reachability verification. The globally failing R03
  Performance Report must remain visible and cannot be waived.

Next ready ID: Q04 hosted PR proof after final review. Other dependency-ready
cards remain independently selectable from the current-master queue.

## Final current-master rerun

The candidate was rebased through the A01 integration and its separate
post-merge record onto `4939081925036a462c88fa74836bc1211bc1d9a0` as
`675041985fded3e873eb68f35b264a23a50cd356`. The rebase required only shared
progress-log conflict resolution; the policy source, tests, and configuration
remained the reviewed cohesive extraction. On that exact SHA, all commands
below exited 0 from this worktree: the focused predicate test; the
lightweight/full differential; all 23 `cli_check_tests`; `cargo check
--no-default-features`; `cargo check`; `cargo fmt -- --check`; `git diff
--check`; `/usr/local/bin/assura check --format json .`; and all three separate
release artifacts (`assura`, `assura-full`, and `assura-check-cli`).

`target/release/assura performance-report --output
target/performance/q04-current.json --history
target/performance/q04-current.jsonl --website-dir
target/performance/website-data --iterations 3` also exited 0. Its cold
`assura-cli` report measured all eight headline rows faster than LS-Lint, but
retained `two_x_claim_verdict: not-complete` (1/8 targets; aggregate ratio
1.2976). The distinct warm session report is complete, but does not replace
the cold claim or the blocked hosted R03 comparison. Final independent review
and the current-master hosted PR matrix are still required before integration.

## Iteration114: current-master review and consumer proof

Local cwd `/Users/nroth/.codex/worktrees/assura-q04-policy-naming`, branch
`goal/q04-policy-naming`. Clean6fdea34 refreshed via `git merge --no-commit
origin/master`; only backlog/progress conflicts. Resolution preserves both
histories and incoming implemented state. Normal hook passes; merge commit
`f0a3dc9173ff2285b5367f239fd6c9844f18a753` contains current master
`98bd187abd70005b40fa2df89ea3f45f332e4a04` and Q03 merge25a1415.
`git diff --cached 6fdea34 -- src/cli/check.rs src/cli/check src/policy
src/lib.rs tests/cli_command_surface_tests.rs .assura/config.yml` before
commit exits0 with empty output: extraction/test/config source unchanged.

Independent `q04_current_review_114` requests changes for one Important gap:
both frontend subprocesses choose the LS fast engine for the naming-only
fixture. Historical equality proves frontend equivalence, not coverage of
the moved plain validators consumer. Equality alone also allows shared missed
or spurious findings. Root accepts correction: same fixture plus root
files.max_lines100 forces plain validation (explicit fast-plan rejection),
without triggering a line-limit violation; expected ordered tuples must be
asserted independently. Add uppercase regex-internal alternatives rather than
only convention alternatives. No production API or policy weakening needed.

VPS setup96802/c711bb creates one disposable isolated clone at
`/home/ubuntu/data/projects/assura-q04-readiness.j2uKNF/repo` on verified
vps-9cb01956 UID1000. Bundle records exactf0a3dc9/master98bd187; original
A05 clone and dirty overlay untouched. Runner pins installed Rust1.98.1 with
CARGO_BUILD_JOBS4 and its own repo/target. Default remote stable1.93/nightly
1.95 are not silently used. Initial parent-dir Git probe was wrong cwd, not
missing source; actual older A05 clone is under repo/ and remains preserved.
No cache/branch/worktree deletion or unrelated workload stop. This ordinary
validation runner is not exclusive and cannot clear R03 performance acceptance.

### Focused negative control and correction

In the disposable VPS clone only, apply `q04-disable-plain.patch` (SHA-256
2faaf1313075e56845336deedd5ae13f93daf2864283633994ad28c66f463afb),
adding false&& to the generic file-naming predicate call in validators.rs.
No candidate production source is changed. Run
`cargo test --test cli_command_surface_tests
lightweight_and_full_check_preserve_naming_findings -- --exact --nocapture`:

- Old test70443/f0bef3:1pass/0fail/20filtered, exit0/capture0 despite the
  missing plain validation. Baseline log `baseline.upasUT/test.log` SHA
  af24a6b73815bab205c00ece946e7ad46501c1ffee6b1100df9c1301ade326f2.
- New test17637/72436e:0pass/1fail/20filtered, exit101/capture0 because plain
  incorrectly exits0 instead of1. RED log `red.ouPpBp/test.log` SHA
  924f19a824813b4a9d502b96069a07097e03958a69668e69f1dddc4149077f8f.
- Reverse only that patch after a reverse-apply check; `git diff --exit-code
  -- src/cli/check/validators.rs` proves restoration. New test83495/6995e7:
  1pass/0fail/20filtered, exit0/capture0. GREEN log `green.ibhFp3/test.log`
  SHA cdf10928cc6fa961d726b51e6c2583ea5080201ba49dc85dba56e837763606b5.

The new test also asserts the exact ordered two medium-severity findings,
preserves frontend equivalence and covers README, both uppercase regex
alternatives, multipart, path substitution, snake/Pascal positives. Native
path separators normalize without sorting or dropping diagnostic fields.
Frozen test SHA331887b6b89af0516c1916822d7f75c91cd67f5356e855101077367eee0102a9.
Independent scoped rereview clears the original finding; root accepts.

Same83495 run: `cargo test --lib
policy_naming_preserves_supported_naming_predicates -- --nocapture` passes
one actual test plus two empty filtered workspace suites (not coverage),
exit0/capture0. Log `green.Hbdg08/test.log` SHA
5d06aae370b3397d9e28c8b1d32cce44f48bce0087f2080f4c9415dffda5aabc.
`cargo check --no-default-features` also exits0/capture0; log
`minimal.Bvtne3/test.log` SHA
5a8c2a67d587c9b4c9f46ebf4f8be55cda8ab1e186c72709978f080b97e98d6c.
Restored debug binary SHA-256s: assura
1b7af61fd6ae2910b00e920cb192abfe84606217c8da7ef65662dfac0daca29e;
assura-full91c7ee090536b9e1cb80279de46e383afa5bb045b6c497955054c0dec5b32e1f.
These are focused Linux debug results, not release performance evidence.

Website prerequisites41260/ed7516: package-manager routing selects pinned
pnpm10.25.0; frozen install reuses356packages without lockfile changes on
Node22.22.1. Full PR tier includes fast internally; do not duplicate it.

Source checkpoint39263/09a8fe: local `cargo fmt --all -- --check` and normal
pre-commit pass. Commit `5e88b0a49df7531a60e268edb5b6c9d62523f7e8` contains
only the reviewed test change and preserves its SHA-256. VPS transfer verifies
complete staged-tree equality to5e88b0a and no unstaged difference before
updating the branch ref; source is clean, no hard reset or content discard.
`git diff --exit-code f0a3dc9 HEAD -- src .assura/config.yml Cargo.toml
Cargo.lock` exits0: no production change in this review correction.

First PR73901/1ea7fd exits1/capture0 before tests because Rust1.98.1 lacks
rustfmt. Log `pr.HJc5eb/test.log` SHA
d3477f47e92b34801cfc6af306c5b6ce36c5aa074d3e5cc465cb0b7adc507f55.
Install matching rustfmt with `rustup component add --toolchain 1.98.1 rustfmt
clippy` (Clippy already present); rerun the unchanged PR gate at exact5e88b0a.
This prerequisite repair is not a passing test or a product-code workaround.

Root also identifies inherited task metadata still naming the documentation
handoff branch. Independent scoped review approves changing only task.json's
branch to actual goal/q04-policy-naming; status/PRD/authority and target-state
gate logic remain unchanged. This is not applied mid-run on the VPS: preserve
the exact5e88b0a PR result, then validate the corrected metadata separately.

### Repository gates and current hold

PR92929/b280f3 at5e88b0a:103 test-result reports total1288passed/0failed/
1ignored/8empty. Ignored and empty results are not passing coverage. Formatting,
minimal/full frontend checks, Rust suites (including all naming/notation/CLI
contracts), structure1781files/394directories/0violations and9evidence policy
cases pass. Overall exit1/capture0 because task branch/PRD ownership checks fail;
Clippy/docs are not reached in that invocation. Retained log
`pr.OHY7ld/test.log` SHA
8bf1ed79a14aafb67b4703bf87fd7f7408b9eb56b3d51fd1361eda0821aa406f.

Reviewed metadata-only checkpoint `2ff1041bad712331cf82825e4c0b79760ee2a7bd`
changes only task.branch and passes normal hook. Complete source/test/config/
Cargo diff against5e88b0a is empty. Clean VPS clone fast-forwards to it before
running the remaining components, session96856/505843, each exit0/capture0:

- `cargo xtask target-state`: pass, `evidence.ODt7Xi/test.log` SHA
  c1c2df4b6dc48c78c115d0bf25ba34b08ce58734e30259c70529ddc7a198f044.
- `cargo clippy --all-targets --all-features -- -D warnings`: pass,
  `green.8BBVy0/test.log` SHA
  8a7bcea88fb9d9b0cc0992c6f28cc13a93f6321b92fd9b1418bd4efcc9e0ba23.
- `cargo xtask docs`: generated-demo checks and48page build pass,
  `docs.hBj0v7/test.log` SHA
  d1dec32f4f15cd1d7c42daf0c31b3554e176bebaaf641f150f0dc7efa1e88714.

Existing dependency future-incompatibility warning for proc-macro-error2 is
retained. Rust gates were not rerun for the one-line metadata fix; this is a
component-by-component completion record, not a rewritten overall PR exit0.
All10logs are copied into the local Q04 ignored runtime q04-proof directory.
Final debug hashes are recorded by the runner; docs rebuilding the full binary
has SHAa95afc5a8c342de5a6b0cba4679e865f594ecf6309633dd7eb2fb4153600fb24.

Live PR164 refresh07d1be: original remote head6fdea34 remains OPEN/CONFLICTING
with macOS and Performance Report failures. Nothing pushed or published.
Current local source/master ancestry and independent review cannot replace
hosted exact-head checks, the prescribed comparable performance row, or R03's
unresolved runner/measurement acceptance. The VPS is not exclusive; do not run
another unchanged R03 comparison or waive a failed threshold to close Q04.
Cloudflare push/merge side effects remain unapproved. Q04 is blocked with local
preparation recorded; no merged/done/performance-accepted claim. Next independent
local card is Q06 current-master readiness, reusing its owned checkout.

## Iteration115: exact-head hosted acceptance and integration

This section supersedes the preceding historical PR164 hold for Q04 only.
Candidate `8556f362c1c7091196a7a0b23c1d7d5e005dcee5` was independently
rereviewed after the focused predicate and lightweight/full-consumer contracts
passed locally and the full `cargo xtask pr` gate exited 0. Its PR was based on
`ef822993c799476653cecdb9ab0bba67537c8291`, the then-current
`origin/master`; a final fetch confirmed that same base, a clean owned
worktree, a clean merge state, and no non-passing hosted checks.

The fresh exact-head hosted matrix passed: Rust CI run `34256745872` (including
Linux, macOS, and Windows suites; all five adoption smokes; MSRV; coverage;
Performance Report; release bundle; and Windows installer), Documentation run
`34256745825`, and Security run `34256745859`. GitGuardian also passed. These
are actual completed passing jobs; no skipped job is counted as a passing
source gate.

PR [#164](https://github.com/rothnic/assura/pull/164) merged on 2026-09-08 as
`9f1b8e31a5110c390d209fc6ddb27de67e95a7ad`, and the candidate is reachable
from `origin/master`. The source PR changed no website or documentation files.
The documentation workflow's Pages deployment is restricted to pushes to
`refs/heads/main`; this integration was a `master` merge, so no deployment or
publication was performed or claimed.

Q04 is done. This records a current passing Q04 performance gate; it does not
waive, modify, or mark the separate R03 investigation complete.

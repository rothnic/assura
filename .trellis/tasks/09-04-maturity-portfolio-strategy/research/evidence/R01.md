# R01 local verification evidence

State: verified (local proof only; hosted-platform and independent-review proof
remain required before `done`).

## Source and scope

- Source SHA before this card commit: `ff24018c8ae17cbd23957d452afc6d1dd6b00ce4`.
- CWD: `/Users/nroth/.codex/worktrees/assura-r01-watch-scope`.
- Branch: `goal/r01-watch-scope`.
- Binary exercised by integration tests: Cargo's
  `CARGO_BIN_EXE_assura-full` debug binary for this checkout.
- Owned changes: `src/cli/watch_tests.rs`, `tests/watch_cli.rs`, this evidence,
  and the R01 row in `research/backlog.json`. `src/cli/watch.rs` was inspected
  but did not require a behavioral change.

## Reproduction and decision

The current hosted macOS failure was the over-constrained assertion in
`watch_uses_a_full_report_when_project_wide_policy_is_configured`: it required
`project_wide_policy`. The local and Linux path emits that fallback for a
single changed path under a project-wide paired-file policy. macOS can also
truthfully receive a `need_rescan` event from FSEvents; shared watch-state
correctly maps that event to `DirtyProject::Full`, so the full requested-scope
report has fallback reason `full_rescan_event`.

The patch keeps both safety properties observable: the report remains a
full requested-scope report and still reports the failed paired-file policy.
It does not discard or downgrade a rescan/overflow event.

## Test-first evidence

The focused RED command was:

```text
cargo test --test watch_cli assert_no_event_rejects_a_disconnected_event_reader -- --exact --nocapture
```

Exit: `101`. Before the helper change, it failed as expected with `test did not
panic as expected`: `WatchProcess::assert_no_event` treated a disconnected JSON
reader as quiet. The minimal helper change distinguishes timeout, unexpected
JSON (included in the failure), and reader disconnection.

## Local commands and results

All commands below ran from the recorded CWD and exited `0`, except the RED
command above.

```text
cargo fmt --check
cargo test project_wide_policy_fallback_keeps_the_requested_scope -- --exact --nocapture
cargo test watch_scope_filters_an_outside_event_and_retains_an_inside_event -- --exact --nocapture
cargo test --test watch_cli watch_uses_a_full_report_when_project_wide_policy_is_configured -- --exact --nocapture
cargo test --test watch_cli watch_honors_the_requested_directory_scope -- --exact --nocapture
cargo test --test watch_cli
cargo test --lib
```

The full integration target passed `13 passed; 0 failed`. The library tier
passed `514` Assura tests and `15` `assura-watch-state` tests. After the full
local tier was green, the exact requested-scope integration test passed on 20
consecutive runs (`watch_honors_the_requested_directory_scope`, each `1 passed;
12 filtered`).

`cargo xtask evidence` also exited `0`. `cargo xtask docs` reached the
repository documentation validations (including 20 release-surface mappings and
54 YAML examples) but exited `1` because this checkout has no
`website/node_modules` and `astro` is unavailable. `cargo run --quiet -- check
--format json .` exited `2`: it reports the existing `B00.md` and this required
`R01.md` evidence filenames as medium file-naming violations, in addition to
the pre-existing low line-length advisories. These are not an R01 behavior
failure; R07 owns their explicit policy disposition.

## Negative controls and limitations

- Deterministic normalization coverage proves a `docs/BadName.ts` event is
  ignored while an existing `src/BadName.ts` event is retained.
- Existing deterministic config tests prove unchanged config-parent events are
  ignored and changed configuration requests reload/full validation.
- Deterministic project-wide-policy and overflow tests prove full
  requested-scope fallback with `project_wide_policy` and
  `event_channel_overflow`, respectively. The latter retains overflow safety.
- The direct macOS FSEvents event path was not captured locally; platform
  closure therefore requires hosted macOS, Linux, and Windows runs against the
  reviewed commit. The card also requires independent review before PR/merge.
- The website build remains unverified in this checkout until its declared
  Node dependencies are installed; no dependency installation was performed by
  this card.

## Integration and next state

No remote push, PR, merge, release, or deployment was performed. The card is
locally verified only. Obtain independent review, then create the R01 PR and
require hosted macOS/Linux/Windows proof before setting it `done`. R02 and P01
remain independently pending; R01's next action is review/hosted proof rather
than another local implementation pass.

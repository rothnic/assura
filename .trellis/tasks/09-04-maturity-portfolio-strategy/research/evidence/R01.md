# R01 local verification evidence

State: active. A hosted regression after the prior closure invalidated `done`;
the current candidate is locally verified only and still needs independent
review plus a fresh macOS/Linux/Windows matrix.

## Source and scope

- Source SHA before this card commit: `ff24018c8ae17cbd23957d452afc6d1dd6b00ce4`.
- Initial implementation and local-verification SHA:
  `b52dc4db7c986ca305b0f594d5d23b99543da29a`.
- Review follow-up SHA: `c3bd50c5994e5c980ad5c0f9607ca0e32c7559b5`.
- Reviewed evidence-only SHA: `2f29520ef6a3bac35877aadd9a757e80ff0ca848`.
- Spawned-binary diagnostic follow-up SHA:
  `0654d3cd360ce9a4796b490d9392e5197559506b`.
- Concrete diagnostic assertion follow-up SHA:
  `3929905fa4493b1ed3db3160e193012ddbb01cfc`.
- Portable diagnostic-kind assertion follow-up SHA:
  `0b53c21fe2c415d3153d7dcde27e49c1fc158422`.
- Hosted-fallback contract follow-up SHA:
  `2061749b82b8f7bcebd55c79c4a291c2158fd3b5`.
- Hosted-ordering diagnostic follow-up SHA:
  `4a33bae0b2dfeb89a0fbbc86891063db92fbf68b`.
- Constrained config-predecessor follow-up SHA:
  `b000820b8568cd5f57b72620f6e7d0f123f3c043`.
- CWD: `/Users/nroth/.codex/worktrees/assura-r01-watch-scope`.
- Branch: `goal/r01-watch-scope`.
- Binary exercised by integration tests: Cargo's
  `CARGO_BIN_EXE_assura-full` debug binary for this checkout.
- Owned changes: `src/cli/watch_tests.rs`, `tests/watch_cli.rs`, this evidence,
  and the R01 row in `research/backlog.json`. `src/cli/watch.rs` was inspected
  but did not require a behavioral change.

## Reproduction and decision

The current hosted macOS failure was the over-constrained assertion in
`watch_uses_a_full_report_when_project_wide_policy_is_configured`: it required
`project_wide_policy`. The local and Linux path emits that fallback for a
single changed path under a project-wide paired-file policy. macOS can also
truthfully receive a `need_rescan` event from FSEvents; shared watch-state
correctly maps that event to `DirtyProject::Full`, so the full requested-scope
report has fallback reason `full_rescan_event`.

The patch keeps both safety properties observable: the report remains a
full requested-scope report and still reports the failed paired-file policy.
It does not discard or downgrade a rescan/overflow event.

## Test-first evidence

The focused RED command was:

```text
cargo test --test watch_cli assert_no_event_rejects_a_disconnected_event_reader -- --exact --nocapture
```

Exit: `101`. Before the helper change, it failed as expected with `test did not
panic as expected`: `WatchProcess::assert_no_event` treated a disconnected JSON
reader as quiet. The minimal helper change distinguishes timeout, unexpected
JSON (included in the failure), and reader disconnection.

The independent-review follow-up added test-only normalization capture. Its
focused RED was:

```text
target/debug/deps/assura-7cedbdfcbdcd2949 cli::watch::tests::normalization_capture_records_rescan_event_inputs --exact --nocapture
```

Exit: `101`, failing at `normalization capture` before capture recording was
implemented. The capture records filtered event paths, debug event kind,
`need_rescan`, post-normalization config-dirty state, and whether the event
invalidated the batch. It is thread-local to prevent parallel-test
cross-contamination and does not change production event classification.

## Local commands and results

All commands below ran from the recorded CWD and exited `0`, except the RED
command above.

```text
cargo fmt --check
cargo test project_wide_policy_fallback_keeps_the_requested_scope -- --exact --nocapture
cargo test watch_scope_filters_an_outside_event_and_retains_an_inside_event -- --exact --nocapture
cargo test --test watch_cli watch_uses_a_full_report_when_project_wide_policy_is_configured -- --exact --nocapture
cargo test --test watch_cli watch_honors_the_requested_directory_scope -- --exact --nocapture
cargo test --test watch_cli
cargo test --lib
```

The full integration target passed `13 passed; 0 failed`. The library tier
passed `514` Assura tests and `15` `assura-watch-state` tests. After the full
local tier was green, the exact requested-scope integration test passed on 20
consecutive runs (`watch_honors_the_requested_directory_scope`, each `1 passed;
12 filtered`).

`cargo xtask evidence` also exited `0`. `cargo xtask docs` reached the
repository documentation validations (including 20 release-surface mappings and
54 YAML examples) but exited `1` because this checkout has no
`website/node_modules` and `astro` is unavailable. `cargo run --quiet -- check
--format json .` exited `2`: it reports the existing `B00.md` and this required
`R01.md` evidence filenames as medium file-naming violations, in addition to
the pre-existing low line-length advisories. These are not an R01 behavior
failure; R07 owns their explicit policy disposition.

After the review follow-up, each command below exited `0`:

```text
cargo fmt --check
cargo test --lib cli::watch::tests -- --nocapture
cargo test --test watch_cli
```

The affected watch-unit subset passed `9 passed; 0 failed`; the integration
target again passed `13 passed; 0 failed`.

The local behavioral checks through the test-only capture follow-up were run on
`c3bd50c5994e5c980ad5c0f9607ca0e32c7559b5`. `2f29520` is evidence-only and
was the SHA reviewed for that follow-up; it did not alter watch behavior. The
spawned-binary diagnostic behavior below was added and tested on `0654d3c`.

The focused spawned-binary RED command was:

```text
cargo test --test watch_cli watch_debug_diagnostic_reports_normalized_event_inputs -- --exact --nocapture
```

Exit: `101`. Before the debug transport existed, the spawned binary produced
its normal JSON report but the diagnostic receiver timed out after 10 seconds.
With debug assertions and `ASSURA_WATCH_NORMALIZATION_DEBUG=1`, it now emits a
prefixed JSON diagnostic on stderr after normalization. The diagnostic includes
normalized paths, event kind, `need_rescan`, config-dirty state, and the
invalidation result. The integration harness parses only that prefixed stderr
line; normal watch JSON output remains on stdout and release builds do not emit
this diagnostic.

On `0654d3c`, each command below exited `0`:

```text
cargo fmt --check
cargo test --test watch_cli watch_debug_diagnostic_reports_normalized_event_inputs -- --exact --nocapture
cargo test --test watch_cli
cargo test --lib
```

The spawned-binary diagnostic integration test passed, `watch_cli` passed `14
passed; 0 failed`, and the library tier passed `515` Assura tests plus `15`
`assura-watch-state` tests.

The final diagnostic assertion follow-up first assumed an ordinary write would
normalize as `Modify(Data(Content))`. The same focused command exited `101`:
the emitted record proved the deterministic fixture actually reports
`Create(File)`. The final assertion is concrete and the focused hosted log now
prints the parsed record:

```text
{"config_changed":false,"event_kind":"Create(File)","invalidated":true,"need_rescan":false,"paths":["src/BadName.ts"]}
```

On `3929905`, `cargo fmt --check`, the focused diagnostic command, and
`cargo test --test watch_cli` all exited `0`; the target again passed `14
passed; 0 failed`. This is a test/evidence-only follow-up: it changes neither
the debug transport nor watch policy. The next evidence-only commit records
this final SHA without treating it as hosted-platform closure.

The final review noted that `event_kind` is backend diagnostic evidence rather
than a portable contract. On `0b53c21`, the integration test keeps the concrete
normalized path and stable `need_rescan`, config-state, and invalidation
assertions, prints the parsed diagnostic for hosted logs, and now requires only
a nonempty event kind. Its focused RED used an intentionally wrong stable path
and exited `101`; restoring `src/BadName.ts` made the focused command and
`cargo test --test watch_cli` pass again (`14 passed; 0 failed`). This is the
final test-only assertion refinement; R01 remains `verified` pending review and
hosted platform proof.

Hosted macOS run `34000436713` then failed only
`watch_uses_full_reports_while_project_is_already_failing`: the assertion
required `project_not_clean`, while the observed FSEvents batch truthfully
reported `full_rescan_event`. The other 13 watch integration tests passed,
including the spawned-binary diagnostic. This is a platform fallback-reason
contract issue, not a scope-validation defect.

On `2061749`, a focused deterministic RED first expected
`full_rescan_event` for a non-clean project with one known changed path; it
exited `101` with actual `project_not_clean`. The follow-up retains two
separate deterministic contracts: dirty project/path yields `project_not_clean`
and `DirtyProject::Full` yields `full_rescan_event`, both with a full requested
scope and failing report. The E2E now accepts only those two truthful fallback
reasons while still requiring `warm_full`, `requested_path`, and the existing
failed report. All commands below exited `0`:

```text
cargo test --lib cli::watch::tests::project_not_clean_fallback_keeps_the_requested_scope -- --exact --nocapture
cargo test --lib cli::watch::tests::rescan_fallback_overrides_project_not_clean_reason -- --exact --nocapture
cargo test --test watch_cli watch_uses_full_reports_while_project_is_already_failing -- --exact --nocapture
cargo test --test watch_cli
```

The local integration target passed `14 passed; 0 failed`. Hosted macOS/Linux/
Windows proof against the reviewed follow-up SHA remains required before
closure.

Hosted macOS run `34000811631` failed two remaining assertions. First,
`watch_emits_initial_and_warm_edit_reports` expected `warm_incremental` but
received a truthful `warm_full` rescan fallback. Second,
`watch_observes_an_explicit_config_outside_the_project` received one real
`good-name.ts` incremental report (coalesced events: 2) before its config
reload report; Windows was canceled only by macOS fail-fast. The local
debug-gated spawned-binary capture observed the ordinary branch as
`Create(File)`, incremental, with `need_rescan: false`; the explicit external
config write locally reloaded correctly.

The hosted failures are the focused RED evidence for this portability follow-up.
On `4a33bae`, the warm-edit E2E accepts `warm_incremental` with an affected-path
report or `warm_full` only with `full_rescan_event` and requested-path report;
both retain the invalid naming violation. The explicit-config test now has a
bounded readiness/consumption boundary: it logs and accepts at most one actual
filesystem report before requiring the config report, and adjusts the expected
sequence exactly by that observed event. It neither sleeps nor silently drops
events. The following commands exited `0`:

```text
cargo test --test watch_cli watch_emits_initial_and_warm_edit_reports -- --exact --nocapture
cargo test --test watch_cli watch_observes_an_explicit_config_outside_the_project -- --exact --nocapture
cargo test --test watch_cli
```

The local watch target passed `14 passed; 0 failed`. Independent review and a
new macOS/Linux/Windows hosted matrix against the final reviewed SHA remain
required; R01 is still `verified`.

Final review found that the bounded predecessor reader was too broad. On
`b000820`, it still permits at most one predecessor but now requires it to be
either a successful incremental affected-path report naming `good-name.ts`, or
a successful full requested-scope report explicitly marked
`full_rescan_event`; any other predecessor fails visibly in the test log before
the required config reload. The hosted macOS predecessor event is the focused
RED evidence for this branch; this host did not reproduce it. Locally,
`cargo test --test watch_cli
watch_observes_an_explicit_config_outside_the_project -- --exact --nocapture`
and `cargo test --test watch_cli` both exited `0` (14 passed, 0 failed). This
test-only refinement preserves state `verified` and requires final independent
review plus hosted platform proof.

Hosted macOS job `101400402921` passed
`watch_debug_diagnostic_reports_normalized_event_inputs`, but Cargo captured
that test's `eprintln!` output, leaving no normalization-boundary record in the
successful job log. The remaining evidence gap is retained directory-scope
diagnostic output on macOS, Linux, and Windows; it is not a watch-policy
defect.

For the follow-up after `f156914`, the directory-scope test first required a
normalization diagnostic while its spawned binary still had diagnostics
disabled. Its focused command exited `101` after the receiver timed out:

```text
cargo test --all-features --test watch_cli watch_honors_the_requested_directory_scope -- --exact --nocapture
```

The test now enables only the existing debug-gated test transport, prints the
parsed record, and asserts the retained `src/BadName.ts` path plus nonempty
backend event kind, `need_rescan: false`, `config_changed: false`, and
`invalidated: true`. The existing three-OS `Test Suite` matrix keeps its full
`cargo test --all-features` gate and then runs this exact test once with
`ASSURA_WATCH_NORMALIZATION_DEBUG=1` and `--nocapture`; this retains the parsed
record in successful hosted logs without affecting normal watch output or
release behavior.

The implementation/test SHA for this follow-up is
`e5376ff624da71c549940dbf9a2b4cd6147f20ba`; the evidence-only follow-up is
`baf677d07c1a918665e508b22f71b3f0135e4826`. Each local command below exited
`0`:

```text
cargo fmt --all -- --check
cargo test --all-features --test watch_cli watch_honors_the_requested_directory_scope -- --exact --nocapture
cargo test --all-features --test watch_cli -- --nocapture
cargo xtask evidence
```

The focused GREEN output retained:

```json
{"config_changed":false,"event_kind":"Create(File)","invalidated":true,"need_rescan":false,"paths":["src/BadName.ts"]}
```

The full watch target passed `14 passed; 0 failed`. `cargo xtask evidence`
passed its CI workflow scope policy check. This checkout has no R01-owned
workflow-contract test surface; the workflow change is intentionally limited
to the existing matrix and requires independent review plus a new hosted
macOS/Linux/Windows matrix for final proof.

Hosted macOS run `34001788327` then exposed two additional over-constrained
E2E fallback assertions. `watch_emits_feedback_during_sustained_edits` required
`max_batch_window` but received the truthful `full_rescan_event`; and
`watch_ignores_paths_excluded_by_project_policy` required silence but received
a successful `warm_full` requested-scope `full_rescan_event` with empty
`changed_paths` after the excluded write. These are FSEvents rescan overrides,
not watch-policy defects: ordinary excluded content events still must be
silent, while an invalidating rescan must never be discarded.

The hosted run is the direct RED evidence for both portable E2E branches. A
new deterministic unit initially asserted `max_batch_window` after an excluded
path carried a rescan flag; its focused command exited `101` with actual
`full_rescan_event`:

```text
cargo test --all-features --lib cli::watch::tests::rescan_after_an_excluded_path_keeps_full_scope_feedback_observable -- --exact --nocapture
```

The same fixture initially used `EventKind::Any` for the ordinary control,
which is itself rescan-classified. On this macOS checkout it also needed
canonical paths because the temporary root resolves from `/var` to
`/private/var`; this is a test-fixture correction, not production behavior.
The final deterministic coverage proves an ordinary excluded content event
does not invalidate, while an explicit rescan after it yields a successful
`warm_full`, `requested_path`, `full_rescan_event` report with no changed paths.
A separate deterministic test preserves successful full-scope
`max_batch_window` feedback for ordinary sustained batching.

On implementation/test SHA `ec93e1bf3961f780c34ed3da3f29d102ddd8575b`, the
two E2Es retain their ordinary contracts and accept only their truthful rescan
forms: sustained edits accept full scope only for `max_batch_window` or
`full_rescan_event`, always require a successful report, and excluded-path
writes remain silent or may yield only a successful full requested-scope
`full_rescan_event` with empty changed paths. No failed report is accepted and
no rescan safety behavior is weakened.

Each command below exited `0`:

```text
cargo fmt --all -- --check
cargo test --all-features --lib cli::watch::tests::rescan_after_an_excluded_path_keeps_full_scope_feedback_observable -- --exact --nocapture
cargo test --all-features --lib cli::watch::tests::max_batch_window_keeps_successful_full_scope_feedback_observable -- --exact --nocapture
cargo test --all-features --test watch_cli watch_ignores_paths_excluded_by_project_policy -- --exact --nocapture
cargo test --all-features --test watch_cli watch_emits_feedback_during_sustained_edits -- --exact --nocapture
cargo test --all-features --test watch_cli -- --nocapture
cargo test --all-features --lib cli::watch::tests -- --nocapture
```

The affected targets passed `14 passed; 0 failed` integration tests and `13
passed; 0 failed` watch-unit tests. R01 remains `verified`: independent review
and a fresh hosted macOS/Linux/Windows matrix against the reviewed SHA are
still required before closure.

## Negative controls and limitations

- Deterministic normalization coverage proves a `docs/BadName.ts` event is
  ignored while an existing `src/BadName.ts` event is retained.
- Existing deterministic config tests prove unchanged config-parent events are
  ignored and changed configuration requests reload/full validation.
- Deterministic project-wide-policy and overflow tests prove full
  requested-scope fallback with `project_wide_policy` and
  `event_channel_overflow`, respectively. The latter retains overflow safety.
- The direct macOS FSEvents event path was not captured locally; platform
  closure therefore requires hosted macOS, Linux, and Windows runs against the
  reviewed commit. Those successful runs must retain the directory-scope
  normalization diagnostic added above. The card also requires independent
  review before PR/merge.
- The website build remains unverified in this checkout until its declared
  Node dependencies are installed; no dependency installation was performed by
  this card.

## Integration and next state

No remote push, PR, merge, release, or deployment was performed. The card is
locally verified only. Obtain independent review, then create the R01 PR and
require hosted macOS/Linux/Windows proof before setting it `done`. R02 and P01
remain independently pending; R01's next action is review/hosted proof rather
than another local implementation pass.

## Post-merge closure

PR [#144](https://github.com/rothnic/assura/pull/144) merged as `dcb1fb57ba100f77a7cb7e48c4f14507d3106231` on 2026-09-06. `git fetch origin` followed by `git merge-base --is-ancestor dcb1fb57ba100f77a7cb7e48c4f14507d3106231 origin/master` exited 0. The final reviewed head `4741e560c28620a0a8813f9a8635ae52c67ca3cc` passed hosted macOS, Linux, and Windows test suites in Rust CI run [34004956631](https://github.com/rothnic/assura/actions/runs/34004956631), including the macOS scope-normalization diagnostic. R01 is done; this does not authorize a release or deployment.

## Regression reopening (2026-09-06)

The later hosted Rust CI run
[34011050005](https://github.com/rothnic/assura/actions/runs/34011050005), on
installer branch SHA `4fd9c39ed0470d3ee297bec7e632a6adf4668ea9`, invalidates
that completion claim. Its macOS job `101426839197` failed
`watch_honors_the_requested_directory_scope` and
`watch_ignores_assura_runtime_output`; Linux and Windows test jobs were
cancelled by fail-fast, so they are not proof either way.

Both failures received the same extra sequence-2 event: `warm_full`,
`requested_path`, empty `changed_paths`, successful report, and
`fallback_reason: full_rescan_event`. The candidate was created from current
`origin/master` SHA `e841ab8fc12355d451e9e99ba46e6b3a727520ae` in
`/Users/nroth/.codex/worktrees/assura-r01-watch-regression`, branch
`goal/r01-watch-regression`. The behavior/test candidate is
`5aef17b688d00a7686b0e4530349c475e50142e4`.

Root-cause tracing found that `record_message` deliberately retains
`Event::need_rescan()` after ordinary paths are filtered. Notify 6.1.1's
macOS FSEvents backend emits a pathless `Flag::Rescan` for dropped or
must-scan-subdirectories conditions; Notify documents that callers must assume
any watched file changed. The existing deterministic
`rescan_after_an_excluded_path_keeps_full_scope_feedback_observable` unit test
already proves that this must remain a safe full requested-scope report. The
two failed E2Es alone still required silence, unlike the existing excluded-path
E2E which accepts this exact conservative fallback.

The minimal candidate adds `assert_no_event_or_scoped_rescan` and applies it
only to those two assertions. It accepts either no event or exactly the safe
rescan contract above; it does not accept a failed report, a changed path, an
incremental event, or any other fallback reason. No production watch policy was
weakened.

Local commands from the candidate worktree, using Cargo's
`CARGO_BIN_EXE_assura-full` debug binary, exited `0`:

```text
cargo fmt --check
cargo test --test watch_cli watch_honors_the_requested_directory_scope -- --exact --nocapture
cargo test --test watch_cli watch_ignores_assura_runtime_output -- --exact --nocapture
cargo test --lib
```

The focused tests each passed `1`; the default library tier passed `519` Assura
and `15` `assura-watch-state` tests. The final all-feature commands below also
exited `0` from the same CWD:

```text
cargo test --all-features --test watch_cli -- --nocapture
cargo test --all-features --lib
cargo run --quiet --bin assura-full -- check --format json .
```

They passed `14` watch integration tests, `522` Assura library tests, and `15`
watch-state tests. The self-check command exited `0`; its advisory output is
owned by R07 and was not promoted to a zero-violation claim. No push, PR,
merge, release, or deployment occurred. Next: obtain independent review, then
push for a fresh three-platform hosted matrix before considering R01 done.

## Post-merge closure

Independent review found the exact safe-fallback helper constrained to a quiet
result or the documented `full_rescan_event` contract; no production policy
changed. PR #155, head `ca3d4efb1ca3366bb157074d81601693347eb723`, passed its
full hosted matrix. It merged as `fc5dd2214483b463be6c8a0b6823adf810b36388`,
verified reachable from `origin/master` on 2026-09-06. R01 is done.
# 2026-09-06 external-config rescan repair

Q04's hosted Rust CI run `34031296355` exposed a cross-platform watch regression: `watch_observes_an_explicit_config_outside_the_project` emitted a second unrelated `full_rescan_event` after writing a sibling of the explicit config. The same run retained the separate R03 performance failure. The watch fix is scoped to R01: known nonempty events whose paths are all outside both watched scope and selected config are ignored; pathless rescans and in-scope excluded-path rescans remain observable full-scope fallbacks. A new unit test was RED (`invalidating_events: 1`) before the change and green afterward. Focused watch-unit tests, the 14-test `watch_cli` suite, `cargo check`, format, diff, and the repository structure check passed locally. Independent review found the exact safety partition clean. Hosted proof remains required.

PR #173 at `1cf517fdd93a81600a987a5370b764753f3cad34` disproved that first partition. Rust CI run `34032050900` failed macOS with two extra `full_rescan_event` reports: `watch_observes_an_explicit_config_outside_the_project` and `watch_honors_the_requested_directory_scope`; Linux and Windows matrix jobs were cancelled by fail-fast. The macOS log showed a coalesced event batch with no retained changed paths. A new focused RED test, `cli::watch::rescan_tests::rescan_coalescing_external_config_and_runtime_output_is_ignored`, failed with `invalidating_events: 1` before the correction. The refined predicate ignores only a nonempty rescan whose paths are all either outside the scope/config or Assura runtime-output paths; it preserves the pathless-rescan and excluded-path safety fallbacks. The rescan-specific tests were moved to `watch_rescan_tests.rs` to retain the enforced source-file limit rather than weaken policy. Independent review correctly required a direct pathless-rescan regression; `pathless_rescan_remains_an_observable_full_scope_fallback` now proves that safety branch. The three rescan-specific tests, the excluded-path negative control, all 16 `cli::watch` units, and all 14 `watch_cli` integration tests pass locally. The reviewed SHA and a fresh hosted matrix are still required.

The first fresh matrix at `f640bf1086275f1a90ee07992ab6c5d25c1f4756` narrowed the remaining macOS failure to `watch_honors_the_requested_directory_scope`: an out-of-scope FSEvent rescan was explicitly allowed by the test helper as sequence 2, but the subsequent in-scope edit was still asserted as sequence 2 instead of 3. This is a test sequencing correction, not a policy relaxation: the tolerated event remains required to be a successful `full_rescan_event` with no changed paths, and the following in-scope mutation must still be incremental and failing. The focused requested-directory test, all 14 `watch_cli` tests, all 16 `cli::watch` units, format, diff, and structure checks pass locally. Fresh independent review and hosted proof remain required.

## External-config rescan post-merge closure

Independent review accepted reviewed head `32353b20698ff3b1efb3647fe4338c793d1455e0` after confirming the pathless and excluded-path safety branches remained explicit. Rust CI run `34033281376` passed all required hosted gates: Linux, macOS, and Windows suites; MSRV; check, format, Clippy, coverage, evidence, performance; four-platform adoption; Windows installer and release-bundle smoke. Documentation run `34033281466` and Security Audit run `34033281390` also passed; GitGuardian succeeded. The audit job was in scope and passed (unlike prior scope-skipped audits, which were not counted as a pass). PR [#173](https://github.com/rothnic/assura/pull/173) merged as `b3002b1aae05721c6517a67f6e99e4658e9fea15` on 2026-09-06. After fetch, `git merge-base --is-ancestor b3002b1aae05721c6517a67f6e99e4658e9fea15 origin/master` exited 0; R01's restored watch outcome is present on current master. The post-merge record's `cargo run --quiet -- check --format json .` and `cargo xtask evidence` could not compile locally because the volume had only 117 MiB free and `ld` exited 28; this is not counted as a local pass. `git diff --check` and the installed Assura agent check passed. No tag, release, deployment, or public communication was made. Next independent ready card: A02.

# A05 partial implementation preservation

State: implemented; acceptance and integration incomplete.

The goal-owned worktree
`/Users/nroth/.codex/worktrees/assura-a05-native-harness-lifecycle` remains on
`goal/a05-native-harness-lifecycle` at base
`6d613f809feabd65b520c57bfd7aaf1b521a107e`. On 2026-09-06 it had six dirty paths:
onboarding source/templates, native quality integration tests, backlog,
progress, and its local evidence. They were inspected and left untouched.

Existing tests inspect generated plans/configuration; that is not proof of
native command execution. Independent review found missing source/config/
toolchain coverage and generic merging that can append duplicate expensive
native commands. Correct those contracts before expansion, then complete the
packet's detection, fixture-cwd positive/seeded-failure execution, CI recipe,
cumulative/deduplicated phases, preservation, review and performance gates.

This record intentionally does not import stale passing-test counts from the
partial worktree as current proof. No A05 product change is included here.

## Continuation: reviewed planning core and fixture execution

State: active; the historical partial branch remains preserved and is not an
integration source.

PR #190 merged at `be56244ab21c8646572a809babcb5f3633f98db6` after independent
review, scoped rereview, local `cargo xtask pr`, and 24 successful hosted PR
checks (one Security Audit job was explicitly skipped, not treated as passing).
It contributes configuration-aware Rust, Bun, and Python quality planning;
unavailable local tools remain advice rather than generated runnable commands.

The continuation owner is
`/Users/nroth/.codex/worktrees/assura-a05-quality-execution` on
`goal/a05-quality-execution`, created clean from that merge commit. Its focused
contract executes the candidate binary's generated Rust PR plan in a real
temporary Cargo fixture cwd: policy check, format, locked test, and Clippy pass;
a deliberately malformed source makes the planned format check fail; restoring
the source makes it pass again. The test also asserts four unique planned
commands, so the execution proof cannot hide duplicate expensive work.

Current local evidence: `cargo test --locked --test quality_onboarding_contract`
exited 0 with 11 passed, 0 failed, 0 ignored. This is bounded local evidence,
not full-card completion: independent review, full affected gates, hosted
platform proof, integration, and any still-uncovered packet outcomes remain
required before A05 can advance beyond active.

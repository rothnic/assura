# A05 native quality-gate execution

State: active; the first current-master execution slice is integrated, while
the card's full acceptance remains incomplete.

## Preserved historical work

The user-owned worktree
`/Users/nroth/.codex/worktrees/assura-a05-native-harness-lifecycle` remains on
`goal/a05-native-harness-lifecycle` at base
`6d613f809feabd65b520c57bfd7aaf1b521a107e`. On 2026-09-06 it had six dirty paths:
onboarding source/templates, native quality integration tests, backlog,
progress, and its local evidence. They were inspected and left untouched.
This current slice neither imports nor folds that work into its evidence.

## Current execution slice

Source SHA: `f1f33120a84072e28bdbbd5014f24328cb4265dd` (PR #200 merge)

The merged test in `tests/quality_onboarding_contract.rs` obtains the generated
Rust `pr` plan and executes only its declared closed set of four commands from
the fixture cwd: `assura check`, `cargo fmt --all -- --check`,
`cargo test --locked`, and `cargo clippy --all-targets -- -D warnings`. A seeded
formatting failure makes the generated formatter command fail; restoring the
source makes that same command succeed. The helper rejects any undeclared plan
command, so this is execution evidence for the generated Rust contract rather
than a general command executor.

Checks from
`/Users/nroth/.codex/worktrees/assura-a05-current-audit`:

- `cargo test --test quality_onboarding_contract generated_rust_pr_plan_executes_in_fixture_cwd_and_detects_a_seeded_failure -- --exact --nocapture` — exit 0, one test passed.
- `cargo test --test quality_onboarding_contract` — exit 0, 11 tests passed.
- `cargo xtask pr` — exit 0, including target-state audit and website build.
- Hosted PR #200, run `34234500882` — all applicable Rust CI jobs passed,
  including the 7m28s Performance Report, five installable-adoption lanes,
  Windows Installer Smoke, release smoke, coverage, and all three stable OS
  test suites. `Security Scope` passed; the separately conditional `Security
  Audit` was scope-skipped and is not counted as a passing test.

Independent scoped review approved the exact test and task-ownership metadata
change with no findings. PR [#200](https://github.com/rothnic/assura/pull/200)
merged at the source SHA above. The fresh owned execution branch
`goal/a05-current-audit` was then rebased cleanly to that merge commit.

## Unconfigured-policy diagnostic slice

Source SHA: `186426de519fef0b98a25911a47624da40528435` (PR #202 merge)

PR [#202](https://github.com/rothnic/assura/pull/202) adds the missing contract
for a valid Assura project with no `quality.scopes`: `assura quality plan`
exits 2, emits no successful plan on stdout, and names the missing quality
configuration. It does not guess a runnable native gate. The initial
independent review found an incidental stderr keyword restriction; removing
only that brittle assertion retained the exit, no-plan, and diagnostic
contracts, and scoped rereview approved the resolution.

The exact focused test and the 12-test `quality_onboarding_contract` target
passed locally, followed by a complete `cargo xtask pr` exit 0. Hosted run
`34236730287` passed its applicable Rust matrix, including Performance Report
(5m56s), five installable-adoption lanes, Windows Installer Smoke, release
smoke, coverage, and Linux/macOS/Windows test suites; Documentation and
marketing-site jobs also passed. `Security Scope` passed; the separately
conditional `Security Audit` was scope-skipped and is not counted as test
evidence. The clean owned execution branch was rebased to the merge commit
after integration.

## Cumulative overlapping-scope planning slice

Source SHA: `dd2efaf9eececbc97e177393cc82827bd1721120` (PR #204 merge)

PR [#204](https://github.com/rothnic/assura/pull/204) adds the missing
observable contract for overlapping configured scopes. Its fixture sends a
Rust source through both a broad base scope and a Rust scope, repeats each
expensive native command, and adds one valid Rust-only `cargo check --locked`
command. The resulting `frequent`, `pre-push`, `pr`, and `merge` plans must be
cumulative and stable: shared commands occur exactly once, and the Rust-only
command follows the complete shared base-phase sequence. This tests the CLI
planner rather than reimplementing it.

The first independent review correctly noted that identical scope payloads
would not reveal traversal-order drift. The accepted test-only correction made
that order observable without weakening the duplicate-command contract; scoped
rereview found no unresolved defect.

Checks from
`/Users/nroth/.codex/worktrees/assura-a05-current-audit`:

- Exact cumulative-plan contract — exit 0, one test passed.
- `cargo test --test quality_onboarding_contract` — exit 0, 13 tests passed.
- `cargo fmt --check` and `git diff --check` — exit 0.
- `cargo xtask pr` — exit 0, including target-state audit and website build.
- Hosted PR #204, run `34239230434` — all applicable Rust CI jobs passed,
  including the 7m37s Performance Report, five installable-adoption lanes,
  Windows Installer Smoke, release smoke, coverage, and Linux/macOS/Windows
  stable test suites. Documentation and website verification also passed.
  `Security Scope` passed; the separately conditional `Security Audit` was
  scope-skipped and is not counted as test evidence.

The owned execution branch was rebased cleanly to the integrated merge commit
after the current-master mergeability check.

## Existing CI preservation and opt-in policy slice

Source SHA: `6dd92bfdb6e5e1948e85398df1917212a3e5b4e2` (PR #206 merge)

PR [#206](https://github.com/rothnic/assura/pull/206) completes the packet's
CI ownership boundary. A supported Rust fixture starts with a project-owned
`.github/workflows/ci.yml`; `agent onboard` leaves those exact bytes unchanged
while its generated policy provides the opt-in Rust `pr` plan. The test neither
edits nor executes the workflow, so configured policy is not mislabeled as
active CI enforcement. The released-binary opt-in recipe remains explicit in
`docs/validation.md` and does not overwrite a project's workflow.

Independent review found no defect. The focused preservation contract, all 14
`quality_onboarding_contract` tests, formatting and diff checks, and complete
`cargo xtask pr` gate passed locally. Hosted PR #206, run `34241406754`, passed
all applicable Rust CI jobs: Performance Report (5m39s), five installable
adoption lanes, Windows Installer Smoke, release smoke, coverage, and all
Linux/macOS/Windows stable suites, plus documentation and website verification.
`Security Scope` passed; conditional `Security Audit` was scope-skipped and is
not counted as test evidence. The current owner rebased cleanly to the merged
commit.

## Acceptance conclusion

The combined A05 contracts now cover declared Bun scripts and unavailable Bun
advice; actual configured pytest/Ruff/mypy availability; Rust configuration,
lockfile, toolchain, and docs-only routing; honest missing-policy feedback;
stable cumulative de-duplication; fixture-cwd native pass/fail execution; and
opt-in CI policy that preserves existing workflows. Each merged slice had
independent review and current-master hosted evidence. A05 is done.

Next ready action: refresh the ledger and select only a dependency-ready card
in the clean, owned current-master worktree.

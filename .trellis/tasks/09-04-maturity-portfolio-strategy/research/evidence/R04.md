# R04 verification evidence

State: verified locally; independent review and hosted CI pending

Initial analysis baseline: `d787c0f28a32d0c826f52600cb3a1dd8de02736c` (`origin/master` fetched 2026-09-05).

Final parent/source baseline: `3ff8c889e18381e83cc960803ccf9ddfba35f1d3` (`origin/master` fetched and rebased 2026-09-05). This is the parent of the final local R04 commit and the source against which the post-rebase commands below ran.

Worktree and command cwd: `/Users/nroth/.codex/worktrees/assura-r04-verified-msrv` on `goal/r04-verified-msrv`. The worktree was created from the current `origin/master` source and was clean before this card's edits.

## Reproduction and floor decision

The declared `1.70.0` floor was false for the locked default host graph. `cargo metadata --offline --locked --format-version 1` exited 0. `cargo tree --locked -i icu_normalizer` exited 0 and showed `icu_normalizer 2.2.0` through `idna_adapter -> idna -> jsonschema` in the default full CLI graph. The minimal public launcher graph does not include it: `cargo +1.86.0 tree --locked --no-default-features --features json-output,yaml-config -i icu_normalizer` exited 101 with `package ID specification 'icu_normalizer' did not match any packages`.

After installing the requested verifier toolchain, `cargo +1.85.0 check --locked --workspace --all-targets --quiet` exited 101. Cargo identified `icu_collections`, `icu_locale_core`, `icu_normalizer`, `icu_properties`, `icu_provider`, and `idna_adapter` at `2.2.0`/`1.2.2` as requiring Rust 1.86. This is a real locked-graph failure, not a manifest estimate. We raised the declared floor rather than downgrading maintained dependencies solely to retain an untested historical claim.

## Changes

- Updated the root and all internal workspace package `rust-version` values and the enforced manifest-semantics policy to `1.86.0`.
- Updated contributor and source-install documentation to the same floor.
- Added a pinned Rust `1.86.0` CI job. It retains the existing stable jobs and verifies both the default workspace target matrix and the no-default-features public launcher, with `--locked` so a newer lockfile requirement fails the MSRV lane.

## Local verification before rebase

- `cargo +1.86.0 check --locked --workspace --all-targets --quiet`: exit 0.
- `cargo +1.86.0 build --locked --bin assura-full --quiet`: exit 0 (full default CLI).
- `cargo +1.86.0 build --locked --bin assura --no-default-features --features json-output,yaml-config --quiet`: exit 0 (promised minimal public launcher).
- `cargo +1.86.0 --version`: `cargo 1.86.0 (adf9b6ad1 2025-02-28)`; `rustc +1.86.0 --version`: `rustc 1.86.0 (05f9846f8 2025-03-31)`.
- `cargo +1.85.0 check --locked --workspace --all-targets --quiet`: exit 101, expected negative control described above.
- `cargo xtask fast`: exit 0; its Rust suite reported 522 unit tests passed plus the integration suites it invokes.
- `cargo xtask pr`: exit 1 after the test suites and configured-policy check passed, solely because the shared active Trellis task is assigned to a different branch and has no `prd.md` in this worktree (`active task branch must match current branch`; `active task needs prd.md`). This card neither changes the task binding nor weakens that gate.

## Local verification rerun after rebase onto `3ff8c88`

- `git diff --check`: exit 0.
- `ruby -e 'require "yaml"; YAML.load_file(".github/workflows/ci.yml"); YAML.load_file(".assura/config.yml")'`: exit 0.
- `cargo metadata --offline --locked --format-version 1`: exit 0.
- `cargo +1.86.0 check --locked --workspace --all-targets --quiet`: exit 0.
- `cargo +1.86.0 build --locked --bin assura-full --quiet`: exit 0.
- `cargo +1.86.0 build --locked --bin assura --no-default-features --features json-output,yaml-config --quiet`: exit 0.
- `cargo +1.85.0 check --locked --workspace --all-targets --quiet`: exit 101. After the manifest update, Cargo additionally rejects the workspace package declarations at `1.86.0`; the pre-rebase run above independently established that the unchanged locked ICU/idna dependency chain also requires Rust 1.86.
- `cargo xtask check`, `cargo xtask evidence`, and `cargo run --quiet -- check --format json .`: exit 0. The configured-policy check reports only the pre-existing non-blocking max-line advisories.

## Limitations and review state

The MSRV job has not run on GitHub Actions, so local verification does not prove the hosted Ubuntu runner. The website docs build was not runnable in this clean worktree because `website/node_modules` is absent and `astro` is not installed; `cargo xtask docs` exited 1 after its Rust-side documentation checks. The `cargo tree` negative-control exit is expected because the minimal graph correctly omits `icu_normalizer`; it does not claim target-specific Windows dependency coverage. No lockfile dependency was downgraded, no release/tag/deploy/PR/push was performed, and no historical analysis document was rewritten.

Independent review is required before integration. Review the manifest/document floor consistency, CI job YAML and locked command matrix, and whether the default host-only ICU path justifies this support policy.

Commit/PR/integration: local verified commit was rebased onto `origin/master` at `3ff8c889e18381e83cc960803ccf9ddfba35f1d3`; no PR, push, merge, tag, release, or deployment.

Next ready ID: Q02 remains blocked on P01 and Q01; R05 is independently eligible after B00.

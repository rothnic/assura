# Q02 contribution and review contract evidence

State: done.

## Current-master integration — 2026-09-08

Current candidate SHA: `9ecf97d47044aa6500bd5b17406e034daeeb0243`.
Integration: [PR #158](https://github.com/rothnic/assura/pull/158) merged as
`01e2699523780d00888f47c0e4f127540ed855f8`; the candidate is reachable from
the refreshed `origin/master` at that merge SHA.

The current-source repair retained full-PR `policy_review` paths when a
synchronize delta narrows execution work, and added MSRV plus Alpine adoption
checks to the required prior-head set. Missing either required check now selects
full scope. Its black-box fixture proves both safe-fallback cases; it neither
removes an execution job nor turns a skipped job into passing evidence.

Checks in `/Users/nroth/.codex/worktrees/assura-q02-contribution-contract`:

- `bash scripts/check-ci-scope.sh` exited 0, including the full-PR review-path,
  missing-Alpine, and missing-MSRV controls.
- `cargo xtask pr` exited 0. The configured self-check retained three existing,
  nonblocking low line-length advisories; they are not Q02 changes or a clean
  result claim.
- Independent read-only review of `9ecf97d` returned clean: full review paths
  are retained, absent required checks fail safely to full scope, and no
  execution-job, protection, or self-approval behavior changed.

Fresh hosted checks attached to `9ecf97d` all passed: Documentation run
`34253819758`, Rust CI run `34253819768`, and Security Audit run `34253822521`.
That includes Linux/macOS/Windows suites, every adoption smoke, Windows
installer, release bundle, coverage, performance report, MSRV, formatting,
Clippy, evidence, documentation, CI scope, and security. Historical failures
and cancellations on the earlier PR head are not used as current evidence.

Branch protection remains explicitly pending a repository-administrator
decision; no setting, invitation, credential, or identity was created. Q02's
acceptance permits admin settings to be verified or explicitly pending, and the
merged source still receives the independent-review and fresh-gate treatment
described above.

The historical PR is [#158](https://github.com/rothnic/assura/pull/158),
original head `2e10bcd6b4c7044ebe22955e56f98c9d62fe2a31`, branch
`goal/q02-contribution-contract`. Its historical macOS failure and canceled
Linux/Windows jobs are not passing evidence. The branch is being refreshed
against current master before any merge decision; historical local checks below
are retained as implementation history, not current certification.

Source SHA: `ae6837fe107b86841b74c58831c951a0f74dd317`. Worktree:
`/Users/nroth/.codex/worktrees/assura-q02-contribution-contract`.

Changed artifacts: `CONTRIBUTING.md`, `.github/PULL_REQUEST_TEMPLATE.md`,
`.github/CODEOWNERS`, `docs/contributing-agent-changes.md`, and the exact
`.github/CODEOWNERS` allowlist in `.assura/config.yml`. They establish current
toolchain/validation guidance, required PR disclosure fields, author ownership,
AI-assistance disclosure without private prompt transcripts, policy-change
review triggers, scoped maintainer routing (including `src/cli/check*`), and
release-authority boundaries.

Validation from the stated worktree exited 0: `cargo run --quiet -- check
--format json .`, `cargo xtask evidence`, and `git diff --check`. The first
self-check rejected uppercase CODEOWNERS for naming and allowlisting; the final
exact-name allowance proves no broad naming exception was used.

The scope classifier now emits `policy_review=true` with flagged changed paths
for test, current severity, performance-report, and CI-scope/workflow surfaces.
It cannot approve a merge; it is a visible deterministic review prompt.

A fresh, read-only contributor dry run took about three minutes. For a tiny
documentation correction it selected the documentation/Trellis tier and found
the missing `pnpm --dir website install --frozen-lockfile` prerequisite plus a
contradictory blanket full-test instruction. Both were repaired before review.
After the bootstrap, `cargo xtask docs` completed successfully (54 YAML
examples and a 48-page static site build); the final structure and evidence
checks remained green.

Read-only GitHub evidence on 2026-09-06: `master` protection returned 404
`Branch not protected`; `rothnic` has admin permission. Nick cannot approve
Nick's own PR, so a sole-owner gate would be unsatisfiable. A human must select
an already-authorized independent reviewer or bot route before an administrator
can configure required checks and review. This goal does not authorize that
branch-protection change, invitations, credentials, or identity creation.

No test deletion, exclusion, severity reduction, performance-threshold change,
CI-scope relaxation, tag, release, deployment, or public communication occurred.
Independent review: the final reviewed SHA is `ae6837fe107b86841b74c58831c951a0f74dd317`.
Review found and the candidate repaired CODEOWNERS self-protection, exact
single-file and recursive `src/cli/check` coverage, policy-review routing for
`.assura/config.yml` and `xtask/src/main.rs`, and ownership of CI-scope
scripts. The final re-review reported no remaining findings.

Historical hosted integration attempt: PR #158 on the earlier reviewed SHA ran Rust CI
`34028084232`. Every completed non-performance job passed, including Linux,
macOS, and Windows suites; four platform adoption smoke jobs; Windows installer;
release bundle; coverage; formatting; Clippy; MSRV; security; evidence; and
documentation. The unchanged Performance Report job failed with exit 1:
`many_configured_scopes_regression: assura-cli 15.704 ms > ls-lint-cli 14.339 ms`.
Q02 changes no validator, fixture, benchmark, or no-slower policy. This repeats
R03's cross-card release-train failure and is not attributed to Q02 or bypassed.
No merge occurred at that historical point.

The remaining external administrator decision is whether and how to configure
master branch protection; this goal does not authorize that change. It is a
pending hardening decision, not an unrecorded Q02 source or hosted-gate failure.

The branch-protection/reviewer route remains explicitly pending external
administrative action; it is not represented as an active protection. It does
not substitute for the independent review and complete local and hosted gates
that the merged current source received.

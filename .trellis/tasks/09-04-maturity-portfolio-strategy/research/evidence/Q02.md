# Q02 contribution and review contract evidence

State: blocked on an external reviewer and branch-protection decision.

Source SHA: `09310aa4939fc2e5f7eb1ef652e33b579d83f393`. Worktree:
`/Users/nroth/.codex/worktrees/assura-q02-contribution-contract`.

Changed artifacts: `CONTRIBUTING.md`, `.github/PULL_REQUEST_TEMPLATE.md`,
`.github/CODEOWNERS`, `docs/contributing-agent-changes.md`, and the exact
`.github/CODEOWNERS` allowlist in `.assura/config.yml`. They establish current
toolchain/validation guidance, required PR disclosure fields, author ownership,
AI-assistance disclosure without private prompt transcripts, policy-change
review triggers, scoped maintainer routing (including `src/cli/check*`), and
release-authority boundaries.

Validation from the stated worktree exited 0: `cargo run --quiet -- check
--format json .`, `cargo xtask evidence`, and `git diff --check`. The first
self-check rejected uppercase CODEOWNERS for naming and allowlisting; the final
exact-name allowance proves no broad naming exception was used.

The scope classifier now emits `policy_review=true` with flagged changed paths
for test, current severity, performance-report, and CI-scope/workflow surfaces.
It cannot approve a merge; it is a visible deterministic review prompt.

A fresh, read-only contributor dry run took about three minutes. For a tiny
documentation correction it selected the documentation/Trellis tier and found
the missing `pnpm --dir website install --frozen-lockfile` prerequisite plus a
contradictory blanket full-test instruction. Both were repaired before review.
After the bootstrap, `cargo xtask docs` completed successfully (54 YAML
examples and a 48-page static site build); the final structure and evidence
checks remained green.

Read-only GitHub evidence on 2026-09-06: `master` protection returned 404
`Branch not protected`; `rothnic` has admin permission. Nick cannot approve
Nick's own PR, so a sole-owner gate would be unsatisfiable. A human must select
an already-authorized independent reviewer or bot route before an administrator
can configure required checks and review. This goal does not authorize that
branch-protection change, invitations, credentials, or identity creation.

No test deletion, exclusion, severity reduction, performance-threshold change,
CI-scope relaxation, tag, release, deployment, or public communication occurred.
Next ready independent card: A01, W01, or F01.

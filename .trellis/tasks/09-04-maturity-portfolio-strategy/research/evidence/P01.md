# P01 completion evidence

State: done — independently reviewed and merged after hosted proof

Source baseline SHA: `d787c0f28a32d0c826f52600cb3a1dd8de02736c`
(`Merge pull request #145 from rothnic/goal/r01-r02-r07-postmerge`).

Integration candidate worktree:
`/Users/nroth/.codex/worktrees/assura-p01-current-master` on
`docs/p01-scope-current-master`.

## Reconciliation

The current source manifest has 31 release-surface rows. The checked source
declares `0.4.0`; GitHub release inspection on 2026-09-05 identified `v0.3.0`
as the latest published release. P01 therefore records every row's published
version truthfully: `v0.3.0` rows are published, while `v0.4.0` rows are
candidate-only and must not be represented as default-installer availability.

Current-source reconciliation also inspected merged PRs #144 and #145. #144
merged the R01 watch-scope repair and #145 recorded closure of merged
reliability cards. Their only P01-owned overlap is the backlog's R01/R02/R07
state update; neither changes the 31-row release manifest, support boundary,
published-release state, or P01 owner assignments.

`docs/analysis/assura-scope-decisions.md` is the manifest-backed ledger of
command/surface, classification, release state, evidence, consumer reference,
and owner card. It keeps existing behavior classified as-is, freezes new
semantic-search, knowledge-platform, maturity-score, hosted-orchestration,
remote-pattern, marketplace, and arbitrary auto-repair work, and assigns any
containment decision to `Q05` rather than deleting or relabeling behavior.

The retained managed-integration promise still covers Codex, Claude Code,
OpenCode, and Pi. No narrowing decision is claimed; all four remain proof
obligations.

## Changes

- `.trellis/spec/assura/roadmap.md`: routes active work to the Maturity
  Execution Train and preserves older claim-complete work as history.
- `docs/data/public-roadmap.json`: replaces broad capability promotion with
  current repository-policy, trust, initialization, evaluation, and
  evidence-based release work.
- `docs/data/release-surfaces.json`: records the checked-source version,
  current published release, and canonical scope-ledger path without changing
  any individual surface classification.
- `docs/support-policy.md`: adds the current growth boundary and the retained
  four-host decision without changing individual surface classifications.
- `docs/goals/assura-claim-complete-v0-4-and-v1.md`: archives stale active
  branch/release instructions with explicit supersession links and labels its
  objective, revalidation, delivery, scope, completion, validation, and review
  material as an August 2026 historical snapshot.
- `docs/analysis/assura-scope-decisions.md`: adds the 31-row ledger.

## Checks

Commands ran from `/Users/nroth/.codex/worktrees/assura-p01-scope` with source
build tooling (`cargo xtask`):

| Command | Exit | Result |
| --- | ---: | --- |
| `cargo xtask target-state` (before correcting P01 data) | 1 | Caught an invalid support-policy public-roadmap link and mismatch between the recommended goal and `now` item; both were corrected. It also reported pre-existing active-task branch/`prd.md` workflow-gate drift for this isolated execution branch. |
| `git diff --check` | 0 | No whitespace errors. |
| `cargo xtask target-state` | 1 | P01 data checks passed after the focused correction; the only remaining messages are the pre-existing active-task branch/`prd.md` workflow-gate topology mismatch in this isolated checkout. |
| `cargo xtask evidence` | 0 | Review-evidence policy checks passed. |
| `cargo xtask docs` | 0 | Validated 20 marketed mappings, 54 YAML examples, current demo data, and a 48-page Astro build. The first attempt correctly failed because this worktree lacked `website/node_modules`; `pnpm --dir website install --frozen-lockfile --offline` restored the lockfile-pinned local dependencies without changing tracked files. |
| `cargo run --quiet -- check --format json .` | 1 | 18 findings: existing low line-limit advisories plus blocking `B00.md` and `P01.md` uppercase evidence names. The backlog prescribes `evidence/<ID>.md`; R07 owns the policy/exception decision, so P01 does not silently weaken or bypass it. |

## Negative control

P01 rejects treating Cargo's checked `0.4.0` version as proof that the public
installer serves it. It also rejects a one-host managed-integration result as
four-host closure and rejects changing an existing surface's support status
solely to make positioning copy pass.

## Review and integration

Reviewed scope commits were replayed without conflict onto current
`origin/master` as `261c610`, `fc434c9`, and `02a5481` on
`docs/p01-scope-current-master`; no push, PR, merge, tag, release, deployment,
or public communication has occurred from this candidate. This remains
`verified`: independent review and hosted docs/CI proof remain required before
integration. Because the independent review evaluated the pre-#144/#145
candidate, it must be revisited against this rebased candidate and its updated
baseline/evidence.

Independent review correction: the initial archival edit left active-sounding
body headings, including “Live Revalidation” and “The goal is valid.” P01 now
labels that retained provenance as a historical snapshot rather than an active
routing instruction. Focused negative claim check confirmed no exact active
headings or “The goal is valid as of” sentence remain; `cargo xtask evidence`
exited 0 and `cargo xtask docs` exited 0 (20 marketed mappings, 54 YAML
examples, 48-page Astro build). `cargo xtask target-state` still exits 1 only
for the isolated execution branch's pre-existing task branch/`prd.md` topology
mismatch.

## Limitations and next card

The current isolated branch cannot satisfy the planning task's branch-bound
workflow-gate requirement, so target-state reports that pre-existing execution
topology limitation separately from P01's content checks. `R01` and `R02` are
the next independently ready trust cards; `A01` and `Q03` depend on P01 after
its reviewed integration is present in their ancestry.

## Hosted proof and integration

PR [#146](https://github.com/rothnic/assura/pull/146) merged at `380b9bc889b8c653b57da375bd7bf06f174f8f2f` on 2026-09-06. Its source-scoped GitHub Actions run [`34005926408`](https://github.com/rothnic/assura/actions/runs/34005926408) passed Evidence Gates, CI Scope, all four adoption smokes, release-bundle smoke, and Windows installer smoke; Rust behavior checks were explicitly skipped because the change was documentation/public-metadata scope, not treated as passes. Documentation and security scope checks passed. After fetch, the merge commit is an ancestor of current `origin/master`.

The candidate-versus-published distinction remains: checked source declares `0.4.0`, while `v0.3.0` was the published release at the recorded inspection. This integration neither tags/releases/deploys nor changes public support claims.

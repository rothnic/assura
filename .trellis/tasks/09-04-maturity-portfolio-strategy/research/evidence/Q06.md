# Q06 — release-readiness command ownership

Iteration116 below supersedes the historical continuation115 integration hold.
The earlier material records the original attempt and its repair; the card is
now independently reviewed, fully gated at its exact current head, and merged.

Base `25a141525ce2420f02f3686affbeb03b0915f607`; worktree
`/Users/nroth/.codex/worktrees/assura-q06-release-readiness`.

The focused red test referenced `crate::release_readiness::report_from_inputs`
before the module existed and failed with `E0433` (exit 101). The extraction
moves command argument handling and report construction into
`xtask/src/release_readiness.rs`; `main.rs` retains dispatch only.

Local proof: `cargo test -p xtask` passed 36 tests; fixed-input schema test
passes; `cargo xtask release-readiness --format json` exited 0 before and after
the extraction and its JSON was byte-identical. `--help` retained the command;
missing mode exits 2, unknown mode exits 1, and invalid format exits 1.

Remaining: card-tier evidence/target-state gates, independent review, hosted
matrix, and merge. No release or publication action is authorized.

`cargo xtask evidence` exited 0. `cargo xtask target-state` exited 1 only for
the known shared active-task topology messages (`active task branch must match
current branch`; `active task needs prd.md`), also recorded by R02, Q01, Q03,
and R04. The extraction does not alter that workflow state or weaken the gate.

Hosted closure attempt: PR [#166](https://github.com/rothnic/assura/pull/166)
at `ef3325b5f159dab7d1029ad47d49c04ab046a0a1` passed all hosted checks except
`Performance Report`. The failed no-slower gate in run
[34022750062](https://github.com/rothnic/assura/actions/runs/34022750062)
reported the existing `realistic-equivalent` many-scope regression:
`many_configured_scopes_regression: assura-cli 13.601 ms > ls-lint-cli 13.212
ms`. This card does not change the performance evaluator or the measured
surface, so the result is attributed to the separate blocked R03 repair. The
PR is intentionally unmerged; its independent review found no Q06
implementation issue. Next ready ID: R03, after its named attribution blocker
is resolved.

## Iteration115 current-master review

Cwd `/Users/nroth/.codex/worktrees/assura-q06-release-readiness`, branch
`goal/q06-release-readiness`. Clean1e185d2 refreshed from origin/master
`98bd187abd70005b40fa2df89ea3f45f332e4a04`; sole backlog conflict preserves
named R03 hold. Task.branch names actual checkout before gates. Normal hook
passes; local merge `f5649046715d256c757f3df163c053e22779f39b`.
Q01e841ab8/R02dcb1fb5 ancestry and per-card post-merge evidence verified.

Independent review identifies two Important gaps: command-specific tests and
exclusive fixture stayed in main, widening report_from_inputs solely for them;
argument/missing-input contracts lacked automation and schema coverage repeated
partial assertions. Root accepts bounded correction: tests/fixture move into
the module, report helper becomes private, full literal JSON oracle and exact
argument/missing-document/manifest/release-query error assertions added. Shared
version/release-surface helpers remain in main for other gate consumers.
Scoped independent rereview approves; production behavior is unchanged.
Captured-input tests do not establish filesystem read/discovery behavior.

Frozen SHA-256s: main.rs
fde15bb2fd62d567478c1e25a3dd72dcd231fbb85fdeb58459222c88541a8a6d;
release_readiness.rs
22dbc141767c261179eff2544ec799de0a086b582c6242fa1752fc0f46963161.

## Mutation control and CLI proof

Isolated clone `/home/ubuntu/data/projects/assura-q06-readiness.7g2iMb/repo`
on vps-9cb01956 UID1000 uses own target/logs, Rust1.98.1/CARGO_BUILD_JOBS4.
Original Q04/A05 clones and51local worktrees preserved. Bundle pins f564904
and master98bd187. No exclusive-host or performance claim.

Clone-only mutation returns Ok for a missing --format value. Command
`cargo test -p xtask release_readiness -- --nocapture`:

- Baseline95674/628def:4old tests pass/0fail/35filtered, exit0/capture0.
  `baseline.R8u169/test.log` SHA
  33274adb66a2245f600189b6c9b0d8d77f0eb0dcaac3e6ee42d9ccd0571c58e0.
- New43395/bd2004 with same defect:6pass/1fail/35filtered, exit101/capture0.
  Invalid-argument test catches erroneous Ok. `red.VX9gdA/test.log` SHA
  769d099458f080cf92e5d1afa83e3e1c091e9782e6a62ff80d99fcac5eb666c6.
- Reverse only deliberate patch after reverse-check;16370/417844
  `cargo test -p xtask`:42pass/0fail/0ignored/0filtered, exit0/capture0.
  `green.v1mDu8/test.log` SHA
  ae4c4816fab381c561681335c86a9e93154299e06e0d28a54104e4020b9aaf47.

`cargo build -p xtask` exits0/capture0, `green.y29ZBH/test.log` SHA
f871ced3f36c48ecdc8887545fe10df0b57719bd9d3067b1b0dc08c83f512a4a.
Actual debug xtask SHA
2fbb678c4c8c4e1836db39b5084577c58529cb6989a13d3aa24f7d533346c5bd.
Real probes from empty cwd (`cli.yFrqks`): help exits0 and names command;
missing mode2; unknown mode1; missing format1; invalid format1; unknown option1;
trailing unknown option1. All seven assert exact diagnostic fragments. A gh
sentinel positive control exits90 and creates its marker; all command probes
leave that marker absent, proving zero gh invocation in these error/help paths.
Logs retain stdout/stderr separately. No actual release lookup/publication is
claimed by those probes, and no broken mutation is committed.

## Exact-source full gate and integration hold

Reviewed source commit `265d9c2e0722122b7beb20c6bdd0c77758538c96` preserves
both frozen file hashes above. Local formatting/diff checks and normal commit
hook pass. Remote clone receives that exact commit and is clean before
`RUSTUP_TOOLCHAIN=1.98.1 CARGO_BUILD_JOBS=4 cargo xtask pr`, using its own
target directory. Session18397 terminates in b512ab: exit0/capture0.

- Rust:1290passed,0failed,1ignored across103test-result reports;8empty reports
  are not coverage. Includes42xtask tests.
- Formatting, minimal/full frontend checks, structure1781files/393dirs with
  zero violations,9evidence policy cases, target-state and full Clippy pass.
- Documentation examples,20release-surface mappings,54YAML examples,
  generated demo freshness and Astro48page build pass. Dependency future-
  incompatibility warning and missing404content warning remain visible.
- Log `pr.AwUL4E/test.log` SHA-256
  559f4d9f8f6ac8a4fc4f7a9ca7cc3eae126a3651d732b069490fcfc322d909b5;
  matching local copy `.trellis/.runtime/q06-pr.log`.
- Debug assura SHA-256
  8df04587878b8af8fa84d7dad1224ef5329eb2245114c1c3a4129fa0fb4ec6e0;
  assura-full
  f33fdd23291a7e6206c1ca2c99432c9e55060c4eef5904b52cbba1c75629a28c;
  xtask retains the CLI-proof hash above.

Root approves the bounded implementation and local proof, not merge/release.
`git diff --exit-code origin/master HEAD -- src tests Cargo.toml Cargo.lock`
exits0. That unchanged engine surface is not a performance waiver: no
comparable benchmark or exclusive-host claim was produced by this gate.
PR166 still has old head1e185d2, conflicting base and failed Performance Report;
none of those hosted results test265d9c2. Before update/merge, obtain authority
for automatic Cloudflare publication effects or verify no-deploy separation,
then pass exact-head hosted and valid performance gates with resolved review.
No push, PR write, merge, release, deployment or resource cleanup occurred.

Next executable integration step is conditional on those holds; do not start
dependent work merely because this candidate is locally ready. Controller
A05 carries the cross-card queue, not this branch-local historical snapshot.

Documentation checkpoint: session64335/65b9a2 validates the three root-owned
evidence/ledger edits over265d9c2 using its hashed debug binaries: structure
1781files/393dirs/0violations and9evidence cases pass, exit0. Only those three
documentation files are changed in the validation clone; source stays exact.

## Iteration116: exact-head acceptance and current-master integration

The repaired candidate `6cce9ea029fa080570f9b4723402b2b113adfdd6` was rebased
on `f127b6455c8e00e775e1ef7bf918c06fb6a3bab0`. It passed the focused
`cargo test -p xtask release_readiness -- --nocapture` suite (7 passed),
`cargo xtask target-state`, and a fresh complete `cargo xtask pr` run after
the frozen website dependency install. The known advisory line-length findings
in unrelated agent CLI surfaces remain visible and were not changed or waived.

Independent rereview found the private-module extraction behavior-preserving:
dispatch stays exact; report inputs and error paths are retained; the complete
fixed-input JSON oracle and invalid-argument controls travel with their
exclusive fixture; and the `gh` inspection remains read-only. It found no Q06
finding to resolve.

Fresh hosted workflows for that exact head all passed: Rust
[`34259585581`](https://github.com/rothnic/assura/actions/runs/34259585581),
Documentation
[`34259585554`](https://github.com/rothnic/assura/actions/runs/34259585554),
and Security
[`34259585558`](https://github.com/rothnic/assura/actions/runs/34259585558).
This includes the required performance report and the full platform, coverage,
release-bundle, installer, website, and adoption-smoke matrix. PR
[#166](https://github.com/rothnic/assura/pull/166) merged as
`50e131ec096b31f728423a395070d9b8ed6c211f`, which is now reachable from
`origin/master`.

No deployment, release, publication, protection change, or pilot action was
performed. This evidence and ledger reconciliation is a documentation-only
follow-up; it does not resolve the separate R03 performance-attribution work.

# A07 — Blinded candidate evaluation evidence

State: active. This record is preparation only and is not a screening, holdout,
or acceptance result.

## 2026-09-08 selection and protocol boundary

The dependency ledger is current at `origin/master` commit
`5cddfbf818ce37b9bcbb3cda159ed4a9af707d44`: A04 is complete through reviewed
PR #220, A05 through its merged quality-contract slices, and A06 through
reviewed PR #222. A07 is therefore the next dependency-ready initialization
card. Its owner is the clean worktree
`/Users/nroth/.codex/worktrees/assura-a07-blinded-evaluation` on
`goal/a07-blinded-evaluation`.

A07 must not reuse partial A01 controls as acceptance. Before any initializing
agent receives the fixed prompt `Initialize this project with Assura`, this
card will freeze the current-master source SHA, exact candidate-binary hash,
model/harness version, two product-input conditions, three-stack fixtures, and
the evaluator contract. The screening allocation is 3 stacks × 5 repetitions
× 2 conditions (30 isolated runs). A separate fresh agent must then perform
the specified follow-up feature for each evaluated initialized project.

Before screening, the card must also freeze six unseen holdout layouts (two per
stack) and their hashes after hand-verifying native commands. One holdout per
stack must retain a valid configured naming/layout exception and local policy;
the other must retain a working supported hook manager plus custom hook action.
The initialization agents must not receive evaluator contracts, prior-run
memory, parent coaching, or manual repair. The holdout allocation is 18 runs
(three per layout); all 18 require the full A01 contract and follow-up feature
check. A failed holdout returns to its owning behavior card, and any rerun is
recorded as regression evidence rather than an untouched holdout.

No A07 result can be reported until the final 10-per-stack candidate batch
meets at least 9/10 acceptable per stack with zero destructive overwrites and
zero missed critical seeded violations. Required unavailable host/tool evidence
is a failure, not a substitute fixture. The public result will be redacted and
will report counts, sample sizes, medians/p95 where observable, and failure
examples without claiming a population success rate.

## Existing evaluator boundary

`scripts/evaluate-agent-init.py` and
`tests/agent_init_evaluator_tests.py` are the A01 evaluator foundation. A01's
merged partial-contract result is intentionally insufficient for A07: an
explicit partial `--dimensions` invocation is acceptance-ineligible, while a
full invocation with an unavailable dimension renders a failing acceptance
verdict. A07 will use full contracts only and record each exact evaluator
output separately from agent-visible project contents. Candidate freezing,
fixture hand verification, and blind harness isolation are the next actions.

## 2026-09-08 candidate freeze

The frozen source is `a379cba3ed88fe0e6c973431db5ea19e63e8266d`, a
documentation-only A07 preparation commit whose code parent is current
`origin/master` `5cddfbf818ce37b9bcbb3cda159ed4a9af707d44`. The exact local
candidate is `target/release/assura-full`, version `0.4.0`, SHA-256
`c017d91439585a2717becdece5407cb67634793e17e470be42772ea9c64f2afd`.
It was built with Rust `1.94.1 (e408947bf 2026-03-25)` on Darwin x86_64,
Intel i9-9880H @ 2.30 GHz, with 32 GiB RAM.

The evaluator harness version is `codex-cli 0.153.2`; each independent run
must additionally record its actual model/harness identifier before receiving
the fixed prompt. The frozen ordinary-fixture content hashes, excluding their
tracked contracts and variants, are Rust
`b3f5e2d7c57c0c784c23bf89945fcd0f861f00b09fac7086ea9d5d3d3d1bb940`,
TypeScript `231b26f1ad40e43b10a566330103df294d2919ceccfa1e3322c8efec99e18dde`,
and Python `65d5d494335884f7fa34938c23d74d1de24a937545016f9a8714d661556d692e`.
Their existing A01 contract hashes are respectively Rust
`3e9b827b708cf09d4f2205bc2336eae5485ae652109795753c7199683827f664`,
TypeScript `79b3943ba0acaab7f478c99e9dbc1e2f197200d335c756cc8ee4f43e263003ec`,
and Python `e528dfa67916396e708744a7ddbcfa70724ac76a953f49e9d1e8836cdc7da921`.
Those A01 contracts are partial controls and are recorded for provenance only;
they cannot evaluate an A07-accepting run.

Independent protocol review of `a379cba` was clean: the record keeps evaluator
contracts opaque to initializing agents, retains 30 screening and 18 holdout
runs, and claims no run or acceptance result.

## Ordinary-fixture negative control

The frozen binary evaluated each existing ordinary fixture with its tracked A01
contract in full mode. These are control runs, not initialized-agent runs and
not A07 screening results. Each invocation exited `1` with
`acceptance_pass: false`: Rust lacked `native`, `hooks`, and `guidance`; both
TypeScript and Python lacked `hooks` and `guidance`. The evaluator correctly
recorded policy, structure, preservation, and idempotence where contracted;
TypeScript and Python also recorded their native commands. This confirms that
the current partial ordinary contracts cannot produce an A07 acceptance pass
and must not be promoted or weakened.

## Unconfigured-onboarding pre-screen control

A disposable Rust project copied only ordinary source/manifest files from the
frozen fixture, excluding `.assura`, contracts, and variants. The exact frozen
candidate ran `agent onboard <project> --agent codex --activate --format json`
with a structurally verified Codex integration and unavailable host approval.
It generated the expected onboarding packet, `AGENTS.md`, and
`.codex/hooks.json`; this is a candidate control, not a user-agent run.

The prescribed critical negative probe then added `src/BadName.rs`. The exact
candidate command `assura-full check <project> --format json` exited `0` with
`success: true` and zero violations. The generated unconfigured policy lacks
the fixture's strict Rust naming rule, so this input condition cannot satisfy
A07's zero-missed-critical-violation acceptance rule. The correct repair path
is the initialization-specialization behavior in A02/A03, not changing the
evaluator, seed, acceptance threshold, or A07 ledger. Screening and holdout
runs remain unstarted pending that owner correction and a new frozen candidate.

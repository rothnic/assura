# A07 — Blinded candidate evaluation evidence

State: active. This record is preparation only and is not a screening, holdout,
or acceptance result.

## Current candidate-bound canary — 2026-09-11 UTC (`origin/master=71adc2e`)

- Owner/phase: process coordinator `/root` / `candidate-bound-canary`. The
  source was refreshed from `origin/master`, and a clean owned worktree built
  Assura `0.4.0` with the pinned Rust/Cargo `1.94.1` toolchain. The candidate
  source revision, source tree, binary, fixed prompt, public contract and
  login-shell shim identities were frozen privately before launch.
- The first two disposable fixture attempts are retained as unfavorable,
  no-credit setup evidence: the candidate identity matched, but the source
  fixture omitted the contract directory required by its own structure policy,
  so both initializer runs exited nonzero. They were not reused or counted.
- Two corrected source-only fixtures were then launched beneath separate
  sibling-free disposable parents. In the same login-shell environment,
  `command -v assura`, the version and the fixed candidate target matched the
  pre-launch freeze. Both composed Codex initialization routes exited `0`.
- The full private evaluator ran with the same absolute candidate for both
  one-variable content-template conditions. Both results exited `0` with
  `verification_scope=full`, `acceptance_eligible=true`,
  `acceptance_pass=true`, zero critical failures and all seven dimensions
  passing. The seeded negative policy probe exited `1` and matched the
  expected naming rule; the native Cargo command collected and passed a test;
  preservation, guidance, hook and idempotence checks also passed.
- This is a fresh candidate-bound **no-credit canary**. It does not allocate a
  screening cell or count toward the 30-run screen, untouched holdout,
  follow-up feature, or final per-stack threshold. Private fixture,
  contract, prompt, command-path, identity and raw evaluator records remain
  outside this evidence file.
- The immutable six-handle holdout construction and exactly-two-condition
  manifest were rebound to this candidate. An independent isolated metadata
  protocol review returned `PASS`; all allocation, credit and external
  authority flags remain false.
- Next owner/action: seek the separately authorized screening gate after
  refreshing source and ledger again. Earlier packets and protocol `PASS`
  records remain historical metadata-only evidence; this current `PASS` is
  still no-credit preparation.

## Historical pre-71adc2 candidate checkpoint (superseded by current `71adc2e`) — 2026-09-11 UTC (`origin/master=af005a7`)

- At the prior checkpoint, PR #275 had merged the reviewed
  process/documentation reconciliation at
  `af005a7dc5c367bc9388e08203fbf351bc0c88f2`; its tree and owned closure are
  verified, and its applicable hosted checks plus post-merge workflows passed.
  No product, evaluator, threshold, allocation or authority state changed.
- The 129a249 candidate freeze, login-shell identity controls, two source-only
  canaries, six-handle binding and exactly-two-condition manifest passed their
  no-credit protocol checks but are historical after this source advance. No
  af005a7 candidate was frozen at that checkpoint; the subsequent 71adc2
  refresh and canary above supersede this routing note.
- The ledger remains 32 items, `ready_pending=0`, five unfinished and three
  held: A07 active, W03 verified, R01/W02/F01 held. At that historical
  checkpoint the next owner/action was to refresh source, release/tag, PR/CI
  and topology state, rerun the ledger and topology audit, re-freeze the
  candidate, run a fresh no-credit canary, verify/rebind the holdout and
  manifest, and obtain isolated protocol `PASS`; the current section above
  records that later progression. Prior PASS records did not authorize
  screening, allocation or product acceptance.

## Historical 8be post-merge contract-correction route (superseded by current `9ad926a`; previously `b7043ab`) — 2026-09-10 UTC (`origin/master=8be6103`)

- PR #268 merged the reviewed process documentation route at `8be6103`; it
  changed no product, evaluator, threshold, allocation or authority state.
  The two `9b410e9` candidate-bound composed-init canaries are retained as
  historical no-credit preparation, not current screening evidence.
- The private 9b candidate rebind was independently rereviewed. Finding
  `A07-CONTRACT-HASH-001` identified a contract digest mismatch between the
  manifest rows and the supplied contract bytes; the smallest private fix was
  applied to bind all recorded contract digests to those bytes, and the scoped
  rereview returned `PASS`. That result is historical metadata-only evidence
  after the 8be advance; it is not current candidate or screening credit.
- The fresh `8be6103` candidate was rebuilt and identity-checked, and two fresh
  source-only composed-init canaries plus full evaluator runs passed all
  required dimensions with the negative control behaving as expected. These
  are no-credit canaries. The first isolated 8be protocol review then found
  three concrete metadata gaps: unbound holdout handles, historical candidate
  hashes in the holdout record, and a stale 9b review reference. The private
packet now contains a current candidate freeze, immutable six-handle binding,
second read-only confirmation, explicit raw-hook exclusion, and current
manifest reference. A follow-up correction canonicalizes the exact toolchain
identity across all packet records and adds six immutable per-handle creation
time/evidence records mirrored by the second-readonly check. Supplemental and
primary isolated metadata reviews passed against the corrected packet; this is
still no-credit preparation only.
- The next owner/action is to refresh source/ledger, freeze a new candidate
  identity and prepare only the separately authorized no-credit screening gate.
  Private values, mappings, fixtures, evaluator output and child transcripts
  remain outside this record; no product, release,
  deployment, publication or invitation authority changed.

## Historical current as-of merged-candidate canary — 2026-09-10 UTC (`origin/master=9b410e9`)

- Owner/phase: process coordinator `/root` / `candidate-bound-canary`; the
  clean current-master checkout and exact candidate identity are retained in
  the private harness. The merged source is
  `9b410e936e2afe85f0adc4cd83ef614e35c43d2c`; the release binary is Assura
  `0.4.0` built with Rust/Cargo `1.94.1`.
- The actual login-shell command environment matched the fixed candidate for
  `command -v assura`, version, target path and target SHA. Two fresh
  source-only fixtures used the composed `init --agent codex --activate`
  route followed by one reviewed `--content-template` value each. Both
  initializer runs exited `0`; both full A01 evaluator runs exited `0` with
  `acceptance_eligible: true`, `acceptance_pass: true`, all seven dimensions
  passing and the seeded negative control rejecting as expected.
- These are fresh candidate-bound no-credit canaries only. They do not count
  toward the 30-cell screening matrix, 18-run untouched holdout, follow-up
  feature check or final 10-per-stack threshold. Earlier generic/omitted-policy
  and restrictive-existing-config attempts remain retained as unfavorable
  no-credit evidence and were not retried unchanged.
- The prior private protocol `PASS` remains valid only for its reviewed
  metadata/candidate snapshot. The next owner/action is to rebind the private
  manifest rows and supplied-input receipts to `9b410e9`, obtain an isolated
  scoped protocol rereview `PASS`, and only then allocate screening cells.
  Private fixture contents, mappings, evaluator output and child transcripts
  remain outside this record; no product, release, deployment, publication or
  invitation authority changed.

## Historical current as-of protocol review PASS — 2026-09-10 UTC (`origin/master=af73d8a`)

- Owner/phase: process coordinator `/root` / `review`; the clean owned
  checkout is `/private/tmp/assura-a07-manifest-review` on
  `docs/a07-condition-manifest-review`, based on freshly fetched
  `origin/master=af73d8a5004ea8c0d2298202467d1635853434c9`. The candidate
  binary is `assura 0.4.0`, built with Rust/Cargo `1.94.1`; source and target
  identity are recorded privately.
- Reset proof: workflow/context routing passed (`43/43`); the
  revision-pinned ledger is `items=32`, `ready_pending=0`, `unfinished=5`,
  `held=3` (A07 active, W03 verified, R01/W02/F01 held). No pending card has
  independently executable merged dependencies.
- The owner-controlled private store now contains a versioned manifest with
  exactly two explicit values for the supported `--content-template` input,
  a separate unblinded mapping, six frozen holdout handles and 30 reserved
  cells. Private values, mappings, fixtures, contracts, raw output and child
  transcripts remain outside this record.
- Two earlier receipt attempts are retained privately as invalid/no-credit
  evidence: one had a contaminated parent, and one used non-identical fixture
  text. The current receipts came from separate parents containing only one
  fixture each; their fixture trees are byte-identical, both commands exited
  `0`, and receipt metadata matches the frozen candidate identity. No
  screening, holdout or acceptance credit is created.
- An isolated protocol reviewer `/root/a07_protocol_review` reviewed only the
  manifest, mapping, supplied-input receipts and matrix metadata and returned
  a redacted `PASS` with no findings. Raw evaluator output, child transcripts
  and private fixture contents were excluded. The disposition is sufficient
  for the private protocol gate only; no product, evaluator, threshold,
  allocation, release, deployment, publication or invitation state changed.
- Next owner/action: refresh source/ledger, freeze a new current-master
  candidate identity and run a fresh candidate-bound no-credit canary. The
  protocol `PASS` does not prove fixture contents, child isolation,
  evaluator outcomes, screening or acceptance; any mismatch earns no credit
  and requires a fresh canary.

## Historical current as-of private-readiness audit — 2026-09-10 UTC (`origin/master=afcbe967`, superseded by `af73d8a`)

- Owner/phase: process coordinator `/root`; `investigate-prepare` in the clean
  owned checkout `/private/tmp/assura-a07-route.WCNcQp` on
  `docs/a07-manifest-readiness-refresh`, based on the freshly fetched
  `origin/master=afcbe967fd6f1fcc4fe0d7a606bfa288eb4c8df5`. Candidate: this
  process-only evidence slice (recorded at commit).
- Reset proof: context-routing is `43/43 PASS`; the revision-pinned ledger is
  `items=32`, `ready_pending=0`, `unfinished=5`, `held=3`; A07 is active,
  W03 verified, and R01/W02/F01 retain separate holds. No pending card has
  independently executable merged dependencies.
- Metadata-only private-harness inspection found an owner-controlled `0700`
  store with candidate/contract/holdout/run records, but no discoverable
  versioned exactly-two-condition screening manifest or isolated protocol-review
  artifact. Private values, mappings, fixtures, contracts, raw output and child
  transcripts remain outside this public record; no screening, holdout or
  acceptance credit is created.
- Independent impasse/process review by `/root/context_routing_review_fast`
  returned `PASS`: the empty pending queue is an intermediate route, A07 owns
  the next action, and no pending card is independently executable. The review
  saw no concrete process finding.
- Next owner/action: the A07 acceptance coordinator privately creates or
  locates and validates the exact-two-condition manifest, supplied-input
  receipt, blinded mapping, complete 30-cell matrix and isolated protocol
  review `PASS`; only then refresh source/ledger and bind a fresh no-credit
  candidate canary. This checkpoint changes no product/evaluator/threshold,
  allocation, release, deployment, publication or invitation state.

## Historical current as-of route reconciliation — 2026-09-10 UTC (`origin/master=6fa806d`, superseded by `afcbe967`)

- Owner/session: process coordinator `/root`; phase: `post-merge-reconcile` /
  `route-audit`; closure: `active`. PR #263 merged reviewed process candidate
  `4cfd9174d1c305a8d62c88dee9a3a49368a37885`, based on
  `bcd0386b4f864163e3f3a08e81d6106a790491c2`, as
  `6fa806d05d4fab7245f35a1f57d21c237a72a809`. Candidate/merge tree equality,
  reachability, independent review and applicable Documentation/CI/Security/
  Evidence/GitGuardian checks passed. This remains process-only evidence.
- Current ledger: `items=32`, `ready_pending=0`, `unfinished=5`, `held=3`.
  A07 is active with no live worker/initializer/evaluator/review handle; W03
  is verified; R01/W02/F01 retain their smallest-resolution holds. The
  context-routing audit is `43/43 PASS`.
- The private exactly-two-condition manifest, supplied-input receipt, blinded
  mapping, complete 30-cell matrix and isolated protocol-review `PASS` remain
  required. Only after that redacted disposition may source/identity be
  refreshed for a fresh candidate-bound no-credit canary. No product,
  screening, holdout, publication, release, deployment or invitation credit
  changed. Refresh this as-of pointer before use.

## Historical post-merge reconciliation — 2026-09-10 UTC (`origin/master=040babb`, superseded by current as-of `6fa806d`)

- Owner/session: process coordinator `/root`; phase: `post-merge-reconcile` /
  `route-audit`; closure: `active`. Clean owned checkout:
  `/private/tmp/assura-a07-postmerge.Au57JM` on `docs/a07-postmerge-route`.
- PR [#262](https://github.com/rothnic/assura/pull/262) merged reviewed
  candidate `756865ec0d99993c25532802874d87b12d328851`, based on
  `2d4607bf6213c55a919fd230a31a09d64004213d`, as
  `040babbcfb7dbe744d89a1c13838c2111d3a3b10`. Candidate/merge tree equality,
  ancestry and current `origin/master` reachability are verified. This is a
  process-only reconciliation, not a new A07 product candidate or product
  credit.
- Applicable hosted Documentation, CI, Security, Evidence and GitGuardian
  checks: `PASS`; independent scoped rereview: `PASS`. Scope-skipped checks
  remain non-applicable/non-passing and are not merge proof. The prior owned
  `docs/a07-current-route` branch and
  `/private/tmp/assura-a07-current-route.vcLjcl` worktree were clean and then
  removed with exact local/remote ref, reachability and tree-equality proof.
- The revision-pinned ledger remains 32 items, `ready_pending=0`, unfinished=5,
  held=3. A07 is active with no live worker, initializer, evaluator or review
  handle; W03 is verified; R01/W02/F01 retain their smallest-resolution holds.
  The detached A07 checkout `77b41fe` is historical and must not be reused.
  No product, acceptance, screening, holdout, publication, release,
  deployment or invitation credit changed.
- Exact next action: privately produce and independently review the
  exactly-two-condition manifest, supplied-input receipt, blinded mapping,
  complete 30-cell matrix and isolated protocol-review `PASS`; only then
  refresh source/candidate identity and run a fresh candidate-bound no-credit
  canary. No screening or acceptance credit is implied.
- Held-action resolution remains unchanged: R01 requires the exact raw
  paths/kinds/rescan/config-state trace for hosted run
  `34090768850`/job `101643647551` sequence 2/count 3, or a specific maintainer
  native-readiness decision; W02 requires explicit Cloudflare approval for
  hosted integration with deployment separate; W03 publication and F01
  participant selection/invitations remain authority-held. No speculative
  retry, release, deployment, publication or invitation action is authorized.
  Context level is not exposed; closure stays `active` while the private
  protocol action exists.

## Historical current-master route checkpoint — 2026-09-10 UTC (`origin/master=2d4607bf`, superseded by current as-of `6fa806d`)

- Owner/session: A07 checkpoint writer / Trellis implementer
  `/root/a07_checkpoint_writer`; phase: `post-merge-reconcile` /
  `route-audit`; closure: `active`. Clean owned checkout:
  `/private/tmp/assura-a07-current-route.vcLjcl` on
  `docs/a07-current-route`.
- Source SHA: `2d4607bf6213c55a919fd230a31a09d64004213d` (`origin/master`);
  route checkpoint commit: `5d92af27805b85841d3999b4ed568858dd8026cf` in the
  documentation-only checkout. The route commit is based on, but is not the
  same tree as, the source SHA; neither is a new A07 binary, initializer,
  canary, screening, holdout or acceptance candidate. The detached A07
  checkout at `77b41fe` remains historical and must not be reused. The PR #261
  process branch/worktree has already been removed after its merged-tree and
  reachability proofs.
- The refreshed revision-pinned ledger reports 32 items,
  `ready_pending=0`, five unfinished and three held: A07 is active with no
  live worker/initializer/evaluator/review handle, W03 is verified, and
  R01/W02/F01 retain their existing smallest-resolution holds. The layered
  context-routing audit is `42/42 PASS`. No product, acceptance, screening,
  publication, release, deployment or invitation credit changed.
- Exact next action: privately validate the exactly-two-condition manifest,
  supplied-input receipt, blinded mapping, complete 30-cell matrix and
  isolated protocol-review `PASS`. Only after that `PASS` may source and
  candidate identity be refreshed for a fresh candidate-bound no-credit
  canary. No screening or acceptance credit is created by this checkpoint.
- Held-action resolution is unchanged: R01 needs the exact raw
  paths/kinds/rescan/config-state trace for hosted run
  `34090768850`/job `101643647551` sequence 2/count 3, or a specific
  maintainer native-readiness decision; W02 needs explicit Cloudflare
  approval for hosted integration with deployment kept separate; W03's
  publication remains authority-held; F01 participant selection and
  invitations remain Nick-authorized. Do not perform speculative retries or
  any release, deployment, publication or invitation action from this route.
- Detached-checkout `cargo xtask target-state` exits `1` when the active
  multi-card task has `branch: null`; this is accurately retained as a
  non-passing caveat, not waived evidence, and no task branch policy changed.
  Context level is not exposed. Closure remains `active` while the private
  protocol action is live.

## Historical post-merge continuation checkpoint — 2026-09-10 UTC (`origin/master=373fb01`, superseded by current as-of `6fa806d`)

- Owner/phase: process coordinator / `post-merge-reconcile`. PR #259 merged
  the independently reviewed process candidate `ccd3bd2dcfe8ba6177b59ffc16ecc09560558bf8`
  (based on `8c198dcb4c94095e7db1b017908eaadb813cb9e7`) as
  `373fb01ac268285d9b21cd6948862050bd7d8c6e`; the merged tree equals the
  reviewed candidate. The fresh ledger remains 32 items with
  `ready_pending=0`, five unfinished and three held: A07 active, W03 verified,
  and R01/W02/F01 separately held.
- The candidate identity below was produced at the parent `c34f9178` and is
  historical no-credit preparation, not a current canary. The six valid
  holdouts remain frozen, while the private exactly-two-condition manifest,
  supplied-input receipt, blinded mapping, complete 30-cell matrix and
  isolated protocol-review `PASS` remain absent. The next action is to validate
  that private protocol, then refresh source and identity before a no-credit
  canary. No screening, holdout or final-acceptance credit is created here.
- The PR #258 `docs/maturity-goal-continuation` and PR #259
  `docs/a07-postmerge-checkpoint` checkouts, branches, and remote refs were
  each removed after clean-status, merged-tree, and reachability proofs. The
  topology report completed; strict mode remains nonzero only for preserved
  external/user and historical worktree/branch conditions. No unrelated
  cleanup occurred.

## Historical post-merge continuation checkpoint — 2026-09-10 UTC (`origin/master=8c198dcb`, superseded by `373fb01`)

- This checkpoint records the state immediately after PR #258 and before PR
  #259. Its source pointer and cleanup statement are historical; future
  continuations must fetch `origin/master` and use the live section above.

## Historical candidate-freeze checkpoint — 2026-09-10 UTC (`origin/master=c34f9178`, superseded by `8c198dcb`)

- Owner/phase: A07 acceptance coordinator / `candidate-freeze`. A fresh fetch
  and revision-pinned ledger resolved `origin/master` to
  `c34f917866e45cc122ec07412fa0c630d460f663`; the ledger has 32 items,
  `ready_pending=0`, five unfinished and three held. A07 is the sole active
  executable lane; W03 is verified and R01/W02/F01 retain separate holds.
- A clean detached current-master checkout built `assura 0.4.0` with
  Rust/Cargo `1.94.1` (release build exit `0`). The non-symlink candidate's
  SHA-256 is
  `95c93052bd1993566d9f8209bdba5625c2b39a19287ed6358d710015d2ac59ff`.
  `command -v assura`, `assura --version`, and the hash matched from the same
  minimal `zsh -lic` environment intended for an initializer. This is a
  no-credit identity proof; no global binary or startup file was changed.
- The private store remains owner-controlled (mode `700`) and its holdout
  construction record independently confirms six valid frozen layouts plus one
  disqualified draft. No screening, holdout, or final-acceptance credit exists.
  The private screening manifest still lacks exactly two named product-input
  conditions, supplied-input evidence, a blinded mapping, and the complete
  30-cell matrix; no historical run is reclassified.
- The product surface review confirms that local `--recipe-file`, bundled
  `--recipe`, and onboarding `--content-template` are supported inputs, but the
  packet does not select concrete condition values. The held action is to
  choose one supported input and record two explicit values plus a supplied
  receipt in the private manifest; inventing values or treating an ambient
  default as supplied would invalidate the protocol. After an isolated
  protocol-review `PASS`, refresh the source and run the fresh no-credit canary.
- A serialized VPS probe found the configured `vps` host reachable with 16
  CPUs and about 43 GiB available memory, but its root disk is 94% used with
  about 20 GiB free, it has nightly Rust 1.95 and no Bun. The documented
  `vps-dev` alias is unresolved. No remote build ran; local exact-toolchain
  proof remains the source of candidate identity and hosted/platform gates
  remain mandatory.

## Dated continuation checkpoint (refresh required) — 2026-09-10 UTC

This route was reconciled at `origin/master=062f6c3` and integrated by PR #252
as `a92b109c2158113a1f2316b8cdd5ef79f6668571`. It is provenance, not a live
source pointer: fetch `origin/master` and rerun the revision-pinned ledger
before binding any canary or using a SHA from this section.

PR #251 merged the reviewed process-only continuation correction as
`062f6c39d15babc8b12299863576a29febda5dd5` from candidate `e777ee7`, based
on `1bd78cc`; the merged tree matches the reviewed candidate. This updates
command identity and R01 routing only and changes no A07 product, evaluator,
threshold, allocation or authority state.

The revision-pinned ledger remains at 32 items with `ready_pending=0`, five
unfinished and three held: A07 is `active`, W03 is `verified`, and R01/W02/F01
retain their card-level holds. At every continuation the coordinator first
fetches `origin/master`, reruns the revision-pinned ledger and freezes the
candidate identity (that observation is not screening credit), then confirms
the six unseen holdouts. The private metadata-only audit still lacks the
exactly-two-condition manifest, supplied-input proof, blinded mapping, complete
30-cell matrix and isolated protocol-review `PASS`; screening, holdout and
final-acceptance credit remain zero. The next authorized action after that
refresh is the private manifest/protocol review, followed only after a redacted
`PASS` by a fresh no-credit canary against the frozen identity.

## Historical continuation checkpoint — 2026-09-10 UTC (`origin/master=2eda17e`; superseded by PR #251)

Owner/phase: process coordinator / `investigate-prepare`; the A07 acceptance
coordinator owns the next card action. The refreshed current source is the
PR #249 merge `2eda17e82d9dab12338805479a33a5774560451f`; its reviewed
process candidate `5f17f7f` was based on `755c28d` and its merged tree
matches. This process-only reconciliation adds continuation-control routing
and changes no A07 product, evaluator, threshold, allocation or authority
state.

Session/live handle: none; the metadata-only audit is complete and no
initializer, evaluator, CI or review process is running. Worktree/branch:
private A07 evidence lane / no shared checkout. Trigger/proof: the current
ledger has `ready_pending=0`, and the redacted metadata audit below confirms
the required manifest and protocol-review fields are absent. The exact next
action is the A07 coordinator's isolated manifest/protocol review.

The revision-pinned ledger has 32 items, `ready_pending=0`, five unfinished
and three held. A07 remains `active` with zero screening, holdout and
final-acceptance credit. A redacted metadata-only private audit still finds
seven layout directories (six frozen and one disqualified draft), a draft
manifest without discoverable condition, supplied-input, mapping or matrix
fields, and no isolated protocol-review artifact. Existing run objects are
not reclassified from names or historical outcomes.

Independent impasse/process review recorded `A07-MANIFEST-04` as evidence
missing. The smallest resolution is a versioned private manifest with exactly
two named product-input conditions differing in one variable, supplied-input
proof, blinded mapping, six frozen holdouts and a complete 30-cell matrix,
followed by an isolated protocol-review `PASS`. Only after that disposition
may the coordinator fetch again, bind the current candidate identity and run
the no-credit canary; no screening cell is authorized before those gates.

The owner must keep private condition values, mappings, fixtures, evaluator
output and child transcripts outside this repository. Public evidence may
record only the redacted disposition, candidate identity and aggregate
outcomes. This card-level hold is not a whole-goal stop; the supported runtime
goal remains active and its next route is the private manifest/protocol check.

## Historical source-pointer reconciliation — 2026-09-10 UTC (`origin/master=755c28d`; superseded by PR #249)

Owner/phase: process coordinator / `reconcile-handoff`. PR #248 merged the
reviewed source-pointer lifecycle correction as
`755c28ded66d1f2d82b38d27633be83a0233f30e` from candidate
`89a0430ba9f4bbf141e717ed78de72b41c5ab3b6`, based on `6ed43c3`; the merged
tree matches the reviewed candidate. The current ledger still routes A07 as
`active` with zero screening, holdout and final-acceptance credit.

A metadata-only private audit still finds seven layout directories (six
frozen-layout entries and one disqualified raw-hook draft), a draft holdout
manifest without discoverable condition/mapping/matrix fields, and no isolated
protocol-review artifact. The next authorized observation is the private
exactly-two-condition manifest and isolated protocol-review `PASS`; only after
that disposition may the candidate identity be refreshed and the no-credit
canary run against the then-current master. This process correction changes no
product, evaluator, threshold, allocation or authority state.

## Historical post-merge source checkpoint — 2026-09-10 UTC (`origin/master=6ed43c3`; superseded by PR #248)

Owner/phase: process coordinator / `reconcile-handoff` complete. PR #247
merged the reviewed source-pointer correction as
`6ed43c3c63fab7b60a86f1d587c067c9b04ded93` from candidate
`577b6d63030aab338238088c85cfa7c7750baeb2`, based on `d62dd40`. The current
ledger still routes A07 as `active` with zero screening, holdout and
final-acceptance credit. This process merge changed no product, evaluator,
threshold, allocation or authority state.

At the next resume, fetch `origin/master` and rerun the ledger before using any
source pointer. This checkpoint is superseded by current `062f6c3` after PR
#251; the active
A07 route is unchanged: obtain the private manifest
and isolated protocol-review `PASS`, refresh candidate identity against the
fetched master, then run the no-credit canary. The readiness audit below is a
historical candidate-base record from the PR #247 parent.

## Historical candidate-base/private-manifest readiness re-audit — 2026-09-10 UTC (`origin/master=d62dd40`; superseded by PR #247)

Owner/phase: A07 acceptance coordinator / `investigate-prepare`. A fresh
read-only reset resolved `origin/master` to
`d62dd40f0d915e693db25cdea2fb29d1000e9b50`; this is the routing source only,
not a new candidate or screening result.

The private evidence store was inspected without copying private values,
fixture contents, evaluator contracts, child transcripts, or raw results. Its
construction document is still explicitly titled a draft. It contains seven
layout directories: six valid frozen layouts (two per stack) and one
disqualified raw-hook construction draft. The required separate private
screening record was not found: there are no discoverable `condition_id` /
`product_input` rows, unblinded mapping, supplied-input proof, complete
30-cell matrix, or isolated protocol-review `PASS`. Five distinct
`rust-screen-*` record families exist, but they are historical controls or
attempts made before the manifest gate; their private outcomes remain outside
this repository and receive no screening, holdout, or final-batch credit.

Disposition: A07 remains `active` with zero screening, holdout, and
final-acceptance credit. The held action is to create or locate a versioned
private manifest satisfying the public contract, validate the exactly-two
condition mapping and 30-cell matrix in an isolated protocol-review session,
and record only a redacted `PASS` or concrete findings. The smallest next
observation is that protocol-review disposition; only after it passes may the
coordinator refresh the candidate identity against `d62dd40` and run the
required no-credit canary. Existing run files must not be reclassified from
their names or acceptance booleans.

## 2026-09-08 selection and protocol boundary

The dependency ledger is current at `origin/master` commit
`5cddfbf818ce37b9bcbb3cda159ed4a9af707d44`: A04 is complete through reviewed
PR #220, A05 through its merged quality-contract slices, and A06 through
reviewed PR #222. A07 is therefore the next dependency-ready initialization
card. Its owner is the clean worktree
`/Users/nroth/.codex/worktrees/assura-a07-blinded-evaluation` on
`goal/a07-blinded-evaluation`.

A07 must not reuse partial A01 controls as acceptance. Before any initializing
agent receives the fixed prompt `Initialize this project with Assura`, this
card will freeze the current-master source SHA, exact candidate-binary hash,
model/harness version, two product-input conditions, three-stack fixtures, and
the evaluator contract. The screening allocation is 3 stacks × 5 repetitions
× 2 conditions (30 isolated runs). A separate fresh agent must then perform
the specified follow-up feature for each evaluated initialized project.

Before screening, the card must also freeze six unseen holdout layouts (two per
stack) and their hashes after hand-verifying native commands. One holdout per
stack must retain a valid configured naming/layout exception and local policy;
the other must retain a working supported hook manager plus custom hook action.
The initialization agents must not receive evaluator contracts, prior-run
memory, parent coaching, or manual repair. The holdout allocation is 18 runs
(three per layout); all 18 require the full A01 contract and follow-up feature
check. A failed holdout returns to its owning behavior card, and any rerun is
recorded as regression evidence rather than an untouched holdout.

No A07 result can be reported until the final 10-per-stack candidate batch
meets at least 9/10 acceptable per stack with zero destructive overwrites and
zero missed critical seeded violations. Required unavailable host/tool evidence
is a failure, not a substitute fixture. The public result will be redacted and
will report counts, sample sizes, medians/p95 where observable, and failure
examples without claiming a population success rate.

## Existing evaluator boundary

`scripts/evaluate-agent-init.py` and
`tests/agent_init_evaluator_tests.py` are the A01 evaluator foundation. A01's
merged partial-contract result is intentionally insufficient for A07: an
explicit partial `--dimensions` invocation is acceptance-ineligible, while a
full invocation with an unavailable dimension renders a failing acceptance
verdict. A07 will use full contracts only and record each exact evaluator
output separately from agent-visible project contents. Candidate freezing,
fixture hand verification, and blind harness isolation are the next actions.

## 2026-09-08 candidate freeze

The frozen source is `a379cba3ed88fe0e6c973431db5ea19e63e8266d`, a
documentation-only A07 preparation commit whose code parent is current
`origin/master` `5cddfbf818ce37b9bcbb3cda159ed4a9af707d44`. The exact local
candidate is `target/release/assura-full`, version `0.4.0`, SHA-256
`c017d91439585a2717becdece5407cb67634793e17e470be42772ea9c64f2afd`.
It was built with Rust `1.94.1 (e408947bf 2026-03-25)` on Darwin x86_64,
Intel i9-9880H @ 2.30 GHz, with 32 GiB RAM.

## Private evaluator and calibration boundary

The evaluator version, fixture identities, contract content, control commands,
provenance hashes, and required-evidence details are private. Independent
protocol review confirms that no initializer receives them. Private controls
established the intended calibration boundaries, but are not initialized-agent
runs and do not count toward screening, holdout, or final allocations. The
evaluation threshold and ledger remain unchanged; blind runs were not authorized
until the private freeze protocol completed.

## Private pre-screen calibration

The corrected evaluator and frozen candidate have privately completed
hand-configured calibration controls. They demonstrate that the evaluator can
distinguish an intact native project from partial or zero-work evidence without
disclosing fixture, command, contract, hash, or required-evidence details to an
initializer. Those controls are neither blinded initializer runs nor A07
screening, holdout, or final-acceptance results; they do not count toward any
run allocation. All pre-screen layouts are now separately frozen under the
private protocol before any blind run begins.

## Holdout-freeze progress

Six valid layouts are frozen in the private A07 harness. Their hand verification,
full private contract, preservation checks, candidate hash, and independent
review all satisfy the freeze checklist. Exact layout and manager details remain
private so that initializer sessions cannot infer unseen holdout content.

No valid layout remains to freeze. A disqualified construction draft remains
excluded from the allocation. No holdout contract or construction record has
been disclosed to an initializer, and no blind screening or holdout run has
started. The next required work is to begin the authorized blind screening
protocol without exposing private construction records.

## 2026-09-09 composed-init correction slice

The live implementation owner is the clean worktree
`/private/tmp/assura-a07-composed-init` on `goal/a07-composed-init`, based on
current `origin/master` `4e188720c5b5a643f68c80e13e1f7c90a5eee656`. The slice
at commit `af77cd9` adds an explicit `assura init --agent <host> --activate`
route that composes the existing onboarding path. `--agent` and `--activate`
are paired requirements; plain `assura init` remains config-only, and the
composed route does not create `.git` or silently claim host runtime approval.

Focused proof is green: `cargo test --test cli_command_surface_tests init_ --
--nocapture` (5 passed, including atomic rejection of `auto` and `generic`),
`cargo test --test agent_integration_cli onboarding_ -- --nocapture` (2 passed),
and `cargo fmt --all -- --check` (exit 0). Independent review identified and
the follow-up correction at `544d6f5` resolved a high-severity partial-write
path: ambiguous targets are rejected before starter materialization. The full
`cargo xtask fast` result, scoped rereview and hosted checks are required before
a PR or integration decision. This is an implementation slice, not A07
acceptance; the full blinded screening, holdout allocation and final per-stack
threshold remain mandatory after the merged candidate is frozen.

The frozen candidate then passed `cargo xtask fast` at `1dd731e` (exit 0): all
workspace Rust test suites executed and passed, the source check reported
`success: true`, and the evidence/CI-scope policy checks passed. The source
check retained five pre-existing low-severity line-count advisories; no test
was skipped or treated as a pass. Clippy, docs, hosted checks and current-base
integration remain required at the PR boundary.

The first PR-boundary attempt at `1dd731e` correctly failed instead of masking
missing prerequisites: the required website gate reported `astro: command not
found` because the clean worktree had no installed website dependencies. After
the locked `pnpm --dir website install --frozen-lockfile` in this owned
worktree, clippy also exposed the new init signature's eight arguments. That
failure was resolved without relaxing lints by the documented options struct in
`7d7222b`. The resumed `cargo xtask pr` at `7d7222b` exited 0: all workspace
tests, source/evidence checks, target-state audit, clippy and website build
passed. These failures remain retained as process evidence; neither was
treated as a merge pass.

The subsequent evidence-only update at `7d39045` also passed
`cargo fmt --all -- --check`, `git diff --check`, `cargo xtask evidence`, and
`cargo xtask docs` (all exit 0, including the Astro build). The final hosted
checks must still evaluate the complete branch against current master.

## 2026-09-09 composed-init integration reconciliation

The reviewed composed-init slice was integrated through [PR #229](https://github.com/rothnic/assura/pull/229).
The reviewed head was `1989fffeeede82cb1609e269585971b118afbfd6`, based on
`4e188720c5b5a643f68c80e13e1f7c90a5eee656`; the squash merge is
`f1595fc670b7571781630b82cd091e0f0bcdf971`, verified as the current
`origin/master` after fetch. The merge preflight reported a clean merge state,
and the independent rereview of the final evidence-only delta returned no
findings.

The exact-head hosted proof completed successfully: Rustfmt, Clippy, Check,
MSRV, Ubuntu/macOS/Windows test suites, release bundle smoke, Windows
installer smoke, five installable-adoption jobs across four OS families
(Ubuntu x86_64, Alpine x86_64, macOS arm64, macOS x86_64, and Windows
x86_64), code coverage, Evidence Gates, Performance Report, and the scoped
documentation/security checks all passed. The Security Audit job was
classifier-skipped and is retained as skipped evidence rather than counted as
a test pass. The local `cargo xtask pr` and documentation/evidence gates were
already recorded above; no deployment, release, publication, or invitation
occurred.

This closes only the explicit composed-init implementation slice. A07 remains
`active`: no blind screening or holdout run has been counted yet. The next
owned action is to build and hash the exact merged candidate, run the authorized
full-contract screening without exposing private evaluator material, and record
the first redacted run result. The 30-run screening, 18-run untouched holdout,
follow-up feature checks, and final 10-per-stack >=9/10 acceptance gate remain
mandatory; a passing implementation slice cannot promote the card to done.

## 2026-09-09 candidate-binding protocol probe

A fresh source-only Rust probe was started with the fixed prompt
`Initialize this project with Assura` and the frozen merged candidate source
`f1595fc670b7571781630b82cd091e0f0bcdf971`. The evaluator was given the exact
`0.4.0` candidate binary with SHA-256
`210fe2c8deabd8387bc7ee5492626e9e21ea31be2b504e8a97320f7b2ba7cd7f`, and its
full contract result exited `1` (`acceptance_eligible: true`,
`acceptance_pass: false`). However, the initializer's tool shells resolved
`assura` through `/usr/local/bin/assura` rather than the frozen candidate: its
help output lacked the merged `init --agent` surface, and the event log records
the global path. The parent `PATH` prefix was lost by the login-shell tool
wrapper. The evaluator therefore measured a different binary's output; this
probe is invalid for candidate screening and receives no screening, holdout, or
final-batch credit. The observed red dimensions are retained as protocol
failure evidence, not product evidence.

The explicit-route control on a separate fresh fixture used the frozen binary
by absolute path and passed the full contract. It is a calibration control only,
not a blinded run and not an allocation result. It proves the composed route
and evaluator can pass when candidate identity is actually bound, but does not
repair the invalid blind probe.

The held action is therefore runner correctness, owned by this A07 acceptance
coordinator: make the candidate executable identity observable and invariant
inside every initializer shell, then run a fresh canary that records
`command -v assura`, `assura --version`, and the candidate SHA before allowing
any allocation. The smallest correction is to use the regular absolute
candidate executable (or a disposable shim that `exec`s one fixed absolute
target), record both resolved command and target paths, canonicalize and hash
the actual target, and assert version/hash in the captured event stream; do not
rely on inherited `PATH`, aliases, functions, or opaque symlink chains. Only a
candidate-bound canary may enter the 30-run screening. A07 remains active and
its acceptance thresholds are unchanged.

## 2026-09-09 candidate-bound canary

The runner correction was exercised in a new source-only Rust fixture using a
disposable login-shell startup shim. The initializer event stream recorded
`command -v assura` as the frozen absolute candidate path and `assura --version`
as `0.4.0`; an independent command in the same login-shell environment recorded
the expected candidate SHA-256
`210fe2c8deabd8387bc7ee5492626e9e21ea31be2b504e8a97320f7b2ba7cd7f` and the
merged `init --agent`/`--activate` surface. Candidate identity therefore
passed the canary precondition; the earlier `PATH` mismatch is corrected and
must not be repeated.

The candidate-bound initializer still selected plain `assura init .`. The full
private contract evaluator exited `1` with `verification_scope: full`,
`acceptance_eligible: true`, and `acceptance_pass: false`; the redacted result
reported `policy`, `preservation`, `native`, and `idempotence` pass, with
`structure`, `guidance`, and `hooks` fail. This is now valid product evidence,
but it is a canary rather than a screening allocation and receives no run
credit. A separate explicit-route control with the same candidate remains a
passing calibration control, so the failure is in fresh-agent discovery/default
selection, not candidate binding or evaluator calibration.

The correction returns to the A02 initialization-discovery owner because the
public plain-init handoff is owned by the init command surface. The smallest
reviewed product change is a high-salience, normative plain-init
handoff that tells an agent the config-only command is incomplete and directs
it to the explicit Codex onboarding route, while preserving the tested
plain-init boundary for non-agent users. Add a focused output regression and
rerun a fresh candidate-bound canary. Do not spend the 30-run screening,
holdouts, or final batch until that canary passes the full contract; any failed
canary returns to the owning behavior card with its evidence retained.

## Reconciliation checkpoint

Owner: this thread's A07 acceptance coordinator. The reconciliation worktree is
`/private/tmp/assura-a07-composed-init-reconciliation` on branch
`goal/a07-composed-init-reconciliation`, commit
`0ce5598d5678d1b84217293bcf0eb37ad5d2284a` before this probe record is
committed. The worktree was clean immediately before recording this probe; this
evidence change is the sole owned delta until it is reviewed and committed.
The topology report identified this worktree, the original composed-init
worktree, and the detached merged-candidate build as clean, while preserving the
unknown dirty root. Both `--report` and `--strict` exited 128 because the
pre-existing `/private/tmp/assura-github-master-eval.4GDgrR/repo` registration
points at a missing gitdir; no prune or removal was performed. The checkpoint is
therefore `candidate-bound-canary / discovery-correction-pending`, with the
next owner remaining this coordinator for a scoped rereview and the smallest
A02/A03 discovery correction, then a fresh canary before any A07 allocation.
The invalid probe, candidate-bound canary, and explicit control are preserved
separately; neither invalid evidence nor calibration evidence changes the A07
allocation or acceptance thresholds.

## 2026-09-09 merged-candidate runner-context check

The A02 correction is now current at `origin/master` `922d7f0`. A release
candidate built from that source was recorded as Assura `0.4.0` with its
immutable SHA-256 in the private run record. A new source-only initializer
attempt used the fixed public task, but the child explicitly selected an
ambient Assura installation and inspected evaluator-only context. Because the
initializer did not remain candidate-bound and context-isolated, this attempt
is invalid protocol evidence; it receives no screening, holdout or final-batch
credit even though later disposable checks invoked the intended candidate.

The durable runner contract now requires a fresh one-shot child, source-only
fixture, fixed public prompt, minimal login-shell-safe `PATH`, same-environment
`command -v assura`/version/hash proof, and separate evaluator/redacted output.
The next owner is the A07 acceptance coordinator: run that fresh canary against
the merged candidate, then begin screening only if its identity, context
separation and full product contract all pass. This does not alter A07's
30-run screening, untouched holdout, follow-up feature, or final per-stack
thresholds. It supersedes the older reconciliation checkpoint's A02/A03
discovery-correction wording; the acceptance coordinator now owns only this
fresh isolation canary and its evidence transition.

## 2026-09-09 candidate-bound canary passed on then-current master

The source was then refreshed to `origin/master`
`77b41fed7ee625333ea97ef2791da609f0ed5cc4`; that SHA is now historical after
PR #235. The live execution baseline is refreshed `origin/master`
`5329abd880fb26b0eeded5176e2d308b654d2cc9`. No candidate-bound canary has yet
been run against the live baseline, so this record cannot authorize screening
allocation until that identity canary is refreshed after private manifest
validation and isolated protocol review.
The clean detached checkout `/private/tmp/assura-a07-canary-current` built the
candidate with `cargo build --release` (exit `0`, `0.53s`) using Rust
`1.94.1 (e408947bf 2026-03-25)` and Cargo `1.94.1 (29ea6fb6a 2026-03-24)`.
The candidate executable is
`/private/tmp/assura-a07-canary-current/target/release/assura`, reports Assura
`0.4.0`, exposes the expected `init --agent`/`--activate` surface, and has
SHA-256
`fa48d6f7ec0eeaa544eab07f1776e17893de82e2f87d8486607342756dd9cd52`.

A fresh one-shot child received only the fixed public prompt
`Initialize this project with Assura` in the exact frozen Rust source fixture.
The fixture matched the private frozen contract without exposing fixture or
contract provenance in this repository.
The fixture lived beneath a dedicated parent containing no sibling worktrees,
contracts or prior-run artifacts. Its isolated login-shell environment resolved
`assura` to the disposable candidate path and reported `0.4.0`; the captured
initializer event stream recorded the same path and no evaluator, coordinator,
memory, or out-of-scope parent-path access. Private event and identity records
remain retained outside this repository.

After the child exited, the full private evaluator ran with the same absolute
candidate. Raw evaluator output and its redacted aggregate are retained in a
private evidence store outside this repository; public evidence records no
private path, raw-artifact hash, contract hash, fixture ID, or identity-record
hash. The evaluator exited `0` with `verification_scope=full`,
`acceptance_eligible=true`, `acceptance_pass=true`, zero critical failures, and
all seven dimensions passing: structure, hooks, native, policy, guidance,
preservation and idempotence. The evaluator recorded a negative policy probe
exit `1` matching `snake_case` and a native Cargo test exit `0`; idempotence
also passed. This is a valid candidate-bound **canary**, not a screening
allocation or final acceptance result.

The first fresh attempt in a shared temporary parent and the second attempt
with a non-frozen fixture remain preserved as invalid/no-credit protocol
evidence. The runner-isolation reference, executor prompt and continuation
prompt now require a sibling-free disposable parent to prevent recursive parent
discovery from leaking private artifacts. The next owned action is ordered:
create and validate the private two-condition manifest, obtain isolated
protocol-review `PASS`, refresh the candidate-bound canary against live
`5329abd`, and only then begin the authorized 30-run batch (3 stacks × 5
repetitions × 2 product-input conditions), one fresh sibling-free fixture and
child per run, before any holdout or final-batch claim.

## Historical current-master pointer reconciliation — 2026-09-10 (`8cabc536`; superseded)

A fresh read-only fetch after PR #237 identifies the live execution baseline as
`origin/master=8cabc53658530a239c00a0c55cbae9b050ad74ca`. The prior
`5329abd` checkpoint is historical and remains retained above for provenance;
the earlier `77b41fe` canary is also historical/no-credit. This process-only
reconciliation changes no evaluator input, scoring threshold, fixture, or
allocation.

The A07 card remains `active` with zero screening, holdout, and final-acceptance
credit. No candidate-bound canary has been run against `8cabc536`, and no
screening allocation is authorized here. The next action remains: create and
validate the private exactly-two-condition manifest and complete 30-cell
matrix, obtain an isolated protocol-review `PASS`, then run a fresh
candidate-bound canary against `8cabc536` before any screening cell. The private
manifest, mappings, fixtures, raw evaluator output, and identity records stay
outside this public evidence file.

## Historical private-manifest readiness audit — 2026-09-10 (superseded)

During continuation at `origin/master=e03278e25e1d81d6c5d84beb1bac992ea6059869`,
the A07 coordinator performed a read-only readiness audit of the private
evidence store. It found holdout-construction records and historical/no-credit
run records, but no discoverable screening artifact that proves the required
exactly-two product-input conditions, one-variable supplied-input evidence,
blinded mapping, complete 30-cell matrix, and isolated protocol-review `PASS`.
Historical filenames and explicit controls remain no-credit evidence and were
not used to infer condition names or allocation state. This audit did not
inspect or copy private values, fixture/contract identities, child transcripts,
or raw evaluator output into the repository.

This section supersedes the earlier `8cabc536` pointer snapshot above; that
SHA remains historical provenance only.

The held action remains with the A07 acceptance coordinator: create or locate
the versioned private manifest, validate it and the complete matrix in an
isolated protocol-review session, and record only a redacted `PASS` or concrete
findings. Until that observation exists, A07 remains `active` with zero
screening, holdout, and final-acceptance credit. A fresh candidate-bound canary
against the current master is the next observation after protocol `PASS`; no
historical canary or run is reused.

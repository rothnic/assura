# A07 — Blinded candidate evaluation evidence

State: active. This record is preparation only and is not a screening, holdout,
or acceptance result. Historical candidate and protocol records are preserved
in [a07-history-2026-09-11.md](../a07-history-2026-09-11.md).

## Current post-merge candidate-bound canary — 2026-09-12 UTC (`origin/master=36e5a840`, no-credit)

- Owner/phase: `/root` / post-merge A07 candidate and protocol preparation. PR
  #310 merged the reviewed process slice at
  `36e5a84024b1680b887bd789ed89ec8a0490f30f` with tree
  `292936709bd59a9f87531e7b0fb5d77826eb8751`; exact-head Documentation,
  CI Scope, Evidence Gates, Security Scope and GitGuardian plus applicable
  merge-SHA Documentation, Security Audit and Rust CI workflows passed. The
  ledger remains `items=32; ready_pending=0; unfinished=5; held=3` with A07
  active, W03 verified and R01/W02/F01 held.
- The 9c1 candidate and packet are candidate-base/no-credit after this source
  advance. Fresh owned checkout `/private/tmp/assura-train-postmerge-36e5`
  uses exact Rust/Cargo `1.94.1`, version `assura 0.4.0`, and binary/shim
  SHA-256 `7144ed4b77096daf6efe3419bf14a7df59bab5adc33e48444390a618344be87d`.
  Login identity passes; wrong-target and wrong-root controls reject.
- Fresh seeded, sibling-free source-only retries for conditions A (`none`) and
  B (`agent-project`) completed with initializer exit `0`; full post-exit
  evaluators pass all seven dimensions with zero critical failures, the
  expected negative policy rejection, native tests and preserved source hashes.
  Redacted summary: `screening/canary-2026-09-12-current-36e5.json`. The
  earlier empty-fixture setup failure remains retained as unfavorable evidence.
- The fresh private packet `/private/tmp/assura-a07-private-36e5` contains six
  unique holdouts, exactly two conditions and 30 reserved cells. Its adapted
  metadata validator is `valid=true` with zero errors; isolated protocol review
  is `PASS_NO_CREDIT`. The final reviewer artifact is timestamped
  `2026-09-12T03:26:10Z` and hashes to
  `a5e51d32930603645e582bb8ef00ea559568d96d59fca1d3c30a7764745fd4e8`.
  Screening, allocation, credit and acceptance remain false.
- No product, threshold, screening, allocation, release, deployment,
  publication, invitation or protection authority changed. The protocol
  rereview corrected the final construction digest binding after the status
  transition; a candidate-bound canary or protocol result is still no-credit,
  and any source advance requires another current candidate sequence before
  separately authorized screening.

## Historical candidate-bound canary — 2026-09-12 UTC (`origin/master=3e67d5f`, no-credit; superseded by `9c1b68a`)

- Owner/phase: `/root` / current-source A07 canary and packet validation. The
  reset fetched `origin/master=3e67d5fe123c6ebcf3ac05617966919151e44868`
  with tree `1b3b2c130a294b1020f0c63d84df120434bebf42`; the ledger is
  `items=32; ready_pending=0; unfinished=5; held=3` with A07 active, W03
  verified and R01/W02/F01 held.
- The clean candidate checkout `/private/tmp/assura-train-current-3e67` was
  built with Rust/Cargo `1.94.1`, version `assura 0.4.0`, and binary/shim
  SHA-256 `bbb244635770c44862db1becfab5ae46c107c904821baec303bcc52ae6bd05fa`.
  Login-shell identity passes; wrong-target and wrong-root controls reject.
  A first condition attempt is retained as unfavorable no-credit evidence
  because stale `ZDOTDIR` resolved an old shim; the runner was corrected before
  the accepted current-source attempts and was not blindly retried unchanged.
- Corrected fresh sibling-free source-only conditions A (`--content-template
  none`) and B (`agent-project`) each completed the one-shot child with exit
  `0`. Post-exit evaluation then passed all seven declared dimensions with
  zero critical failures, the expected negative policy probe and a collected
  native test. Public redacted summary:
  `screening/canary-2026-09-12-current-3e67.json`.
- The private packet `/private/tmp/assura-a07-private-3e67` is bound to this
  candidate and current public-contract digest `03e41cc2ae1242e8873b569f49484125521607956fff37ef3a7977a1d2088da3`.
  It contains six unique holdouts (two per stack), exactly two conditions and
  30 reserved cells. An adapted metadata-only validator reports `valid=true`
  with zero errors. Independent metadata-only protocol review is
  `PASS_NO_CREDIT`; the packet is protocol-ready but remains no-credit and no
  screening or acceptance authority exists.
- No product, threshold, screening, allocation, release, deployment,
  publication, invitation or protection authority changed. If a public
  process slice merges, this candidate becomes candidate-base/no-credit and
  must be rebuilt before any later screening decision.

## Historical post-merge reconciliation — PR #307 — 2026-09-11 UTC (`origin/master=eef7e1a`, no-credit; superseded by `3e67d5f`)

- Owner/phase: `/root` / current-source reconciliation. PR #307 merged
  reviewed head `9723a6730ac4cff2c8567035a8cd4f1fe21e25f4` from base `66e0b7e`
  as `eef7e1a7400c84ec35be33a58914ad07da7fa376`. Exact-head Documentation,
  CI Scope, Security Scope, Evidence Gates and GitGuardian passed; merge-SHA
  Documentation `34660366526`, Security Audit `34660366986` and Rust CI
  `34660366735` passed, including CI Scope `103461414969` and Evidence Gates
  `103461446382`. Scope-skipped product, Rust, performance and release rows
  remain non-proof.
- The ledger is still `items=32; ready_pending=0; unfinished=5; held=3` with
  A07 active, W03 verified and R01/W02/F01 held. The `66e0b7e` process
  checkpoint and all earlier candidate/canary/packet/protocol artifacts are
  historical candidate-base/no-credit. They cannot be reused for screening or
  acceptance; no product, threshold, allocation, release, deployment,
  publication or invitation authority changed.
- Current source tree is `1bfbcc27e5504ff23af489c1d076c4f6aba448a0`. Before any A07 continuation,
  refresh source/release/tag/PR/CI/topology and ledger, then build a fresh
  current-master candidate, rerun identity and no-credit canary gates, rebind
  all six holdouts and exactly two conditions, and obtain isolated protocol
  `PASS`. This record remains preparation-only.

## Historical candidate-bound preparation — PR #305 — 2026-09-11 UTC (`origin/master=dc031527`, no-credit; superseded by 66e0b7e)

- Owner/phase: `/root` / fresh A07 candidate, canary and packet rebind. PR
  #305 merged reviewed head `5363b4a5d2e39beed6087bea6a5f6fa2d2aaf17c` from
  base `284e781` as `dc031527afd400be52dfd8fe9cfdabc7a6caa685`. Its applicable
  exact-head and merge-SHA Documentation, Rust CI and Security checks passed;
  scope-skipped product/performance/release rows remain non-proof.
- The clean candidate checkout is
  `/private/tmp/assura-a07-current-reconcile-dc031`, built with Rust/Cargo
  `1.94.1`. Source tree `b68b66cc47f7e4081afd2dc21c1387de34b30c32`, binary and
  login-shell shim `3cdaf976d7da61043a5867cb17ffd789b92b484e26f1e7d3a7989bd25ce816cf`;
  identity, wrong-target and wrong-root controls pass/reject as intended.
- Two fresh sibling-free source-only children and their post-exit full
  evaluators pass both conditions with zero critical failures and all seven
  dimensions passing. Private path scans found no evaluator/contract/run,
  foreign fixture or repository-checkout exposure. Generic ambient skill
  metadata and the generated-hook verifier discrepancy remain retained
  limitations; this evidence is no-credit preparation only.
- The private packet is
  `/private/tmp/assura-a07-private-dc031`. Its current rebind has six unique
  holdouts (two per stack), exactly two conditions, 30 reserved cells,
  current receipt/evaluator aliases and explicit construction/second-readonly
  references. The metadata-only validator reports `valid=true`, zero errors;
  screening, allocation, credit and product acceptance remain false.
- Independent metadata-only protocol review returned `PASS_NO_CREDIT` after
  correcting the stale current contract digest and construction hash; the
  private artifact records the findings, corrections and scoped rereview. The
  candidate canary and packet do not complete A07 or authorize screening,
  release, deployment, publication, invitation or participant allocation.

## Historical post-merge reconciliation — PR #304 — 2026-09-11 UTC (`origin/master=284e781`, candidate-base; superseded by dc031527)

- Owner/phase: `/root` / post-merge reconciliation. PR #304 merged reviewed
  head `17fa9197ed79083be4b8ba0714851aeaae9bdbf3` from base `d228472` as
  `284e78156370d49d8315f04391fcc74eaba3acb2`. Independent exact-diff review
  returned `PASS`; exact-head Documentation, CI Scope, Security Scope,
  Evidence Gates and GitGuardian passed. Merge-SHA Documentation
  `34651068660`, Rust CI `34651068675` (CI Scope `103433100783`, Evidence
  Gates `103433136825`) and Security Audit `34651068699` completed
  successfully for applicable scope. Product, performance, release and other
  scope-skipped rows remain non-proof.
- The revision-pinned ledger at this source is
  `items=32; ready_pending=0; unfinished=5; held=3`: A07 active, W03
  verified, and R01/W02/F01 held. The d228472 candidate, its two-condition
  no-credit canaries/evaluators, byte-identical freeze and ambient-context
  limitation are now candidate-base historical preparation after this source
  advance; no holdout/manifest rebind or protocol credit carried forward.
  The initial root-level freeze placement failure remains retained as
  unfavorable evidence.
- The owned `docs/a07-canary-d228` branch/worktree was clean, merged and
  removed. Root unknown dirt, foreign dirty work, stale/prunable registrations
  and unfavorable R01 evidence remain outside ownership. No product,
  screening, allocation, release, deployment, publication or invitation state
  changed.
- Next owner/action: refresh source, release/tag, PR/CI, topology and the
  revision-pinned ledger again before choosing a route. If A07 preparation is
  separately authorized, build a fresh exact-toolchain candidate from
  `284e781`, rerun the sibling-free no-credit canary, rebind the six holdouts
  and exactly-two-condition manifest, and obtain isolated protocol `PASS`; if
  not, continue an independently authorized held recovery slice. Never reuse
  d228472 evidence for credit, and never treat an empty pending queue as a
  stop condition.

## Historical candidate-bound canary — 2026-09-11 UTC (`origin/master=d228472`, superseded by `284e781`)

- Owner/phase: `/root` / A07 candidate-bound canary preparation. The current
  source is `origin/master=d2284724192dbca848bbd135afacc33d2533e06f` with tree
  `dc30855addffae4e3b19c0589bfcbd08aa4ebb45`; the revision-pinned ledger is
  unchanged at `items=32; ready_pending=0; unfinished=5; held=3` (A07 active,
  W03 verified, R01/W02/F01 held). Refresh before packet work or authority.
- In clean owned checkout `/private/tmp/assura-a07-current-d228`,
  `cargo +1.94.1 build --release` exited `0` in approximately 368 seconds.
  Candidate version is `assura 0.4.0`; binary SHA-256 is
  `0ec57fa90bcb53bee9bed7b042e89ab546f850a85251e3b2202aba02afa8aa1c` and the
  regular login-shell shim SHA-256 is
  `825af24be5cf438a795d9d0305d0667f1e041494b23e7c47d9a80e58cf3375e6`.
  Login-shell identity, wrong-target and wrong-root controls pass. Public and
  private freeze copies are byte-identical. A first root-level freeze
  placement failed the structure gate and was corrected under ignored
  `target/a07-evidence/`; that setup result remains unfavorable no-credit
  evidence.
- Two fresh sibling-free source-only fixtures completed the fixed public
  `Initialize this project with Assura` child run. Condition A's child exit is
  `0` with a 128,549-byte event stream; condition B's child exit is `0` with a
  74,325-byte stream. Scans found zero references to the private evaluator
  contract/output, foreign fixture, repository checkout or sibling condition.
  Generic ambient user-level skill metadata was visible in both streams despite
  `--ignore-user-config`; this is an explicit isolation limitation, not a pass
  upgrade. Initializer events and evaluator receipts remain separate.
- The full evaluator ran only after each child exited, using the same absolute
  candidate and the declared seven dimensions (`structure`, `policy`,
  `guidance`, `hooks`, `native`, `preservation`, `idempotence`). Both evaluator
  exits are `0`; both are `verification_scope=full`,
  `acceptance_eligible=true`, `acceptance_pass=true`, all seven dimensions
  `pass`, and `critical_failure_count=0`. The positive policy probe exits `0`,
  the trusted negative naming probe exits `1` with the expected rule, and the
  native Cargo suite collects and passes a test. These are bounded no-credit
  canaries only.
- No holdout or manifest packet has been rebound to `d228472`; protocol review,
  screening, allocation and credit remain false. Next owner/action is to rebind
  all six immutable holdouts and the exactly-two-condition manifest to this
  candidate, run the isolated protocol reviewer, and record its concrete
  disposition before any separately authorized screening. Do not treat the
  canary/evaluator pass, ambient-context limitation or a process merge as
  product acceptance.

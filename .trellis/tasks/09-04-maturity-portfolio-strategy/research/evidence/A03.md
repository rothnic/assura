# A03 — Agent-led specialization evidence

## Completed

- Branch: `goal/a03-agent-specialization`
- Worktree: `/Users/nroth/.codex/worktrees/assura-a03-agent-specialization`
- Base: `origin/master` at `c60f0c5be8fd4136aff94afe427bb1b6e53c4819`
- Candidate head: `02cf94af02fc6fa2977d9b53a62a5b28bb32c334`
- Implementation PR: #177, merged as `2e882aec8451a920855a93a75bc8f9f801522220`.
- Evaluator-contract PR: #178, merged as
  `00d09e67496ea7b408ad782f584ad6127a2bce27`.

## Iteration log

1. Replaced the generic agent-next questionnaire with the packet's ordered
   evidence-first specialization procedure; the generated questions file is
   now an exceptions-only record rather than routine discovery prompts.
2. Added deterministic `profile-selection.json` materialization for detected
   Rust, Node, Python, and local-policy sources, including source hashing,
   decision evidence, conflicts, and config verification.
3. Split specialization evidence from the onboarding command and added a
   truthful `configured_unverified` state for recognized stacks. A profile
   record is not treated as verified; proving a negative policy case remains
   required.
4. Added deterministic record validation, Cargo/Bun/pytest profile fixtures,
   local-policy precedence evidence, and a persisted conflict state. The
   conflict record identifies the recommended-rule source while retaining the
   project-owned value.
5. A rerun test found that Assura's generated `docs/` directory could change
   an ambiguous repository into a `docs-heavy` inferred type. Detection is now
   deliberately limited to explicit Rust, Node, and Python manifests; an
   ambiguous repository remains reversible, creates no package manifest, and
   produces a byte-identical profile record on rerun.

## Focused proof so far

- `cargo test --test agent_onboarding_specialization --test project_intelligence_onboarding`
  — exit 0; 12 tests passed, 0 failed, 0 ignored.
- `assura check --format agent .` — exit 0; 0 structural violations.

## Repository-tier proof

- `cargo test --workspace --quiet` — exit 0. All observed test groups passed;
  two pre-existing explicitly ignored tests remained ignored and are not counted
  as passing tests.
- `cargo clippy --workspace --all-targets -- -D warnings` — exit 0.
- `cargo xtask evidence` — exit 0.
- `cargo xtask docs` — initially exit 1 only because the generated website
  onboarding demo was stale and website dependencies were absent. After
  `cargo xtask website-demo-data` and locked `pnpm --dir website install
  --frozen-lockfile`, the exact docs gate exited 0 and built 48 pages.
- `assura check --format agent .` and `git diff --check` — exit 0 after the
  generated demo update.

## A01 partial-evaluator proof

After #178 added fixture-owned guidance assertions, a disposable Rust fixture
was onboarded and evaluated with the requested
`structure,policy,guidance,preservation,idempotence` dimensions.

- Cwd: `/Users/nroth/.codex/worktrees/assura-a01-guidance-evaluator-contract`
- Candidate binary: `/Users/nroth/.codex/worktrees/assura-a03-agent-specialization/target/debug/assura`
- Candidate binary SHA-256: `2538b9a2ee122870f38bb2151f8219362996fe418ae31e912dadd97b4c749db6`
- Disposable fixture: `/private/tmp/assura-a03-proof.R1u6qG/project`
- Command: `python3 scripts/evaluate-agent-init.py --project <fixture> --contract <fixture-contract> --assura-bin <candidate-binary> --output <result> --dimensions structure,policy,guidance,preservation,idempotence`
- Exit: `1`, expected because `verification_scope: partial` is not
  acceptance-eligible.
- Result: all five requested dimensions passed; `critical_failures` was empty.
- Negative control: `src/BadName.rs` exited `1` and named the expected
  `snake_case` rule.
- Guidance control: `.assura/onboarding/agent-next.md` existed and matched the
  evidence-first ordered-procedure assertion.
- Limitation: this is A03 scope proof only; A04/A05 own hooks/native gates and
  A07 owns full acceptance.

The evaluator implementation received independent review before #178. Its
hosted retry passed all required Rust, docs, platform, installer, coverage, and
performance gates. The first performance run failed the existing R03
many-configured-scopes comparison (`19.247 ms` versus `17.821 ms`); the fresh
retry passed without changing the benchmark or gate.

## Independent review and repair

Independent review of `origin/master...d3d15d8` found four valid issues:
multiple manifests were silently prioritized, the serialized onboarding state
changed under the v1 schema, conflict objects were not structurally validated,
and the unavailable A01 guidance dimension prevents A03 packet acceptance.

The repaired candidate now detects multiple Cargo/package/pyproject manifests
as `ambiguous`, records each manifest as a structured conflict, and emits
`conflict_requires_user` without overwriting policy. It validates `kind`,
`source`, and `detail` for every conflict. The JSON report schema is explicitly
`assura.agent-onboarding.v2`; tests, the published onboarding guide, and the
generated website demo are updated to describe the expanded specialization
states. The A01 guidance limitation remains an open, non-passing acceptance
boundary and is not hidden by the schema update.

Re-review found one stale later guide section that still described generic
questions and `answers.yml`. It is replaced by the evidence-first procedure;
the packet table now describes `questions.md` as unresolved exceptions. A
fixture test asserts the public v2 schema and absence of the retired procedure.
The final re-review found one further stale specialization-flow sentence; it is
now evidence-first and the fixture test rejects that exact retired phrase.

## Target-state verifier repair

Post-merge target-state verification found that the public guide had moved to
the accepted A03 evidence-first procedure, but `cargo xtask target-state` still
required three retired questionnaire markers. The exact baseline at
`6d613f809feabd65b520c57bfd7aaf1b521a107e`, with the controller-owned branch
binding applied, failed only for `## Agent-Next Questions`, `Ask remaining
specialization questions`, and `.assura/onboarding/questions.md` (session
`26624`, exit 1).

Implementation `932306ee7d503f5b957f2d9495b957b8de5041ed` extracts the guide
contract into the checker-owned `check_agent_onboarding_guide` helper. It keeps
all existing navigation, entry-point, marker, and unsupported-command checks;
requires the current `Agent-Next Procedure`, unresolved-exceptions action, and
`agent-next.md` affected path; and explicitly rejects the three retired
procedure markers. It does not reject contextual, exceptions-only references to
`questions.md` and does not change the guide.

- Focused RED: `cargo test -p xtask agent_onboarding_guide -- --nocapture`,
  session `83550`, exit 101, all three tests failed against the stale checker.
- Focused GREEN: the same command, session `69103`, exit 0, three passed.
- Focused post-commit proof: the same command at `932306e`, session `76083`,
  exit 0, three passed.
- `cargo xtask target-state`: session `71953`, exit 0.
- `cargo xtask fast`: session `86991`, exit 0.
- Exact-implementation `cargo xtask pr`: session `39008`, exit 0. It included
  the complete fast and target-state gates, all-target/all-feature Clippy,
  current website demo data, and a 48-page website build.
- Website dependencies were installed with `pnpm --dir website install
  --frozen-lockfile` (session `31834`, exit 0). The docs helper's required local
  `target/debug/assura-full` path used a temporary worker-owned symlink to the
  authorized shared target cache; the symlink was removed after the gate.
- Independent review of `6d613f8..932306e` reported no findings and marked both
  specification and quality review PASS.

Negative controls independently remove each current marker and add each retired
marker, producing one focused finding per mutation. This remains an exact
public-document marker contract rather than semantic interpretation of prose.

## Next-ready cards

- A04 — actual hook lifecycle and preservation proof.
- A05 — quality-plan/native-gate proof.

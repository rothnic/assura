# R03 performance repair evidence

State: blocked on a separately justified measurement-method decision; candidate rejected and source-only revert complete.

## 2026-09-07 terminal diagnostic: controls invalid, stop preserved

Root run25650 exited1 with `stop-block-1-overhead-failure`, not acceptance.
Exactly48 Assura and48 LS-Lint launches were retained, eight per treatment,
all in block1. No invocation failed or timed out; block2 did not start.
Independent read-only audit ac2749 exited0 and recomputed ordering, counts,
medians, binary identities and before/after fixture manifests.

| Side and contrast | Signed delta ms | Absolute percent | Control |
| --- | ---: | ---: | --- |
| Baseline original to disabled | -0.107611 | 1.846495 | fails absolute bound |
| Baseline disabled to enabled | -1.159652 | 20.272782 | fails both bounds |
| Candidate original to disabled | -0.469804 | 9.775565 | fails both bounds |
| Candidate disabled to enabled | +0.064160 | 1.479672 | passes both bounds |

The controls require absolute perturbation below both2% and0.1ms.
Negative deltas do not earn overhead credit. The observer/reference method
did not demonstrate comparability: neither causal source attribution nor
confirmed runner noise follows. The candidate remains rejected and reverted.
No second block, replacement measurement, profile or source optimization is
authorized by this result. Next requires a separately justified, independently
reviewed method decision that establishes unchanged-binary comparability under
the existing bounds. This is a card stop, not whole-goal completion or a
performance-contract amendment; independent W02 work continues.

Frozen runner SHA-256:
`880550659bc873277c06be6c92298d5d89e2bdb6ffa4e0123505f4b99fef31a2`.
Raw samples:
`8623a1f93dd0f7a8fd31d173efccee669d6c35beba98d6c17169e7d7ad8ae351`.
Summary:
`4295e0280b13b1cf5bdfdb4716c989051f19b9d86502ae7a07bb7c4bae8f4657`.
Run log:
`4b812515da08aa1fe0b646a959febf569aa0f5d49dbc6766b23fef936fbedb54`.

Remote root:
`/home/ubuntu/data/projects/assura-r03-public-path-diagnostic-20260907.UKGVrz`
on `vps-9cb01956`. Local preserved audit is under the R03 worktree runtime
`r03-scope-rules-attribution/public-path-diagnostic-20260907.UKGVrz/`.
Copy7426d9 exited0 (3066files/87MiB); checksum dry-run ec8d9d exited0 with
zero artifact/tool/fixture differences. Audit verified all1502 files in each
fixture, eight pinned launcher/companion binaries and successful finalization
on the actual early-stop path. Primary failure is retained without a secondary
identity error. All original rejected-candidate evidence below is historical.

## 2026-09-07 full-run rejection and exact source rollback

The first full before/after run55014 terminated1. Both complete392-row reports
passed schema checks:390 successful16-sample rows plus exactly two documented
diagnostic skips, not passing proof. Independent review recomputed all eight
accepted headline medians from raw samples and confirmed the failed decision.

| Accepted fixture | Baseline ms | Candidate ms | Delta |
| --- | ---: | ---: | ---: |
| ignored_generated_heavy_repo | 1.826567 | 1.573554 | -13.852% |
| many_configured_scopes_regression | 27.715999 | 20.5776125 | -25.755% |
| monorepo_packages | 1.889378 | 1.414225 | -25.149% |
| monorepo_policy | 2.961400 | 2.690889 | -9.135% |
| multipart_extension_regression | 4.529985 | 5.2035125 | +14.868% |
| rule_heavy_repo | 2.361023 | 2.247759 | -4.797% |
| simple_library | 1.741842 | 1.342206 | -22.943% |
| web_app | 1.312819 | 1.146390 | -12.677% |

Target gain7.1383865ms passed both target requirements. Multipart slowdown
0.6735275ms exceeded both preset spillover limits. Candidate absolute
no-slower gate exited0; baseline absolute gate exited1 (diagnostic only),
and before/after comparison exited1. No reversed full run, retry, native or
warm acceptance followed. The candidate is rejected, not rescued by its target
gain. This proves an observed failed prescribed comparison, not a stable,
causally attributed source regression or confirmed runner noise.

Measured baseline was3d9a255; candidate full archive d2a9ca7 retained behavior
source5eb0e339. Original build68398 terminated0 with explicit selected
Rust/Cargo1.98.1 commands; its build output remains the original tool transcript,
not a separate build-log artifact. Actual report fields identify runtime
Rust1.95.0-nightly from the default probe environment. These facts are distinct;
no claim that the report field says1.98.1 or complete CI-environment parity is
made. Generated full-report config bytes were ephemeral and not retained;
semantic/source identities match, but byte identity is not established.

Raw baseline report SHA-256:
`d71a655af1894e34bc9af39a4d29bb3ba123a52ce863f9286fb7296938ba0085`.
Candidate report:
`29656ba6d718cad982e809db57d38d4024fae2e536fd913121b65b81f84a24f2`.
Source manifests707400d4/1df9d5cb, histories, full log, complete executables
and all raw samples are preserved in52MiB/60 local files under runtime
`r03-scope-rules-attribution/acceptance-run1/`, plus original VPS root
`/home/ubuntu/data/projects/assura-r03-scope-acceptance-20260907.clDMdm`.
The runtime report records complete hashes; download2e8d16 verified36
manifest-listed artifacts/binaries. Preflight306aec exited127 before any
measurement because a zsh path variable shadowed PATH; corrected f78eca
passed. Neither is mislabeled a benchmark retry or product failure.

Before measurements, independent runtime-verifier review found duplicate,
missing/cohort-relabeled, invalid-sample and skipped-row false-pass paths.
Actual behavior RED112a94 reproduced the equal-relabeled acceptance defect;
nine corrected controls passed locally/remotely and root e2b4fa. Final
independent re-review passed exact shell0626f667/evaluator3f5aa3dd/tests
e9d79b7f. Threshold AND/OR rules were unchanged. These synthetic controls
are verifier proof, not measured product speedups.

Source-only rollback69dfc4a210fa7ed25838f32f072f135adeb18d7b removed exactly
the eight candidate product changes through a normal pre-commit-checked commit.
Base and rollback src tree IDs both equal943e9584e25419aa0e2f024af6b453ee74f3c27d;
the whole net branch diff contains only four owned metadata files. No source,
history, binary or failed artifact was silently lost. Structure7030ed passed
1777files/393dirs/0violations; evidencec9a90f passed using a separately hashed
existing xtask with an identical source tree. Workflow/diff checks passed.
No full Cargo tier was needed for this unpublished metadata-only net change.

## Next bounded attribution, not acceptance

Same-RUN1 review found the multipart public static CLI slowed while separate
in-process walk and later same-static-binary strategy measurements improved.
Those contexts cannot be subtracted to explain the public excess. The fixture
has one inherited rule and1500 files, not800 scopes; existing init timing does
not identify an owning added cost. Rejection stands.

Parent authorized one diagnostic described in runtime
`public-path-diagnostic-plan.md`: identical tiny temporary exports of existing
walk timing on the actual public path, a byte-identical shared multipart
fixture, explicit toolchain/binary/source identities, original/disabled/enabled
overhead controls, two mirrored blocks and at most96 measured Assura launches.
Temporary probe/script review precedes measurements. No allocator interception,
bad-callchain inference, new product optimization, acceptance retry or
fixture-specific opt-out. Inconclusive, overhead-invalid or unlocalized results
stop the diagnostic and require a specific next decision. Local product source
remains master-equivalent. R01 is separately blocked under its packet rule.

## 2026-09-07 bounded complete-descendant candidate

The one authorized candidate is committed as
`5eb0e3391c0ff0ce2a7fdb95d31a178b5c97beea` (normal commit35987 exit0;
pre-commit Assura passed). All eight independent-review source hashes matched
after commit; the four parent-owned metadata files remained unstaged.
It reuses exact
fast rules only when stripping direct-content policy is provably a no-op;
every populated direct-policy field, including Some(empty), keeps distinct
descendants. Composition materializes distinct results and compiled artifacts
retain the existing wire contract. Temporary diagnostic instrumentation is
absent. Cohesive helper/test modules satisfy existing structure limits without
configuration relaxation.

Actual allocation-contract RED chunk49f3b0 exited101 with reused2145 and
distinct2145; GREEN35834 exited0. Earlier compile error14355 and zero-test
84275 are not RED evidence. Final focused50301 passed3 reuse and14 plan tests;
broader78090 passed501 library plus15 watch-state, compiled artifact1,
plain/compiled equivalence1, and23 check CLI tests; all-target/all-feature
Clippy passed. Two incorrectly filtered zero-test commands are not counted.
Structure1d8f02: exit0, success true, no violations. Independent eight-source
review found no correctness or quality findings; metadata was excluded.

Two predeclared balanced16-pair exact-public `assura check --quiet` comparisons
used the same fixture and YAML bytes (SHA-256
`8375c53fcdf6731fa20a5675503079c754ef62faf3deeafb74be00d607b6324c`).
Session94222 exited0 with all32 commands successful: baseline34.006160ms,
candidate32.199554ms (-5.31%). Reversed-start session12679 exited0 with all32
successful:34.571053ms versus30.290471ms (-12.38%). Both had11/16 faster
candidate pairs. Host load remained low but unrelated services existed; no
exclusive-host claim is made. Candidate static binary prefix4273f61e was
built from the reviewed working source; these are exploratory source-hash
results, not final committed-SHA acceptance. Build28893 compiled successfully
but its wrapper exited127 when a bare version command required the absent
full companion; actual check preflights passed. This is not represented as a
successful complete release-bundle build.

Canonical loader selection is inferred from the exact Config serialization,
absence of top-level rules, source branch ordering and successful public
checks; it was not directly instrumented at runtime. Independently generated
YAML can vary in HashMap ordering, so byte identity is asserted only for the
fixed-fixture paired commands above.

Next: build exact comparable complete release bundles from committed5eb0e339,
then bounded full392-row before/after reports and cold/native/warm gates.
Retention still requires target public assura-cli gain >=5% AND >=0.75ms,
exact quiet gain >=3%, no accepted fixture regression >5% OR0.25ms, and two
comparable agreeing runs. Narrow gains do not authorize retention or merge.

## 2026-09-07 exact/descendant plan attribution

Current worktree: `/Users/nroth/.codex/worktrees/assura-r03-scope-rules-attribution`,
branch `goal/r03-scope-rules-attribution`, created clean from
`3d9a255f832733082c864edf703fc98c854e7f6e`. The historical unavailable-runner
state below is superseded: SSH alias `vps` reaches `vps-9cb01956`. The owned
probe directory is
`/home/ubuntu/data/projects/assura-r03-scope-attribution-20260907.zkuMM3`.
Use Rust1.98.1 and the exact static-primary release protocol; no unrelated
service was stopped and no exclusive-host claim is made.

PR185 run34088013090 failed the unchanged cold gate at19.364834ms Assura
versus17.6041085ms LS-Lint,392rows and16balanced iterations. Artifact SHA-256:
`48d32d6f361ad78b20a6afcebb7be3a8e9dda34e0a9b53de75305a862cb1d00b`.
Its commit efa1db33 is the verified GitHub synthetic merge of3d9a255 and1852b61,
not a stale source claim. The next corrected-test head3ec25fd produced
run34090768850:8/8accepted cold comparisons, many-scopes18.6063435ms versus
20.1631345ms, still392rows/16iterations. Artifact SHA-256:
`4f479d723bc44c73866a6511d58c814c34785f08bad490d0cde9b1e2d2232ac8`.
Its synthetic merge4404a52 has verified parents3d9a255 and3ec25fd. The later
pass does not erase the failure or establish a confirmed-noise amendment.

The exact generated fixture algorithm and LS-Lint converter produce800 reset
children plus one inherited root. Sixteen successful counting runs observed
reset800/inherit1/composition0,800 no-op file strips and one self-directory
strip. All801 Arc observations immediately before make_mut had strong count3.
Descendant strip/rebuild performs9,603 allocations/1,337,156bytes; exact
construction performs9,606/1,337,516. These are alloc/dealloc counters, with
reallocations separately qualified. Whole-checker destruction frees23,225
allocations/5,236,727bytes, not all attributable to descendant plans.

Independent probe review required a genuinely allocator/counter-free timing
variant, not merely disabling the diagnostic environment variable. Sixteen
timing-only invocations succeeded. Interim medians: fast-plan3.270ms, exact
construction0.978ms, descendant strip0.398ms, descendant matcher rebuild0.559ms,
plan-body residual1.277ms, entire checker destruction0.930ms. Separate medians
are not algebraically additive proof of public-command savings. Exact includes
effective-bundle construction; plan-body residual is not total public overhead.
Full raw logs/patches are retained under
`.trellis/.runtime/r03-scope-rules-attribution/`.

Decision: reject the standalone no-op make_mut guard as too small under the
existing decision matrix as a prioritization decision. Its observed instrumented
strip-bucket median is0.398ms, not a proven upper bound on public savings;
teardown and nonlinear/cache effects are unquantified. No implementation or
acceptance claim is authorized for that alone. Reusing the
entire identical descendant plan is a distinct, measured-cost proposal, still
pending a trustworthy uninstrumented baseline and public before/after proof.
A copied-target stale fingerprint returned the diagnostic binary during the
first baseline attempt; that observation is invalid baseline evidence. A fresh
explicit target then rebuilt the uninstrumented static primary in session60106,
exit0, SHA-256
`ac0f9c5899ab42e3788157cbeef15eaed12176f5194cbe6044046e97f9d1cc70`.
The final split timing binary was
`6912391b3dafa288e16d9dabfd86d19556886b7a17da87b7264a82a581f6c36a`.
Balanced16-pair overhead control cf62d7 completed32 successful commands:
uninstrumented32.635ms versus timing35.524ms. Large host variance prevents
crediting a production speedup from these diagnostic timing buckets.

Parent authorized one bounded test-first candidate for entire identical
descendant-plan reuse, not the rejected clone-only guard. It must retain
distinct descendants for every direct-content policy, preserve composition
and compiled serialization, and pass plain/fast/compiled policy oracles.
Retention still requires actual public5%/0.75ms improvement, exact quiet3%,
all accepted-row spillover limits and unchanged cold/native/warm gates on
comparable same-host runs. Implementation now exists as described above;
acceptance remains pending.

No threshold, accepted row, plain-checker oracle, release surface or production
policy changed. Temporary source instrumentation is diagnostic only and must
not enter the retained candidate; the diagnostic worker restored production
source before the new tests began. R01/A04 remain separate active repairs.

## Historical blocked baseline

Source SHA: `dcb1fb57ba100f77a7cb7e48c4f14507d3106231` (`origin/master` fetched
2026-09-05).

Worktree and command cwd:
`/Users/nroth/.codex/worktrees/assura-r03-many-scope` on
`goal/r03-many-scope`; source was clean before and after evidence capture.

Problem reproduced: GitHub Actions Rust CI run
[34005367783](https://github.com/rothnic/assura/actions/runs/34005367783),
Performance Report job `101411447744`, generated its 16-iteration artifact
successfully then failed the unchanged gate: `many_configured_scopes_regression`
was `assura-cli 19.582 ms > ls-lint-cli 18.445 ms` (exit 1). The retained
artifact is `target/performance/r03-hosted/ls-lint-comparison.json`.

Local release comparison (partial protocol, not an exact CI bundle): built with
`cargo build --locked --release --bin assura --no-default-features --features json-output,yaml-config`,
`cargo build --locked --release --bin assura-full`, and
`cargo build --locked --release -p assura-check-cli` (all exit 0; both
launchers report `assura 0.4.0`). The local primary launcher omitted CI's
Linux-only static command, `cargo rustc --release --bin assura
--no-default-features --features json-output,yaml-config -- -C
target-feature=+crt-static -C link-arg=-lgcc_eh`; it cannot establish
GitHub-runner parity. Then
`target/release/assura performance-report --output target/performance/r03-baseline-lslint.json --iterations 16`
exited 0 and retained 392 rows. Its balanced alternating samples did **not**
reproduce the failure: Assura median/p95 `44.747/49.171 ms`, LS-Lint
`46.813/51.665 ms`; `cargo xtask performance-no-slower
target/performance/r03-baseline-lslint.json` exited 0. The local phase medians
were config discovery `0.049 ms`, config load `8.781 ms`, checker init
`3.102 ms`, configured structure `0.022 ms`, walk/validate `8.884 ms`, and
report sort `0.000048 ms`. The hosted artifact's corresponding medians are
`0.016`, `3.779`, `1.831`, `0.013`, `5.671`, and `0.000030 ms`.

Change: none. The plain checker remains the oracle. No fast-plan/cache change
was made because the local paired release evidence and hosted failure disagree,
so neither per-scope matching nor config normalization is sufficiently
attributed for a correctness-risking optimization.

Negative control: no benchmark row, tolerance, policy, or CI condition was
relaxed. `ssh vps-dev 'hostname; uptime; pgrep -af "cargo|rustc|assura"'`
could not start because `vps-dev` was not resolvable from this host, so no
comparable isolated Linux rerun was available.

Review: pending independent review after a comparable-runner reproduction and
an attributed patch.

Remaining: obtain an approved, quiet Linux runner (GitHub-equivalent or
restored `vps-dev`), build the complete CI bundle including the static primary
launcher command above, and rerun the same 16-iteration release command before
selecting a branch. Preserve normalized plain/fast/compiled diagnostics for
overlapping scopes, inherited rules, exclusions, missing directories, and
invalid names before any implementation.

Integration: evidence-only PR #147 is rebased on current master at
`36367dec867830a43c86c91202399b4e82a351af`; no performance change has been
implemented. R03 remains blocked until the named Linux attribution run exists.

Next ready ID: R03 after a comparable runner is available; R02's independent
artifact behavior is already in the current source ancestry.

## 2026-09-06 diagnostic update

No source candidate was retained. A local alternating release experiment made
exact configured-scope `HashSet` membership look favorable, but the stronger
same-fixture VPS public-command record at
`docs/goals/assura-performance-polish-program.md:384` rejects it: `assura
check --quiet` regressed `23.741` to `23.964` ms (+0.9%) with mixed accepted-row
spillover. The experiment was fully reverted. Its local reports are diagnostic
only, not Linux-static parity; no benchmark row, threshold, or evaluator changed.
`ssh vps` now reaches `vps-9cb01956`; a future attempt needs a distinct
phase-attributed hypothesis and the exact release helper/protocol.

## 2026-09-06 current-master attribution check

Current source was refreshed to `25167c84af4c352ef5564bcf6ff17c36a9847766`
in `/Users/nroth/.codex/worktrees/assura-r03-attribution` on
`goal/r03-attribution`. The unchanged hosted gate failed again on Q06 PR #166
head `1e185d2a7821d787698dd36d2a7295e70c530c31`: Rust CI run
[34023443201](https://github.com/rothnic/assura/actions/runs/34023443201),
Performance Report job `101460146427`, reported
`many_configured_scopes_regression: assura-cli 19.492 ms > ls-lint-cli 19.115
ms` and exited 1. All other required hosted checks on that head succeeded.
This repeat retains the evaluator unchanged and does not attribute the loss to
Q06's release-readiness extraction.

The approved `vps` alias is reachable and has the shared canonical fixture,
but its read-only preflight found unrelated active `cargo-watch` and
`cargo run --release --bin open_agent` processes. Its load average was
`0.03, 0.27, 0.25`, but the protocol requires paired cold runs without
concurrent workloads, so no comparison was started. R03 remains blocked until
an approved quiet Linux runner is available for the complete static-primary
release build, 16-iteration report, unchanged no-slower gate, and a distinct
phase-attributed hypothesis. No source, benchmark row, tolerance, CI condition,
or acceptance criterion changed.

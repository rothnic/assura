# R03 performance repair evidence

## 2026-09-08 R03 resolution: reviewed candidate merged after current-hosted gate

Reviewed PR #197 merged as `369507cd8577e2385abdefb84edc2340c0416bfd` after
the reviewed source head `469a874774dd77a69424222d52c116098205e8e9` was based
on then-current `origin/master` `8233f08f63a3306805e9d4c3ef19fa432ac7f0d6`.
The PR's complete hosted suite finished successfully, including the unchanged
`Performance Report` in run `34229525747`; no fixture, threshold, or CI
condition changed. The runner tested merge commit
`ca3b5c6bd84b46cd177b9a5a117cb03f77ef3824`. Its accepted
`many_configured_scopes_regression` cold CLI row passed at Assura median
`11.808043 ms` (p95 `12.816459 ms`) versus LS-Lint median `13.230079 ms`
(p95 `18.786731 ms`) over 16 balanced alternating samples. This is the
required gate result, not a portable speedup claim.

The independently reviewed source change retains the two-field `FastScope`
lookup model and shares compiled naming rules with `Arc` only where the
existing direct-content stripping is a no-op. Focused tests cover all compiled
naming forms, retained direct-policy rebuilding, and compiled-artifact
round-trip behavior. The independent rereview found no concrete finding.

State: done. The earlier failed same-gate observations remain diagnostic
history; they are not hidden or reclassified. The merge made no release,
deployment, protection, pilot, or publication change.

## 2026-09-08 reviewed no-op direct-content bundle candidate

Candidate commit `d046115` retains the existing two-field `FastScope` layout
and all direct descendant accesses. It constructs `exact` once and clones it
only when stripping direct-content policy is an exact no-op; otherwise it uses
the existing stripped-policy rebuild. This is deliberately distinct from the
rejected `5eb0e339` Option/accessor experiment: no `Option` representation,
descendant accessor, matcher, or compiled-plan format changed.

The predicate covers `limit_children` and every field removed by
`strip_direct_content_policy` for files, directories, and self-directories.
Explicit `Some(Vec::new())`, `Some(HashMap::new())`, and `Some(false)` values
remain non-reusable. Focused tests prove immutable bundle sharing in the no-op
case, direct policies still disappear from descendants, and every stripped
field selects the existing rebuild. The fast-plan suite passed 16/16 and the
repository structure check reported zero blocking violations.

Independent post-implementation review found no concrete finding: it confirmed
the predicate mirrors the stripping function, composition and artifact format
are untouched, and the focused tests fail under the former no-op rebuild.

Supporting local evidence on the exact committed release bundle used
`performance-report --iterations 5` followed by the unchanged
`cargo xtask performance-no-slower` gate. The gate passed all accepted
realistic-equivalent rows. For `many_configured_scopes_regression`, the median
was Assura `54.974122 ms` versus LS-Lint `55.884649 ms`. This macOS collection
is not a before/after speedup claim and cannot resolve the documented
hosted-runner variance. The candidate still requires a current-head hosted
Performance Report pass, with no fixture, threshold, or CI-condition change,
before R03 can be considered resolved.

State: active with a separately reviewed current candidate and retained
hosted-runner variance diagnostic. The prior candidate remains rejected and its
source-only revert remains complete. A favorable retry cannot be treated as
resolution without retained repeated controls.

## 2026-09-08 hosted same-SHA variance diagnostic prepared

Master commit `5ac4eae4ee9675d6e98e1b58728fdb9f3d8f1020` failed only the
strict hosted `Performance Report` no-slower gate in run `34210314166`.
The exact accepted row was `many_configured_scopes_regression`: Assura
`20.0594385 ms` versus LS-Lint `18.755812 ms` across the existing 16 balanced,
alternating samples. Native and warm performance gates passed. The immediately
preceding R03 PR head `e6b6623b5711cffc18488ac54b3a21690a81ca80` passed the
same row in run `34209592405` at Assura `18.2605375 ms` versus LS-Lint
`21.2583825 ms`. The merge changes documentation and diagnostic-plan surfaces,
not the measured product path; neither result alone establishes source cost or
runner variance.

The retained `R03 Performance Variance Diagnostic` manual workflow measures an
explicit ref on the CI's `ubuntu-latest` runner class. It builds the same release
binary set as the production performance job, freezes commit and executable
SHA-256 identities, and runs three identical `performance-report --iterations
16` collections. Each collection retains its complete raw report, exact strict
gate exit, selected many-scope/phase rows, and report hash. Identical Assura
executables across those collections are the A/A control; each underlying
report retains the production alternating Assura/LS-Lint pairing. The diagnostic
exits nonzero if any report, exact gate, or binary-identity check fails, while
uploading raw artifacts with `always()`.

This does not change the production workflow or threshold, authorize a source
candidate, make a performance claim, or accept a passing retry as resolution.
After independent review, the workflow may collect the bounded evidence. If the
A/A controls are stable and Assura remains slower, attribute and optimize the
measured source path; if they establish excess variance, propose any contract
amendment separately with the retained raw evidence.

## 2026-09-08 finite Callgrind attribution proposal awaiting independent review

The hosted performance gate failed again on PR #191 after the reviewed A05
fixture-execution change. Its tested merge commit `93563712d3e6fecd6fddabebb894f5b3fadade48`
recorded 16 alternating samples and failed only
`many_configured_scopes_regression`: Assura 18.624937 ms versus LS-Lint
18.2206575 ms. Its phase rows retain config load 3.8806365 ms, checker init
1.75899 ms, and walk/validate 5.894547 ms. This is not evidence that A05
caused the regression: the same many-scope row already failed on the current
master merge. The prior timing method remains stopped. This proposal therefore asks a different question:
whether the retained baseline and rejected-candidate executables have a stable
modeled userspace instruction-event difference under one isolated software
instrumentation tool. It is diagnostic only; it cannot pass the hosted
no-slower gate, justify a tolerance change, or make the rejected candidate
eligible for restoration.

Before any of the six collection samples runs, the prepared isolated Valgrind
prefix must pass all of these health controls in one bounded driver: (1) its
packaged launcher and Callgrind engine hashes match the
2026-09-07 retained inventory; (2) `--tool=none /usr/bin/true` exits zero;
(3) `--tool=callgrind /usr/bin/true` creates a parseable output with a numeric
`summary:` event count; (4) a driver-owned, no-I/O POSIX shell loop with 1,000
`:` builtins (content hash retained in the raw record) produces a strictly
greater numeric `summary:` than the true probe under the same Callgrind options;
and (5) the exact retained
baseline and candidate binary hashes plus the valid fixture manifest match the
evidence above. The driver then performs one separate Callgrind compatibility
probe for each exact retained static-PIE executable, using its frozen valid
fixture argv, cwd, and scrubbed environment; each must exit as expected,
produce the expected normalized diagnostic digest, and create a numeric
summary. These two probes are health controls, not part of the six-run sample.
Every Callgrind health and collection invocation uses
`timeout --signal=KILL 30s`; the driver records a timeout distinctly and
rejects partial output. A failed control stops collection. It does not
authorize package installation, path changes, an alternate profiler, a timing
retry, or a source change.

If every control passes, the driver may collect exactly six runs: three baseline
and three rejected-candidate invocations, alternating B/C/C/B/B/C, using the
same absolute executable, argv, cwd, valid fixture, and scrubbed environment
as the frozen A/A evidence. Each run uses Callgrind with cache and branch
simulation disabled, a unique output path, and the exact 30-second KILL timeout.
It records only the parsed `summary:` instruction-event count, exit status,
expected normalized diagnostic digest, executable/fixture identities, tool
version, and output-file hash. No run is compared with LS-Lint because that is
not the distinguishing question and no wall-clock statistic is accepted from
this driver.

The driver must stop and retain raw output if an invocation times out, has an
unexpected exit or diagnostic digest, lacks a numeric summary, changes a
verified identity, or produces an observer/control failure. Otherwise it may
report the six raw counts and per-group median only. A source-cost hypothesis is
eligible for follow-up only if all three candidate counts exceed all three
baseline counts under the same verified setup. Any other ordering is an
inconclusive result, not evidence of host noise or a reason to optimize. Even a
separated result requires a new, scoped correctness-preserving implementation
proposal and the unchanged hosted performance contract before merge.

Independent review must verify that this is materially different from the
failed wall-time A/A method; that static-PIE compatibility, observer
sensitivity, identity, timeout, and inference controls are concrete; and that
the plan cannot be used to bypass the required hosted gate. Until that review,
this section is a proposal, not an authorization to run remote collection.

## 2026-09-07 isolated software-tool prerequisite prepared

After hardware access failed, root selected isolated package extraction as the
next prerequisite, not a profiler run. Independent method/driver review found
inherited-PATH validation and missing post-extraction debug inventory; both
were corrected before execution and scoped re-review cleared the exact driver.
Plan SHA2560a40653413e2c8c59b2b1fc3ceddbf08343c597bfdc3472da3fc86a76a319084;
driver3a1a0c476a84a4cde45da11e4e8e4de735f657b9b348c14abf5b91b61aaa1466.
Both live under the A04 repair runtime a04-orchestration as
r03-valgrind-setup-plan.md and r03-valgrind-setup-83.sh.

Session14035/878a87 terminated0/e063e1. Invocation:
`ssh vps '/usr/bin/env -i PATH=/usr/bin:/bin LC_ALL=C /bin/bash
--noprofile --norc -s' < .trellis/.runtime/a04-orchestration/r03-valgrind-setup-83.sh`
from /Users/nroth/.codex/worktrees/assura-a04-hook-ownership-repair at80ad01d.
Hostvps-9cb01956/UID1000/amd64/libc6=2.42-0ubuntu3.1 were checked before any
download. One60second KILL-bounded apt-get download, retries0, fetched the two
pinned packages; package metadata, SHA256 and archive member paths passed before
dpkg-deb extraction. No apt install/update/configure or maintainer script ran.

New owned prefix root:
/home/ubuntu/data/projects/assura-r03-valgrind-preflight.m3DTGw
(129196KiB including downloads). Package hashes:

- Valgrind1:3.25.1-0ubuntu1 amd64:
  a66ef89d6501b84bfdda231594a4d709e94e897330a4c096416684e3872d7de5.
- libc6-dbg2.42-0ubuntu3.1 amd64, matching the installed libc6:
  581b628ee696b9d0056f2b6b619748530ecc262b7678be4965e77c9ee29bdd5a.

Packaged launcher prefix/usr/bin/valgrind SHA256
4652774bd116cb49951ef74115ad4237cad5021b2bd4d80002f09d986ec438b9;
engines live in prefix/usr/libexec/valgrind, with callgrind-amd64-linux
c605140d428cac75e7e8a5d40eb0f6a840a1aa67aafaf029c86028bd6f9e02ed
and none-amd64-linux5ccd1b9169e03c1060b3ed1154702c650ad134f040277524c9359bb28c1d64cd.
The273regular debug files have sorted relative-path/content inventory SHA256
488ef8924a715b2247bd70b68b2c81ce69bec749da16678d7c9d6f04a27db580.
Full raw log r03-valgrind-setup-83.log SHA256
2e64f071f42e74f2a8c5406eb26bf8ecb1f51091459b57bc94381ff09dd9927a.

Stop boundary honored: no extracted executable, Assura/LS-Lint, fixture or
instrumented control ran. No system package/path/HOME/config/permission/service
change or deletion was performed. The prefix is a retained goal-owned resource,
not a registered worktree; include it in final resource cleanup accounting.
Setup success is not tool health, binary compatibility, source attribution,
a performance pass or R03 completion.
Post-setup read5675ef still reports libc6=2.42-0ubuntu3.1. Its broad --show
query returned a Valgrind name with an empty version, so the printed
system_valgrind_query=present label is not installation evidence. Follow-up
dpkg-query --status515574 exited1 and explicitly reported Valgrind not installed
with no package information. No package installation or repair was attempted.

Original baseline/candidate audit copies were inspected44bf19 without execution:
both are stripped x86-64 static-PIE. Hashesac0f9c5899ab42e3788157cbeef15eaed12176f5194cbe6044046e97f9d1cc70
and4273f61eaa222cb9f2f2cfc2e873522ff6193c73dacd6ae93223d62deed3674f.
A future Callgrind method can at most initially compare modeled instruction
events; absent DWARF is not evidence for source-line attribution. Native CPU
cost must still be established by the unchanged performance contract. See
https://valgrind.org/docs/manual/cl-manual.html for event semantics, not Assura
acceptance. Root must separately review exact tool-health/static-PIE compatibility
controls, observer sensitivity and a finite collection/inference rule before
product profiling. No automatic fallback or8pair launch is authorized here.

## 2026-09-07 hardware-counter prerequisite tested and unavailable

Root selected a materially different capability-only preflight after the
offline CPU analysis: one process-scoped instructions:u check on /usr/bin/true,
not Assura/LS-Lint or a timing acceptance rerun. Independent review caught a
5second timeout grace beyond the stated30second bound; root removed it and
scoped re-review cleared exact driver SHA256
3e304f5f7367be138d1b7c9dd180a84f6d8d3989927655e151db5267ab655bf9.
Plan SHA2562dc1ba799c8040b2baa027556233333da3b6315150898133b5ae79a41fcaa512.

Session28740/b042a3, terminal3d547b, ran via `ssh vps 'bash -s'` with the
reviewed local r03-counter-access-82.sh as stdin. Verified hostvps-9cb01956,
UID1000, Linux6.17.0-40, cwd
/home/ubuntu/data/projects/assura-w02-install-proof-6PXW5N. Effective/permitted/
ambient capabilities were zero; perf_event_paranoid remained4. Exact probe:
`/usr/bin/timeout --signal=KILL 30s /usr/bin/env -i PATH=/usr/bin:/bin LC_ALL=C
/usr/bin/perf stat --no-big-num -x ';' -e instructions:u -- /usr/bin/true`.
It exited255 with a performance-monitoring access denial. The outer driver
exited0 only after printing probe_exit=255; this is not a successful counter
measurement, product test or performance gate. No retry followed.

Perf6.17.13 SHA256d6b94eaedb6b3dfb7037ec99557f9a1540ea339c1654ae49a50d14915e77f0e9;
true SHA256c31622a57ae49ff39da1748f8359694c31b5a6488c1a58b20d405c9fe7cf5593.
Complete raw log in the A04 repair worktree's runtime a04-orchestration/
r03-counter-access-82.log SHA256
8a92aa3ff1ad5d4cefc85252c453660465f1fe3dddbbb357597f90e1d54f1e46.
No settings, capabilities, packages, source, user environment or acceptance
changed. Valgrind is absent; strace is present, not approved for execution.

Root ruling: hardware instruction collection is unavailable to this exact
user/session under unchanged policy. This says nothing causal about the
candidate regression or host noise. Do not repeat the failed access probe,
follow its printed permission-change advice, or launch the proposed8pairs.
The suggested syscall-count fallback does not distinguish the selected
userspace-work question and is not approved. Next internal method decision is
whether a finite isolated software-instrumentation method can answer it with
explicit compatibility/observer controls. Read-only prerequisite research is
allowed; tool setup and product execution require a reviewed concrete method.

## 2026-09-07 retained CPU evidence narrows the next question

Independent offline analysis used the existing stopped A/A samples only; no
executable, fixture, measured process, runner setting or acceptance changed.
The helper samples RUSAGE_CHILDREN around one synchronous subprocess, so these
are accounted child/descendant CPU deltas, not controller CPU or measured wait.
Retained fields have approximately0.001ms granularity. For Assura, A/B median
total CPU is4.250/4.658ms, accompanying wall4.4990245/4.918235ms. Both tool-order
subgroups show the same direction. This rejects a primarily elapsed-only
explanation for that observed block separation, not every scheduling effect.

User CPU medians rise2.065 to2.5055ms overall and rise in both order subgroups;
system CPU contrasts are inconsistent across positions. This weakly favors a
userspace-work observable over a kernel-only explanation. A/B use the SAME
binary and labels remain time-correlated; no source cost or causal host-noise
conclusion follows. Medians are non-additive. LS-Lint total CPU exceeds wall
in these observations, so wall-minus-CPU cannot generally be called wait time.

Root ruling: retain this narrow elimination and keep R03 stopped. The proposed
next observable is process-scoped retired instructions with CPU accounting,
but a collection method has not been approved. Read-only live VPS inspection
f8d80f on hostvps-9cb01956 found /usr/bin/perf, perf_event_paranoid=4 and a
present CPU PMU; presence proves neither access nor correct child attribution.
Do not install tools, change permissions, collect counters, retry timings or
optimize source on the strength of this report. A justified independently
reviewed method/runner decision remains root-owned, not automatically a human
approval blocker or completion of this card.

Report: W02 runtime r03-existing-cpu-analysis.md, SHA256
9f5e05b96c74a64e80e9d6e51cbdeac8c5dbf164fa99a7741f27381dc628f03b.
Input remains the previously audited aa-run-20260907.xq2ovy/run-1/samples.jsonl
with SHA2562b2f80d6418e835b488cabcd60452d4348a6458cae3e31836b7d1594d7b2d264.
Analysis did not rebuild binaries or rerun the stopped A/A experiment. Root
reviewed the measurement semantics and contrasts; all original failed rows,
candidate rejection/revert and performance thresholds remain intact.

## 2026-09-07 read-only source hypothesis elimination

Independent reviewer w02_whole_branch_review inspected preserved candidate
5eb0e339, its baseline and the original multipart fixture. Root accepts the
narrow finding: the proposed1500-fold descendant-accessor amplification is
not present in the intended public minimal-check path. No files, binaries,
tests, benchmarks, profilers or host settings were changed for this review.

Source pointers in the preserved R03 worktree:

- src/cli/performance_report/counterexample_fixtures.rs:7 creates one src
  directory with1500files; the retained config contains one inherited root
  naming rule.
- src/cli/check.rs:177 constructs new_for_check after loading config;
  src/cli/check/compiled_config.rs:62 compiles the scopes.
- src/cli/check/ls_fast.rs:107 selects rules per visited directory, :184 for a
  child directory, and :200 reuses borrowed rules for each file. The minimal
  build does not use full-CLI parallel traversal. Expected traversal selections
  are one root exact lookup and two src descendant lookups, not1500accessors.
- Candidate src/cli/check/ls_fast_plan.rs:354-358 replaces two FastRules
  constructions with one plus a no-op-policy predicate for this fixture;
  its descendant accessor is at196-197. Scope composition returns early for
  the lone root scope (ls_fast_scope_composition.rs:3-7). Regex collection and
  indexing add per-scope setup accesses, not a new per-file loop. The existing
  ls_fast_naming.rs:157 suffix lookup is unchanged.

This is static algorithmic reasoning, not an executed work count or proof of
machine-code equivalence. It does not exclude code layout, memory effects or
other remaining mechanisms, and does not explain the observed0.6735275ms loss.
Earlier801-scope allocation evidence does not attribute the multipart loss.

Root ruling: do not build a count-only probe that merely confirms these calls;
it would leave the source-cost question unanswered. No new measurement method
is approved. A future proposal needs a specific remaining mechanism, a
distinguishing observable and independent review before execution. Failed
A/A controls do not establish causal host noise. Candidate rejection, rollback,
all retained rows and frozen performance acceptance gates remain unchanged.

## 2026-09-07 A/A comparability decision executed and stopped

Iteration67's root-owned internal method decision was completed, not deferred
to the user. Read-only audit of frozen25650 samples found sequential treatment
cohorts and unchanged LS medians spanning1.352459ms. Method0c639a60 received
independent review: one baseline/LS pair of immutable executables, balanced
ABBA/BAAB labels,32measured pairs maximum, four separate correctness controls,
unchanged <2% AND <0.1ms bounds, early stop on either tool and final identities.
This was a new null-control diagnostic, not a repeat of the failed candidate.

Root finished the runtime helper after the delegated lane stopped at a7-line
RED stub. Actual schedule REDa9a509 and empty-inventory RED5b9bb6 exited1.
Additional control9cf9d2 exposed an uncaught missing-executable error. Review
then found exact-boundary float rounding, invalid monotonic inputs, incomplete
fixture directory/type identity and offline environment trust. Real raw-ns
RED603869 falsely passed exactly0.1ms before the correction. Integer-duration
Decimal medians and all four fixes passed14controls in343c4a and independent
583066. These earlier flawed helpers never ran a real Assura/LS measurement.

Final helper SHA256:
074b08f7c84a2e94b19007a8be6925c7d8bc276ff0681b2ff97d7c1f9abd9078.
Tests:
9171dddee62f5982e1fbf632cb91d1332a4f6ead27847b797328749ced6e1a63.
No product source, build, benchmark report, threshold or release changed.

Actual host vps-9cb01956/UID1000/Linux6.17 was refreshed. Preflight4695
terminated0/c368f2: inspected competing process names,97%idle in the sampled
vmstat interval, matching binaries and exactly.assura/src fixture directories.
Unrelated services remained; no exclusive-host or historical-noise claim.
Transfer8ef059 exited0; remote330091 verified exact script hashes and14controls.

ONE actual command ran via ssh vps from wrapper cwd
/home/ubuntu/data/projects/assura-r03-aa-20260907.xq2ovy:
python3 aa_comparability.py --output
/home/ubuntu/data/projects/assura-r03-aa-20260907.xq2ovy/run-1.
Session66774 ended1/de949d. Every measured child ran from preserved
/home/ubuntu/data/projects/assura-r03-public-path-diagnostic-20260907.UKGVrz/fixture/valid,
with absolute tools/baseline-original/assura check --quiet or absolute
tools/ls-lint-linux-amd64-2.3.0 under that original diagnostic root. Both labels
used the same argv/cwd/environment. Baseline primary hashac0f9c58 and LSbc38be56
match the frozen original; full hashes and manifests are in run metadata.

| Block1 tool | A median ms | B median ms | Absolute delta ms | Delta percent |
| --- | ---: | ---: | ---: | ---: |
| Assura | 4.4990245 | 4.918235 | 0.4192105 | 9.3178088 |
| LS-Lint | 11.4149945 | 11.5215615 | 0.106567 | 0.9335703 |

Both comparisons fail the unchanged AND control bounds. Exactly32measured
observations (16Assura/16LS) plus four separately retained valid/invalid
controls exist; every expected exit/output check passed. Block2 did not run.
Before/after binary, file, directory and entry-type identities match, with no
secondary failure. A/A variation is observed; its mechanism is not localized.
This cannot establish source cost, rehabilitate5eb0e339, confirm host noise or
authorize a performance-contract amendment.

Local copy24881 ended0/b0954c under the R03 runtime
aa-run-20260907.xq2ovy/. Checksum dry-run19225 verified equal content; copied
file timestamps differ, not bytes. Independent raw audit6e228e7e recomputed
exact integer-duration medians, sequence/counts, identities and the stop.
Raw samples SHA256:
2b2f80d6418e835b488cabcd60452d4348a6458cae3e31836b7d1594d7b2d264.
Summary:
a9cdaaf1d3f018ee43233ecc60edc336698da13ac0afc183ed0936a0410a277d.
Metadata:
101d3a911b9502b57b65c4b15877324951acabec78fe3281335bd5f66f281b06.
Controls:
2436bfa99e4e217e2021d9142259773aad0dfa93dfed4b13a5fba765a564805a.

Disposition: stop this method; no block2, extra sample, changed schedule,
profile, candidate measurement or acceptance retry. Root owns any materially
different next attribution/runner decision; this is not automatically external
approval or whole-goal blockage. Resume independent W02 local preparation
after this evidence checkpoint. The rejected candidate stays reverted and all
product performance/hosted gates remain required before any behavior merge.

## 2026-09-07 terminal diagnostic: controls invalid, stop preserved

Root run25650 exited1 with `stop-block-1-overhead-failure`, not acceptance.
Exactly48 Assura and48 LS-Lint launches were retained, eight per treatment,
all in block1. No invocation failed or timed out; block2 did not start.
Independent read-only audit ac2749 exited0 and recomputed ordering, counts,
medians, binary identities and before/after fixture manifests.

| Side and contrast | Signed delta ms | Absolute percent | Control |
| --- | ---: | ---: | --- |
| Baseline original to disabled | -0.107611 | 1.846495 | fails absolute bound |
| Baseline disabled to enabled | -1.159652 | 20.272782 | fails both bounds |
| Candidate original to disabled | -0.469804 | 9.775565 | fails both bounds |
| Candidate disabled to enabled | +0.064160 | 1.479672 | passes both bounds |

The controls require absolute perturbation below both2% and0.1ms.
Negative deltas do not earn overhead credit. The observer/reference method
did not demonstrate comparability: neither causal source attribution nor
confirmed runner noise follows. The candidate remains rejected and reverted.
No second block, replacement measurement, profile or source optimization is
authorized by this result. Next requires a separately justified, independently
reviewed method decision that establishes unchanged-binary comparability under
the existing bounds. This is a card stop, not whole-goal completion or a
performance-contract amendment; independent W02 work continues.

Frozen runner SHA-256:
`880550659bc873277c06be6c92298d5d89e2bdb6ffa4e0123505f4b99fef31a2`.
Raw samples:
`8623a1f93dd0f7a8fd31d173efccee669d6c35beba98d6c17169e7d7ad8ae351`.
Summary:
`4295e0280b13b1cf5bdfdb4716c989051f19b9d86502ae7a07bb7c4bae8f4657`.
Run log:
`4b812515da08aa1fe0b646a959febf569aa0f5d49dbc6766b23fef936fbedb54`.

Remote root:
`/home/ubuntu/data/projects/assura-r03-public-path-diagnostic-20260907.UKGVrz`
on `vps-9cb01956`. Local preserved audit is under the R03 worktree runtime
`r03-scope-rules-attribution/public-path-diagnostic-20260907.UKGVrz/`.
Copy7426d9 exited0 (3066files/87MiB); checksum dry-run ec8d9d exited0 with
zero artifact/tool/fixture differences. Audit verified all1502 files in each
fixture, eight pinned launcher/companion binaries and successful finalization
on the actual early-stop path. Primary failure is retained without a secondary
identity error. All original rejected-candidate evidence below is historical.

## 2026-09-07 full-run rejection and exact source rollback

The first full before/after run55014 terminated1. Both complete392-row reports
passed schema checks:390 successful16-sample rows plus exactly two documented
diagnostic skips, not passing proof. Independent review recomputed all eight
accepted headline medians from raw samples and confirmed the failed decision.

| Accepted fixture | Baseline ms | Candidate ms | Delta |
| --- | ---: | ---: | ---: |
| ignored_generated_heavy_repo | 1.826567 | 1.573554 | -13.852% |
| many_configured_scopes_regression | 27.715999 | 20.5776125 | -25.755% |
| monorepo_packages | 1.889378 | 1.414225 | -25.149% |
| monorepo_policy | 2.961400 | 2.690889 | -9.135% |
| multipart_extension_regression | 4.529985 | 5.2035125 | +14.868% |
| rule_heavy_repo | 2.361023 | 2.247759 | -4.797% |
| simple_library | 1.741842 | 1.342206 | -22.943% |
| web_app | 1.312819 | 1.146390 | -12.677% |

Target gain7.1383865ms passed both target requirements. Multipart slowdown
0.6735275ms exceeded both preset spillover limits. Candidate absolute
no-slower gate exited0; baseline absolute gate exited1 (diagnostic only),
and before/after comparison exited1. No reversed full run, retry, native or
warm acceptance followed. The candidate is rejected, not rescued by its target
gain. This proves an observed failed prescribed comparison, not a stable,
causally attributed source regression or confirmed runner noise.

Measured baseline was3d9a255; candidate full archive d2a9ca7 retained behavior
source5eb0e339. Original build68398 terminated0 with explicit selected
Rust/Cargo1.98.1 commands; its build output remains the original tool transcript,
not a separate build-log artifact. Actual report fields identify runtime
Rust1.95.0-nightly from the default probe environment. These facts are distinct;
no claim that the report field says1.98.1 or complete CI-environment parity is
made. Generated full-report config bytes were ephemeral and not retained;
semantic/source identities match, but byte identity is not established.

Raw baseline report SHA-256:
`d71a655af1894e34bc9af39a4d29bb3ba123a52ce863f9286fb7296938ba0085`.
Candidate report:
`29656ba6d718cad982e809db57d38d4024fae2e536fd913121b65b81f84a24f2`.
Source manifests707400d4/1df9d5cb, histories, full log, complete executables
and all raw samples are preserved in52MiB/60 local files under runtime
`r03-scope-rules-attribution/acceptance-run1/`, plus original VPS root
`/home/ubuntu/data/projects/assura-r03-scope-acceptance-20260907.clDMdm`.
The runtime report records complete hashes; download2e8d16 verified36
manifest-listed artifacts/binaries. Preflight306aec exited127 before any
measurement because a zsh path variable shadowed PATH; corrected f78eca
passed. Neither is mislabeled a benchmark retry or product failure.

Before measurements, independent runtime-verifier review found duplicate,
missing/cohort-relabeled, invalid-sample and skipped-row false-pass paths.
Actual behavior RED112a94 reproduced the equal-relabeled acceptance defect;
nine corrected controls passed locally/remotely and root e2b4fa. Final
independent re-review passed exact shell0626f667/evaluator3f5aa3dd/tests
e9d79b7f. Threshold AND/OR rules were unchanged. These synthetic controls
are verifier proof, not measured product speedups.

Source-only rollback69dfc4a210fa7ed25838f32f072f135adeb18d7b removed exactly
the eight candidate product changes through a normal pre-commit-checked commit.
Base and rollback src tree IDs both equal943e9584e25419aa0e2f024af6b453ee74f3c27d;
the whole net branch diff contains only four owned metadata files. No source,
history, binary or failed artifact was silently lost. Structure7030ed passed
1777files/393dirs/0violations; evidencec9a90f passed using a separately hashed
existing xtask with an identical source tree. Workflow/diff checks passed.
No full Cargo tier was needed for this unpublished metadata-only net change.

## Next bounded attribution, not acceptance

Same-RUN1 review found the multipart public static CLI slowed while separate
in-process walk and later same-static-binary strategy measurements improved.
Those contexts cannot be subtracted to explain the public excess. The fixture
has one inherited rule and1500 files, not800 scopes; existing init timing does
not identify an owning added cost. Rejection stands.

Parent authorized one diagnostic described in runtime
`public-path-diagnostic-plan.md`: identical tiny temporary exports of existing
walk timing on the actual public path, a byte-identical shared multipart
fixture, explicit toolchain/binary/source identities, original/disabled/enabled
overhead controls, two mirrored blocks and at most96 measured Assura launches.
Temporary probe/script review precedes measurements. No allocator interception,
bad-callchain inference, new product optimization, acceptance retry or
fixture-specific opt-out. Inconclusive, overhead-invalid or unlocalized results
stop the diagnostic and require a specific next decision. Local product source
remains master-equivalent. R01 is separately blocked under its packet rule.

## 2026-09-07 bounded complete-descendant candidate

The one authorized candidate is committed as
`5eb0e3391c0ff0ce2a7fdb95d31a178b5c97beea` (normal commit35987 exit0;
pre-commit Assura passed). All eight independent-review source hashes matched
after commit; the four parent-owned metadata files remained unstaged.
It reuses exact
fast rules only when stripping direct-content policy is provably a no-op;
every populated direct-policy field, including Some(empty), keeps distinct
descendants. Composition materializes distinct results and compiled artifacts
retain the existing wire contract. Temporary diagnostic instrumentation is
absent. Cohesive helper/test modules satisfy existing structure limits without
configuration relaxation.

Actual allocation-contract RED chunk49f3b0 exited101 with reused2145 and
distinct2145; GREEN35834 exited0. Earlier compile error14355 and zero-test
84275 are not RED evidence. Final focused50301 passed3 reuse and14 plan tests;
broader78090 passed501 library plus15 watch-state, compiled artifact1,
plain/compiled equivalence1, and23 check CLI tests; all-target/all-feature
Clippy passed. Two incorrectly filtered zero-test commands are not counted.
Structure1d8f02: exit0, success true, no violations. Independent eight-source
review found no correctness or quality findings; metadata was excluded.

Two predeclared balanced16-pair exact-public `assura check --quiet` comparisons
used the same fixture and YAML bytes (SHA-256
`8375c53fcdf6731fa20a5675503079c754ef62faf3deeafb74be00d607b6324c`).
Session94222 exited0 with all32 commands successful: baseline34.006160ms,
candidate32.199554ms (-5.31%). Reversed-start session12679 exited0 with all32
successful:34.571053ms versus30.290471ms (-12.38%). Both had11/16 faster
candidate pairs. Host load remained low but unrelated services existed; no
exclusive-host claim is made. Candidate static binary prefix4273f61e was
built from the reviewed working source; these are exploratory source-hash
results, not final committed-SHA acceptance. Build28893 compiled successfully
but its wrapper exited127 when a bare version command required the absent
full companion; actual check preflights passed. This is not represented as a
successful complete release-bundle build.

Canonical loader selection is inferred from the exact Config serialization,
absence of top-level rules, source branch ordering and successful public
checks; it was not directly instrumented at runtime. Independently generated
YAML can vary in HashMap ordering, so byte identity is asserted only for the
fixed-fixture paired commands above.

Next: build exact comparable complete release bundles from committed5eb0e339,
then bounded full392-row before/after reports and cold/native/warm gates.
Retention still requires target public assura-cli gain >=5% AND >=0.75ms,
exact quiet gain >=3%, no accepted fixture regression >5% OR0.25ms, and two
comparable agreeing runs. Narrow gains do not authorize retention or merge.

## 2026-09-07 exact/descendant plan attribution

Current worktree: `/Users/nroth/.codex/worktrees/assura-r03-scope-rules-attribution`,
branch `goal/r03-scope-rules-attribution`, created clean from
`3d9a255f832733082c864edf703fc98c854e7f6e`. The historical unavailable-runner
state below is superseded: SSH alias `vps` reaches `vps-9cb01956`. The owned
probe directory is
`/home/ubuntu/data/projects/assura-r03-scope-attribution-20260907.zkuMM3`.
Use Rust1.98.1 and the exact static-primary release protocol; no unrelated
service was stopped and no exclusive-host claim is made.

PR185 run34088013090 failed the unchanged cold gate at19.364834ms Assura
versus17.6041085ms LS-Lint,392rows and16balanced iterations. Artifact SHA-256:
`48d32d6f361ad78b20a6afcebb7be3a8e9dda34e0a9b53de75305a862cb1d00b`.
Its commit efa1db33 is the verified GitHub synthetic merge of3d9a255 and1852b61,
not a stale source claim. The next corrected-test head3ec25fd produced
run34090768850:8/8accepted cold comparisons, many-scopes18.6063435ms versus
20.1631345ms, still392rows/16iterations. Artifact SHA-256:
`4f479d723bc44c73866a6511d58c814c34785f08bad490d0cde9b1e2d2232ac8`.
Its synthetic merge4404a52 has verified parents3d9a255 and3ec25fd. The later
pass does not erase the failure or establish a confirmed-noise amendment.

The exact generated fixture algorithm and LS-Lint converter produce800 reset
children plus one inherited root. Sixteen successful counting runs observed
reset800/inherit1/composition0,800 no-op file strips and one self-directory
strip. All801 Arc observations immediately before make_mut had strong count3.
Descendant strip/rebuild performs9,603 allocations/1,337,156bytes; exact
construction performs9,606/1,337,516. These are alloc/dealloc counters, with
reallocations separately qualified. Whole-checker destruction frees23,225
allocations/5,236,727bytes, not all attributable to descendant plans.

Independent probe review required a genuinely allocator/counter-free timing
variant, not merely disabling the diagnostic environment variable. Sixteen
timing-only invocations succeeded. Interim medians: fast-plan3.270ms, exact
construction0.978ms, descendant strip0.398ms, descendant matcher rebuild0.559ms,
plan-body residual1.277ms, entire checker destruction0.930ms. Separate medians
are not algebraically additive proof of public-command savings. Exact includes
effective-bundle construction; plan-body residual is not total public overhead.
Full raw logs/patches are retained under
`.trellis/.runtime/r03-scope-rules-attribution/`.

Decision: reject the standalone no-op make_mut guard as too small under the
existing decision matrix as a prioritization decision. Its observed instrumented
strip-bucket median is0.398ms, not a proven upper bound on public savings;
teardown and nonlinear/cache effects are unquantified. No implementation or
acceptance claim is authorized for that alone. Reusing the
entire identical descendant plan is a distinct, measured-cost proposal, still
pending a trustworthy uninstrumented baseline and public before/after proof.
A copied-target stale fingerprint returned the diagnostic binary during the
first baseline attempt; that observation is invalid baseline evidence. A fresh
explicit target then rebuilt the uninstrumented static primary in session60106,
exit0, SHA-256
`ac0f9c5899ab42e3788157cbeef15eaed12176f5194cbe6044046e97f9d1cc70`.
The final split timing binary was
`6912391b3dafa288e16d9dabfd86d19556886b7a17da87b7264a82a581f6c36a`.
Balanced16-pair overhead control cf62d7 completed32 successful commands:
uninstrumented32.635ms versus timing35.524ms. Large host variance prevents
crediting a production speedup from these diagnostic timing buckets.

Parent authorized one bounded test-first candidate for entire identical
descendant-plan reuse, not the rejected clone-only guard. It must retain
distinct descendants for every direct-content policy, preserve composition
and compiled serialization, and pass plain/fast/compiled policy oracles.
Retention still requires actual public5%/0.75ms improvement, exact quiet3%,
all accepted-row spillover limits and unchanged cold/native/warm gates on
comparable same-host runs. Implementation now exists as described above;
acceptance remains pending.

No threshold, accepted row, plain-checker oracle, release surface or production
policy changed. Temporary source instrumentation is diagnostic only and must
not enter the retained candidate; the diagnostic worker restored production
source before the new tests began. R01/A04 remain separate active repairs.

## Historical blocked baseline

Source SHA: `dcb1fb57ba100f77a7cb7e48c4f14507d3106231` (`origin/master` fetched
2026-09-05).

Worktree and command cwd:
`/Users/nroth/.codex/worktrees/assura-r03-many-scope` on
`goal/r03-many-scope`; source was clean before and after evidence capture.

Problem reproduced: GitHub Actions Rust CI run
[34005367783](https://github.com/rothnic/assura/actions/runs/34005367783),
Performance Report job `101411447744`, generated its 16-iteration artifact
successfully then failed the unchanged gate: `many_configured_scopes_regression`
was `assura-cli 19.582 ms > ls-lint-cli 18.445 ms` (exit 1). The retained
artifact is `target/performance/r03-hosted/ls-lint-comparison.json`.

Local release comparison (partial protocol, not an exact CI bundle): built with
`cargo build --locked --release --bin assura --no-default-features --features json-output,yaml-config`,
`cargo build --locked --release --bin assura-full`, and
`cargo build --locked --release -p assura-check-cli` (all exit 0; both
launchers report `assura 0.4.0`). The local primary launcher omitted CI's
Linux-only static command, `cargo rustc --release --bin assura
--no-default-features --features json-output,yaml-config -- -C
target-feature=+crt-static -C link-arg=-lgcc_eh`; it cannot establish
GitHub-runner parity. Then
`target/release/assura performance-report --output target/performance/r03-baseline-lslint.json --iterations 16`
exited 0 and retained 392 rows. Its balanced alternating samples did **not**
reproduce the failure: Assura median/p95 `44.747/49.171 ms`, LS-Lint
`46.813/51.665 ms`; `cargo xtask performance-no-slower
target/performance/r03-baseline-lslint.json` exited 0. The local phase medians
were config discovery `0.049 ms`, config load `8.781 ms`, checker init
`3.102 ms`, configured structure `0.022 ms`, walk/validate `8.884 ms`, and
report sort `0.000048 ms`. The hosted artifact's corresponding medians are
`0.016`, `3.779`, `1.831`, `0.013`, `5.671`, and `0.000030 ms`.

Change: none. The plain checker remains the oracle. No fast-plan/cache change
was made because the local paired release evidence and hosted failure disagree,
so neither per-scope matching nor config normalization is sufficiently
attributed for a correctness-risking optimization.

Negative control: no benchmark row, tolerance, policy, or CI condition was
relaxed. `ssh vps-dev 'hostname; uptime; pgrep -af "cargo|rustc|assura"'`
could not start because `vps-dev` was not resolvable from this host, so no
comparable isolated Linux rerun was available.

Review: pending independent review after a comparable-runner reproduction and
an attributed patch.

Remaining: obtain an approved, quiet Linux runner (GitHub-equivalent or
restored `vps-dev`), build the complete CI bundle including the static primary
launcher command above, and rerun the same 16-iteration release command before
selecting a branch. Preserve normalized plain/fast/compiled diagnostics for
overlapping scopes, inherited rules, exclusions, missing directories, and
invalid names before any implementation.

Integration: evidence-only PR #147 is rebased on current master at
`36367dec867830a43c86c91202399b4e82a351af`; no performance change has been
implemented. R03 remains blocked until the named Linux attribution run exists.

Next ready ID: R03 after a comparable runner is available; R02's independent
artifact behavior is already in the current source ancestry.

## 2026-09-06 diagnostic update

No source candidate was retained. A local alternating release experiment made
exact configured-scope `HashSet` membership look favorable, but the stronger
same-fixture VPS public-command record at
`docs/goals/assura-performance-polish-program.md:384` rejects it: `assura
check --quiet` regressed `23.741` to `23.964` ms (+0.9%) with mixed accepted-row
spillover. The experiment was fully reverted. Its local reports are diagnostic
only, not Linux-static parity; no benchmark row, threshold, or evaluator changed.
`ssh vps` now reaches `vps-9cb01956`; a future attempt needs a distinct
phase-attributed hypothesis and the exact release helper/protocol.

## 2026-09-06 current-master attribution check

Current source was refreshed to `25167c84af4c352ef5564bcf6ff17c36a9847766`
in `/Users/nroth/.codex/worktrees/assura-r03-attribution` on
`goal/r03-attribution`. The unchanged hosted gate failed again on Q06 PR #166
head `1e185d2a7821d787698dd36d2a7295e70c530c31`: Rust CI run
[34023443201](https://github.com/rothnic/assura/actions/runs/34023443201),
Performance Report job `101460146427`, reported
`many_configured_scopes_regression: assura-cli 19.492 ms > ls-lint-cli 19.115
ms` and exited 1. All other required hosted checks on that head succeeded.
This repeat retains the evaluator unchanged and does not attribute the loss to
Q06's release-readiness extraction.

The approved `vps` alias is reachable and has the shared canonical fixture,
but its read-only preflight found unrelated active `cargo-watch` and
`cargo run --release --bin open_agent` processes. Its load average was
`0.03, 0.27, 0.25`, but the protocol requires paired cold runs without
concurrent workloads, so no comparison was started. R03 remains blocked until
an approved quiet Linux runner is available for the complete static-primary
release build, 16-iteration report, unchanged no-slower gate, and a distinct
phase-attributed hypothesis. No source, benchmark row, tolerance, CI condition,
or acceptance criterion changed.

## Preserved earlier A04-branch diagnostic history — superseded decisions

The following unique records are retained verbatim from ownership head
9c9a89631701776704f996e2ad853438f8856b7f. Their old active/next-run wording is
historical only: the latest stopped A/A method and rejected/reverted candidate
above take precedence. None authorizes another run, source optimization or
acceptance amendment. The shared historical baseline/rejected-experiment tail
already appears above and is not duplicated.

## Renewed hosted failure — 2026-09-07

PR #185 head `1852b61a7af5a75d4926a58bab58a2166be62762`, Rust run
34088013090, performance job 101635714618 failed the unchanged no-slower gate:
`many_configured_scopes_regression` measured Assura median 19.364834 ms,
p95 19.872527 ms; LS-Lint median 17.6041085 ms, p95 24.368864 ms.
The report retains 392 rows, 16 balanced alternating iterations and 7/8 accepted
fixtures passing comparison. Row `status: pass` means successful execution,
not a passing no-slower comparison. Native and both warm gates still passed.
This contradicts any current all-gates-green claim; the PR cannot merge.

Raw artifact is retained in the R01 worktree at
`.trellis/.runtime/r01-association/hosted-185-performance/ls-lint-comparison.json`,
SHA-256 `48d32d6f361ad78b20a6afcebb7be3a8e9dda34e0a9b53de75305a862cb1d00b`.
Diagnostic phase medians are config-load 3.801826 ms, checker-init 1.78943 ms,
walk/validation 5.711817 ms. They are not automatically additive attribution
for the separate public cold invocation, and cannot justify a speculative fix.

The existing symbolized diagnostic binary was re-profiled with a 65,528-byte
DWARF stack capture rather than 8,192, session 59481 exit 0: 200 exact quiet
invocations, 3,430 samples, zero lost, 215.717 MB raw data. The binary hash
remains `1a4aff3c35a3f35e3b977046384dd1d1d84655df7185d376289c28e5e965c185`.
Artifact: the owned VPS R03 directory's
`target/performance/r03-cpu-profile-wide.data`. Caller chains still contain
implausible addresses; wider capture did not establish reliable whole-stack
attribution. It is not acceptance performance evidence or an exclusive-host run.
Source comparison of the profiled baseline `6d613f8` with current master
`3d9a255` for check/config/performance code, Cargo/lockfile, check package and
CI workflow exited 0 with no differences. Next: a decisive, narrowly scoped
phase/call-site experiment against the renewed failure, preserving all gates.

## Bounded control decision — 2026-09-07

Independent review accepted the current no-slower/native/warm passes as their
scoped evidence, not proof of historical noise. No profiled bottleneck justifies
an optimization; the prior HashSet candidate remains rejected. The remaining
control is two unchanged-source 16-iteration reports in a verified quiet
interval, retaining every row and the existing balanced per-iteration tool
alternation. Strict cold 2x remains optional and must not prolong this card.

Quiet-interval preflight session 10542 exited 0 on `vps-9cb01956` at
2026-09-07 05:38:57–05:39:01 UTC. There was 116 GiB free and low system load,
but `pidstat -u 1 3` showed unrelated Node PID 2020641 averaging 10.20% of
one CPU and Temporal PID 1927886 averaging 2.96%, plus other shared services.
Those are observed interval values, not cumulative `ps` CPU percentages.
No control report was started and no unrelated service was modified. This
does not establish the packet's no-concurrent-workload condition.

PR #184 performance job 101630975079 in run 34086323317 succeeded at unchanged
reviewed `d42094c`; the run still failed its independent executable-hook test.
That fresh gate pass does not resolve historical attribution or authorize
merging the failing PR. Next: obtain the named quiet-interval condition (or
a same-job hosted control), then perform the bounded comparison without
changing thresholds or choosing an unsupported source optimization.

## Current runner refresh — 2026-09-06

The old unavailable-runner block below is superseded: the approved `vps` host
has 16 CPUs, about 64 GB RAM and 122 GB available project storage. Exact-name
`pgrep -a -x cargo` and `pgrep -a -x rustc` returned no running compiler
processes. A short `pidstat` sample still shows shared services, so measurements
must be labeled shared-host diagnostics, not exclusive-runner release proof.

An immutable archive of master `6d613f809feabd65b520c57bfd7aaf1b521a107e`
is building in the goal-created directory
`/home/ubuntu/data/projects/assura-r03-attribution-20260906.3mFoLH`.
`run-baseline.sh` and `baseline.log` retain the exact command sequence and
outputs. The protocol includes the Linux static-primary linker flags, full
companion binary, check package, 16 report iterations, all rows, and unchanged
no-slower gate. Four build jobs bound load; no unrelated process was stopped.

The remote default compiler is `rustc 1.95.0-nightly (eda76d9d1 2026-01-21)`;
this is explicitly not hosted stable-toolchain parity. A stable toolchain is
installed and must be selected for a later same-toolchain baseline/candidate
acceptance comparison. This initial diagnostic cannot certify a fix.
At launch no passing benchmark result existed. The prior rejected HashSet experiment
and all earlier failures remain below as evidence, not erased history.

### First diagnostic result and controlled toolchain follow-up

The initial run subsequently finished with exit 0: 392 retained rows, 16
iterations, and all eight accepted fixtures passing the unchanged no-slower
gate. Many-scope medians were `assura-cli 19.7450405 ms` and
`ls-lint-cli 20.0674145 ms`. Assura p95 was `27.527479 ms`. Phase medians:
config load `4.7478145 ms`, checker init `2.0583635 ms`, walk/validate
`5.586357 ms`. This narrow-margin shared-host nightly result does not fix R03.

Raw artifact: `target/performance/r03-baseline.json` in the remote directory
above; SHA-256 `4f4fe323a1a63e94d20db144403d13fc1a35a2441c2c01861d669cbf0ae9af25`.
A local runtime copy is retained as `r03-nightly-baseline.json`. Full build/report
log remains `baseline.log`; tool session 35497 returned the terminal exit 0.

Hosted job `101551963741` records `rustc 1.98.1 (48a229cea 2026-09-01)`,
not either preinstalled VPS compiler. A separately logged follow-up selects
`RUSTUP_TOOLCHAIN=1.98.1` without changing the host default, retaining its own
`r03-ci-toolchain-baseline.json` and `ci-toolchain-baseline.log`. This varies
the compiler deliberately on the same immutable source; it is not a blind
retry of the passing nightly run. Shared hardware/OS differences still remain.

### Matched compiler and independent native/warm results

The Rust 1.98.1 run finished with exit 0 (session 37106), again retaining 392
rows and passing all eight accepted no-slower fixtures. Many-scope medians were
`assura-cli 21.5229185 ms` and `ls-lint-cli 23.8091165 ms`; respective p95s were
`25.855166 ms` and `34.524336 ms`. Phase medians were config load `4.249159 ms`,
checker init `1.9289635 ms`, and walk/validate `5.367328 ms`.
`r03-ci-toolchain-baseline.json` SHA-256 is
`5549cd41021a97ea702b9836e17d8716e2ad8ca91f1c289249341b6d6de8d0b9`.

The same source, cwd, compiler and built release bundle then ran independent
native and warm-loop checks, recorded in `run-native-warm.sh` and
`native-warm.log`. Session 13829 finished; each of the following commands had
its own exit 0 (not inferred from the wrapper script's final exit):

```text
target/release/assura performance-report --suite native --output target/performance/r03-native.json --history target/performance/r03-native-history.jsonl --website-dir target/performance/native-website --iterations 5
cargo xtask native-performance-no-regression target/performance/r03-native.json
cargo xtask warm-loop-benchmark --binary target/release/assura-full --iterations 20 --output target/performance/r03-warm.json --history target/performance/r03-warm-history.jsonl
cargo xtask warm-loop-no-regression target/performance/r03-warm.json
```

All five warm p95 budget rows passed. Native and warm artifact SHA-256 values:
`7d3c0d9dd582a96881a7e7fe69e8b718681c199502e49af2b92651bf35fa0645`
and `85ff1da8052affd30b84fc2b57555e1f39e3b1aa3a3d25b3ae4dfdb9ac76521a`.
Local diagnostic copies are preserved under
`.trellis/.runtime/a04-orchestration/sdd/a04-correction-plan/`.

Release binary SHA-256 values (same remote directory):

| Binary | SHA-256 |
| --- | --- |
| `target/release/assura` | `ac0f9c5899ab42e3788157cbeef15eaed12176f5194cbe6044046e97f9d1cc70` |
| `target/release/assura-full` | `e23ed9af8666b14ccf2dfb5d66702909b7fbba24d9c7874433eadcfaa7ee2594` |
| `target/release/assura-check` | `3f34d9ec3dd49ba173d2f484677e813c732b2b67c6638c7ab1d066c1c78cb442` |

Disposition: baseline evidence only. No source optimization, threshold change,
omitted row, or claim that runner noise is proven. Shared-host measurements and
phase timings do not yet identify a dominant profiler-backed repair hypothesis.
R03 stays active; final candidate PRs still need their own hosted performance
gates. These results supersede only the unavailable-runner status, not the
historical failures or rejected experiment below.

### Bounded CPU attribution

Unprivileged `perf stat` was denied by the host's existing paranoid setting 4.
No sysctl, capability, service, or security setting was changed. A narrowly
scoped `sudo -n perf` invocation worked; profiling inherited events only from
its own child tree, with the measured command dropped back to uid/gid 1000.

An independent symbols-enabled release build used the same source/toolchain
and static primary flags, with `CARGO_PROFILE_RELEASE_DEBUG=1` and
`CARGO_PROFILE_RELEASE_STRIP=false`, under `target/profile-symbols`. Build
session 78755 exited 0. Binary SHA-256:
`1a4aff3c35a3f35e3b977046384dd1d1d84655df7185d376289c28e5e965c185`.
It is a diagnostic binary, not a replacement for the measured acceptance bundle.

`r03-profile-loop.sh` ran 200 exact `assura check --quiet` invocations from
`/home/ubuntu/data/projects/manual-many-scopes-common`. `r03-run-profile.sh`
recorded `cpu-clock:u` at 997 Hz with DWARF call chains into
`target/performance/r03-cpu-profile.data`; session 13694 exited 0, retained
2,978 samples (25.330 MB) and reported zero lost samples. Raw data, call-tree
report, commands and `profile.log` are retained in the remote directory.

The leading self-time symbols were allocation (`_int_malloc` 8.23%), memory
copying (7.49%), free (4.67%), path component iteration (3.29%), and multiple
YAML scanner symbols. The initial profile does not establish per-scope selector
matching as a dominant cost. Do not revive the rejected HashSet experiment or
retain an optimization based only on these broad categories. These are CPU
samples, not wall-time regression proof or independently timed phase totals.

### Subsequent exact-head hosted guardrail

A03 verifier-only PR #183 passed Performance Report job `101624653775` in
[run 34084062813](https://github.com/rothnic/assura/actions/runs/34084062813),
on reviewed/tested head `9d4bf50643155e8cd14e37b1a601f5a7b2a7399f`.
Hosted logs explicitly show accepted no-slower, native, checked warm contract,
and generated warm p95 gates all passing. The 16-iteration comparison retains
392 rows and eight accepted faster fixtures. Many-scope medians were Assura
`9.2078775 ms` and LS-Lint `9.5744845 ms`; p95s were `10.707341 ms` and
`12.048921 ms`. Four of eight strict-2x rows remain incomplete, not relabeled.

Artifact `ls-lint-performance-report` ID `10004703152` is retained locally as
`.trellis/.runtime/a04-orchestration/sdd/a04-correction-plan/r03-hosted-183/ls-lint-comparison.json`;
SHA-256 `d4422f1fbd33c5d0a3f522d0984c24e9cc8061b3d0f0bd3d8a9424e7348d2d46`.
The source change was the document verifier, not a cold-path optimization.
Different absolute timings across runners are not a before/after speedup and
do not establish a noise-based amendment. R03 attribution stays open and each
later PR retains the unchanged performance gates.

# R03 completion evidence

State: blocked

Source SHA: `dcb1fb57ba100f77a7cb7e48c4f14507d3106231` (`origin/master` fetched
2026-09-05).

Worktree and command cwd:
`/Users/nroth/.codex/worktrees/assura-r03-many-scope` on
`goal/r03-many-scope`; source was clean before and after evidence capture.

Problem reproduced: GitHub Actions Rust CI run
[34005367783](https://github.com/rothnic/assura/actions/runs/34005367783),
Performance Report job `101411447744`, generated its 16-iteration artifact
successfully then failed the unchanged gate: `many_configured_scopes_regression`
was `assura-cli 19.582 ms > ls-lint-cli 18.445 ms` (exit 1). The retained
artifact is `target/performance/r03-hosted/ls-lint-comparison.json`.

Local release comparison (partial protocol, not an exact CI bundle): built with
`cargo build --locked --release --bin assura --no-default-features --features json-output,yaml-config`,
`cargo build --locked --release --bin assura-full`, and
`cargo build --locked --release -p assura-check-cli` (all exit 0; both
launchers report `assura 0.4.0`). The local primary launcher omitted CI's
Linux-only static command, `cargo rustc --release --bin assura
--no-default-features --features json-output,yaml-config -- -C
target-feature=+crt-static -C link-arg=-lgcc_eh`; it cannot establish
GitHub-runner parity. Then
`target/release/assura performance-report --output target/performance/r03-baseline-lslint.json --iterations 16`
exited 0 and retained 392 rows. Its balanced alternating samples did **not**
reproduce the failure: Assura median/p95 `44.747/49.171 ms`, LS-Lint
`46.813/51.665 ms`; `cargo xtask performance-no-slower
target/performance/r03-baseline-lslint.json` exited 0. The local phase medians
were config discovery `0.049 ms`, config load `8.781 ms`, checker init
`3.102 ms`, configured structure `0.022 ms`, walk/validate `8.884 ms`, and
report sort `0.000048 ms`. The hosted artifact's corresponding medians are
`0.016`, `3.779`, `1.831`, `0.013`, `5.671`, and `0.000030 ms`.

Change: none. The plain checker remains the oracle. No fast-plan/cache change
was made because the local paired release evidence and hosted failure disagree,
so neither per-scope matching nor config normalization is sufficiently
attributed for a correctness-risking optimization.

Negative control: no benchmark row, tolerance, policy, or CI condition was
relaxed. `ssh vps-dev 'hostname; uptime; pgrep -af "cargo|rustc|assura"'`
could not start because `vps-dev` was not resolvable from this host, so no
comparable isolated Linux rerun was available.

Review: pending independent review after a comparable-runner reproduction and
an attributed patch.

Remaining: obtain an approved, quiet Linux runner (GitHub-equivalent or
restored `vps-dev`), build the complete CI bundle including the static primary
launcher command above, and rerun the same 16-iteration release command before
selecting a branch. Preserve normalized plain/fast/compiled diagnostics for
overlapping scopes, inherited rules, exclusions, missing directories, and
invalid names before any implementation.

Integration: evidence-only PR #147 is rebased on current master at
`36367dec867830a43c86c91202399b4e82a351af`; no performance change has been
implemented. R03 remains blocked until the named Linux attribution run exists.

Next ready ID: R03 after a comparable runner is available; R02's independent
artifact behavior is already in the current source ancestry.

# Q07 execution evidence

State: active

Source: `c9ad106e586c5494d8e787bfa52a22c148b259d8` from `origin/master`,
worktree `/Users/nroth/.codex/worktrees/assura-q07-error-contract`, branch
`goal/q07-error-contract`.

## Launcher spawn contract

The focused red test was `cargo test --bin assura
tests::present_unlaunchable_companion_is_not_treated_as_absent -- --exact`.
It initially exited `1`: a real present, non-executable `assura-full` fixture
caused `run_companion` to return `None`, which would invoke the missing-
companion fallback. A first command used an incomplete exact test path and ran
zero tests; it is explicitly not proof.

The minimal repair changes `run_companion` from `Option<i32>` to
`std::io::Result<Option<i32>>`. Only an absent companion returns `Ok(None)`;
a spawn failure now carries the companion path and OS error, prints to stderr,
and exits with runtime-error status `1`. The same fully qualified focused test
then exited `0` with one passing test. The fixture is removed after each test.
No fallback, CLI schema, cache behavior, policy, benchmark row, or CI scope
changed.

## Report output and cache fallback contracts

`agent_onboarding_report` now writes through a narrow `Write` boundary. The
focused writer test uses a real failing writer; `cargo test --lib
cli::agent_onboarding_report::tests::serialized_report_returns_the_writer_error
-- --exact` exited `0` with one test after first proving the missing writer
contract. Serialization/output failure now reaches the command's existing
runtime-error path instead of becoming an empty success payload. Two incomplete
exact test filters ran zero tests during discovery and are not proof.

The selected check-cache audit adds a real cache-record fixture. `cargo test
--lib cli::check::cache::tests::corrupt_cache_record_falls_back_to_fresh_validation
-- --exact` exited `0` with one test: after corrupting a generated record and
then adding `BadName.rs`, the next cached invocation performed fresh validation
and reported `file_naming`. Existing cache-status diagnostics retain their
observable shared-cache fallback reason; the corrupt-record path is a safe
cache miss, not a successful cache hit. No cache fallback, cleanup behavior, or
cache policy was weakened.

Remaining: add packaged-launcher absent/good/error coverage, then run the Q07
integration and release checks. Next independent card remains held by the R03
performance gate; Q07 continues as its own current-master behavior card.

## Packaged launcher proof

`cargo test --test launcher_error_contract -- --nocapture` exited `0` with one
Unix package-boundary test. It builds a minimal launcher in a disposable target
directory, so the proof cannot use the default-feature development binary. A
copied full companion propagates `--version`; absence produces the established
installation guidance and exit `127`; a present non-executable companion emits
`failed to launch companion` and exits `1`; and `check --help` still runs on
the lightweight path. Windows-specific invalid-executable behavior remains for
the required hosted Windows matrix; this local Unix fixture is not presented as
cross-platform proof.

Remaining: run the required agent integration/onboarding, minimal/full build,
release-smoke, and broader repository gates; obtain independent review; then
open a current-master PR for hosted platform/release proof.

## Local tier

The attached `cargo xtask fast` rerun exited `0`. It covered the all-features
workspace test tier (including the Q07 package test), the repository structure
check with zero violations, CI-scope policy checks, and review-evidence policy
checks. Focused `agent_integration_cli` and `agent_onboarding_config_merge`
also passed 13 and 5 tests respectively; minimal and full binary builds exited
`0`. The next local gate is `cargo xtask pr`, followed by independent review of
the exact candidate SHA and the required hosted matrix. No release action was
taken.

## PR-tier limitation

Attached `cargo xtask pr` completed its code, package, all-features, structure,
CI-scope, and evidence-policy checks, then exited `1` solely in the xtask
Trellis workflow check: it enumerated a historical active task whose branch
does not match `goal/q07-error-contract` and whose directory has no `prd.md`.
The direct `python3 ./.trellis/scripts/workflow_gate.py --platform codex`
reports `Task: none` and `Ready: yes` in the same clean checkout. This is the
same known shared routing discrepancy recorded by R02 and R04; Q07 does not
alter task routing or weaken the gate. The PR tier is therefore recorded as
nonzero, not passing; hosted CI remains required.

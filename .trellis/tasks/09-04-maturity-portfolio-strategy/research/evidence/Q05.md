# Q05 — maturity-score containment

## Scope and consumer decision

Base: `25a141525ce2420f02f3686affbeb03b0915f607` in
`/Users/nroth/.codex/worktrees/assura-q05-maturity-containment` on
`goal/q05-maturity-containment`.

Consumer enumeration found no active CLI caller for `MaturityDetector`,
`MaturityDecisionEngine`, `MaturityReport`, `StatusReporter`, or
`CliConfig::maturity_config`. `MaturityLevel` remains an internal input to the
experimental constraint severity and trigger helpers. The unsupported scorer,
its proxy-based signals, its report renderer/configuration, and its
constant-restatement tests are therefore retired. The retained level is
documented as manually selected internal constraint state, not a measured
project-quality claim.

`ProjectObservations` is deliberately bounded: it reports local facts without a
score. `ci_config_present` requires a workflow file rather than an empty
directory, `ci_execution_verified` is `unverified` without an external evidence
source, `[tool.black]` is required for Black configuration, and package manifests
are listed without affecting a quality level.

## Test-first evidence

Before production implementation,

```text
cargo test --test maturity_observations_tests -- --nocapture
```

failed with exit 101 and `E0432`: `CiExecutionState` and
`ProjectObservations` did not exist in `assura::maturity`. After the minimal
observation implementation, the same command exited 0 with 2 passed, 0 failed.

## Local verification

| Command | Exit | Result |
| --- | --- | --- |
| `cargo fmt -- --check` | 0 | Formatting clean. |
| `cargo test --test maturity_observations_tests --test constraint_tests` | 0 | 2 observation and 22 constraint tests passed. |
| `cargo check --no-default-features` | 0 | Minimal build passed. |
| `cargo check` | 0 | Default/full build passed. |
| `/usr/local/bin/assura check --format json .` | 0 | 1,754 files and 394 directories checked; zero violations. |
| `git diff --check` | 0 | No whitespace errors. |
| `cargo xtask docs` | 0 | Passed after `pnpm --dir website install --frozen-lockfile` restored the ignored, lockfile-pinned local website dependencies. |

## Negative controls and limitations

The focused tests use real temporary repositories: an empty `.github/workflows`
directory yields `ci_config_present: false`; a bare `pyproject.toml` yields
`black_config_present: false`; adding `Cargo.toml` changes only the observation
list, while CI execution remains `unverified`.

The candidate is not ready to merge until independent review, a clean commit,
and the required hosted matrix. Q04 remains blocked by its hosted Performance
Report failure, which is owned by R03 and was not waived here.

## Next ready action

Independent review found no findings for exact candidate
`c23c34921ec1b2f64089c433ec2e71ea771e44e4`; it confirmed the source/test/config
tree retires only unreachable paths and keeps the constraint tier explicitly
manual. PR [#165](https://github.com/rothnic/assura/pull/165) now owns the
hosted matrix. Do not merge Q04 while R03 performance evidence remains failing.

Follow-up candidate `57b7c5cfc5b721e99766c6870f5cae9cff2b0209` removes the retired
`anyhow`, `git2`, and `git-signals` dependencies and replaces the stale MSRV
`git-signals` maturity invocation with `cargo test --locked --workspace --lib`.
The replacement passed locally with 491 Assura library tests and 15 watch-state
tests; `cargo check --all-features` and `cargo xtask hygiene` also passed.
Independent review found the replacement preserves the MSRV contract.

## Hosted closure

The final independently reviewed head `1b45771a9434af27aa1c0a52fd819787e6e72c2b`
passed every required hosted check in PR
[#165](https://github.com/rothnic/assura/pull/165), including MSRV, all three
test platforms, all adoption and installer smokes, coverage, documentation,
security, and the Performance Report. PR #165 merged as
`a85fdeb18dc0a976ef563c07b3d53c6bfbd13bba`; `git merge-base --is-ancestor
1b45771a9434af27aa1c0a52fd819787e6e72c2b origin/master` exited 0 after a
fresh fetch. The scorer is retired and bounded observation behavior is now
reachable from current master. Q05 is done.

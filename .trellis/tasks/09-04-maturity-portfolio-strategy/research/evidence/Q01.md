# Q01 completion evidence

State: implemented; local policy and skill checks pass, with independent review
and hosted integration pending.

Initial source SHA: c09ef59 (current `origin/master` at worktree creation).
The implementation was rebased onto `52de1c0` (R07 integration) as
`a315ec6`; it is the committed candidate whose policy and skill behavior this
evidence describes.

Worktree: `/Users/nroth/.codex/worktrees/assura-q01-rust-quality-skill`

Problem reproduced: before the policy change, a disposable project copied from
this worktree accepted `.agents/skills/unapproved-rust-skill/SKILL.md` with a
successful configured check and no finding for that path.

Change: add the concise `assura-rust-quality` index and four routed local
references; route it once from `AGENTS.md`; make the project skill directory an
explicit allowlist including the new skill and existing entries. The references
map the actual configured check, non-authoritative cache fallback versus lost
effects, comparable performance rows, and a real positive/negative fixture.

Checks:

- `cargo run --quiet -- check --format json /private/tmp/assura-q01-skill-allowlist-20260905/project`
  from this worktree exited 0 before the change. Its JSON had `success: true`
  and no finding for `unapproved-rust-skill`.
- A first candidate using only `directories.allowed_names` still exited 0 for
  the disposable skill, so it did not override the explicit wildcard skill
  rule. The final candidate closes that wildcard and enumerates approved
  entries instead.
- `cargo run --quiet -- check --format json .` from this worktree exited 1
  with the disposable unapproved skill present and emitted one blocking
  `exists_count` finding for that path. The follow-up JSON assertion exited 0.
  After removing that disposable fixture, the same candidate command exited 0:
  `success: true`, 18 low-severity pre-existing R07 advisories, and no Q01
  finding.
- `python3 /Users/nroth/.codex/skills/.system/skill-creator/scripts/quick_validate.py .agents/skills/assura-rust-quality`
  exited 0 (`Skill is valid!`); the index is 34 lines, below the 150-line
  acceptance limit.
- `cargo xtask evidence` exited 0; `git diff --check` exited 0.
- `cargo xtask target-state` exited 1 because this isolated goal branch does
  not match the active task branch and lacks that branch's expected task PRD;
  it is a worktree workflow limitation, not a Q01 policy failure.

Candidate binary: `target/debug/assura`, `assura 0.4.0`.

Negative control: the baseline disposable project at
`/private/tmp/assura-q01-skill-allowlist-20260905/project` accepted the
unapproved skill (exit 0), proving the prior wildcard policy did not enforce the
required allowlist.

Review: independent routing-index review found one valid medium evidence issue:
the earlier text called committed candidate `9e8ec97` uncommitted. This record
now separates the initial baseline, committed implementation, and pending
hosted integration. No routing ambiguity or contract issue was accepted.

Remaining: independent review is complete; hosted proof, PR, and integration
remain pending. The allowlist intentionally requires an explicit policy change
for each future project skill. External guidance links were accessed on
2026-09-05 and are advisory only.

Integration: committed local candidate `a315ec6`; no remote branch, PR, hosted
proof, or merge exists yet.

Next ready ID: Q02 remains dependency-blocked until Q01 and R04 are done;
Q03/Q05/Q06/Q07 require Q01 completion or review disposition.

## Post-merge closure

PR #152, head `b64c6542a709c09e77b488dcdfbe7dc2abb7d553`, merged as
`e841ab8fc12355d451e9e99ba46e6b3a727520ae`; that merge is in current master.
Q01 is done, so its dependent quality cards may proceed when otherwise ready.

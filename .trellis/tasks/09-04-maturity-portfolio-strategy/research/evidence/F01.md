# F01 pilot-kit evidence

State: locally prepared; blocked on Nick's authorization for participant
selection and invitations.

## Current-source validation — 2026-09-12 — `origin/master=943fc45d`

Owner/phase: `/root` / prove (read-only packet validation). The clean owned
checkout `/var/folders/0v/0z62tzvn037cpx7drn6wp97m0000gp/T/assura-f01-env.XXXXXX.ziG6WkUyV4`
is based on source `943fc45d1b794eb41eb49f9c1ebdae9ab7287508` with tree
`cb5f0cb879403fd39bb580e941e1cd06a24939ab` on branch
`goal/f01-validation-environment`.

Pinned website setup `pnpm --dir website install --frozen-lockfile` exited 0
with pnpm `10.25.0` and 355 packages. On that same checkout, canonical
`cargo xtask docs` exited 0 (Astro built 48 pages) and `cargo xtask evidence`
exited 0 (all evidence-policy checks passed). `cargo run --quiet -- check
--format json .` also exited 0 with no reported errors or warnings in a clean
current-source checkout. No invitations, participant selection, recordings,
external messages, credentials, publication, release, or deployment occurred.

An earlier docs attempt set `CARGO_TARGET_DIR` outside the checkout and exited
1 with `No such file or directory`; diagnosis showed xtask's hard-coded
`target/debug/assura-full` lookup disagreed with that target location. That
attempt is retained as invalid-environment evidence, not a product failure;
the corrected default-target run above is the applicable proof.

This validation changes no F01 authority or acceptance state. The smallest
next action remains Nick authorizing participant selection and invitations;
until then the pilot kit stays locally prepared and no demand result exists.

Current-master rebase baseline: `9b032d3150d376189ddc5a4e7a7c11ee86a78984` from
`origin/master`. Worktree:
`/Users/nroth/.codex/worktrees/assura-f01-pilot-kit`; branch:
`goal/f01-pilot-kit`.

`docs/analysis/assura-pilot-kit.md` contains the prescribed invitation, target
participant frame, 20-minute counterexample-first interview script, anonymized
evidence schema, privacy boundary, and a five-conversation go/narrow/stop rule.
It explicitly prohibits treating the draft as outreach, installation, adoption,
or a demand result.

Independent review found and repaired a privacy gap: re-identification data is
now excluded from repository evidence. Optional follow-up contact notes require
participant consent, remain Nick-controlled and separate, and must be deleted
when follow-up ends.

No invitations, external messages, participant selection, recordings, public
claims, releases, deployments, or credentials were created or used. Local docs
and structure/evidence validation passed: `cargo xtask docs`, `cargo run --quiet
-- check --format json .`, and `cargo xtask evidence` all exited 0 in the named
worktree. A later card may record real conversation evidence only after Nick
specifically authorizes invitations.

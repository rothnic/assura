# A04 — Verified hook lifecycle evidence

State: active, reopened after independent acceptance review on 2026-09-06.
The historical integration below remains valid history, but its full-card
completion claim is superseded by this acceptance correction.

## 2026-09-08 host-approval reporting slice merged

PR #208 merged as `284ccba33a5cbf6a8815b1f790c081e7122bf878` from reviewed
source commit `939ed1bdc7a409b771349b96096082b1a26d989a`. The `agent onboard
--agent auto --activate` report now distinguishes generated and activated
managed configuration from structural verification, reports the immediate
post-activation status and doctor facts, and names host approval as
`unavailable` with the host-specific user follow-up. The generated onboarding
website artifact exposes the same boundary; it does not imply that a host event
or runtime delivery occurred.

The first independent review identified two concrete release-surface gaps:
the generated artifact omitted the unavailable-approval warning, and its
browser assertion still expected the retired `verified=true` presentation.
The accepted correction requires and renders the `Host status` line, regenerates
the artifact, and checks both the structural and unavailable-approval facts.
Scoped rereview found no remaining finding. Focused browser coverage,
`cargo xtask website-demo-data --check --released`, and the complete local
`cargo xtask pr` gate passed. The current-base hosted matrix for #208 passed
all source, security, documentation, marketing, coverage, performance, stable
OS test, adoption, release-bundle, and Windows-installer lanes before merge.

A04 remains active. The real Git-hook advisory and opt-in blocking event is
already established by the merged effective-hook-path fixture; it is retained
as completed evidence rather than scheduled again. The next independently
executable acceptance is the remaining integration contract: append the Assura
command through an existing supported hook manager's project configuration,
while preserving an unknown custom hook and reporting only a proposed change.
Host trust or approval remains user-owned and unavailable; no host-event claim,
deployment, release, protection, pilot, or publication action occurred in this
slice.

## 2026-09-08 current-master effective-path slice merged

Reviewed PR #196 merged as `16ec35e161e0107c265c717341f6297419e34ffa` after
the current head `d86370a4147a56b6014fdb11474891000b7b7273` was rebased onto
master `c3caa0bfbf1cd4801a3f8a10b141a054cfa45c62`. The unchanged full hosted
matrix in run `34231501024` passed, including all platform test and adoption
lanes, installer smoke, and the required `Performance Report`. The complete
local `cargo xtask pr` gate also passed. Independent current-base rereview
found no new finding: the hook-path patch was equivalent to the earlier
reviewed behavior, and the execution-branch ownership companion rejects
malformed metadata rather than broadening authorization.

This merges only the effective Git hook-path and execution-branch ownership
slice. A04 remains active: the packet's later host, permission, and runtime
acceptance work is not represented as complete, and no deployment, release,
protection, pilot, or publication action occurred.

## 2026-09-07 local master refresh checkpointed

Merge commit80ad01dcb62e11a9a7f67c536b642ddc2cf136e8 preserves ownership head
9c9a896 and incorporates master98bd187. Normal pre-commit30653/fdc8a7 passed;
the worktree was clean and workflow Ready yes. Master ancestry and unchanged
Rust/tests/xtask/Cargo/workflows/website/policy/skills/specs were verified.
Independent resolution review caught stale leading W02 routing in R01 evidence;
root corrected it and scoped re-review passed on exact staged tree
8a4beab837a5bc6af18d153e543395ea5dbc0d28 before commit. Covering gates and
unchanged prior full-tier proof below remain correctly scoped, not new hosted
evidence. PR184 is not updated or merged; publication, hosted/R01/R03 holds
and full effective-path/host-runtime acceptance remain. No later behavior
slice may be stacked before this ownership integration.

## 2026-09-07 current-master reconciliation boundary

The A04 worktree prepared the metadata-only merge of master
98bd187abd70005b40fa2df89ea3f45f332e4a04 into ownership head
9c9a89631701776704f996e2ad853438f8856b7f. Five task-metadata conflicts were
resolved with prior evidence/history preserved. Source diff from that exact
tested head is empty for src/crates/tests/xtask/Cargo/workflows/website/policy/
skills/specs. No hook behavior or test changed in this refresh.

Preserved fast8047 and full PR25099 both terminated0 on9c9a896. Root recounted
the retained PR log:1310passed/0failed/1ignored across104binaries, including
8zero-pass binaries not counted as coverage. Log SHA256
3783aa2298b0ca46243d7befc4ecd81369ce94583efa279cc32cfb9e3cc4f9ba.
Both current binary hashes still match that gate: primary78fe43c980ff5be52c69110e5623888ce8308363142086d0e9f558cffe53efaa;
fullcb5f769731e5535eeb0afa0d65d044974c1dec7053a4526f5e2ff8149f69d991.
Covering metadata gates74364/1013c3 exited0: diff check, preserved assura-full
structure1787files/393dirs/0violations, CARGO_BUILD_JOBS=4 cargo xtask evidence
using this worktree's existing target, and JSON parsing/counts. The unchanged
full Rust tier was not duplicated or represented as a new runtime repair.

Independent merge-resolution review and local checkpoint are required before
this prepared refresh is complete. Hosted PR184 remains oldd42094c/DIRTY with
Ubuntu failure and cancelled macOS/Windows; no local proof clears those states.
Ownership integration and its required exact-head hosted gates come first. Effective
hook-path and real host/permission/runtime slices remain serialized after that
integration; do not stack or mark full A04 done. R01/R03 holds remain.

The newer controller queue at 790b0a9 keeps A04 active and W02 locally complete
but externally blocked. It is evidence coordination, not W02 behavior ancestry
or permission to publish, rerun stopped performance measurements, or start the
later A04 slices. Root owns the next proof/runtime progress checkpoint.

## 2026-09-07 latest local snapshot, not hosted closure

Local head9c9a89631701776704f996e2ad853438f8856b7f is clean and independently
reviewed. Fast session8047 and PR session25099 both terminated0 on that head;
the PR log and exact binary identities are retained in the A04 repair worktree's
`.trellis/.runtime/a04-orchestration/checkpoint-51.md` and its linked log.
The remote PR184 head remainsd42094c949a9cb09cab7a1a7ac9940fe426a0503;
the later local test-isolation repair has not been pushed or hosted-verified.
Current hosted checks are failing; local checks do not clear those failures.
The ownership slice awaits current-master and hosted revalidation, while full
A04 still lacks effective-hook-path and real host/permission/runtime acceptance.
R01/R03 holds remain. Earlier sections below are preserved chronological
evidence, not authorization to rerun a superseded diagnostic or mark A04 done.

## 2026-09-07 executable-launch blocker

PR #184 head `d42094c949a9cb09cab7a1a7ac9940fe426a0503` passed independent
source/docs review and local fast session 29959 / PR session 56973, both exit
0. The later Rust CI run 34086323317 failed Ubuntu job 101630975101: direct
execution in `generated_wrapper_preserves_non_utf8_project_path_bytes_end_to_end`
returned OS 26, `Text file busy`. Ownership passed 23/24 and lifecycle 10/10;
the failed target is not a pass. macOS and Windows were cancelled by fail-fast.
All adoption lanes and performance passed; merge approval remains withheld.

Passing Linux syscall traces show writer handles closed before rename in those
observed executions, not proof against a transient race in the failed hosted
schedule. A deterministic same-inode writable-holder control reproduces errno
26. No production source defect has been established. Approved next work is
failure-only bounded diagnostics around one direct installed-wrapper launch,
preserving the failure result and excluding process command lines/environments.
No automatic retry, sleep, shell bypass or policy weakening is authorized.
The diagnostic patch requires focused negative controls, independent review
and fresh gates; a subsequent pass alone must not be called a proven repair.

## Acceptance correction

### Executable fixture isolation follow-up

Test-only commit `d79a80c12ab0960cccd9bfbbf6a44e2ceeeeeedd` removes a
deterministically demonstrated writer/fork window from the ownership test
binary. A standalone Linux fork/pipe control (6b1c35, exit 0) showed that a
child can inherit an O_CLOEXEC writable descriptor before exec: after the
parent closes and renames the file, direct execution still fails with errno
26 until the child releases its writer. O_CLOEXEC does not prevent fork
inheritance. The exact holder in the hosted failure was not captured, so this
mechanism proof does not establish that particular process identity.

A test-local mutex now guards fixture mutation and subprocess lifetimes across
the ownership integration binary. It does not serialize production or other
test binaries. Channel/try-lock coverage proves a launcher cannot enter while
a writer owns the guard. Guard RED 08315e was a missing-helper compile failure,
not an executable behavioral reproduction; GREEN e60db1 passed one test.
The separate fork/pipe control supplies the actual mechanism reproduction.
An intentional held-writer test still requires the direct single execution to
fail (3ff691, one pass). No retry, sleep or shell bypass clears an error.

Failure-only Linux diagnostics record escaped executable identity and bounded
same-inode FD evidence, inspect the current PID first, and omit command lines
and environments. Enumeration is capped at 4,096 processes, 16,384 FDs and
32 holders; incomplete/racy observations are labeled, never proof of absence.

On `vps-9cb01956`, Rust 1.98.1, final ownership suite 1dcab9 passed 27/27;
focused Clippy 1efe01 exited 0. Local macOS suite/Clippy d53ade passed 22/22
applicable tests and lint. Structure/fmt/diff 96e8a4 exited 0 at 927 lines,
within the existing 1,000-line ceiling. Test-file SHA-256 was identical locally
and on Linux:
`2c2c9b354675b46dce77353030cabb7e367ccfc761237fa1b9583ae596ddd03e`.
Independent source/spec review passed with no findings on that exact file.
Normal commit ae1f45 exited 0 after real pre-commit validation. All commands,
diagnostic negative controls and full artifact identities remain in
`.trellis/.runtime/a04-orchestration/sdd/a04-correction-plan/task-1-report.md`.

Final committed-head branch review and local/hosted gates remain required.
PR #184 still points to d42094c; no failed hosted job was retried. R01 and R03
remain merge holds, and full A04 effective-hook-path/host acceptance remains
unfinished even after this ownership slice integrates.

Refreshed base: `6d613f809feabd65b520c57bfd7aaf1b521a107e`.
Repair worktree: `/Users/nroth/.codex/worktrees/assura-a04-hook-ownership-repair`.
Plan: [bounded repair slices](../a04-correction-plan.md).

Independent review and controller inspection found direct forced installation
still bypasses custom-wrapper protection, sidecar ownership is not checked
before overwrite/removal, effective `core.hooksPath` is ignored, and onboarding
drops configuration-verification scope/host approval requirements. These violate
the packet's no-user-loss and actual-activation acceptance even though the
earlier narrow tests and hosted matrix passed.

The first repair covers mutation ownership, path safety and failed-install
rollback. Effective hook manager integration and honest host runtime/permission
reporting remain separate repairs. A04 must not return to done after this first
slice alone. A06/A07 remain dependent on full A04 acceptance.

The previous statement that local fast/pr could not finish within an observation
window is inconclusive, not a platform limit: tool observation timeout is not
process termination. Subsequent gates retain and poll actual session handles
through their terminal exits; no new merge may substitute hosted results for
missing required local gate evidence.

## Ownership repair in progress

The initial whole-branch PASS on `07599a9` is superseded by a subsequent
parent-triggered independent finding: UTF-8 legacy wrappers with shell
metacharacters remain recognized as current/ready and are left unchanged by
default install, allowing their historical double-quoted path assignment to
execute command substitution. Exact ownership must remain separate from safe
readiness; test-first default upgrade is required before approval.

The repair branch now contains merged A03 verifier fix PR #183
(`3d9a255f832733082c864edf703fc98c854e7f6e`), with ancestry verified. The only
rebase conflict was task branch metadata, resolved to this A04 branch; workflow
gate returned ready. All final local/hosted gates remain required after repair.

### Fresh UTF-8 legacy-path reproduction

Separate fresh-source legacy reproduction after rebase: session 50868 passed
the 10 lifecycle and 19 macOS-applicable ownership baseline tests. Session
36327 then ran three new legacy readiness/default-upgrade tests and exited 101
with all three failing. Session 88504 independently exercised a UTF-8 path
with `$()`, backticks and embedded double quotes: default install left the
legacy wrapper, its invocation altered the path and created `dollar-sentinel`.
This is actual source-bound RED, superseding the reviewer's earlier diagnostic
using an existing binary whose provenance had not been independently rebuilt.
The owning correction is in progress; these failures are not approval.

Implementation `258282a2cae9eeb6ecf53c00fb7439b3942b32b4` now distinguishes
current safe content from exact legacy ownership. Direct and bulk default
installation transactionally upgrade the legacy pair; legacy status is not
ready, and custom/drifted pairs remain preserved. `HookStatus` moved to a
cohesive sibling module to retain file-size policy without relaxation.

Intermediate GREEN: three legacy tests (12517), one rollback test (40016),
10 lifecycle plus 21 ownership tests (8376), and one subsequently added
legacy/drift preservation test (88004), all exit 0. Full library run 22574
passed 500 primary plus 15 watch-state tests; a zero-test sibling crate is not
counted. Clippy 65787 and structure 77829 exited 0 (1,786 files, 393 dirs,
zero violations). Normal pre-commit 53072 passed. These establish the source
candidate only; final reviewed-head fast/PR and hosted gates are still pending.

### Linux byte-path baseline and candidate

Host: `vps` (`vps-9cb01956`), Rust 1.98.1, four build jobs. Source archives and
all logs remain in goal-owned
`/home/ubuntu/data/projects/assura-a04-byte-proof-20260906.q3lHgD`.
The baseline cwd is its `baseline` child: production
`783ff054fd8a23b1d4db0f0e75e1af2149568b26` plus only the test-file overlay from
`07599a9ebc7936b4518996a6d2352cc44eb81a27`. Session 17578 exited 101:

```text
cargo test --locked --release --test git_hook_ownership non_utf8_project_path -- --nocapture
```

Both tests actually ran and failed: the generated wrapper reported ready but
could not invoke the literal byte-path sidecar, and the lossy legacy wrapper
was incorrectly treated as managed. No ignored/zero tests count as RED.

The candidate cwd is the `candidate` child, an exact archive of `07599a9`.
Session 18898 exited 0 after:

```text
cargo test --locked --release --test git_hook_ownership --test git_hook_lifecycle -- --nocapture
cargo test --locked --release --lib cli::hooks -- --nocapture
```

Results: 10 lifecycle, 21 ownership and 6 private hook tests passed, with zero
ignored relevant tests. Two unrelated workspace libraries selected zero tests
by the `cli::hooks` filter and are not counted as proof. The exact commands,
compiler, explicit `CARGO_TARGET_DIR`, and fresh package cleans are retained in
`run-red.sh`, `run-green.sh`, `red.log`, and `green.log`. The cloned dependency
cache was never treated as source provenance: Assura's package artifacts were
cleaned before each source variant was compiled.

Candidate binary SHA-256 values:

- `build-target/release/assura-full`:
  `84b75bd17731a2fa486b16f7ad29422232541bb99a940efe5611576fd133b579`
- `build-target/release/deps/git_hook_ownership-53191124fe27a439`:
  `9beb6fb1dc09fa808b67f4a7208d4ec9344ad7d898f86b9f0769669025e1c0ef`

This closes the raw-byte-path proof only. It does not accept the subsequently
confirmed unsafe UTF-8 legacy-upgrade case; the later source correction above
still needs independent review and all required final-head gates.

### Earlier ownership repair sequence

Current branch: `goal/a04-hook-ownership-repair`, in the repair worktree above.
Initial ownership implementation `0c46f8f` follows reviewed documentation handoff
`b48e8a2`; `783ff05` additionally preserves arbitrary non-UTF-8 hook file bytes.
No repair PR is yet approved or merged.

Tests first reproduced forced wrapper overwrite, sidecar drift, orphan-content
loss, symlink escapes, failed per-hook pair publication, and shell metacharacter
expansion. Corrected RED sessions 17101 and 13383 exited 101. Initial focused
GREEN 19971 passed 28 tests. File-content follow-up RED 17350 exited 101;
GREEN 97870 passed 29 focused tests and clippy. These are intermediate source
results, not final tested/reviewed SHA equality.

`cargo xtask fast` session 83294 exited 0. `cargo xtask pr` session 74898 exited
1 on stale A03 guide markers plus mismatched active task metadata. The same
guide-marker failures were separately reproduced on unchanged master in the
A03 verifier repair worktree; direct clippy (9339) and docs (32630) passed but
do not substitute for a passing full PR tier. The A03 owning repair has since
integrated as recorded above; complete A04 gates remain required after repair.

Independent review of `0c46f8f` found two important cases: arbitrary-byte custom
hook files (fixed by `783ff05`) and lossy non-UTF-8 Unix paths (fixed by
`07599a9`, with Linux proof above).
The review also requested accurate preservation wording and stronger rollback
observables. Freshly recompiled focused session 29697 exited 101 with 19
passing cases, a preservation-wording failure, and macOS rejecting the raw-byte
path with OS error 92. That filesystem rejection is not valid end-to-end
wrapper RED evidence; the required Linux proof is recorded above.
Session 4702 is excluded: an APFS cache clone reused stale Assura artifacts;
only the repair worktree's own
package artifacts were cleaned before the valid RED run.

Guarantees remain bounded: rollback is per hook's wrapper/sidecar pair, not an
atomic `install_all`; static symlinks are preserved, but arbitrary hostile
concurrent filesystem mutation is not claimed safe. Effective Git hook paths
and host permission/runtime reporting remain uncompleted A04 slices.

## Historical implementation (superseded completion claim)

- Branch: `goal/a04-verified-hook-lifecycle`
- Base: `origin/master` at `4ef1601aaf3951b13f28f0b4cc70603ae51b35e9`
- Worktree: `/Users/nroth/.codex/worktrees/assura-a04-verified-hook-lifecycle`
- Candidate binary: `target/debug/assura-full`
- Candidate binary SHA-256:
  `a5c8fc0cffc2581479a4161d5f5633c023b57b2f39f066b623a7f8c3f47d5454`

## Reproduction and focused proof

- RED: custom hook install had no distinct preservation outcome; tests now require
  `installed`, `unchanged`, `refreshed`, and `preserved` states.
- RED: `--force` overwrote a custom hook; a custom hook containing the former
  marker text is now preserved because ownership requires exact generated
  delegator content.
- RED: direct and bulk uninstall could misreport or remove custom hook content;
  tests now preserve marker-collision hooks and a sidecar used by a custom
  wrapper.
- `cargo test --test git_hook_lifecycle` — exit 0, 10 tests passed.
- `cargo test cli::hooks::tests --lib` — exit 0, 4 tests passed.
- `cargo test --test agent_integration_cli` — exit 0, 13 tests passed.
- `cargo fmt`, `assura check --format agent .`, and `git diff --check` — exit 0.
- `cargo clippy --all-targets --all-features -- -D warnings` and
  `cargo xtask evidence` — exit 0.
- `cargo xtask docs` — exit 1 after validating Assura docs/config examples and
  release-surface mappings because this isolated worktree has no website
  dependencies (`astro: command not found`). This is unavailable local website
  build evidence, not a passing docs gate; hosted docs verification is required.
- `cargo xtask fast` and `cargo xtask pr` cannot complete within the interactive
  observation window: partial output is retained, but neither is claimed as a
  terminal pass. Hosted PR gates, including Performance Report, remain mandatory
  before merge.

## Real hook event

- Disposable Git repository: `/private/tmp/assura-a04-final-hook-proof-20260906b/project with spaces`
  on branch `proof-hook-lifecycle`, with a local bare remote; seeded
  `src/BadName.rs` violates an `exists:0` policy.
- The SHA-recorded candidate `assura-full hooks install <fixture>` installed all
  three hooks, and `hooks status` reported each as Assura-managed and runnable.
  A `PATH` entry supplied that exact candidate to actual `git push` hook events.
- Advisory `git push --set-upstream origin proof-hook-lifecycle` exit: `0`; it
  named the seeded violation, stated the pre-push hook is advisory, and created
  the remote branch.
- After an actual feature-branch commit (whose pre-commit hook warned but exited
  `0`), `ASSURA_BLOCKING_PUSH=1 git push origin proof-hook-lifecycle` exit: `1`;
  it named the same violation, emitted the opt-in blocking error, and the push
  failed. This is a local Git hook proof, not a merge-protection claim.

## Review and limitations

- Independent review found and the implementation repaired managed/custom
  reporting confusion, marker-collision overwrite/delete risks, bulk-uninstall
  false reporting, and custom-sidecar deletion. A second review required and
  cleared positive managed refresh/removal and orphan-sidecar tests. Final
  re-review: clean.
- PR [#181](https://github.com/rothnic/assura/pull/181) merged at
  `41949fd589f37d01222ef6a695a6a4c3f61ec9a7` on 2026-09-06. Its reviewed head
  `ec2cf6a57a001a1c266dcb7b99e147a02cc85a94` passed the complete hosted matrix,
  including Documentation, all stable OS tests, MSRV, release bundle, Windows
  installer, all adoption lanes, coverage, evidence/security checks, and
  Performance Report. After `git fetch origin --prune`, that reviewed head was
  confirmed reachable from `origin/master` at the merge commit. A04 is done.
  Host permission gaps remain explicit unavailable states; local hooks are not
  claimed as merge protection.

## Next

- Next independent ready card: A05 (native harness lifecycle evidence). R03
  remains a separate performance-repair block and is not cleared by this
  no-regression PR result.

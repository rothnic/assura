# A04 — Verified hook lifecycle evidence

State: active, reopened after independent acceptance review on 2026-09-06.
The historical integration below remains valid history, but its full-card
completion claim is superseded by this acceptance correction.

## Acceptance correction

Refreshed base: `6d613f809feabd65b520c57bfd7aaf1b521a107e`.
Repair worktree: `/Users/nroth/.codex/worktrees/assura-a04-hook-ownership-repair`.
Plan: [bounded repair slices](../a04-correction-plan.md).

Independent review and controller inspection found direct forced installation
still bypasses custom-wrapper protection, sidecar ownership is not checked
before overwrite/removal, effective `core.hooksPath` is ignored, and onboarding
drops configuration-verification scope/host approval requirements. These violate
the packet's no-user-loss and actual-activation acceptance even though the
earlier narrow tests and hosted matrix passed.

The first repair covers mutation ownership, path safety and failed-install
rollback. Effective hook manager integration and honest host runtime/permission
reporting remain separate repairs. A04 must not return to done after this first
slice alone. A06/A07 remain dependent on full A04 acceptance.

The previous statement that local fast/pr could not finish within an observation
window is inconclusive, not a platform limit: tool observation timeout is not
process termination. Subsequent gates retain and poll actual session handles
through their terminal exits; no new merge may substitute hosted results for
missing required local gate evidence.

## Ownership repair in progress

The initial whole-branch PASS on `07599a9` is superseded by a subsequent
parent-triggered independent finding: UTF-8 legacy wrappers with shell
metacharacters remain recognized as current/ready and are left unchanged by
default install, allowing their historical double-quoted path assignment to
execute command substitution. Exact ownership must remain separate from safe
readiness; test-first default upgrade is required before approval.

The repair branch now contains merged A03 verifier fix PR #183
(`3d9a255f832733082c864edf703fc98c854e7f6e`), with ancestry verified. The only
rebase conflict was task branch metadata, resolved to this A04 branch; workflow
gate returned ready. All final local/hosted gates remain required after repair.

Current branch: `goal/a04-hook-ownership-repair`, in the repair worktree above.
Initial ownership implementation `0c46f8f` follows reviewed documentation handoff
`b48e8a2`; `783ff05` additionally preserves arbitrary non-UTF-8 hook file bytes.
No repair PR is yet approved or merged.

Tests first reproduced forced wrapper overwrite, sidecar drift, orphan-content
loss, symlink escapes, failed per-hook pair publication, and shell metacharacter
expansion. Corrected RED sessions 17101 and 13383 exited 101. Initial focused
GREEN 19971 passed 28 tests. File-content follow-up RED 17350 exited 101;
GREEN 97870 passed 29 focused tests and clippy. These are intermediate source
results, not final tested/reviewed SHA equality.

`cargo xtask fast` session 83294 exited 0. `cargo xtask pr` session 74898 exited
1 on stale A03 guide markers plus mismatched active task metadata. The same
guide-marker failures were separately reproduced on unchanged master in the
A03 verifier repair worktree; direct clippy (9339) and docs (32630) passed but
do not substitute for a passing full PR tier. The exact A03 owning repair must
integrate before A04 rebases and reruns its complete gates.

Independent review of `0c46f8f` found two important cases: arbitrary-byte custom
hook files (fixed by `783ff05`) and lossy non-UTF-8 Unix paths (under repair).
The review also requested accurate preservation wording and stronger rollback
observables. Freshly recompiled focused session 29697 exited 101 with 19
passing cases, a preservation-wording failure, and macOS rejecting the raw-byte
path with OS error 92. That filesystem rejection is not valid end-to-end
wrapper RED evidence; actual Linux baseline/candidate proof is required.
Session 4702 is excluded: an
APFS cache clone reused stale Assura artifacts; only the repair worktree's own
package artifacts were cleaned before the valid RED run.

Guarantees remain bounded: rollback is per hook's wrapper/sidecar pair, not an
atomic `install_all`; static symlinks are preserved, but arbitrary hostile
concurrent filesystem mutation is not claimed safe. Effective Git hook paths
and host permission/runtime reporting remain uncompleted A04 slices.

## Historical implementation (superseded completion claim)

- Branch: `goal/a04-verified-hook-lifecycle`
- Base: `origin/master` at `4ef1601aaf3951b13f28f0b4cc70603ae51b35e9`
- Worktree: `/Users/nroth/.codex/worktrees/assura-a04-verified-hook-lifecycle`
- Candidate binary: `target/debug/assura-full`
- Candidate binary SHA-256:
  `a5c8fc0cffc2581479a4161d5f5633c023b57b2f39f066b623a7f8c3f47d5454`

## Reproduction and focused proof

- RED: custom hook install had no distinct preservation outcome; tests now require
  `installed`, `unchanged`, `refreshed`, and `preserved` states.
- RED: `--force` overwrote a custom hook; a custom hook containing the former
  marker text is now preserved because ownership requires exact generated
  delegator content.
- RED: direct and bulk uninstall could misreport or remove custom hook content;
  tests now preserve marker-collision hooks and a sidecar used by a custom
  wrapper.
- `cargo test --test git_hook_lifecycle` — exit 0, 10 tests passed.
- `cargo test cli::hooks::tests --lib` — exit 0, 4 tests passed.
- `cargo test --test agent_integration_cli` — exit 0, 13 tests passed.
- `cargo fmt`, `assura check --format agent .`, and `git diff --check` — exit 0.
- `cargo clippy --all-targets --all-features -- -D warnings` and
  `cargo xtask evidence` — exit 0.
- `cargo xtask docs` — exit 1 after validating Assura docs/config examples and
  release-surface mappings because this isolated worktree has no website
  dependencies (`astro: command not found`). This is unavailable local website
  build evidence, not a passing docs gate; hosted docs verification is required.
- `cargo xtask fast` and `cargo xtask pr` cannot complete within the interactive
  observation window: partial output is retained, but neither is claimed as a
  terminal pass. Hosted PR gates, including Performance Report, remain mandatory
  before merge.

## Real hook event

- Disposable Git repository: `/private/tmp/assura-a04-final-hook-proof-20260906b/project with spaces`
  on branch `proof-hook-lifecycle`, with a local bare remote; seeded
  `src/BadName.rs` violates an `exists:0` policy.
- The SHA-recorded candidate `assura-full hooks install <fixture>` installed all
  three hooks, and `hooks status` reported each as Assura-managed and runnable.
  A `PATH` entry supplied that exact candidate to actual `git push` hook events.
- Advisory `git push --set-upstream origin proof-hook-lifecycle` exit: `0`; it
  named the seeded violation, stated the pre-push hook is advisory, and created
  the remote branch.
- After an actual feature-branch commit (whose pre-commit hook warned but exited
  `0`), `ASSURA_BLOCKING_PUSH=1 git push origin proof-hook-lifecycle` exit: `1`;
  it named the same violation, emitted the opt-in blocking error, and the push
  failed. This is a local Git hook proof, not a merge-protection claim.

## Review and limitations

- Independent review found and the implementation repaired managed/custom
  reporting confusion, marker-collision overwrite/delete risks, bulk-uninstall
  false reporting, and custom-sidecar deletion. A second review required and
  cleared positive managed refresh/removal and orphan-sidecar tests. Final
  re-review: clean.
- PR [#181](https://github.com/rothnic/assura/pull/181) merged at
  `41949fd589f37d01222ef6a695a6a4c3f61ec9a7` on 2026-09-06. Its reviewed head
  `ec2cf6a57a001a1c266dcb7b99e147a02cc85a94` passed the complete hosted matrix,
  including Documentation, all stable OS tests, MSRV, release bundle, Windows
  installer, all adoption lanes, coverage, evidence/security checks, and
  Performance Report. After `git fetch origin --prune`, that reviewed head was
  confirmed reachable from `origin/master` at the merge commit. A04 is done.
  Host permission gaps remain explicit unavailable states; local hooks are not
  claimed as merge protection.

## Next

- Next independent ready card: A05 (native harness lifecycle evidence). R03
  remains a separate performance-repair block and is not cleared by this
  no-regression PR result.

# R05 installer hardening evidence

State: implemented; hosted/review proof pending. The Unix implementation and
local replacement smoke were tested at
`862340076f27b2800239b8785ec42f4e30751d14`. Final candidate:
`60f1f70b7e87897943b0615ce70692c3f3f11092` adds the Windows CI
replacement-phase rollback assertion. Current final candidate:
`7b477bde110efb22d3f66828637c822b483a71b5` adds the Alpine adoption-output
isolation repair. Final reviewed evidence candidate:
`e65bd67dd1c704f56442f145dfb0e628e304f5ac`. Fresh hosted Windows and Alpine
reruns are pending before either hosted assertion can be treated as proven.
Initial source baseline:
`41908a2a62e40c7ee469a62012fbf1ae1d049962`; rebased before commit onto
`c09ef59d32a74122ad5c6ef4dd47c59e5db65f01` (`origin/master`). Worktree:
`/Users/nroth/.codex/worktrees/assura-r05-installer-current-master`.

Historical inspection found PR #142 (`codex/release-install-paths`, head
`dc990c93575e5da56f5a2afad0f070c01ce67445`) had an Alpine CI failure. The
failure was the host-side adoption artifact upload (`target/adoption-smoke/alpine-x86_64`
missing), not proof that the Alpine installer path passed. The historical
installer also replaced `assura` and `assura-full` sequentially, so it could
leave a mixed pair after a partial destination failure.

Current implementation stages, checksum-verifies, and validates both companions
before mutating the destination; it keeps the prior pair in a same-directory
backup until both replacements succeed. Unix and Windows CI smoke now exercise
both CLIs, invalid checksums, glibc/musl selection, and a real Alpine container
with the smoke output mounted at the uploaded host path.

Focused commands recorded so far:

- `cargo fmt --all -- --check` — exit 1 before formatting only the newly edited
  `xtask/src/main.rs` formatting; no behavioral failure.
- `cargo fmt --all && cargo test -p xtask --quiet` — exit 0; 35 passed, 0 failed.
- `sh -n website/public/install.sh` — exit 0.
- `git diff --check` — exit 0.
- Initial `cargo xtask release-smoke` attempts did not complete because
  overlapping goal-created invocations contended for Cargo's artifact lock.
  This is retained as negative operational evidence, not a pass.
- After confirming no remaining release-smoke or release Cargo process, a single
  `cargo xtask release-smoke` — exit 0. It built a local macOS preview archive,
  installed both CLIs, ran `assura-full --help`, injected a failure after the
  first replacement move, preserved bytes of both existing executables, and
  passed adoption smoke. CI separately retains the checksum-before-mutation
  negative control on Unix and Windows.
- Space-path fixture using `BIN_DIR="$TMP/bin with spaces"` — exit 0; both
  installed CLIs ran. A `chmod 500` destination fixture exited nonzero at
  staging-directory creation before either companion could be replaced.
- `cargo xtask docs` — exit 0 after local `pnpm --dir website install
  --frozen-lockfile`; validated 20 release-surface mappings, 54 documentation
  YAML examples, and the Astro static build.
- `cargo test -p xtask --quiet` — exit 0; 35 passed, 0 failed.
- `git diff --check` and `sh -n website/public/install.sh` — exit 0.
- `cargo run --quiet -- check --format json .` — exit 0 with `success: true`
  and 18 advisory violations; this is not advisory-clean and R07 owns their
  disposition.

`cargo xtask target-state` remains unavailable in this isolated worktree:
it exits nonzero because the canonical active Trellis task belongs to the
planning branch and has no matching branch/PRD attachment here. The canonical
task metadata was intentionally not changed by this card.

Negative control: `cargo xtask release-smoke` injects a post-first-replacement
failure and requires the installed pair to remain byte-identical. Hosted CI
separately exercises the 64-zero checksum-before-mutation control. Hosted Windows coverage remains
required for PowerShell execution and all advertised OS artifacts remain
unverified until a reviewed current-master PR runs CI.

Hosted Windows and all advertised OS artifact proof, independent review, and a
current-master PR remain required. No push, PR, or merge was made by this
worktree. Next ready card after R05 proof is R07; R03 remains separately blocked
on a comparable Linux runner.

Hosted run `34010149879` is RED evidence: Alpine successfully downloaded,
verified, installed, and ran `assura-full`, then adoption failed because its
bind-mounted output directory already contained `.assura/config.yml`. The
follow-up isolates Alpine adoption under `/tmp` and copies it to the uploaded
path only after smoke succeeds; installer policy is unchanged.

Hosted rerun `34011050005` subsequently passed Release Bundle Smoke, Windows
Installer Smoke, and every installable adoption smoke (glibc, Alpine musl,
macOS ARM/Intel, and Windows). Its overall Rust CI remains red only for the
R01 macOS watch regression and R03 no-slower performance regression; cancelled
Ubuntu/Windows suites are not counted as R05 passes. Q03's separate hosted
Performance Report passed, but does not resolve R03's blocked comparable-runner
contract. R05 is rebased onto `bedd73e28e7b86aa64c1d590be8039b7736df117`;
fresh current-master hosted proof and independent review remain pending.

## Current-master local refresh (2026-09-06)

Current candidate: `49adf5fbabc026a3b8149b9e42ce0e6d1090e05a`, parent
`bedd73e28e7b86aa64c1d590be8039b7736df117`; cwd:
`/Users/nroth/.codex/worktrees/assura-r05-installer-current-master`.
The exact candidate binary is the `target/release` archive built by
`cargo xtask release-smoke`.

| Command | Exit | Result |
| --- | ---: | --- |
| `cargo xtask release-smoke` | 0 | Built the preview archive, clean-installed both companions, ran `assura-full --help`, injected a replacement failure and restored the old pair byte-for-byte, then passed adoption and LS-Lint migration smoke. |
| `cargo test -p xtask --quiet` | 0 | 35 passed; 0 failed. |
| `sh -n website/public/install.sh` | 0 | Shell syntax valid. |
| `cargo xtask evidence` | 0 | Evidence policy checks passed. |
| `cargo run --quiet -- check --format json .` | 0 | Current self-check passed. |
| `git diff --check` | 0 | No whitespace errors. |

These are local current-master checks only. The next step is independent review
of this SHA, followed by a fresh hosted matrix from the updated R05 PR; neither
the historical green installer jobs nor skipped/cancelled unrelated jobs are
being reused as current-master proof.

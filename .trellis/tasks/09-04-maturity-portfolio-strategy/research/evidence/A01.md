# A01 evaluator evidence

State: verified locally; focused evaluator contract slice implemented, with
fixtures, partial contract validation, documentation, broader local gates, and
two independent review passes. Hosted PR gates remain required before integration.

Source baseline: `80f42e94b1b0eaa5ba5d4dfb79028ecb6285b086` from
`origin/master`. Worktree:
`/Users/nroth/.codex/worktrees/assura-a01-init-evaluator`; branch:
`goal/a01-init-evaluator`.

Implemented files: `scripts/evaluate-agent-init.py` and
`tests/agent_init_evaluator_tests.py`. The evaluator uses only Python standard
library and evaluates a disposable copy. It currently records structural-path,
preservation-hash, declared hook-path, positive/negative policy-probe, and
native-command evidence. Partial scopes are ineligible for acceptance.

Focused RED evidence was observed before each implementation increment: missing
evaluator output; unmarked partial scope; ignored preservation hashes; ignored
native commands; ignored required/forbidden paths; native execution outside a
partial scope; ignored positive probes; ignored hook states; and zero collected
native tests accepted as pass. A timed-out policy command initially raised
instead of producing result evidence; it now reports a failed, timed-out command
and retains its partial private output. A timed-out negative probe is not
credited as policy rejection. Independent review of `2d950a1` then found that
negative mutations shared a disposable copy, accepted negative IDs could leave
the policy dimension marked pass, and malformed/escaping contract values were
not rejected. Focused RED tests now cover each finding; the current unreviewed
repair starts each negative probe from a fresh copy, maps any accepted negative
to policy failure, validates v1 field types, and rejects `..`/absolute paths.
Re-review then found that a syntactically safe but nonexistent policy-probe cwd
crashed before output; its focused RED test now proves an `unavailable` command
record and a normal result artifact instead.

Current focused command:
`python3 -m unittest discover -s tests -p 'agent_init_evaluator_tests.py'`
exited 0 with 21 tests. The publication-redaction contract first failed because
the evaluator did not accept `--public-output`; it now proves that the private
artifact retains a synthetic command-output secret while the separate public
artifact excludes that secret, command output, cwd/path identifiers, fixture
identifiers, and hashes. Result artifacts include SHA-256 hashes of the exact
contract and candidate binary. `git diff --check` exited 0. The zero-test negative
control uses a trusted fixture command that exits 0 while emitting
`0 tests collected`; the evaluator records it as `fail`. The current regex also
catches pytest `collected 0 items` and Cargo `running 0 tests` output.

The frozen Rust fixture now carries a strict `.assura/config.yml`: it requires
the library/test layout and rejects non-snake-case Rust source files. The
installed `assura 0.4.0` checked that fixture successfully; this verifies the
fixture policy only, not a candidate-binary A01 acceptance run.

Real evaluator control: `python3 scripts/evaluate-agent-init.py --project
tests/fixtures/agent_init/rust --contract
tests/fixtures/agent_init/rust/contracts/evaluation.json --assura-bin
/usr/local/bin/assura --output /private/tmp/a01-rust-evaluation.json` exited 0.
It used `/usr/local/bin/assura` version `0.4.0`, SHA-256
`14949cb48cc1ab70e7b23ad3bfa69e30fb714e6c17ece0bafbfd78627ba774c8`.
Before dimension-completeness enforcement, that control reported acceptance-pass:
its positive check exited 0 and the disposable `src/BadName.rs` negative
mutation was rejected with exit 1 and a named `file_naming` violation. The first
run correctly failed instead on a duplicate YAML key in the fixture config; that
fixture defect was repaired before the policy control. The current rerun exits 1
and correctly reports `guidance`, `hooks`, and `native` as unavailable because
the Rust contract has no assertions for them. This is useful policy/idempotence
control evidence, not A01 acceptance: the candidate is the installed binary,
and those dimensions plus lifecycle and broader acceptance work remain.

The frozen TypeScript/Bun fixture now also has a strict local policy and passed
an installed `assura 0.4.0` structure check. This establishes fixture-policy
validity only; its evaluator contract and native Bun evidence are still pending.

The frozen Python fixture likewise has a strict local package/pytest policy and
passed an installed `assura 0.4.0` structure check. Its evaluator contract and
native pytest evidence remain pending.

Real TypeScript/Bun and Python controls then used their frozen contracts with
the same installed binary. Each first exposed a duplicate YAML key in its
fixture policy while its native command still passed (`bun test` and
`PYTHONPATH=src pytest`, respectively). After repairing those policy files,
both evaluator commands exited 0 under the former incomplete dimension-state
logic. Their unchanged negative probes seeded
`src/BadName.ts` and `tests/BadName.py`; both were rejected by the candidate
policy. They must be rerun under current dimension-completeness enforcement and
remain per-fixture evidence, not a completed A01 batch.

Limitations: this intermediate slice has frozen Rust/TypeScript/Python fixture
baselines and tracked existing-config/hook-manager variations, but no real hook
event variation, no agent-initialization rerun proof, and no hosted proof. It
must not be treated as A01 done. Next work: add contract
evidence for the remaining dimensions without weakening the unavailable rule,
then complete lifecycle acceptance with A04.

The planning review explicitly assigns A01 partial evaluator dimensions to A03,
A04, and A05 for guidance/hook/native closure, and A07 for full acceptance.
Accordingly, Contract v1's unavailable guidance/hook/native states are the
correct A01 boundary—not an excuse to call this full acceptance or to invent a
schema field. Two independent reviews found and verified repairs for mutation
isolation, policy-state mapping, malformed/escaping inputs, zero-test outputs,
and missing policy cwd evidence. A01 is locally verified, pending a hosted PR;
its next ready dependency is A02 only after that integration exists.

`docs/analysis/agent-init-evaluation.md` now documents the evaluator boundary.
Its direct structure check exited 0. `cargo xtask docs` reached the website
build but failed because this isolated worktree has no `website/node_modules`
and `astro` is unavailable; this is an environment limitation, not a passing
rendered-site claim.

Broader local gates after final independent re-review: `python3 -m py_compile
scripts/evaluate-agent-init.py tests/agent_init_evaluator_tests.py` exited 0;
the focused evaluator suite exited 0 with 21 tests; `cargo xtask evidence`
exited 0; and `assura check --format json .` exited 0 with zero blocking
violations. A final `cargo xtask docs` process exceeded its first observation
window, had no child process while disk headroom fell from 2.3 GiB to 1.1 GiB,
and exited before its terminal result could be captured. It is inconclusive and
is not a passing documentation gate; it was not retried under low disk headroom.

The attempted `cargo xtask fast` cold build is inconclusive because its terminal
output was not captured after the live process finished. It must not be counted
as a passing broader gate. Its exact ignored worktree `target/` cache was later
removed through Git-scoped cleanup to recover disk space; no source evidence was
removed.

Current-master rebase and structure-fit follow-up: the candidate was rebased on
`c9ad106e586c5494d8e787bfa52a22c148b259d8` as
`848fa737945217a47f5f7bb5933a3d2c539d84bb`. The first local project check
exited 0 but surfaced five low-severity fixture-only naming advisories for the
frozen Rust/Python/hook-manager special names. The repository already excludes
other committed fixture trees while exercising them through their own contracts,
so the narrow `tests/fixtures/agent_init/**` registration was added rather than
per-file exceptions or a broad naming relaxation. The post-change
`/usr/local/bin/assura check --format json .` exited 0 with zero violations;
the focused evaluator suite still passed 21 tests and `cargo xtask evidence`
exited 0. This records a self-validation boundary only: fixture policy remains
tested by the evaluator and is not treated as excluded product behavior.

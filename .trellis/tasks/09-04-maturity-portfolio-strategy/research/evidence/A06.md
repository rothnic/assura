# A06 bounded feedback execution evidence

State: active. This reviewed, current-master slice establishes bounded Codex
hook rendering and policy-aware cooldown identity; it does not claim full A06
acceptance.

## Source and environment

- Base: `548c0035f5cea765268f9733608835eea357ed94` (`origin/master`)
- Candidate: `18b435682d8fb777f7f7a19587116771093c4690`
- Worktree: `/Users/nroth/.codex/worktrees/assura-a06-bounded-feedback`
- Runtime: macOS/Darwin x86_64; Intel Core i9-9880H; 32 GiB RAM; Rust
  `1.94.1`.
- Exact release binary SHA-256:
  `f21505ac634a00752694be0a384b4b090d465fd2ba8d1e0bc5b10d00483c3149`.
- Measured project size: 1,805 files and 394 directories.

## Implemented contract

- Codex hook context is bounded to 2 KiB, preserves valid UTF-8 and its closing
  marker, and points to the full JSONL payload.
- The first critical finding is selected before any high finding; high is only
  a fallback when no critical finding exists.
- Cooldown identity includes the configured policy path and raw policy bytes, so
  a policy edit does not inherit a stale suppression entry.

Focused verification passed:

- `cargo test --test agent_surface_cli` — 21 tests.
- `cargo test --lib cli::agent_nudge::cooldown::tests` — 2 tests.
- `cargo clippy --all-targets --all-features -- -D warnings` — exit 0.
- `cargo xtask pr` — exit 0, including target-state, evidence, and website
  build after offline lockfile-pinned website dependencies were restored.

Independent review first identified critical-priority, raw-byte, and byte-cap
gaps. The accepted correction was rereviewed cleanly at `52b70be`; the later
lint-only assertion correction is `18b4356` and was covered by focused tests
and the final PR gate.

## Warm runtime measurement

`cargo xtask warm-loop-benchmark --binary target/release/assura-full
--iterations 20 --output target/performance/a06-final-warm-loop.json` followed
by `cargo xtask warm-loop-no-regression target/performance/a06-final-warm-loop.json`
passed all rows:

| Scenario | Median | p95 | Budget |
| --- | ---: | ---: | ---: |
| No-change review | 109.36 ms | 118.60 ms | 150 ms |
| One-file change | 109.71 ms | 121.59 ms | 175 ms |
| Directory create/delete | 109.27 ms | 115.24 ms | 200 ms |
| Config change | 108.78 ms | 113.75 ms | 200 ms |
| Agent nudge | 9.55 ms | 9.86 ms | 150 ms |

The earlier local no-change p95 miss remains historical diagnostic evidence; it
was not hidden or used to change a budget. The exact-head 20-iteration rerun
above is the current result.

## Remaining A06 acceptance work

- Capture the packet-required cold, idle, burst-edit, output-byte, and event
  count observations from the exact release binary.
- Add/verify the explicit resolved/reintroduced-failure contract before closing
  the card. This slice only proves cooldown suppression, policy-change reset,
  daemon fallback, and bounded critical-context preservation.

# R07 self-check advisory closure evidence

State: verified locally; awaiting independent review and hosted integration.

Source baseline: `41908a2a62e40c7ee469a62012fbf1ae1d049962` from `origin/master`.

Worktree and command cwd: `/Users/nroth/.codex/worktrees/assura-r07-advisory-closure` on `goal/r07-advisory-closure`.

Candidate binary: `target/debug/assura-full` through `cargo run --quiet --bin assura-full --`.

## Baseline and disposition

The current-source baseline command exited `0` with `success: true`, but its JSON contained 18 low, non-blocking `max_lines` findings. Exit success is not treated as advisory-clean. R07 retains the directory defaults (500 source and 1,000 test lines) and adds only exact-path ceilings, each reviewed on 2026-09-05 and due for review by 2026-12-05.

| Finding | Ceiling | Deliberate disposition |
| --- | ---: | --- |
| `.agents/skills/trellis-brainstorm/SKILL.md` (548) | 600 | Discovery-router content remains one navigable entrypoint; its special rule does not affect other skills. |
| `AGENTS.md` (295) | 320 | Project router remains cohesive while operational detail is moved into reusable skills; its special rule applies only to this file. |
| `src/cli/agent_integration_bundle.rs` (682) | 700 | Managed integration lifecycle boundary. |
| `src/cli/agent_onboarding.rs` (548) | 600 | Onboarding transaction boundary. |
| `src/cli/args.rs` (515) | 540 | Public CLI argument surface. |
| `src/cli/check/compiled_artifact.rs` (522) | 550 | Compiled-policy artifact boundary. |
| `src/cli/check/markdown_fix.rs` (514) | 540 | Markdown repair boundary. |
| `src/cli/check/patterns.rs` (546) | 575 | Policy pattern-matching boundary. |
| `src/cli/check/rules.rs` (576) | 600 | Check-rule composition boundary. |
| `src/cli/commands.rs` (809) | 850 | Top-level command-dispatch boundary. |
| `src/cli/init_support.rs` (582) | 620 | Initializer-support boundary. |
| `src/cli/watch.rs` (551) | 600 | Watch-lifecycle boundary. |
| `src/cli/watch_tests.rs` (624) | 700 | Watch scenario ownership pending a behavior-preserving split. |
| `src/config/config/structure_notation/directives.rs` (506) | 540 | Structure-notation directive composition. |
| `src/config/config/structure_notation/tests/directive_shorthand.rs` (586) | 650 | Shorthand regression-matrix ownership. |
| `src/config/config/structure_notation/tests.rs` (525) | 560 | Structure-notation composition regression set. |
| `src/config/config/structure_notation.rs` (677) | 720 | Structure-notation parser boundary. |
| `tests/project_intelligence_onboarding.rs` (1026) | 1100 | End-to-end onboarding scenario set. |

The first 16 source/test exceptions are path-aware entries under the root `files.max_lines_patterns` map. The two documentation routers use dedicated rule fragments, because their inherited shared entrypoint rules would otherwise make a local path pattern ineffective. No exclude, severity change, generated-content trick, or shared directory ceiling change was used.

## Negative control

Temporarily added one comment to neighboring unexcepted `src/cli/daemon.rs`, taking it from 500 to 501 lines. The candidate JSON check exited `0` (advisories are non-blocking) and returned exactly one advisory: `src/cli/daemon.rs`, rule `max_lines`, 501 exceeding 500. The temporary line was removed before final validation. This proves the root exception map does not grant a directory-wide bypass.

## Commands and results

| Command | Exit | Result |
| --- | ---: | --- |
| `cargo run --quiet --bin assura-full -- check --format json .` (baseline) | 0 | 18 low, non-blocking `max_lines` findings. |
| Same command with temporary 501-line `src/cli/daemon.rs` | 0 | One `daemon.rs` low `max_lines` advisory; all scoped exceptions remained quiet. |
| `cargo test --test pattern_scoped_file_directives` | 0 | 5 passed, 0 failed. |
| `cargo xtask evidence` | 0 | Docs/Trellis, Rust, Cargo, installer, performance, website, workflow, validation-command, prefix, and review-evidence policy checks passed. |
| `cargo run --quiet --bin assura-full -- check --format json .` (final) | 0 | `success: true`, 0 violations, 0 blocking findings. |

## Limitations and handoff

This is local evidence only. It does not claim hosted CI, a reviewed SHA, or a merge. The explicit ceilings must be independently reviewed for semantic cohesion and review-date suitability; any future growth beyond a listed ceiling remains an advisory. No release or public claim was changed.

Next ready independent card: `Q01` (subject to current-master dependency and ownership refresh). R07 should become `done` only after independent review, required hosted checks, and post-merge ancestry verification.

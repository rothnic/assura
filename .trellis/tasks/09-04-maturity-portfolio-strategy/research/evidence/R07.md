# R07 implementation evidence

State: implemented; not done

Source SHA: `f3a8f664e849d6ced223448dea63466405ced544`

Worktree and command cwd: `/Users/nroth/.codex/worktrees/assura-r07-self-check` on `goal/r07-self-check`.

## Baseline classification

`cargo run --bin assura-full -- check --format json .` exited `1` and returned
17 advisory findings: one medium naming finding and 16 low `max_lines`
findings. There were no high or critical findings. The nonzero exit is retained
as an advisory-not-clean result, not reported as a passing self-check.

| Finding | Disposition |
| --- | --- |
| `.trellis/tasks/09-04-maturity-portfolio-strategy/research/evidence/B00.md` (`file_naming`, medium) | Repaired. The execution backlog declares evidence as `<ID>.md`; the smallest durable policy scope is this train's `research/evidence/` directory. Its `*.md` files now require `regex:^[A-Z][0-9]{2}$`. This permits card-addressable evidence such as `B00.md` and future `R01.md`, but not arbitrary uppercase or kebab-case filenames. Review this exception when the execution train closes. |
| `.agents/skills/trellis-brainstorm/SKILL.md` (548/500) | Deferred for a separate router/content split. The skill is an entrypoint and must remain navigable; raising its shared entrypoint limit would weaken every skill. |
| `AGENTS.md` (295/160) | Deferred for a separate router extraction. Raising the shared agent-entrypoint limit would hide growth in every agent router. |
| `src/cli/agent_integration_bundle.rs` (682/500), `src/cli/agent_onboarding.rs` (548/500), `src/cli/args.rs` (515/500), `src/cli/commands.rs` (809/500), `src/cli/init_support.rs` (582/500) | Deferred for an agent-surface module-boundary card. These files cross public CLI/onboarding behavior; a file-specific line allowance would conceal cohesive-boundary work rather than resolve it. |
| `src/cli/check/compiled_artifact.rs` (522/500), `src/cli/check/markdown_fix.rs` (514/500), `src/cli/check/patterns.rs` (546/500), `src/cli/check/rules.rs` (576/500) | Deferred for a check-policy module-boundary card. Each needs a behavior-preserving split with focused tests, not a larger `src/cli/check` allowance. |
| `src/config/config/structure_notation/directives.rs` (506/500), `src/config/config/structure_notation/tests/directive_shorthand.rs` (586/500), `src/config/config/structure_notation/tests.rs` (525/500), `src/config/config/structure_notation.rs` (677/500) | Deferred for the existing structure-notation composition work (Q03/Q04 review before assignment). The production parser and its test topology must be assessed together; a waiver would work against the card's stated structure objective. |
| `tests/project_intelligence_onboarding.rs` (1026/1000) | Deferred for a focused integration-fixture split. It exceeds the test limit by 26 lines, but the correct boundary is scenario ownership, not a blanket test limit increase. |

## Bounded repair and negative control

The `.assura/config.yml` exception is nested at:

`structure -> .trellis/ -> children.tasks/ -> children.09-04-maturity-portfolio-strategy/ -> children.research/ -> children.evidence/ -> files.naming`.

It applies only to direct evidence files for this execution train and uses
`regex:^[A-Z][0-9]{2}$` on the file stem. The parent task policy remains
kebab-case for every other task file.

Negative control: added temporary adjacent
`research/evidence/not-an-id.md`, ran the JSON self-check, then removed it.
The command exited nonzero as expected and reported exactly one matching medium
`file_naming` violation for that path. The temporary file is absent from the
final tree.

## Verification

- `cargo run --bin assura-full -- check --format json .` before the repair:
  exit `1`, 17 advisories (one medium naming, 16 low length).
- Same command with the negative-control file: process exit `1`; the parsed
  result had 17 findings, including the expected `not-an-id.md` medium naming
  violation.
- Same command after removing the negative control: exit `0`; JSON reported
  `success: true` with exactly 16 remaining low `max_lines` advisories and no
  finding under this train's `research/evidence/` directory.
- `cargo test --test cli_check_warn_tests`: exit `0`; 6 passed, 0 failed.

## Remaining work

R07 remains active/implemented because 16 low-severity length advisories are
classified but not repaired. It cannot be marked done until each is resolved by
semantic cohesion work and the self-check has no unclassified or blocking
findings. Recommended follow-on work is: an agent-router split, an
agent-CLI/onboarding module-boundary card, a check-policy module-boundary card,
a structure-notation composition card coordinated with Q03/Q04, and an
onboarding integration-scenario split. These require their own focused tests
and independent review; none is waived by this card.
